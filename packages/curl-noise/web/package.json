{
  "name": "@bit-physics/web-curl-noise",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Stack-B WebGPU web build — curl-noise (Bridson 2007 + the divergence-free-noise frontier). Grid-free analytic per-tracer field with exact-integer-hash simplex noise, persistence-trail render stack, 13 templates incl. a break-the-certificate anti-demo, and a verification-visible PROVE layer (Phase-6 verification-demo track).",
  "scripts": {
    "gen": "node gen-verification.mjs",
    "prebuild": "node gen-verification.mjs",
    "predev": "node gen-verification.mjs",
    "build": "vite build",
    "dev": "vite",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@webgpu/types": "0.1.70",
    "typescript": "6.0.3",
    "vite": "6.4.3"
  }
}
