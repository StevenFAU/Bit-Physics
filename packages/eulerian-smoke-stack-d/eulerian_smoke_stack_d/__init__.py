"""eulerian-smoke Stack-D port (Taichi-DSL / CPU).

FIFTH spec-Phase-2 per-sim cross-stack port. Content-equivalent Taichi-DSL CPU
reimplementation of the Phase-1-frozen Stam-Fedkiw stable-fluids NumPy reference
(`stack.name="numpy-reference"`); cross-stack-validated against the Phase-1
canonical captures at `relative = 1e-4` (gate 14, `smoke` tolerance category).
"""
