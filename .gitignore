# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/
.tox/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# uv
.uv-cache/

# Node / pnpm
node_modules/
.pnpm-store/
*.tsbuildinfo

# Editor / OS
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Local secrets / env
.env
.env.local

# Mutation-testing artifacts
.mutmut-cache

# Claude Code local agent config (per-checkout; not project content)
.claude/

# Dear ImGui runtime window-layout (per-machine; written by C++ UI runs)
imgui.ini

# Held-local gate-14 reference captures — eulerian-smoke 3D Taylor-Green
# (128³, step-500, ~738 MB each). Deliberately NOT committed per D13/D14
# (LFS-bandwidth conservation); their sha256 digests are recorded in the
# eulerian-smoke-stack-{d,e} audits. Ignored to de-clutter `git status`
# (cleanup §13 #24); these are intentional held-local artifacts, NOT stray
# junk — remove the matching line if a future operator routing commits them.
captures/eulerian-smoke-stack-d/taylor-green-128cube-seed42-step500.*
captures/eulerian-smoke-stack-e/taylor-green-128cube-seed42-step500.*

# common-3dgs 3dgs-smoke local render outputs (PNG/HDF5/PLY). The canonical
# capture is committed to the schema-corpus at tests/fixtures/legacy-captures/;
# the examples/smoke_3dgs/out/ dir is a scratch render target.
common/common-3dgs/examples/smoke_3dgs/out/

# common-ts is pnpm-managed (tracked pnpm-lock.yaml; ts-strict.yml uses
# `pnpm install --frozen-lockfile`). An accidental `npm install` there writes a
# conflicting package-lock.json (happened 2026-06-07; deleted at the
# post-phase-5 housekeeping sweep) — guard against recurrence. The npm
# lockfiles in packages/*/web and the headless driver dir stay TRACKED (npm is
# canonical there per web-deploy.yml `npm ci`).
common/common-ts/package-lock.json
common/common-ts/preflight/package-lock.json

# symlinked per-sim web node_modules (gitignore "node_modules/" only
# matches real directories, not symlinks)
packages/*/web/node_modules
