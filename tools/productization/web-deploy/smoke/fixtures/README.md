# Fixtures for web-deploy smoke tests.

Browser capture bundles are reconstructed at test time from the committed
canonicals (`verify.canonical_as_browser_bundle`); no static fixtures are checked in.
