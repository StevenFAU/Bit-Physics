# phase-field-fracture-at-constants — derivation

Closed-form AT1/AT2 constants (spec-ref.md § 7.G; Pham–Amor–Marigo–Maurini
2011, Tanné et al. 2018 JMPS 110 Table 1):

- AT1 strength: `σ_c = sqrt(3 Gc E / (8 ℓ))` — the homogeneous elastic
  limit of the AT1 model (`w(d) = d`, `c_w = 8/3`).
- AT1 threshold: `H_crit = 3 Gc / (16 ℓ)` — no damage evolves below it.
- AT2 homogeneous peak: `σ_c = sqrt(27 E Gc / (256 ℓ))` at
  `ε_c = sqrt(Gc / (3 E ℓ))` — from maximizing
  `σ(ε) = (1−d)² E ε` with the homogeneous AT2 equilibrium
  `d = 2H/(Gc/ℓ + 2H)`, `H = E ε²/2`.

Test points: Miehe SENT steel (MPa, N/mm, mm), Borden glass (MPa, N/mm,
mm), and the non-dimensional Miehe groups (Gc = ℓ = 1, spec-ref.md § 9).
The `independent_reference` fields recompute each constant with reordered
exact arithmetic (`sqrt(3)·sqrt(Gc)·sqrt(E)/sqrt(8ℓ)`) to guard against
transcription slips; generation asserts agreement to 1e-9 relative.

Generated by the spike-adjacent generator (2026-07-09) from
`packages/phase_field_fracture/reference.py` closed forms; no vendored
upstream code.
