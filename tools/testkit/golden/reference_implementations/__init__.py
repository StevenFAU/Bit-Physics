"""Canonical Python reference implementations for golden-value algorithms.

Each module exposes `evaluate(inputs: dict) -> dict` per
`docs/phases/phase-0-plan.md` § 3.3.4. These are the sole Python
implementations in the repo for the algorithms they cover; Block 5
INTEGRITY imports from here rather than re-implementing.
"""
