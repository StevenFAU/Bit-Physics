Holds real-world artifacts that should *pass* every Cat check.
The meta-test's no-false-positives leg uses the live repo as the
implicit known-good corpus; this directory holds standalone fixtures
when the live repo doesn't naturally exercise a Cat. None at Phase 0.
