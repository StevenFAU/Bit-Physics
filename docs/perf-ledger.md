# Performance Regression Ledger

Per `architecture.md` § 2.15. Each row records first-landing or
significant-change wall-clock for a `(sim, stack, descriptor)` tuple.
Non-blocking — surfaces at landing-audit review time.

A row is appended:

- On first canonical landing of a sim (first-landing baseline row).
- On every subsequent CI run that produces wall-clock differing by >10%
  from the prior recorded value.
- Rows with `wall_clock_seconds > 2 × first_landing_baseline` are flagged
  `regression: WATCH`.

| sim | stack | descriptor | wall_clock_seconds | hardware_id | commit_sha | date | regression |
|---|---|---|---|---|---|---|---|
| reaction-diffusion-2d | numpy-reference | gray-scott-lambda-128sq-seed42-step2000 | 0.931 | i7-7700HQ-linux-6.17 | (this commit) | 2026-05-19 | baseline |
| strange-attractors | numpy-reference | lorenz-trajectory-seed42-step10000 | 0.061 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-20 | baseline |
| mandelbulb-explorer | numpy-reference | de-probe-points-seed42 | 0.006 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-20 | baseline |
| boids-3d | numpy-reference | flock-3agents-canonical-seed42-step1000 | 0.033 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-20 | baseline |
| boids-3d | numpy-reference | flock-1000agents-seed42-step1000 | 17.592 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-20 | baseline |
| physarum | numpy-reference | network-canonical-seed42-step5000 | 3.128 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-20 | baseline |
| reaction-diffusion-3d | numpy-reference | gray-scott-lambda-64cube-seed42-step2000 | 10.144 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-20 | baseline |
| sph-water | numpy-reference + scipy.cKDTree + numba-@njit(fastmath=False, cache=True) | dam-break-100K-particles-seed42-step1000 | 1291.854 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-21 | baseline (100K-instance of Phase 1 R8 canonical 1M descriptor; full N=1M is Phase-2+ Stack-C scope per spec-ref § 5; R12+R16+R17+R18+R19+R20 routing arc) |
| eulerian-smoke | numpy-reference | taylor-green-128cube-seed42-step500 | 691.587 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-22 | baseline (Stam-Fedkiw stable-fluids 3D; cadence-50 capture per Stage 0 Task 0.4 — conventions doc § N first practical exercise; MacCormack-corrected semi-Lagrangian + n_jacobi=20 collocated projection) |
| eulerian-smoke | numpy-reference | lid-driven-cavity-128sq-re100-seed42-step1000 | 5.099 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-22 | baseline (Stam-Fedkiw 2D; full-cadence capture; periodic-BC approximation of the Dirichlet lid-driven cavity per sim spec-ref § 5 — Phase-2+ Stack-C C++ port implements proper Dirichlet) |
| lattice-boltzmann-d3q19 | numpy-reference | poiseuille-64x32-seed42-step1000 | 3.784 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-22 | baseline (D3Q19 BGK Qian-1992; full-cadence capture per Stage 0 Task 0.4 — second sub-phase exercising conventions doc § N as established discipline; N_z=3 z-periodic depth-3 slab convention; bounce-back y-walls + constant body-force in x; Guo 2002 forcing scheme) |
| lattice-boltzmann-d3q19 | numpy-reference | couette-32x16-seed42-step500 | 0.604 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-22 | baseline (D3Q19 BGK Qian-1992; full-cadence capture; N_z=3 depth-3 slab; bounce-back walls with moving top-plate at u=0.05 lattice) |
| mpm-multimaterial | numpy-numba-reference | drop-impact-128cube-seed42-step500 | 158.052 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-23 | baseline (MLS-MPM Hu-2018 quadratic-B-spline; 1M particles + 128³ grid; cadence-50 capture per Stage 0 Task 0.4 — third sub-phase exercising conventions doc § N as established discipline; soft neo-Hookean elastic (E=4kPa, ν=0.3); sticky floor at z-index 4; @njit(fastmath=False, cache=True) on P2G/G2P/grid-update/stress/deformation kernels per conventions doc § G; second numba-using sub-phase after sph-water; Stage 1 wall-clock 0.6× of Stage 0 projection — third data point on production-correction factor empirical range [0.5×, 2.6×]) |
| reaction-diffusion-2d | taichi-cpu | gray-scott-lambda-128sq-seed42-step2000 | 0.568 | i7-7700HQ-linux-6.17 | (this commit) | 2026-05-23 | baseline (FIRST per-sim Stack-D cross-stack port under spec-Phase-2; sibling row to Stack-B numpy-reference baseline 0.931s at 2026-05-19; Stack-D Taichi-cpu warm-cache wall-clock 0.61× Stack-B numpy-reference baseline — well below the 2× regression band; canonical descriptor + manifest + payload sha256s recorded in docs/_audits/phase-2/sub-phase-reaction-diffusion-2d-stack-d/stage-1b-checkpoint-2026-05-23T*.md) |
| sph-water | taichi-cpu | dam-break-100K-particles-seed42-step1000 | 252.346 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-24 | baseline (SECOND per-sim Stack-D cross-stack port; sibling row to Stack-B numpy-reference baseline 1291.854s at 2026-05-21; Stack-D Taichi-cpu wall-clock 0.195× the numpy-reference baseline — well below the 2× regression band. R-S3 instrumentation: combined DFSPH iters/step = 1 — the Phase-1 reference trajectory is explicit-Euler rigid free-fall + per-step SPH density side-effect, NOT an iterative pressure solve; the Stage-0 R-S3 ~28-32min @ k≈10 estimate assumed an iterative solver the reference does not use. Inlined 27-cell spatial-hash neighbor search; f64 via f64-typed ti.types.ndarray. Canonical descriptor + .h5/.json sha256s recorded in docs/_audits/phase-2/sub-phase-sph-water-stack-d/stage-1b-checkpoint-2026-05-24T*.md) |
| lattice-boltzmann-d3q19 | taichi-cpu | poiseuille-64x32-seed42-step1000 | 4.954 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-24 | baseline (THIRD per-sim Stack-D cross-stack port; sibling row to Stack-B numpy-reference baseline 3.784s at 2026-05-22; Stack-D Taichi-cpu wall-clock 1.31× the numpy-reference baseline — within the 2× regression band; per-step kernel-launch overhead on the small 64x32x3 grid sits above the NumPy floor as the Stage-0 probe predicted. FIRST cross-stack port with genuine in-kernel f64 reductions: per-cell 19-term collision-moment sums seeded ti.f64(0.0) per the Stage-0 banked finding (set_taichi_deterministic does not set default_fp). Integer streaming bit-exact vs np.roll. Both .h5/.json sha256s recorded in docs/_audits/phase-2/sub-phase-lattice-boltzmann-d3q19-stack-d/stage-1b-checkpoint-2026-05-24T*.md) |
| lattice-boltzmann-d3q19 | taichi-cpu | couette-32x16-seed42-step500 | 0.973 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-24 | baseline (THIRD per-sim Stack-D cross-stack port, SECOND of two canonical captures (D4 dual-capture); sibling row to Stack-B numpy-reference baseline 0.604s at 2026-05-22; Stack-D Taichi-cpu wall-clock 1.61× the numpy-reference baseline — within the 2× regression band. Couette moving-top-plate at u=0.05 lattice; N_z=3 depth-3 slab; FIRST port adding TWO perf-ledger rows for one sub-phase) |
| mpm-multimaterial | taichi-cpu | drop-impact-128cube-seed42-step500 | 360.773 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-24 | baseline (FOURTH per-sim Stack-D cross-stack port; sibling row to numpy-numba-reference baseline 158.052s at 2026-05-23; Stack-D Taichi-cpu wall-clock 2.28× the numpy-numba-reference baseline — EXCEEDS the 2× regression band, FLAGGED per spec § 2.15 for landing-audit review. Attributable to posture-(i) cpu_max_num_threads=1 serialisation (REQUIRED for deterministic P2G atomic-scatter per Stage-0 Task 0.3: threads=8 is NOT run-to-run bit-exact) + per-step kernel-launch overhead (500 steps × ~6 @ti.kernel launches = ~3000 launches) over 1M particles; the serialised posture trades parallel speed for bit-exact determinism — first Stack-D port to exceed 2×. f64 accumulator seeds ti.f64(0.0) per Stage-0 + LBM banked. R-M2: j_det=1.000000 + n(j_det<=0)=0 across all 11 frames — the canonical drop-impact is rigid free-fall (the blob does not deform within the 500-step horizon), so the informal gate-14 cross-stack diff vs the numba reference is bit-exact particle_pos (0.0) / ~6.2e-28 particle_vel / ~1.5e-32 grid_mom (far below 1e-4; below the prior pairs' ~1e-15). Capture .h5/.json sha256s in docs/_audits/phase-2/sub-phase-mpm-multimaterial-stack-d/stage-1b-checkpoint-2026-05-24T*.md) |
| eulerian-smoke | taichi-cpu | lid-driven-cavity-128sq-re100-seed42-step1000 | 8.470 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-24 | baseline (FIFTH per-sim Stack-D cross-stack port; sibling row to numpy-reference baseline 5.099s; Stack-D Taichi-cpu wall-clock 1.66× the numpy-reference baseline — within the 2× regression band. MacCormack-2D velocity advect + fixed-20-sweep Jacobi project + plain-SL density advect; collocated cell-centered periodic. HARD-RULE-2 FINDING (gate-14): this 2D canonical is numerically UNSTABLE — the thin lid-shear-layer on a periodic grid is Kelvin-Helmholtz unstable (reference u→1.6e3 by step 5), NOT the steady-laminar Re=100 the plan-drafting probe characterized; cross-stack within_tolerance=False (IC-15 aspect #1 chaotic EXERCISED). The port matches NumPy to ~1e-16 while stable (step ≤2). Operator routing pending; see stage-1 checkpoint) |
| eulerian-smoke | taichi-cpu | taylor-green-128cube-seed42-step500 | 698.986 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-24 | baseline (FIFTH per-sim Stack-D cross-stack port, SECOND of two canonical captures (D4); sibling row to numpy-reference baseline 691.587s; Stack-D Taichi-cpu wall-clock 1.01× the numpy-reference baseline — essentially parity, within the 2× regression band. Plain trilinear SL + 7pt diffuse + fixed-20-sweep 3D Jacobi (inv6 seeded ti.f64(1.0)/ti.f64(6.0) per banked precedent #7 — NON-vacuous; bare 1.0/6.0 infers f32 and leaked ~1e-9). HARD-RULE-2 FINDING (gate-14): this 3D canonical is numerically UNSTABLE — the reference Taylor-Green blows up under collocated-grid/under-resolved-Jacobi numerics (reference max|u| 0.999→8.1e7 [step50]→5.1e19 [step250]; Stack-D →1.2e19), NOT the laminar decaying vortex characterized; cross-stack within_tolerance=False (IC-15 aspect #1 chaotic EXERCISED; backends diverge completely). The 738MB capture is held local pending operator routing (LFS-bandwidth conservation, D13); see stage-1 checkpoint) |
| mpm-multimaterial | warp-cpu | drop-impact-16cube-seed42-step50 | 0.108 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-25 | baseline (SIXTH per-sim cross-stack port; FIRST Stack-E port; DIAGNOSTIC-tier row at Stage 1a — the full 128cube canonical warp-cpu row lands at Stage 1b. NVIDIA Warp 1.13.0 @wp.kernel MLS-MPM/APIC neo-Hookean single-material; 16³ grid × 5K particles × 50 steps. Warp CPU wp.launch is structurally serial → P2G wp.atomic_add bit-exact (D5/banked #8; no cpu_max_num_threads=1 equivalent); f64 throughout with wp.float64() literal seeds (banked #7 / O-W7). R-A1 anchor: the production P2G reproduces the Stage-0 Task-0.6 verification digest a8f6e654…07ff1fe1 EXACTLY; step-1 P2G mass-conservation abs_err 2.22e-16 (partition-of-unity exact). Trajectory reproduces the Phase-1 diagnostic to printed precision (max|vel|=2.049050, min pos_z=0.491517 at step 50 — rigid free-fall; gate-14 cross-stack expected at FP-round-off, Stage 1c). Capture .h5/.json sha256s in the stage-1a checkpoint) |
| mpm-multimaterial | warp-cpu | drop-impact-128cube-seed42-step500 | 304.492 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-25 | baseline (SIXTH per-sim cross-stack port; FIRST Stack-E port; CANONICAL row at Stage 1b; sibling to numpy-numba-reference baseline 158.052s + Stack-D taichi-cpu 360.773s. Warp-cpu wall-clock 1.93× the numpy-numba-reference baseline — WITHIN the 2× regression band (contrast Stack-D's 2.28× FLAGGED) and 0.84× of Stack-D taichi-cpu. 1M particles × 128³ grid × 500 steps cadence-50 (11 frames; 1,125,718,712 bytes ≈ 1.05 GiB, matching the Phase-1 ref capture size; LFS-tracked). Warp CPU wp.launch structurally serial → P2G wp.atomic_add bit-exact (D5/banked #8; no cpu_max_num_threads=1 equivalent); per-step NumPy↔Warp marshalling over ~3000 launches. CANONICAL-SCALE DETERMINISM 2/2 content-digest MATCH (dd62c046…7586ac80 both runs); canonical-scale mass-conservation abs_err 4.44e-16 (partition-of-unity exact; same order as the diagnostic 2.22e-16). R-M2: the drop-impact is rigid free-fall (the blob does not deform within the 500-step horizon → F=I → zero stress), so the gate-14 cross-stack diff vs the numba reference is expected at FP-round-off (within_tolerance=True predicted; Stage 1c). Capture .h5/.json sha256s in the stage-1b checkpoint) |
| eulerian-smoke | warp-cpu | lid-driven-cavity-128sq-re100-seed42-step1000 | 5.897 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-25 | baseline (SEVENTH per-sim cross-stack port; SECOND Stack-E port; spec § 11.3 item 2.4 Stack-E half; sibling to numpy-reference baseline 5.099s + Stack-D taichi-cpu 8.470s. Warp-cpu wall-clock 1.16× the numpy-reference baseline — WITHIN the 2× regression band, 0.70× of Stack-D taichi-cpu. NVIDIA Warp 1.13.0 @wp.kernel Stam-Fedkiw stable-fluids 2D: MacCormack velocity advect + fixed-20-sweep Jacobi project + plain-SL density advect; collocated cell-centered periodic; 128² × 1000 steps cadence-100 (11 frames; 4,385,176 bytes — byte-size-identical to the Phase-1 ref capture; LFS-tracked, the schema-corpus representative-subset per D14/§5.4). common-warp socket-only (D7); own wp.array(dtype=wp.float64) (D15); Warp CPU wp.launch structurally serial → gather kernels bit-exact (no atomic scatter; no cpu_max_num_threads=1 equivalent). CANONICAL-SCALE DETERMINISM 2/2 content bit-identical (worst_abs_diff 0.0; O-2 checkpoint 3). STEP-1 cross-stack BIT-EXACT (max_abs_err 0.0 vs the Phase-1 NumPy reference — np.roll order + np.mod replicated). HARD-RULE-2: this 2D canonical is CHAOTIC (Kelvin-Helmholtz; reference u→1.6e3 by step 5) → gate-14 within_tolerance=False is the CORRECT R-P2 verdict (D10; the SECOND R-P2 instance, FIRST on Stack-E — stack-portable); Stage 1c formalizes. .h5/LFS-oid sha256 aa67929f…214734; .json committed-blob sha256 in the stage-1b checkpoint) |
| eulerian-smoke | warp-cpu | taylor-green-128cube-seed42-step500 | 541.977 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-25 | baseline (SEVENTH per-sim cross-stack port, SECOND of two canonical captures (D4); sibling to numpy-reference baseline 691.587s + Stack-D taichi-cpu 698.986s. Warp-cpu wall-clock 0.78× the numpy-reference baseline — UNDER baseline, well within the 2× regression band; 0.78× of Stack-D taichi-cpu. Plain trilinear SL + 7pt diffuse + fixed-20-sweep 3D Jacobi (inv6 seeded wp.float64(1.0)/wp.float64(6.0) per banked #7 / O-W7 — bare 1.0/6.0 infers f32 and leaked ~1e-9 in Taichi). 128³ × 500 steps cadence-50 (11 frames; 738,260,192 bytes ≈ 738 MB — byte-size-identical to the Phase-1 ref capture; HELD LOCAL per D14, NOT committed, LFS-bandwidth conservation). STEP-1 cross-stack BIT-EXACT (max_abs_err 0.0 vs the Phase-1 NumPy reference). HARD-RULE-2: this 3D canonical is CHAOTIC — the reference Taylor-Green blows up under collocated-grid/under-resolved-Jacobi numerics (energy 2.6e5 → 3.2e19 [step50] → ~6.7e45 plateau; max|u|~5e19), resolution-dependent (R-SME9: 64³ decays, 128³ blows up) → gate-14 within_tolerance=False is the CORRECT R-P2 verdict (D10); Stage 1c formalizes. Held-local .h5 sha256 6b5158e8…b47fe5; .json sha256 b8875b91…bf43c0 in the stage-1b checkpoint) |
| lattice-boltzmann-d3q19 | warp-cpu | poiseuille-64x32-seed42-step1000 | 5.565 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-25 | baseline (EIGHTH per-sim cross-stack port; THIRD Stack-E port; SECOND LBM port; spec § 11.3 item 2.5 Stack-E half; sibling to numpy-reference baseline 3.784s + Stack-D taichi-cpu 4.954s. Warp-cpu wall-clock 1.47× the numpy-reference baseline — WITHIN the 2× regression band, 1.12× of Stack-D taichi-cpu; per-step kernel-launch overhead on the small 64x32x3 grid sits above the NumPy floor as the Stage-0 §N estimate predicted. NVIDIA Warp 1.13.0 @wp.kernel D3Q19 BGK Qian-1992 equilibrium + Guo-2002 forcing; fused collide kernel + positive-mod streaming gather over an own wp.array(dtype=wp.float64, ndim=4); 64×32×3 × 1000 steps cadence-1 (1001 frames; 202,350,128 bytes — byte-size-identical to the Phase-1 ref capture; LFS-tracked). common-warp socket-only (D7); reciprocal-form feq + wp.float64(0.0) reduction seeds → the in-kernel f64 collision reproduces the NumPy reference byte-for-byte (Stage-0 R-A1; D8/R-LBME2). CANONICAL-SCALE DETERMINISM: O-2 ckpt-2 gate-10 production reproduction digest 393ef934…42179 (2/2 bit-identical). FULL-HORIZON cross-stack BIT-EXACT (max_abs_err 0.0 vs the Phase-1 NumPy reference across all 1001 frames; shape (a), the THIRD instance, FIRST on a LAMINAR trajectory — laminar Ma<0.1, BGK tau=0.7 dissipative; gate-14 within_tolerance=True predicted, Stage 1c formalizes). .h5/.json sha256s in the stage-1b checkpoint) |
| lattice-boltzmann-d3q19 | warp-cpu | couette-32x16-seed42-step500 | 0.883 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-25 | baseline (EIGHTH per-sim cross-stack port, SECOND of two canonical captures (D4 dual-capture); sibling to numpy-reference baseline 0.604s + Stack-D taichi-cpu 0.973s. Warp-cpu wall-clock 1.46× the numpy-reference baseline — WITHIN the 2× regression band, 0.91× of Stack-D taichi-cpu. Couette moving-top-plate at u=0.05 lattice; N_z=3 depth-3 slab; bottom no-slip + top moving-wall half-way bounce-back (Kruger 2017 momentum injection); 32×16×3 × 500 steps cadence-1 (501 frames; 27,405,152 bytes — byte-size-identical to the Phase-1 ref capture; LFS-tracked, the schema-corpus representative-subset per D14/§5.4). O-2 ckpt-3 canonical-scale 2-run determinism: h5 byte-identical run1==run2 (71cd6e14…). FULL-HORIZON cross-stack BIT-EXACT (max_abs_err 0.0 vs the Phase-1 NumPy reference across all 501 frames incl. the moving-wall injection; shape (a)). The within-sim cross-backend contrast to Stack-D taichi-cpu shape (b) ~6e-15 confirms methodology § 6.7 (seed-difference is a backend-pair property; Stage 2 note). .h5/.json sha256s in the stage-1b checkpoint) |
| reaction-diffusion-2d | vulkan-cpp | gray-scott-lambda-128sq-seed42-step2000 | 1.13 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-25 | baseline (8th and FINAL spec § 11.3 cross-stack port; FIRST Stack-C / Vulkan-C++ port; gate-12 row added at **Stage 2** — it was omitted at Stage 1b, restored here per the established per-port convention, banked S2-RD2C1). GLSL `double` Gray-Scott f64 on Mesa lavapipe (`VK_DRIVER_FILES=lvp_icd.json`, `LP_NUM_THREADS=0`), `precise`→SPIR-V `NoContraction`; warm-cache 1.13s (3-run stable). NOTE the RD-2D Stack-B numpy-reference (0.931s) + Stack-D taichi-cpu (0.568s) baselines are on **i7-7700HQ** — a DIFFERENT host — so a same-hardware cross-stack ratio is not available here; the >2× source-stack-baseline flag (spec § 2.15) is a Stage-9 cross-cutting concern (operator-deferred; Phase-2 plan § 2.12), not adjudicated at this sub-phase's Stage 2. FULL-HORIZON cross-stack BIT-EXACT (gate-14 max_abs_err 0.0 vs the Phase-1 NumPy reference across all 11 frames × {U,V}; shape (a), the THIRD instance overall and FIRST on a non-Warp backend). Determinism witness 493ffbba… (2-run bit-identical; O-2 ckpt 3). Capture .h5 sha256 00081dc42b… in the stage-1b checkpoint) |
| 3dgs-smoke | warp-cpu | 3dgs-smoke-36gaussians-128sq-frame0 | 0.006 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-28 | baseline (Phase-3 common-3dgs task-1; FIRST neural-rendered row. Forward EWA-splatting render-one-frame: 6×6=36 anisotropic Gaussians, SH degree 3, 128² image; NumPy projection/covariance/SH-colour preprocessing + stable depth sort, per-pixel front-to-back compositing in the Warp 1.13.0 @wp.kernel common_3dgs._kernels.composite_splats (warm-cache mean of 5). D-C bit-exact / same-stack-same-hw MEASURED + CONFIRMED: render() twice on identical inputs is byte-identical (max_abs_diff 0.0, identical sha256). Single-stack — no cross-stack gate-14. Render-one-frame, not a stepped trajectory, so no §N cadence applies) |
