# Performance Regression Ledger

Per `architecture.md` § 2.15. Each row records first-landing or
significant-change wall-clock for a `(sim, stack, descriptor)` tuple.
Non-blocking — surfaces at landing-audit review time.

A row is appended:

- On first canonical landing of a sim (first-landing baseline row).
- On every subsequent CI run that produces wall-clock differing by >10%
  from the prior recorded value.
- Rows with `wall_clock_seconds > 2 × first_landing_baseline` are flagged
  `regression: WATCH`.

| sim | stack | descriptor | wall_clock_seconds | hardware_id | commit_sha | date | regression |
|---|---|---|---|---|---|---|---|
| reaction-diffusion-2d | numpy-reference | gray-scott-lambda-128sq-seed42-step2000 | 0.931 | i7-7700HQ-linux-6.17 | (this commit) | 2026-05-19 | baseline |
| strange-attractors | numpy-reference | lorenz-trajectory-seed42-step10000 | 0.061 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-20 | baseline |
| mandelbulb-explorer | numpy-reference | de-probe-points-seed42 | 0.006 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-20 | baseline |
| boids-3d | numpy-reference | flock-3agents-canonical-seed42-step1000 | 0.033 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-20 | baseline |
| boids-3d | numpy-reference | flock-1000agents-seed42-step1000 | 17.592 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-20 | baseline |
| physarum | numpy-reference | network-canonical-seed42-step5000 | 3.128 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-20 | baseline |
| reaction-diffusion-3d | numpy-reference | gray-scott-lambda-64cube-seed42-step2000 | 10.144 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-20 | baseline |
| sph-water | numpy-reference + scipy.cKDTree + numba-@njit(fastmath=False, cache=True) | dam-break-100K-particles-seed42-step1000 | 1291.854 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-21 | baseline (100K-instance of Phase 1 R8 canonical 1M descriptor; full N=1M is Phase-2+ Stack-C scope per spec-ref § 5; R12+R16+R17+R18+R19+R20 routing arc) |
| eulerian-smoke | numpy-reference | taylor-green-128cube-seed42-step500 | 691.587 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-22 | baseline (Stam-Fedkiw stable-fluids 3D; cadence-50 capture per Stage 0 Task 0.4 — conventions doc § N first practical exercise; MacCormack-corrected semi-Lagrangian + n_jacobi=20 collocated projection) |
| eulerian-smoke | numpy-reference | lid-driven-cavity-128sq-re100-seed42-step1000 | 5.099 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-22 | baseline (Stam-Fedkiw 2D; full-cadence capture; periodic-BC approximation of the Dirichlet lid-driven cavity per sim spec-ref § 5 — Phase-2+ Stack-C C++ port implements proper Dirichlet) |
| lattice-boltzmann-d3q19 | numpy-reference | poiseuille-64x32-seed42-step1000 | 3.784 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-22 | baseline (D3Q19 BGK Qian-1992; full-cadence capture per Stage 0 Task 0.4 — second sub-phase exercising conventions doc § N as established discipline; N_z=3 z-periodic depth-3 slab convention; bounce-back y-walls + constant body-force in x; Guo 2002 forcing scheme) |
| lattice-boltzmann-d3q19 | numpy-reference | couette-32x16-seed42-step500 | 0.604 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-22 | baseline (D3Q19 BGK Qian-1992; full-cadence capture; N_z=3 depth-3 slab; bounce-back walls with moving top-plate at u=0.05 lattice) |
| mpm-multimaterial | numpy-numba-reference | drop-impact-128cube-seed42-step500 | 158.052 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-23 | baseline (MLS-MPM Hu-2018 quadratic-B-spline; 1M particles + 128³ grid; cadence-50 capture per Stage 0 Task 0.4 — third sub-phase exercising conventions doc § N as established discipline; soft neo-Hookean elastic (E=4kPa, ν=0.3); sticky floor at z-index 4; @njit(fastmath=False, cache=True) on P2G/G2P/grid-update/stress/deformation kernels per conventions doc § G; second numba-using sub-phase after sph-water; Stage 1 wall-clock 0.6× of Stage 0 projection — third data point on production-correction factor empirical range [0.5×, 2.6×]) |
| reaction-diffusion-2d | taichi-cpu | gray-scott-lambda-128sq-seed42-step2000 | 0.568 | i7-7700HQ-linux-6.17 | (this commit) | 2026-05-23 | baseline (FIRST per-sim Stack-D cross-stack port under spec-Phase-2; sibling row to Stack-B numpy-reference baseline 0.931s at 2026-05-19; Stack-D Taichi-cpu warm-cache wall-clock 0.61× Stack-B numpy-reference baseline — well below the 2× regression band; canonical descriptor + manifest + payload sha256s recorded in docs/_audits/phase-2/sub-phase-reaction-diffusion-2d-stack-d/stage-1b-checkpoint-2026-05-23T*.md) |
| sph-water | taichi-cpu | dam-break-100K-particles-seed42-step1000 | 252.346 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-24 | baseline (SECOND per-sim Stack-D cross-stack port; sibling row to Stack-B numpy-reference baseline 1291.854s at 2026-05-21; Stack-D Taichi-cpu wall-clock 0.195× the numpy-reference baseline — well below the 2× regression band. R-S3 instrumentation: combined DFSPH iters/step = 1 — the Phase-1 reference trajectory is explicit-Euler rigid free-fall + per-step SPH density side-effect, NOT an iterative pressure solve; the Stage-0 R-S3 ~28-32min @ k≈10 estimate assumed an iterative solver the reference does not use. Inlined 27-cell spatial-hash neighbor search; f64 via f64-typed ti.types.ndarray. Canonical descriptor + .h5/.json sha256s recorded in docs/_audits/phase-2/sub-phase-sph-water-stack-d/stage-1b-checkpoint-2026-05-24T*.md) |
| lattice-boltzmann-d3q19 | taichi-cpu | poiseuille-64x32-seed42-step1000 | 4.954 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-24 | baseline (THIRD per-sim Stack-D cross-stack port; sibling row to Stack-B numpy-reference baseline 3.784s at 2026-05-22; Stack-D Taichi-cpu wall-clock 1.31× the numpy-reference baseline — within the 2× regression band; per-step kernel-launch overhead on the small 64x32x3 grid sits above the NumPy floor as the Stage-0 probe predicted. FIRST cross-stack port with genuine in-kernel f64 reductions: per-cell 19-term collision-moment sums seeded ti.f64(0.0) per the Stage-0 banked finding (set_taichi_deterministic does not set default_fp). Integer streaming bit-exact vs np.roll. Both .h5/.json sha256s recorded in docs/_audits/phase-2/sub-phase-lattice-boltzmann-d3q19-stack-d/stage-1b-checkpoint-2026-05-24T*.md) |
| lattice-boltzmann-d3q19 | taichi-cpu | couette-32x16-seed42-step500 | 0.973 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-24 | baseline (THIRD per-sim Stack-D cross-stack port, SECOND of two canonical captures (D4 dual-capture); sibling row to Stack-B numpy-reference baseline 0.604s at 2026-05-22; Stack-D Taichi-cpu wall-clock 1.61× the numpy-reference baseline — within the 2× regression band. Couette moving-top-plate at u=0.05 lattice; N_z=3 depth-3 slab; FIRST port adding TWO perf-ledger rows for one sub-phase) |
| mpm-multimaterial | taichi-cpu | drop-impact-128cube-seed42-step500 | 360.773 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-24 | baseline (FOURTH per-sim Stack-D cross-stack port; sibling row to numpy-numba-reference baseline 158.052s at 2026-05-23; Stack-D Taichi-cpu wall-clock 2.28× the numpy-numba-reference baseline — EXCEEDS the 2× regression band, FLAGGED per spec § 2.15 for landing-audit review. Attributable to posture-(i) cpu_max_num_threads=1 serialisation (REQUIRED for deterministic P2G atomic-scatter per Stage-0 Task 0.3: threads=8 is NOT run-to-run bit-exact) + per-step kernel-launch overhead (500 steps × ~6 @ti.kernel launches = ~3000 launches) over 1M particles; the serialised posture trades parallel speed for bit-exact determinism — first Stack-D port to exceed 2×. f64 accumulator seeds ti.f64(0.0) per Stage-0 + LBM banked. R-M2: j_det=1.000000 + n(j_det<=0)=0 across all 11 frames — the canonical drop-impact is rigid free-fall (the blob does not deform within the 500-step horizon), so the informal gate-14 cross-stack diff vs the numba reference is bit-exact particle_pos (0.0) / ~6.2e-28 particle_vel / ~1.5e-32 grid_mom (far below 1e-4; below the prior pairs' ~1e-15). Capture .h5/.json sha256s in docs/_audits/phase-2/sub-phase-mpm-multimaterial-stack-d/stage-1b-checkpoint-2026-05-24T*.md) |
| eulerian-smoke | taichi-cpu | lid-driven-cavity-128sq-re100-seed42-step1000 | 8.470 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-24 | baseline (FIFTH per-sim Stack-D cross-stack port; sibling row to numpy-reference baseline 5.099s; Stack-D Taichi-cpu wall-clock 1.66× the numpy-reference baseline — within the 2× regression band. MacCormack-2D velocity advect + fixed-20-sweep Jacobi project + plain-SL density advect; collocated cell-centered periodic. HARD-RULE-2 FINDING (gate-14): this 2D canonical is numerically UNSTABLE — the thin lid-shear-layer on a periodic grid is Kelvin-Helmholtz unstable (reference u→1.6e3 by step 5), NOT the steady-laminar Re=100 the plan-drafting probe characterized; cross-stack within_tolerance=False (IC-15 aspect #1 chaotic EXERCISED). The port matches NumPy to ~1e-16 while stable (step ≤2). Operator routing pending; see stage-1 checkpoint) |
| eulerian-smoke | taichi-cpu | taylor-green-128cube-seed42-step500 | 698.986 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-24 | baseline (FIFTH per-sim Stack-D cross-stack port, SECOND of two canonical captures (D4); sibling row to numpy-reference baseline 691.587s; Stack-D Taichi-cpu wall-clock 1.01× the numpy-reference baseline — essentially parity, within the 2× regression band. Plain trilinear SL + 7pt diffuse + fixed-20-sweep 3D Jacobi (inv6 seeded ti.f64(1.0)/ti.f64(6.0) per banked precedent #7 — NON-vacuous; bare 1.0/6.0 infers f32 and leaked ~1e-9). HARD-RULE-2 FINDING (gate-14): this 3D canonical is numerically UNSTABLE — the reference Taylor-Green blows up under collocated-grid/under-resolved-Jacobi numerics (reference max|u| 0.999→8.1e7 [step50]→5.1e19 [step250]; Stack-D →1.2e19), NOT the laminar decaying vortex characterized; cross-stack within_tolerance=False (IC-15 aspect #1 chaotic EXERCISED; backends diverge completely). The 738MB capture is held local pending operator routing (LFS-bandwidth conservation, D13); see stage-1 checkpoint) |
| mpm-multimaterial | warp-cpu | drop-impact-16cube-seed42-step50 | 0.108 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-25 | baseline (SIXTH per-sim cross-stack port; FIRST Stack-E port; DIAGNOSTIC-tier row at Stage 1a — the full 128cube canonical warp-cpu row lands at Stage 1b. NVIDIA Warp 1.13.0 @wp.kernel MLS-MPM/APIC neo-Hookean single-material; 16³ grid × 5K particles × 50 steps. Warp CPU wp.launch is structurally serial → P2G wp.atomic_add bit-exact (D5/banked #8; no cpu_max_num_threads=1 equivalent); f64 throughout with wp.float64() literal seeds (banked #7 / O-W7). R-A1 anchor: the production P2G reproduces the Stage-0 Task-0.6 verification digest a8f6e654…07ff1fe1 EXACTLY; step-1 P2G mass-conservation abs_err 2.22e-16 (partition-of-unity exact). Trajectory reproduces the Phase-1 diagnostic to printed precision (max|vel|=2.049050, min pos_z=0.491517 at step 50 — rigid free-fall; gate-14 cross-stack expected at FP-round-off, Stage 1c). Capture .h5/.json sha256s in the stage-1a checkpoint) |
| mpm-multimaterial | warp-cpu | drop-impact-128cube-seed42-step500 | 304.492 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-25 | baseline (SIXTH per-sim cross-stack port; FIRST Stack-E port; CANONICAL row at Stage 1b; sibling to numpy-numba-reference baseline 158.052s + Stack-D taichi-cpu 360.773s. Warp-cpu wall-clock 1.93× the numpy-numba-reference baseline — WITHIN the 2× regression band (contrast Stack-D's 2.28× FLAGGED) and 0.84× of Stack-D taichi-cpu. 1M particles × 128³ grid × 500 steps cadence-50 (11 frames; 1,125,718,712 bytes ≈ 1.05 GiB, matching the Phase-1 ref capture size; LFS-tracked). Warp CPU wp.launch structurally serial → P2G wp.atomic_add bit-exact (D5/banked #8; no cpu_max_num_threads=1 equivalent); per-step NumPy↔Warp marshalling over ~3000 launches. CANONICAL-SCALE DETERMINISM 2/2 content-digest MATCH (dd62c046…7586ac80 both runs); canonical-scale mass-conservation abs_err 4.44e-16 (partition-of-unity exact; same order as the diagnostic 2.22e-16). R-M2: the drop-impact is rigid free-fall (the blob does not deform within the 500-step horizon → F=I → zero stress), so the gate-14 cross-stack diff vs the numba reference is expected at FP-round-off (within_tolerance=True predicted; Stage 1c). Capture .h5/.json sha256s in the stage-1b checkpoint) |
| eulerian-smoke | warp-cpu | lid-driven-cavity-128sq-re100-seed42-step1000 | 5.897 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-25 | baseline (SEVENTH per-sim cross-stack port; SECOND Stack-E port; spec § 11.3 item 2.4 Stack-E half; sibling to numpy-reference baseline 5.099s + Stack-D taichi-cpu 8.470s. Warp-cpu wall-clock 1.16× the numpy-reference baseline — WITHIN the 2× regression band, 0.70× of Stack-D taichi-cpu. NVIDIA Warp 1.13.0 @wp.kernel Stam-Fedkiw stable-fluids 2D: MacCormack velocity advect + fixed-20-sweep Jacobi project + plain-SL density advect; collocated cell-centered periodic; 128² × 1000 steps cadence-100 (11 frames; 4,385,176 bytes — byte-size-identical to the Phase-1 ref capture; LFS-tracked, the schema-corpus representative-subset per D14/§5.4). common-warp socket-only (D7); own wp.array(dtype=wp.float64) (D15); Warp CPU wp.launch structurally serial → gather kernels bit-exact (no atomic scatter; no cpu_max_num_threads=1 equivalent). CANONICAL-SCALE DETERMINISM 2/2 content bit-identical (worst_abs_diff 0.0; O-2 checkpoint 3). STEP-1 cross-stack BIT-EXACT (max_abs_err 0.0 vs the Phase-1 NumPy reference — np.roll order + np.mod replicated). HARD-RULE-2: this 2D canonical is CHAOTIC (Kelvin-Helmholtz; reference u→1.6e3 by step 5) → gate-14 within_tolerance=False is the CORRECT R-P2 verdict (D10; the SECOND R-P2 instance, FIRST on Stack-E — stack-portable); Stage 1c formalizes. .h5/LFS-oid sha256 aa67929f…214734; .json committed-blob sha256 in the stage-1b checkpoint) |
| eulerian-smoke | warp-cpu | taylor-green-128cube-seed42-step500 | 541.977 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-25 | baseline (SEVENTH per-sim cross-stack port, SECOND of two canonical captures (D4); sibling to numpy-reference baseline 691.587s + Stack-D taichi-cpu 698.986s. Warp-cpu wall-clock 0.78× the numpy-reference baseline — UNDER baseline, well within the 2× regression band; 0.78× of Stack-D taichi-cpu. Plain trilinear SL + 7pt diffuse + fixed-20-sweep 3D Jacobi (inv6 seeded wp.float64(1.0)/wp.float64(6.0) per banked #7 / O-W7 — bare 1.0/6.0 infers f32 and leaked ~1e-9 in Taichi). 128³ × 500 steps cadence-50 (11 frames; 738,260,192 bytes ≈ 738 MB — byte-size-identical to the Phase-1 ref capture; HELD LOCAL per D14, NOT committed, LFS-bandwidth conservation). STEP-1 cross-stack BIT-EXACT (max_abs_err 0.0 vs the Phase-1 NumPy reference). HARD-RULE-2: this 3D canonical is CHAOTIC — the reference Taylor-Green blows up under collocated-grid/under-resolved-Jacobi numerics (energy 2.6e5 → 3.2e19 [step50] → ~6.7e45 plateau; max|u|~5e19), resolution-dependent (R-SME9: 64³ decays, 128³ blows up) → gate-14 within_tolerance=False is the CORRECT R-P2 verdict (D10); Stage 1c formalizes. Held-local .h5 sha256 6b5158e8…b47fe5; .json sha256 b8875b91…bf43c0 in the stage-1b checkpoint) |
| lattice-boltzmann-d3q19 | warp-cpu | poiseuille-64x32-seed42-step1000 | 5.565 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-25 | baseline (EIGHTH per-sim cross-stack port; THIRD Stack-E port; SECOND LBM port; spec § 11.3 item 2.5 Stack-E half; sibling to numpy-reference baseline 3.784s + Stack-D taichi-cpu 4.954s. Warp-cpu wall-clock 1.47× the numpy-reference baseline — WITHIN the 2× regression band, 1.12× of Stack-D taichi-cpu; per-step kernel-launch overhead on the small 64x32x3 grid sits above the NumPy floor as the Stage-0 §N estimate predicted. NVIDIA Warp 1.13.0 @wp.kernel D3Q19 BGK Qian-1992 equilibrium + Guo-2002 forcing; fused collide kernel + positive-mod streaming gather over an own wp.array(dtype=wp.float64, ndim=4); 64×32×3 × 1000 steps cadence-1 (1001 frames; 202,350,128 bytes — byte-size-identical to the Phase-1 ref capture; LFS-tracked). common-warp socket-only (D7); reciprocal-form feq + wp.float64(0.0) reduction seeds → the in-kernel f64 collision reproduces the NumPy reference byte-for-byte (Stage-0 R-A1; D8/R-LBME2). CANONICAL-SCALE DETERMINISM: O-2 ckpt-2 gate-10 production reproduction digest 393ef934…42179 (2/2 bit-identical). FULL-HORIZON cross-stack BIT-EXACT (max_abs_err 0.0 vs the Phase-1 NumPy reference across all 1001 frames; shape (a), the THIRD instance, FIRST on a LAMINAR trajectory — laminar Ma<0.1, BGK tau=0.7 dissipative; gate-14 within_tolerance=True predicted, Stage 1c formalizes). .h5/.json sha256s in the stage-1b checkpoint) |
| lattice-boltzmann-d3q19 | warp-cpu | couette-32x16-seed42-step500 | 0.883 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-25 | baseline (EIGHTH per-sim cross-stack port, SECOND of two canonical captures (D4 dual-capture); sibling to numpy-reference baseline 0.604s + Stack-D taichi-cpu 0.973s. Warp-cpu wall-clock 1.46× the numpy-reference baseline — WITHIN the 2× regression band, 0.91× of Stack-D taichi-cpu. Couette moving-top-plate at u=0.05 lattice; N_z=3 depth-3 slab; bottom no-slip + top moving-wall half-way bounce-back (Kruger 2017 momentum injection); 32×16×3 × 500 steps cadence-1 (501 frames; 27,405,152 bytes — byte-size-identical to the Phase-1 ref capture; LFS-tracked, the schema-corpus representative-subset per D14/§5.4). O-2 ckpt-3 canonical-scale 2-run determinism: h5 byte-identical run1==run2 (71cd6e14…). FULL-HORIZON cross-stack BIT-EXACT (max_abs_err 0.0 vs the Phase-1 NumPy reference across all 501 frames incl. the moving-wall injection; shape (a)). The within-sim cross-backend contrast to Stack-D taichi-cpu shape (b) ~6e-15 confirms methodology § 6.7 (seed-difference is a backend-pair property; Stage 2 note). .h5/.json sha256s in the stage-1b checkpoint) |
| reaction-diffusion-2d | vulkan-cpp | gray-scott-lambda-128sq-seed42-step2000 | 1.13 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-25 | baseline (8th and FINAL spec § 11.3 cross-stack port; FIRST Stack-C / Vulkan-C++ port; gate-12 row added at **Stage 2** — it was omitted at Stage 1b, restored here per the established per-port convention, banked S2-RD2C1). GLSL `double` Gray-Scott f64 on Mesa lavapipe (`VK_DRIVER_FILES=lvp_icd.json`, `LP_NUM_THREADS=0`), `precise`→SPIR-V `NoContraction`; warm-cache 1.13s (3-run stable). NOTE the RD-2D Stack-B numpy-reference (0.931s) + Stack-D taichi-cpu (0.568s) baselines are on **i7-7700HQ** — a DIFFERENT host — so a same-hardware cross-stack ratio is not available here; the >2× source-stack-baseline flag (spec § 2.15) is a Stage-9 cross-cutting concern (operator-deferred; Phase-2 plan § 2.12), not adjudicated at this sub-phase's Stage 2. FULL-HORIZON cross-stack BIT-EXACT (gate-14 max_abs_err 0.0 vs the Phase-1 NumPy reference across all 11 frames × {U,V}; shape (a), the THIRD instance overall and FIRST on a non-Warp backend). Determinism witness 493ffbba… (2-run bit-identical; O-2 ckpt 3). Capture .h5 sha256 00081dc42b… in the stage-1b checkpoint) |
| 3dgs-smoke | warp-cpu | 3dgs-smoke-36gaussians-128sq-frame0 | 0.006 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-28 | baseline (Phase-3 common-3dgs task-1; FIRST neural-rendered row. Forward EWA-splatting render-one-frame: 6×6=36 anisotropic Gaussians, SH degree 3, 128² image; NumPy projection/covariance/SH-colour preprocessing + stable depth sort, per-pixel front-to-back compositing in the Warp 1.13.0 @wp.kernel common_3dgs._kernels.composite_splats (warm-cache mean of 5). D-C bit-exact / same-stack-same-hw MEASURED + CONFIRMED: render() twice on identical inputs is byte-identical (max_abs_diff 0.0, identical sha256). Single-stack — no cross-stack gate-14. Render-one-frame, not a stepped trajectory, so no §N cadence applies) |
| lenia | python (Taichi) | orbium-unicaudatus-64sq-seed42-step100 | 0.797 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-28 | baseline (Phase-3 task-3 sub-phase-phase-3-lenia; FIRST SIM row for Phase 3, FIRST `continuous-ca/lenia` row in the ledger. Stack-D Taichi-cpu real-space Quad4 convolution + clip-Euler update under set_taichi_deterministic(arch='cpu') + cpu_max_num_threads=1 (deterministic posture per docs/common/taichi.md § 2). The descriptor `orbium-unicaudatus-64sq-seed42-step100` reflects the Stage-1b smoke-scale capture (the larger orbium-unicaudatus-256sq-seed42-step1000 canonical from §6.3 K's wall_clock target was de-scoped to the smoke descriptor at Stage 1b to keep the perf row reproducible within the sub-phase session; the descriptor is locked + reproducible at this commit). Wall-clock includes the warm-up Taichi compile of `lenia_convolve` + `lenia_update`; subsequent runs amortise. D-DET MEASURED bit-exact same-stack-same-hw: two runs at the same seed produce np.array_equal field outputs. No atomics in forward conv per spec-ref § 8. §0.3 SHIFT-on-evidence at Stage 1b: the dispatch-suggested mass_approximately_conserved PBT invariant was mathematically falsified for arbitrary IC under Quad4 polynomial growth gn=1; re-declared as monotone_bounds + per_step_change_bounded_by_dt per spec-ref §6. Capture .h5/.json sha256s in the Stage-1b audit) |
| ising-classical | numpy-reference | metropolis-128sq-T2.27-seed42-step10000 | 5.558 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-28 | baseline (Phase-3 task-3a sub-phase-phase-3-ising-classical; FIRST `lattice-spin` row + FIRST Stack-B SIM row in Phase 3. Metropolis-Hastings checkerboard (red/black) Monte Carlo, 128² square lattice, J=1, h=0, T=2.27≈T_c, periodic BCs, 10000 sweeps (capture every 1000 → 11 frames). Wall-clock is the NumPy reference (the CI-visible oracle that produces the committed canonical capture); the Stack-B WGSL parallel-Metropolis kernel (src/metropolis.wgsl, PCG per-cell + checkerboard, no atomics/subgroup ops) is LOCAL-ONLY per spec §7.8 (CI runners have no GPU) and is unmeasured this session — §0.3 note: the §6.3a literal "typescript (WebGPU)" stack-label is recorded as numpy-reference, mirroring the RD-2D perf-row precedent which also records the numpy-reference oracle. D-WEBGPU-DET MEASURED bit-exact same-stack-same-hw: run_twice_and_diff(sim_runner_seeded, seed=42) → np.array_equal spin captures (max_abs_diff 0.0). Single-stack — no cross-stack gate. Closed-form golden anchors Onsager T_c=2/ln(1+√2) + Yang m(T) + Kramers-Wannier duality; aligned-IC MC |m| matches Yang within magnetization_rel=5e-2. Capture .h5/.json sha256s in the Stage-1b audit) |
| articulated-pedagogical | warp-cpu | pendulum-trajectory-seed42-step1000 | 0.194 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-29 | baseline (Phase-3 task-4 sub-phase-phase-3-rigid-body; FIRST `rigid-body` row + FIRST Stack-E SIM row in Phase 3. Featherstone ABA reduced-coordinate forward dynamics for a planar single-revolute pendulum (L=1, m=1, g=9.81, θ₀=2.0 rad); semi-implicit Euler, dt=1e-3, 1000 steps, cadence-10 (101 captured frames). NVIDIA Warp 1.13.0 single-thread `wp.launch(dim=1)` f64 ABA kernel + NumPy host integration loop. D-DET MEASURED bit-exact same-stack-same-hw: assert_deterministic_run 3 runs byte-identical (digest 468cb14f…); registry `[rigid-body.articulated-pedagogical]` class=bit-exact HELD. Single-stack — no cross-stack gate-14, no mutation (sim). Goldens: single-pendulum A1/A2/A3 analytic anchors (scipy ellipk/ellipj independent refs) within pendulum_period_rel=1e-3/trajectory_abs=1e-2; double-pendulum vs independent closed-form-EOM RK4 reference; 6-DOF energy conservation (secular drift 3.3e-5/s < 1e-3). canonical .h5 sha256/LFS-oid ac346f22…5ed45fb1; .json sha256 c3833f04…b3a501c8. Schema-corpus fixture tests/fixtures/legacy-captures/phase-3-rigid-body-pedagogical.{h5,json} (same .h5 payload, oid ac346f22…; .json sha256 873c2cfe…). Capture sha256s in the Stage-1c audit) |
| mass-spring-cloth | cpp (Vulkan) | flag-wind-128x128-seed42-step1000 | 54.32 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-29 | baseline (Phase-3 task-5 sub-phase-phase-3-mass-spring-cloth; FIRST `soft-body` row + FIRST NEW Stack-C SIM row in Phase 3 — RD-2D-Stack-C is a Phase-1 port. XPBD mass-spring cloth (Macklin 2016; Provot structural/shear/bending all as XPBD distance constraints) on Stack C (C++20 / Vulkan compute), 128×128 grid (16384 particles, ~97k constraints), left-edge-pinned flag under gravity (0,-9.81,0) + steady wind (0,0,3.0), dt=1/60, 20 GS iterations/step, 1000 steps (capture every 100 → 11 frames). Wall-clock is the single-invocation SYMMETRIC serial Gauss-Seidel projection on the lavapipe CPU backend (LP_NUM_THREADS=0; the pedagogical determinism posture — one workgroup, one invocation, fixed alternating constraint order, no atomics/subgroups). D-DET MEASURED bit-exact same-stack-same-hw at smaller scale (16×16 witness 0433ccb4… stable across two separate process invocations; assert_deterministic_run tolerance=0 passes); the canonical 128×128 capture used --no-determinism-check (single run; determinism measured separately). Registry `[soft-body.mass-spring-cloth]` class=bit-exact HELD. Single-stack — no cross-stack gate-14, no mutation (sim). Goldens: hanging chain vs analytic catenary (Beer&Johnston Ch7 / hand-derivation / M&T §6.6 — 3 independent anchors) within catenary_shape_rel=2e-3 (MEASURED stiff-limit residual: XPBD inextensible-limit chain matches the analytic catenary to 0.119% of sag); stretched chain uniform linear-elastic within position_abs=1e-2. canonical .h5 sha256/LFS-oid 7954eb6c…68cd290b; .json sha256 6ed0ecb4…84f00e25. Schema-corpus fixture tests/fixtures/legacy-captures/phase-3-mass-spring-cloth.{h5,json} (.h5 oid 0df87788…36d2c70a; .json sha256 451a1d17…667580b6). Capture sha256s in the Stage-1c audit) |
| neural-ca | python (PyTorch) | growing-emoji-64sq-seed42-step1000 (training) | 1271.14 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-29 | baseline (Phase-3 task-6 sub-phase-phase-3-neural-ca; FIRST `continuous-ca/neural-ca` row + FIRST dual-stack SIM + FIRST learned-dynamics row in Phase 3; gate-12 Stack-D row = the characteristic PyTorch TRAINING op per charter §6 (not inference). 64² grid, 16-channel NCA, 1000 training steps, seed 42, Persistent sample-pool experiment (pool_size 1024, rollout 64-96), pixel-wise-L2 (Distill objective) + per-parameter grad normalization + Adam lr=2e-3 (MultiStepLR drop at 700), CPU-only torch 2.12.0+cu130. DETERMINISM: same-seed training reproduces BIT-IDENTICAL run-to-run on this CPU (final L2 0.029722 both runs; checkpoint np.array_equal) — registry `[continuous-ca.neural-ca.training]` class=non-deterministic-by-design (cross-seed) + EFECT distributional bound (training_loss_3sigma_upper 0.07, 5-seed CV 0.21). The Growing variant overgrows by ~step 200; the Persistent variant holds the pattern to step 1000 (recon L2 0.022@step200 / 0.095@step1000). Checkpoint neural-ca-emoji-disk.safetensors (LFS); D-inference capture sha256s in the Stage-1b-D audit) |
| neural-ca | typescript (WGSL/WebGPU) | growing-emoji-64sq-seed42-step1000-wgsl (inference) | 2.18 | i7-12700KF+RX6800XT-linux-6.17 | (this commit) | 2026-05-29 | baseline (Phase-3 task-6; gate-12 Stack-B row = WGSL forward INFERENCE per charter §6. 1000 steps × 2 dispatches (update + two-pass alive-mask) on the AMD Radeon RX 6800 XT (RADV/Vulkan) via wgpu-py (wgpu-native) — the §7.8 local-only path that generates the committed B-inference capture; CI NEVER runs WGSL (§0.3: no Node WebGPU runtime here — only @webgpu/types/no deno — so the committed .wgsl runs via wgpu-py; index.ts is the Phase-5 deploy path). Cross-stack gate-14 D↔B render-similarity: mean PSNR 23.92 / SSIM 0.824 / LPIPS_alex 0.0316 (statistical; PSNR/SSIM below §2.12 floors = QUALITY-CONCERN flag NOT auto-fail; LPIPS PASSES — perceptually equivalent). NumPy oracle reproduces this GPU capture to 3.5e-6 (gate-13). B-inference capture + WGSL artifact sha256s in the Stage-1b-B audit) |
| pinn-poisson | python (PyTorch+Warp-CPU) | poisson-sine-source-64sq-seed42-step1 | 127.9 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-29 | baseline (Phase-3 task-7 sub-phase-phase-3-pinn-poisson; FIRST `learned-dynamics/pinn-poisson` row + FIRST learned-dynamics-CATEGORY gate-12 row; SECOND learned-dynamics row after neural-ca. gate-12 value = the **TRAINING wall-clock** recorded SEPARATELY per charter §7 (S2-RD2C1 lesson — do NOT silently omit): one Adam(2000)→L-BFGS(2000, strong-Wolfe) run of the soft-constraint Raissi-2019 PINN on the Anchor-3 MMS problem (u=sin(πx)sin(πy), f=−2π²·u, zero Dirichlet), seed 42, hidden 4×60 tanh MLP, 3000 interior + 100×4 boundary collocation, CPU-only torch 2.12.0+cu130. **CPU PINN training is iteration-heavy — 127.9s is EXPECTED, NOT a regression** (charter §1.1). The inference + torch→wp→Capture-bridge step is ~0.0022s (recorded separately; the canonical 64×64 capture has no time axis — step1 = single steady-BVP eval). MEASURED rel-L2 vs analytic: A1 3.2e-4 / A2 ~3e-4 / A3 2.27e-4 (all ≤ analytical_l2=1e-3); A3-vs-FD 3.4e-4 (≤ fd_l2=1e-2). D-DET MEASURED: same-seed CPU training BIT-IDENTICAL (registry `[learned-dynamics.pinn-poisson.training]` class=non-deterministic cross-seed + EFECT 3σ 4.44e-6, NO STOP-EFECT; `[…inference]` bit-exact bit-identical). The classical FD reference's observed convergence orders [2.0023,2.0005,2.0001]→O(h²). Single-stack — no cross-stack gate-14, no mutation (D-MUTATION defers the FD-reference target to task-9; confirmed against the live mutmut-config which has NO classical-references target). Checkpoint tools/testkit/golden/checkpoints/pinn-poisson-mms-seed42.safetensors (LFS oid 9c6c179e…); canonical .h5/LFS-oid c935fb58…; schema-corpus fixture tests/fixtures/legacy-captures/phase-3-pinn-poisson.{h5,json}. Capture sha256s in the Stage-1c audit) |
| 3dgs-mpm | warp-cpu | drop-blob-256gaussians-32grid-step300 | 0.313 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-29 | baseline (Phase-3 task-8 sub-phase-phase-3-3dgs-mpm; the Phase-3 FINALE; FIRST `neural-rendered/3dgs-mpm` row + FIRST coupled-pipeline gate-12 row. gate-12 value = the characteristic coupled op: the full MPM-sim-from-scratch -> per-particle deformation-gradient F -> PhysGaussian Eq.(8) coupling Sigma'=F A F^T -> common-3dgs render, over the canonical synthetic blob (256 Gaussians 1:1-bound to 256 MPM particles; 32^3 MLS-MPM grid; neo-Hookean elastic; dt=1e-3; 300 steps; capture+render every 100 -> frames 0/100/200/300). Warm-cache wall-clock 0.313s (3-run min; warp kernel compile excluded). **CPU memory** peak ~169 MB for the sim (the §6.8 "GPU memory peak" note is moot CPU-only); the full pytest suite (with the LPIPS/torch Prong-2 model) peaks ~1.3 GB. D-CI SINGLE-job (no L-PINN-2 two-tier split): the full coupled suite is ~3.7s wall on the CI runner — far below the split threshold (pinn-poisson split at ~70 min training). D-DET MEASURED bit-exact same-stack-same-hw (HELD): two runs byte-identical across frames + particle_F + gaussian params + capture .h5 sha256; registry `[neural-rendered.3dgs-mpm]` class=bit-exact HELD. Two-pronged gate-4: (1) numerical coupling golden 3 anchors (Eq.(8) covariance transform / polar-decomp stretch §2.4-caveat / F=I identity) within 1e-9; (2) render-similarity vs OWN committed golden PNGs PSNR 59.94-63.80 dB / SSIM 0.99973-0.99992 / LPIPS 0.00001 — clears the §2.12 floors (28/0.85/0.15) by a huge margin (deterministic own-pipeline regression; residual = 8-bit PNG quantization only; NOT below-floor -> NO STOP-RENDER-FLOOR). Single-stack — no cross-stack gate-14, no mutation (sim; coupling.py is sim-local, common-3dgs baseline is task-1's, Phase-4 WU-C extends). PhysGaussian CITE-ONLY (no upstream LICENSE; A-7). Golden renders tools/testkit/golden/renders/3dgs-mpm-canonical-frame-{0,100,200,300}.png + schema-corpus fixture sha256s in the Stage-1c audit) |
| common-3dgs | warp-cpu | smoke-render-256-gaussians | 0.143 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-30 | baseline (Phase-4 WU-C; common-3dgs maturation. Warm-cache wall-clock (3-run min) of `render` over a 256-Gaussian synthetic scene (sh_degree 3, seed 42) at 256×256 on the Warp CPU backend — CUDA absent on this host. WU-C extends the Phase-3 task-1 common-3dgs surface with TrainingLoop/TrainingHistory (FD reference optimiser; differentiable-rasterizer training deferred per-sim per plan §2523), PhysicsCoupling (PhysGaussian Eq.(8)), viewer (render_to_image CI-gated + launch_interactive_viewer runtime-only §7.8), and testkit render_similarity ms_ssim (Wang/Simoncelli/Bovik 2003) + RenderSimilarityReport. 3 anchors: Kerbl 2023 Eq.6 alpha-compositing (render_values), PhysGaussian Eq.(8) Σ'=FΣFᵀ (coupling), hand-derived F=I identity. 2 PBT: render_similarity_self_identity + gaussian_serialization_round_trip. Mutation measured in the WU-C audit) |
| common-warp-usd | python (usd-core) | usd-round-trip-100steps-8bodies | 0.032 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-30 | baseline (Phase-4 WU-D; the "USD export validates" half of the operator-ratified CPU-fallback acceptance — spec §12.8 + plan §7.5. Warm wall-clock (3-run min) of export_capture_to_usd over a 100-step, 8-body synthetic capture → time-sampled UsdGeom.Xform USD stage, usd-core 26.5. The companion `common-warp-newton smoke-pendulum` row is BLOCKED: Newton's solver runtime needs CUDA 12 / driver 545+, absent on this CPU-only host (the wrapper lazy-imports + raises a clear RuntimeError; metadata surface + USD export validate on CPU). Newton v1.0.0 d6046f18 Apache-2.0 vendored references/newton/. PBT usd_round_trip_preserves_pose + determinism_declaration_consistent; the suggested solver_no_overpenetration PBT is CUDA-BLOCKED. See docs/_audits/phase-4/wu-d-*.md) |
| common-py-learned | python (PyTorch + Lightning) | smoke-train-1-epoch-fake-data | 0.037 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-30 | baseline (Phase-4 WU-E; learning harness. Wall-clock of a 1-epoch lightning.pytorch.Trainer.fit over a 64-sample fake TensorDataset (batch 8) with a trivial Linear LightningModule on CPU — validates the default_trainer/Lightning path end-to-end. lightning 2.6.5 + nvidia-physicsnemo 2.1.0 (pins re-resolved live; plan's "1.x" stale per A-6). CaptureDataset (deterministic seed-pinned split), CaptureLightningDataModule, default_trainer (seed_everything+deterministic, ModelCheckpoint topk=3, EarlyStopping); warp_to_torch/torch_to_warp bridges; PhysicsNeMoAdapter (lazy physicsnemo; physicsnemo.Module wrapper). common_py.learned NOT eagerly registered (§0.3 SHIFT — avoids forcing torch+lightning on Taichi-based common-py consumers). 2 PBT: dataset_split_no_overlap + seed_determinism_within_lightning. See docs/_audits/phase-4/wu-e-*.md) |
| reaction-diffusion-2d-diff | python (Taichi) | rd2d-diff-recover-Du-16sq-seed42 | 0.504 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-31 | baseline (Phase-4 batch-1 sim 1/4; FIRST `continuous-ca/reaction-diffusion-2d-diff` row + FIRST differentiable-variant gate-12 row in Phase 4. gate-12 value = the characteristic differentiable op: ONE full inverse-problem solve (plant D_u=0.16, synthesize the target final-u field, recover via Adam lr=5e-3 max_iter=400 tol=1e-12 over the tape-differentiable Gray-Scott forward, n=16 grid, 8 steps), warm-cache 3-run min (Taichi ti.ad.Tape compile excluded; ~158 Adam iters to convergence, recovered err ~1e-6). D-DET MEASURED bit-exact same-stack-same-hw: forward AND gradient (ti.ad.Tape adjoint) both bit-identical run-to-run (single-thread CPU f64, seed-pinned smooth IC); registry `[continuous-ca.reaction-diffusion-2d-diff.{forward,gradient}]` HOLD; no EFECT (no training-loss distribution — the differentiable-batch relief vs learned-dynamics). Single-stack — gate-14 N/A (no cross-stack diff sibling); WU-F differentiable-axis variant-equivalence applies (diff.forward == reaction-diffusion-2d-stack-d reference <= 1e-12, well within rel<=1e-3 cap 1e-2). Gradient golden 3 DISTINCT independent anchors: A1 discrete-Fourier-eigenmode analytic (autodiff == analytic to 1.8e-15, EXACT for the discrete periodic Laplacian — Strauss 2e §4.1+Ch.5), A2 central-FD baseline, A3 reaction-ODE-limit analytic dLoss/dF (well-mixed, autodiff == analytic to ~0). FIRST real consumer of the WU-A schema-1.1.0 `gradient_fields` capture key (dLoss_dDu). Closes the 4.1 §1.D `reaction_diffusion_2d_mms` mutation-coverage gap. Capture sha256s in the Stage-1c/landing audit) |
| lenia-diff | python (Taichi) | lenia-diff-recover-mu-16sq-seed42 | 0.693 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-31 | baseline (Phase-4 batch-1 sim 2/4; FIRST `continuous-ca/lenia-diff` row. gate-12 value = the characteristic differentiable op: ONE full inverse-problem solve (recover the growth center mu with sigma held at its known value 0.15, plant mu=0.30, synthesize the target final field, recover via Adam lr=2e-2 max_iter=1500 tol=1e-14 over the tape-differentiable Quad4-Lenia forward — ti.static-unrolled real-space conv + Quad4 polynomial growth + clip-Euler, grid=16, R=3, 4 steps), warm wall-clock (~226 Adam iters to convergence; recovered mu=0.30000000, loss 2.7e-15). D-DET MEASURED bit-exact same-stack-same-hw: forward AND gradient (ti.ad.Tape adjoint) both bit-identical run-to-run (single-thread CPU f64, seed-pinned smooth IC); registry `[continuous-ca.lenia-diff.{forward,gradient}]` HOLD; no EFECT. Single-stack — gate-14 N/A; WU-F differentiable-axis variant-equivalence applies (diff.forward == lenia reference step, MEASURED BIT-EXACT < 1e-12, well within rel<=1e-3 cap 1e-2). Gradient golden 3 DISTINCT independent anchors: A1 closed-form Quad4 growth-parameter analytic (autodiff == analytic to ~1e-14), A2 central-FD baseline, A3 convolution-Jacobian + growth-deriv adjoint dLoss/dA0 (autodiff == analytic to ~1e-14; distinct physical term/parameter-class/method). D-ANCHOR Stage-0 SHIFT: charter A3=dK/dkernel-params ill-posed (Quad4 kernel parameter-free) → re-declared to the convolution-Jacobian field gradient. INVERSE identifiability finding: joint (mu,sigma) non-identifiable in the smooth short-horizon regime → recover mu with sigma known. Consumer of the schema-1.1.0 `gradient_fields` key (dLoss_dmu/dLoss_dsigma). Capture sha256s in the Stage-1c/landing audit) |
| mpm-multimaterial-diff | python (Taichi) | mpm-multimaterial-diff-recover-v0-16cube-seed42 | 1.349 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-31 | baseline (Phase-4 batch-1 sim 3/4; FIRST `hybrid-pg/mpm-multimaterial-diff` row. gate-12 value = the characteristic differentiable op: ONE full inverse-problem solve (recover the shared initial velocity v0; plant v0=(0.30,0.10,-0.20), synthesize the target final particle positions, recover via curvature-scaled Newton SGD lr=0.5/H over the tape-differentiable 3D APIC neo-Hookean MLS-MPM forward — time-indexed needs_grad fields, ti.static-unrolled 27-cell quadratic-B-spline stencil, P2G ti.atomic_add scatter / grid_op / G2P gather / F-update / advect, grid=16, n_particles=6, 8 steps, dt=1e-3), warm-cache 3-run min (29 SGD iters; recovered v0 err ~3e-10, loss 5e-6 → 7e-23). D-DET MEASURED bit-exact same-stack-same-hw: forward AND gradient both bit-identical run-to-run (single-thread CPU f64 serialises the P2G atomic_add scatter, seed-pinned interior cluster IC); registry `[hybrid-pg.mpm-multimaterial-diff.{forward,gradient}]` HOLD (atomic_ops sum-only); no EFECT. Single-stack — gate-14 N/A; WU-F differentiable-axis variant-equivalence applies (diff.forward == mpm-multimaterial-stack-d reference rollout, MEASURED BIT-EXACT, well within rel<=1e-3 cap 1e-2; diff evaluated under conftest f64 before the reference's f32-default re-init). Gradient golden 3 DISTINCT independent anchors: A1 ballistic kinematic analytic 2(dt*STEPS)^2(v0-v0t) (autodiff == analytic ~1e-18, EXACT — single particle F=I C=0 degenerates the APIC round-trip to PIC free-flight), A2 central-FD baseline (autodiff-vs-FD ~1.9e-8), A3 neo-Hookean small-strain constitutive d(sigma00)/deps=2mu+lam (autodiff == analytic 0.0 EXACT; distinct physical term/parameter-class/method). D-ANCHOR Stage-0 SHIFT: charter A3=DiffTaichi (method-only, no storable numeric) → A3-numeric re-declared to the neo-Hookean constitutive analytic, DiffTaichi retained as method cite. D-DT Stage-1b MEASURED: dt=1e-3 is the largest step keeping the stiff E=4e3 dynamics smooth (dt>=5e-3 → ~3% autodiff-vs-FD, ill-conditioned). INVERSE: v0 IS identifiable (near-linear injective map); the small-dt flat-in-v0 loss needs a curvature-scaled Newton step (not fixed-lr Adam). Consumer of the schema-1.1.0 `gradient_fields` key (dLoss_dv0). Capture sha256s in the Stage-1c/landing audit) |
| 3dgs-mpm-sh-update | warp-cpu | sh-update-drop-blob-256gaussians-32grid-step300 | 0.334 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-31 | baseline (Phase-4 batch-2 Sim A; FIRST `neural-rendered/3dgs-mpm-sh-update` row; the SH-update deferred from Phase-3 task-8. gate-12 value = the characteristic coupled op: the full MPM-sim-from-scratch -> per-particle F -> covariance coupling Sigma'=F A F^T (reused from gs_mpm, UNCHANGED) **+ the NEW per-frame SH rotation** (R=polar(F); degree-1 Wigner-D D1(R)=P R P^T on the band-1 coefficients) -> common-3dgs render, over a NEW degree-1 directional-SH blob (256 Gaussians 1:1 MPM particles; 32^3 grid; dt=1e-3; 300 steps; frames 0/100/200/300), warm-cache 3-run min. SIBLING package `packages/3dgs-mpm-sh-update/` (import `gs_mpm_sh_update`) — does NOT mutate the frozen `packages/3dgs-mpm/` MVP; reuses gs_mpm.couple_gaussians + the MPM kernels by import. D-DET MEASURED bit-exact same-stack-same-hw (HELD): two runs byte-identical across frames + gaussian_sh + particle_F (np.array_equal all True; SVD polar + P R P^T deterministic NumPy; render atomic-free serial CPU); registry `[neural-rendered.3dgs-mpm-sh-update]` class=bit-exact; no EFECT. Physics-equivalence-vs-parent HELD by construction (identical kernels/constants/positions -> particle_pos + particle_F bit-equal to gs_mpm.run_canonical_sim). Single-stack — gate-14 N/A; WU-F neural-axis floor applies. Two-pronged gate-4: (1) SH-rotation numerical golden 3 DISTINCT anchors (A1 closed-form D1=P R P^T hand-verified c=(1,0,0)@R_z(90deg)->(0,0,-1) / A2 rotation-equivariance vs the landed renderer _eval_sh = PhysGaussian inverse-rotation-on-view-dirs / A3 identity-frozen R=I recovering the MVP), expected from an INDEPENDENT dipole oracle, impl cross-checked to 1e-12; (2) render-similarity vs OWN committed golden PNGs (deterministic own-pipeline regression; clears the §2.12 floors 28/0.85/0.15). OBSERVABILITY MEASURED: max|rotated-frozen| render delta = 0.0828 > 0 at the deformed frame (the SH rotation genuinely changes the render -> the degree-1 scene makes the gate NON-VACUOUS). Scope: degree <= 1 (degree>=2 raises; documented further extension). PhysGaussian Eq.(9) CITE-ONLY. Mutation `[targets.gs_mpm_sh_update]` advisory. 11/11 pytest; capture fixture phase-4-3dgs-mpm-sh-update sha256s in the Stage-1c/landing audit) |
| eulerian-smoke-neural | warp-cpu | 3dgs-smoke-density-coupling-24grid-256gaussians-step40 | 0.348 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-31 | baseline (Phase-4 batch-2 Sim B; spec § 5.11 "3dgs-smoke"; FIRST `neural-rendered/eulerian-smoke-neural` row + FIRST smoke->3DGS coupling. gate-12 value = the characteristic coupled op: evolve the Stam/Fedkiw smoke (eulerian-smoke-stack-e stable_fluids_step_3d from the Taylor-Green IC, 24^3 grid, 40 steps) **+ the NEW per-frame density->Gaussian coupling** (top-256 densest voxels -> Beer-Lambert opacity 1-exp(-density) via the WU-C default_density_to_opacity hook -> isotropic blobs + degree-0 DC SH) -> common-3dgs render (96x96), capture every 20 -> frames 0/20/40, warm-cache 3-run min. SIBLING package `packages/eulerian-smoke-neural/` (import `eulerian_smoke_neural`) reuses stable_fluids_step_3d by import. D-DET MEASURED bit-exact same-stack-same-hw (HELD): two runs byte-identical across image + density + gaussian_opacities (np.array_equal; argsort + Beer-Lambert deterministic NumPy; render atomic-free serial CPU); registry `[neural-rendered.eulerian-smoke-neural]` class=bit-exact; no EFECT. Physics-equivalence-vs-parent MEASURED bit-equal (the captured per-frame density == a direct eulerian-smoke-stack-e stable_fluids_step_3d rollout at n=24/seed=0, np.array_equal). Single-stack — gate-14 N/A; WU-C neural-axis floor applies. Two-pronged gate-4: (1) coupling golden 3 DISTINCT anchors (A1 Beer-Lambert 1-exp(-d) f64-exact / A2 Kerbl 2023 Eq.6 single-Gaussian compositing centre=alpha*c within compositing_abs 2e-2 / A3 zero-density background); (2) render-similarity vs OWN committed golden PNGs (clears the § 2.12 floors 28/0.85/0.15; deterministic own-pipeline regression). Smoke visibly renders (max opacity 0.61). PBT opacity_monotone_bounded RE-DECLARED on evidence (HARD RULE 2): regime density in [0,10] — float32 opacity storage saturates 1-exp(-d) to 1.0 at d>~16 (physical smoke density O(1)); NOT widened. Small grid (canonical 128 -> 2M voxels CPU-intractable to render). Mutation `[targets.eulerian_smoke_neural]` advisory. 9/9 pytest; capture fixture phase-4-eulerian-smoke-neural sha256s in the Stage-1c/landing audit) |
| eulerian-smoke-diff | python (Warp) | eulerian-smoke-diff-recover-u0-16sq-seed42 | 0.383 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-31 | baseline (Phase-4 batch-1 sim 4/4; FINAL; FIRST `volumetric-grid/eulerian-smoke-diff` row + FIRST Stack-E gate-12 differentiable-sim row. gate-12 value = the characteristic differentiable op: ONE full inverse-problem solve (recover the initial smoke field u0; plant the smooth Gaussian IC, synthesize the target final advected frame, recover the full 16x16 field from a zero init via Adam lr=0.1 over the tape-differentiable constant-velocity semi-Lagrangian advect chain — on-device requires_grad load_field_2d + sl_advect_2d backtrace-gather, grid=16, 3 steps, dt=0.03125), warm-cache 3-run min (1412 Adam iters; recovered-err ~6.5e-7, loss 24.4 -> ~1e-14). D-DET MEASURED bit-exact same-stack-same-hw: forward AND gradient both bit-identical run-to-run (Warp CPU single-thread serial wp.launch, f64, seed-pinned smooth IC); registry `[volumetric-grid.eulerian-smoke-diff.{forward,gradient}]` HOLD (forward atomic_ops none = pure gather; gradient sum-only = the gather adjoint + L2 reduction); no EFECT. Single-stack — gate-14 N/A; WU-F differentiable-axis variant-equivalence applies (diff advect/diffuse primitives == eulerian-smoke-stack-e reference primitives, MEASURED BIT-EXACT max|diff-ref|==0.0, well within rel<=1e-3 cap 1e-2). Gradient golden 3 DISTINCT independent anchors: A1 linear-advection-operator analytic 2(M^K)^T(M^K u0 - target) (autodiff == analytic ~4e-15, EXACT — constant velocity makes the bilinear SL-advect the exact linear operator M; NumPy M mirror bit-faithful to the Warp engine), A2 central-FD baseline (autodiff-vs-FD ~3e-10), A3 discrete-diffusion dLoss/dnu=2(u'-t).(dt*Lap(u0)) (autodiff == analytic 0.0 EXACT; distinct physical term/parameter-class/method). D-ANCHOR Stage-0 SHIFT: charter A3=continuous heat-kernel Gaussian-spread (first-order-accurate only) -> re-declared to the EXACT discrete-diffusion dLoss/dnu analytic, heat-equation retained as source. D-WARP-ADJOINT (Stage-0 BLOCK gate): CONFIRMED differentiable. INVERSE: u0 IS identifiable in the constant-velocity regime (M full-rank well-conditioned -> strictly-convex quadratic); D-INVERSE-SCOPE: diffusion low-pass -> recovery from a diffused target ill-posed (backward heat), canonical recovery scoped to pure advection. Consumer of the schema-1.1.0 `gradient_fields` key (dLoss_du0). Capture sha256s in the Stage-1c/landing audit) |
| articulated-pedagogical-diff | python (Warp) | articulated-pedagogical-diff-recover-state-seed42 | 5.306 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-31 | baseline (Phase-4 batch-3 sim 1/3; the differentiable carry; FIRST `rigid-body/articulated-pedagogical-diff` row. gate-12 value = the characteristic differentiable op: ONE full inverse-problem solve (recover the initial state (q0,qd0); plant q0=0.4 qd0=0.0, synthesise the observed final (q_T,qd_T) of a semi-implicit-Euler rollout, recover from a zero init via Adam lr=0.05 over the tape-differentiable rollout — the landed parent aba_kernel launched on-device inside a wp.Tape with per-step fresh requires_grad arrays, no .numpy() tape-sever; L=m=1 g=9.81, dt=0.01, 50 steps), 3-run min (347 Adam iters; recovered q0 err ~1e-9, loss 8.99e-17). D-DET MEASURED bit-exact same-stack-same-hw: forward AND gradient both bit-identical run-to-run (Warp CPU single-thread serial wp.launch dim=1, f64, fixed config); registry `[rigid-body.articulated-pedagogical-diff.{forward,gradient}]` HOLD (forward atomic_ops none = fixed-size per-link loop; gradient sum-only = the wp.Tape adjoint of the L2 reduction); no EFECT. Single-stack — gate-14 N/A; WU-F differentiable-axis forward-equivalence applies (diff.forward qdd == the landed parent aba_forward_dynamics, MEASURED BIT-EXACT at n=1 AND n=2 — same kernel). Gradient golden 3 DISTINCT independent anchors: A1 analytic state-sensitivity d q_ddot/d q = -(g/L)cos q (autodiff == analytic relerr <= 1.9e-16, EXACT), A2 central-FD baseline (autodiff-vs-FD ~1e-7), A3 analytic torque-sensitivity d q_ddot/d tau = 1/(mL^2) (autodiff == analytic 0.0 EXACT, config-independent; distinct physical term/parameter/method). D-WARP-ADJOINT (Stage-0 BLOCK gate): CLEARED for n=1; n>=2 coupled adjoint MEASURED diverging (relerr 0.197, inward-pass in-place ia[i-1] read-after-write aliasing) -> single-pendulum scope (on-evidence SHIFT; multi-link tape-correct ABA deferred + surfaced). INVERSE: (q0,qd0) IS identifiable (2 unknowns, 2 observations) in the smooth short-horizon regime. PBT energy_drift_bounded re-scoped to horizon >= 1 period (Stage-1a evidence: windowed-mean secular metric ill-posed sub-period; HARD RULE 2, threshold UNCHANGED 1e-3). Consumer of the schema-1.1.0 `gradient_fields` key (dLoss_dq0/dLoss_dqd0). Canonical capture .h5 sha256 fdd26edc6ba1b377f8372b477fe1c0f56602078c8450f0cab9e58555ffd72c11 (LFS oid == sha256); schema-corpus fixture phase-4-articulated-pedagogical-diff.{h5,json} same payload. Capture sha256s in the Stage-1c/landing audit) |
| particle-lenia | python (Taichi) | particle-lenia-64p-seed42-step40 | 0.101 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-31 | baseline (Phase-4 batch-3 sim 2/3; energy-based Particle Lenia; FIRST `continuous-ca/particle-lenia` row. gate-12 value = the characteristic op: a canonical 40-step LOCAL-rule rollout (64 particles; the Taichi particle_force kernel f_i=-grad E(p_i) — analytic energy gradient of E=R-G(U), explicit f64 accumulators, single-thread serial; forward Euler dt=0.1; SOS-article 2D defaults mu_k=4 sigma_k=1 w_k=0.022 mu_g=0.6 sigma_g=0.15 c_rep=1), 3-run min. D-DET MEASURED bit-exact same-stack-same-hw: force AND a 10-step rollout both bit-identical run-to-run (Taichi CPU single-thread serial, f64, seed-pinned cluster IC; no atomics); registry `[continuous-ca.particle-lenia]` class=bit-exact; no EFECT. POINTWISE-vs-TRAJECTORY: pointwise determinism holds; the GOLDEN is the force/symmetry INVARIANT, not the trajectory (sensitive rollouts). Single-stack — gate-14 N/A; parent-vs-frontier REFRAMED to the invariant posture (particle-based, NOT pointwise-comparable to grid Lenia, plan §8.4). Gradient golden 3 DISTINCT independent anchors: A1 analytic -grad E NumPy mirror (engine-vs-mirror ~1e-22, EXACT — bit-faithful cross-impl), A2 central-FD baseline (engine-vs-FD ~2.4e-10), A3 total-energy translation symmetry E_total(P+delta)==E_total(P) (residual ~1e-16, EXACT; distinct quantity/method). OPERATOR ANCHOR CORRECTION: canonical model = LOCAL energy minimisation -> total energy NOT monotonic -> A1 shifted to the force=-grad E identity, NO Lyapunov/monotonicity golden (would be unsound; the SOS article contrasts local vs global descent). LOCAL force sum != 0 (local rule does not conserve momentum); the sound symmetry anchor is the global-energy invariance. Mordvintsev/Niklasson/Randazzo 2022 (Google Research Self-Organising Systems). Canonical capture .h5 sha256 522dd3dcd4706d975850ed438adfa72b1a665b13eb3f26ee7f1adfb898b2ef04 (LFS oid == sha256); schema-corpus fixture phase-4-particle-lenia.{h5,json} same payload. Mutation `[targets.particle_lenia]` advisory. Capture sha256s in the Stage-1c/landing audit) |
| flow-lenia | python (Taichi) | flow-lenia-32sq-seed42-step40 | 0.130 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-31 | baseline (Phase-4 batch-3 sim 3/3 FINAL; mass-conservative Flow Lenia; FIRST `continuous-ca/flow-lenia` row. gate-12 value = the characteristic op: a canonical 40-step reintegration-tracking rollout (32^2 grid; the Taichi convolve->flow_field->reintegrate step — U=K*A Gaussian convolution, F=grad U central diff, forward bilinear-splat ti.atomic_add scatter; dt=0.2; periodic BC), 3-run min. D-DET MEASURED bit-exact same-stack-same-hw: step AND a 10-step rollout both bit-identical run-to-run (Taichi CPU single-thread serial fixes the scatter accumulation order; f64; seed-pinned mass IC); registry `[continuous-ca.flow-lenia]` class=bit-exact atomic_ops=sum-only; no EFECT. **The bit-exact run-to-run determinism is DISTINCT from the mass INVARIANT**, which is conserved only to summation roundoff (~N*eps). Single-stack — gate-14 N/A; parent-vs-frontier REFRAMED to the invariant posture (Flow Lenia is intentionally NOT pointwise-equal to grid Lenia; plan §8.4). Conservation golden 3 DISTINCT independent anchors: A1 mass conservation Sigma A_{t+dt}==Sigma A_t (reintegration mass balance Sum_p I=1; MEASURED rel drift ~1e-16 per step — SUMMATION ROUNDOFF, NOT bit-exact, the operator honest-tolerance instruction), A2 non-negativity (bilinear-splat of non-negative mass with non-negative weights -> A>=0), A3 zero-flow identity (F==0 -> A unchanged pointwise, EXACT residual 0.0). **SOUND HOME of the Phase-3 plain-Lenia mass_approximately_conserved invariant FALSIFIED under Quad4** — re-routed here where it holds by construction (NOT widened). The flow F=grad U is the affinity gradient; the conservation/non-negativity/zero-flow invariants are flow-agnostic (the full alpha-weighted Flow Lenia flow F=(1-alpha)grad U - alpha grad A_sum is a documented extension; bilinear-splat = the point-distribution limit of the paper's uniform square distribution). Plantec et al. ALIFE 2022 (arXiv:2212.07906). Canonical capture .h5 sha256 1a222b973e8c1ca5d8a7bf046b4f10de259af5f15fda3594f3ebf546a957738e (LFS oid==sha256); schema-corpus fixture phase-4-flow-lenia.{h5,json} same payload. Mutation `[targets.flow_lenia]` advisory. Capture sha256s in the Stage-1c/landing audit) |
| ising-classical | pypi-fresh-venv | capture-roundtrip · metropolis-128sq-T2.27-seed42-step10000 | 36.3 | i7-12700KF-linux-7.0 | (this commit) | 2026-06-08 | baseline (Phase-5 sub-phase 5.3 pypi-release build-and-validate; § 3.8 bootstrap gate: build wheel -> install in FRESH isolated venv -> re-emit canonical PROGRAMMATICALLY (`ising_classical.sim.sim_runner_seeded(42,out)`) from the INSTALLED wheel -> `equivalence.harness.compare_captures` vs the in-repo canonical. within_tolerance=True, max_abs=0.0 max_rel=0.0, 11 fields (BIT-EXACT). Wall = full build+venv+install+re-emit+compare; venv reclaimed post-validate. Driven under the uv-managed Python 3.12.13 workspace interpreter (§0.3 SHIFT: system python3 is 3.14, no Taichi/Torch cp314 wheels). Deploy job stays gated OFF — no publish) |
| articulated-pedagogical | pypi-fresh-venv | capture-roundtrip · pendulum-trajectory-seed42-step1000 (sim.name rigid-body-pedagogical) | 33.4 | i7-12700KF-linux-7.0 | (this commit) | 2026-06-08 | baseline (5.3 pypi-release; § 3.8 gate, re-emit `articulated_pedagogical.sim.sim_runner_seeded(42,out)`; compare_captures within_tolerance=True max_abs=0.0 max_rel=0.0, 202 fields BIT-EXACT; tolerance [defaults.rigid-body]=0.0/0.0. Python 3.12.13; venv reclaimed; deploy gated OFF) |
| articulated-pedagogical-diff | pypi-fresh-venv | capture-roundtrip · articulated-pedagogical-diff-recover-state-seed42 | 39.1 | i7-12700KF-linux-7.0 | (this commit) | 2026-06-08 | baseline (5.3 pypi-release; § 3.8 gate, re-emit `articulated_pedagogical_diff.capture.default_capture(out)`; compare_captures within_tolerance=True max_abs=0.0 max_rel=0.0, 4 fields BIT-EXACT; tolerance [defaults.rigid-body]=0.0/0.0. Python 3.12.13; venv reclaimed; deploy gated OFF) |
| mpm-multimaterial | pypi-fresh-venv | capture-roundtrip · drop-impact-128cube-seed42-step500 | 160.6 | i7-12700KF-linux-7.0 | (this commit) | 2026-06-08 | baseline (5.3 pypi-release; § 3.8 gate. VALIDATES the Step-0 checkpoint dep-fix: this Phase-1 TDD-bootstrap package shipped with NO [project.dependencies], so its wheel failed `import numpy`/`from capture import …` in a fresh venv; deps now authored (bit-physics-testkit + h5py + numpy). re-emit `mpm_multimaterial.sim.sim_runner_seeded(42,out)`; compare_captures within_tolerance=True max_abs=0.0 max_rel=0.0, 44 fields BIT-EXACT; tolerance overrides.mpm-multimaterial->mpm 1e-4/0.0 resolves, measured bit-exact. Python 3.12.13; venv reclaimed; deploy gated OFF) |
| mpm-multimaterial-stack-d | pypi-fresh-venv | capture-roundtrip · drop-impact-128cube-seed42-step500 (Taichi) | 409.5 | i7-12700KF-linux-7.0 | (this commit) | 2026-06-08 | baseline (5.3 pypi-release; § 3.8 gate; re-emit `mpm_multimaterial_stack_d.sim.sim_runner_seeded(42,out)`; compare_captures within_tolerance=True max_abs=0.0 max_rel=0.0, 44 fields BIT-EXACT. § 0.3 SHIFT SURFACED here: under system python3=3.14 the fresh-venv install fails — Taichi ships no cp314 wheel; PASSES under the uv-managed Python 3.12.13 workspace interpreter. Python 3.12.13; venv reclaimed; deploy gated OFF) |
| mpm-multimaterial-stack-e | pypi-fresh-venv | capture-roundtrip · drop-impact-128cube-seed42-step500 (Warp) | 338.4 | i7-12700KF-linux-7.0 | (this commit) | 2026-06-08 | baseline (5.3 pypi-release; § 3.8 gate; re-emit `mpm_multimaterial_stack_e.sim.sim_runner_seeded(42,out)`; compare_captures within_tolerance=True max_abs=0.0 max_rel=0.0, 44 fields BIT-EXACT. Python 3.12.13; venv reclaimed; deploy gated OFF) |
| neural-ca | pypi-fresh-venv | capture-roundtrip · growing-emoji-64sq-seed42-step1000 (LFS checkpoint) | 36.6 | i7-12700KF-linux-7.0 | (this commit) | 2026-06-08 | baseline (5.3 pypi-release; § 3.8 gate; the R3/§C-6 named per-sim precondition. re-emit rolls the frozen LFS checkpoint (tools/testkit/golden/checkpoints/neural-ca-emoji-disk.safetensors, materialized locally) forward via the INSTALLED console module `python -m neural_ca infer --grid 64 --steps 1000 --seed 42 --capture-every 50`; pipeline.py `module_main` re-emit kind wired this sub-phase. compare_captures within_tolerance=True max_abs=0.0 max_rel=0.0, 21 fields BIT-EXACT -> MEASURED [defaults.continuous-ca]=0.0/0.0 added to tolerance.toml (measure-then-declare; deterministic same-hw CPU PyTorch, matched stateless-PCG fire mask). Python 3.12.13; venv reclaimed; deploy gated OFF) |
| lenia | pypi-fresh-venv | golden-surrogate · Quad4-kernel + Orbium-trajectory anchors | 35.0 | i7-12700KF-linux-7.0 | (this commit) | 2026-06-08 | baseline (5.3 pypi-release; R3 golden-table SURROGATE — no committed .h5; § 3.8 gate installs the wheel in a fresh venv and runs the sim's OWN committed golden-anchor pytest (tests/test_kernel_anchors.py vs lenia-kernel/orbium golden tables) against the INSTALLED package (site-packages isolation asserted). PASS. Python 3.12.13; venv reclaimed; deploy gated OFF) |
| lenia-diff | pypi-fresh-venv | golden-surrogate · gradient-golden anchors | 52.2 | i7-12700KF-linux-7.0 | (this commit) | 2026-06-08 | baseline (5.3 pypi-release; golden SURROGATE; gradient-golden anchors PASS. VALIDATES the Step-0 pipeline closure-fix completion: lenia-diff declares its base sibling `lenia` as a DEV-only dep (forward-equivalence test), which the wheelhouse closure missed -> `{wheel}[dev]` could not resolve -> pytest collection error sank the run. Closure now also walks [project.optional-dependencies] workspace sources. Python 3.12.13; venv reclaimed; deploy gated OFF) |
| particle-lenia | pypi-fresh-venv | golden-surrogate · force-analytic + energy-translation anchors | 36.5 | i7-12700KF-linux-7.0 | (this commit) | 2026-06-08 | baseline (5.3 pypi-release; golden SURROGATE; force-analytic / energy-translation golden anchors PASS against the installed wheel. Python 3.12.13; venv reclaimed; deploy gated OFF) |
| flow-lenia | pypi-fresh-venv | golden-surrogate · mass-conservation + zero-flow anchors | 38.6 | i7-12700KF-linux-7.0 | (this commit) | 2026-06-08 | baseline (5.3 pypi-release; golden SURROGATE; mass-conservation / zero-flow-residual golden anchors PASS against the installed wheel. Python 3.12.13; venv reclaimed; deploy gated OFF) |
| mpm-multimaterial-diff | pypi-fresh-venv | golden-surrogate · gradient-golden anchors | 80.1 | i7-12700KF-linux-7.0 | (this commit) | 2026-06-08 | baseline (5.3 pypi-release; golden SURROGATE; gradient-golden anchors PASS (no committed -diff capture). Wheelhouse closure pulls the dev sibling mpm-multimaterial-stack-d. Python 3.12.13; venv reclaimed; deploy gated OFF) |
| pinn-poisson | pypi-fresh-venv | golden-surrogate · analytic-L2 + classical-FD-L2 (frozen network) | 355.8 | i7-12700KF-linux-7.0 | (this commit) | 2026-06-08 | baseline (5.3 pypi-release; golden SURROGATE; analytic-L2 + classical-FD-L2 anchors on the frozen network (pinn-poisson-canonical.json) PASS against the installed wheel (Torch). Python 3.12.13; venv reclaimed; deploy gated OFF) |
| 3dgs-mpm | pypi-fresh-venv | golden-surrogate · coupling golden + render-similarity | 44.1 | i7-12700KF-linux-7.0 | (this commit) | 2026-06-08 | baseline (5.3 pypi-release; golden SURROGATE; coupling golden (3dgs-mpm-coupling.json) + render-similarity gate, 11 anchors PASS against the installed wheel. Python 3.12.13; venv reclaimed; deploy gated OFF) |
| 3dgs-mpm-sh-update | pypi-fresh-venv | golden-surrogate · SH-rotation Wigner-D anchors | 41.0 | i7-12700KF-linux-7.0 | (this commit) | 2026-06-08 | baseline (5.3 pypi-release; § 3.8 gate fresh-venv install → golden SURROGATE; SH-rotation Wigner-D golden (`3dgs-mpm-sh-rotation.json`) anchors PASS 9/9 (2 deselected) against the INSTALLED wheel. **RE-VALIDATED at sub-phase 5.4 STEP 0** after the Warp-deprecation migration (commit `21b3b68`, version-adaptive `wp.config.log_level` guard) un-blocked the prior 5.3 C-5 BLOCK: the fresh venv resolves warp-lang 1.14.0 and pytest collection no longer aborts on the deprecated `wp.config.quiet`. Honest re-measure (the gate was actually re-run), not a flipped row — **5.3 is now 15/15**. Python 3.12.13; venv reclaimed; deploy gated OFF) |
| reaction-diffusion-2d-stack-c | binary-cmake-linux | gray-scott-lambda-128sq-seed42-step2000 | 15.7 | i7-12700KF-linux-7.0 | (this commit) | 2026-06-08 | baseline (5.2 binary-release; § 3.8 bootstrap capture_roundtrip — clean out-of-tree CMake build → re-emit from `bit_physics_rd2d_stack_c_capture` → `compare_captures` within_tolerance=True max_abs=0.0 max_rel=0.0 (22 fields; reaction-diffusion 1e-4/0.0; deterministic C++/f64 BIT-EXACT). lavapipe pin (VK_DRIVER_FILES=lvp_icd.json, LP_NUM_THREADS=0). wall = full configure+build-target+re-emit+compare. §0.3 SHIFTs: no Docker → clean-build-dir isolation (env label `binary-cmake-linux`, the plan's `binary-docker-<os>` de-Docker'd); cmake≥4.0 → `-DCMAKE_POLICY_VERSION_MINIMUM=3.5` (no-op on CI). deploy gated OFF) |
| mass-spring-cloth | binary-cmake-linux | flag-wind-128x128-seed42-step1000 | 174.3 | i7-12700KF-linux-7.0 | (this commit) | 2026-06-08 | baseline (5.2 binary-release; § 3.8 witness_pbt_surrogate — in-binary 2-run determinism witness (`assert_determinism`, tolerance 0.0; witness=90c36c37…) + Hypothesis PBT (length_bounded_above + momentum_conservation_free_no_gravity) both PASS against the built `bit_physics_mass_spring_cloth_capture`. No NumPy oracle / no compare_captures soft-body op → NO tolerance row (never fabricated; reconciliation §R3). Committed .h5 payload sha256 is same-hw (R-CPPB2): the re-emit payload sha256 differs cross-build — recorded informationally, NOT gated. wall = full build + 128×128×1000×2-run determinism capture + 10-example PBT (subprocess-per-example, single-threaded lavapipe). deploy gated OFF) |
| eulerian-smoke | render-cycles-blender-4.5.10 | taylor-green-128cube-seed42-step0-density-volume | 9.3 | i7-12700KF-linux-7.0 | (this commit) | 2026-06-08 | baseline (5.4 render-passes; render artifact — bootstrap § 3.8 N/A per Appendix E. **DETERMINISM gate**: two CPU-Cycles renders (seed 42, 128 samples, 512²) → decoded pixel buffers BIT-IDENTICAL (`run1==run2` pixel sha256 `3703e8e2…`; hero file sha256 `00d30f37…`, byte-stable across independent invocations); PSNR=inf SSIM=1.0 over the ≥40dB/≥0.98 floor — never widened. Raw PNG container bytes vary (eXIf/tEXt run-time chunks) so the gate is on decoded pixels; hero re-encoded chunk-free. **ASSET-INTEGRITY**: h5→VDB DoubleGrid round-trip max_abs=max_rel=0.0 (bit-exact). VDB file sha256 is OpenVDB-internal non-deterministic — informational, NOT gated. wall = convert+VDB-export+integrity+render×2+verify. §0.3 SHIFTs: no Docker → pinned Blender 4.5.10 tarball (sha256 `198a4248…`, the de-Docker'd digest pin); R4 "4.4MB CI-friendly" canonical is actually the 705MB 3D Taylor-Green (the 4.2MB lid-driven is 2D); hero=step0 (the passive density scalar homogenises to uniform by step50, MEASURED). deploy gated OFF) |
| pinn-poisson | preprint-extraction-texlive-2026.06 | spec-ref.md -> main.tex + references.bib (deterministic extraction + latexmk clean compile) | 0.76 | i7-12700KF-linux-7.0 | (this commit) | 2026-06-08 | baseline (5.5 preprint-extraction; the FINAL Phase-5 pipeline; preprint artifact — bootstrap § 3.8 N/A per Appendix F, the source sim's physics was gated through Phase-3 acceptance. **DETERMINISTIC-EXTRACTION gate** (the § 3.8 surrogate, STEP-5a): extract.py run TWICE in SEPARATE processes (distinct PYTHONHASHSEED) → main.tex + references.bib BYTE-IDENTICAL (main.tex sha256 `528ed860…`, references.bib sha256 `9da6aa99…`; run1==run2). sort-before-emit makes output a pure function of the input — verified hash-seed-independent (PYTHONHASHSEED 1 vs 999); the named dict/set-iteration trap is pre-empted, NOT tolerated. **CLEAN-COMPILE gate**: latexmk exit 0, 0 unresolved \ref/\cite (\nocite{*} + bibliographystyle plain; pure-ASCII Unicode-mapped output). NO PDF committed (workflow builds on demand). wall = extract×2 + latexmk full build (pdflatex+bibtex+pdflatex). §0.3 SHIFTs: no Docker → pinned portable TeX Live TinyTeX v2026.06 tarball (sha256 `73c8cc30…`, the de-Docker'd digest pin, TeX Live 2026); §12+MANIFEST.toml bib (used_by_sims under [scope]); frontier-story reframed (single-stack). deploy gated OFF) |
| reaction-diffusion-2d | webgpu-headless-chromium | gray-scott-lambda-128sq-seed42-step2000 | 1.88 | i7-12700KF-linux-7.0 | (this commit) | 2026-06-09 | baseline (5.1 web-deploy; browser-WebGPU round-trip, ANGLE-Vulkan on RX 6800 XT, secure-context localhost). Established gate = capture_roundtrip rel=1e-4. **DIVERGES**: browser deterministic (run-twice byte-identical) + IC/early-step correct (step 200 max_abs 1e-6) but the sensitive f32 gray-scott pattern diverges to max_abs **0.074** by step 2000 — cross-implementation f32 (browser Dawn vs canonical wgpu-native), NOT a tolerance to widen (`tolerance.toml` byte-unchanged). Surfaced to sim-owner/operator (§5a). wall = vite-built bundle drive + capture + compare_captures. deploy gated OFF |
| mandelbulb-explorer | webgpu-headless-chromium | de-probe-points-seed42 | 0.83 | i7-12700KF-linux-7.0 | (this commit) | 2026-06-09 | baseline (5.1 web-deploy; browser-WebGPU, ANGLE-Vulkan RX 6800 XT). Established gate = new_canonical (run-twice byte-identical + f32 DE floor). **PASS**: run-twice byte-identical; f32-vs-f64 DE max_abs **1.5e-5** (== the wgpu-native gate). Single-pass closed-form DE → no accumulation → browser matches. deploy gated OFF |
| neural-ca | webgpu-headless-chromium | growing-emoji-64sq-seed42-step1000-wgsl | 1.74 | i7-12700KF-linux-7.0 | (this commit) | 2026-06-09 | baseline (5.1 web-deploy; browser-WebGPU, ANGLE-Vulkan RX 6800 XT). Established gate = capture_roundtrip BIT-EXACT (0/0). **DIVERGES**: browser Dawn f32 ≠ the committed wgsl canonical's wgpu-native bit pattern (max_abs ~0.72–0.79, run-to-run varying) — bit-exactness is implementation-specific; NOT a tolerance to widen. Surfaced (§5a). deploy gated OFF |
| ising-classical | webgpu-headless-chromium | metropolis-128sq-T2.27-seed42-step10000 | 38.17 | i7-12700KF-linux-7.0 | (this commit) | 2026-06-09 | baseline (5.1 web-deploy; browser-WebGPU, ANGLE-Vulkan RX 6800 XT). Established gate = observable (energy z<3.0 vs NumPy ensemble). **PASS**: browser energy_per_spin −1.47 vs NumPy 6-seed ensemble mean −1.418 → **z=1.46** < 3.0. App pins capture seed 42 → single self-averaging sample vs ensemble (same observable + same 3.0 threshold; not widened). wall = 10000 in-browser sweeps. deploy gated OFF |
| strange-attractors | webgpu-headless-chromium | lorenz-trajectory-seed42-step10000 | 0.86 | i7-12700KF-linux-7.0 | (this commit) | 2026-06-09 | baseline (5.1 web-deploy; browser-WebGPU, ANGLE-Vulkan RX 6800 XT). Established gate = new_canonical (determinism + structural). **PASS**: run-twice byte-identical; all 11 browser-subsampled trajectory points lie on the dense f64 Lorenz attractor envelope (0.12 rel margin + 1.5 abs slack — established thresholds, not widened). Browser exposes the trajectory subsampled → determinism + on-attractor containment; the dense min/max/std statistic stays wgpu-native. deploy gated OFF |
| boids-3d | webgpu-headless-chromium | flock-1000agents-seed42-step1000 | 3.27 | i7-12700KF-linux-7.0 | (this commit) | 2026-06-09 | baseline (5.1 web-deploy; browser-WebGPU, ANGLE-Vulkan RX 6800 XT). Established gate = new_canonical (run-twice byte-identical + short-horizon + v_max). **DIVERGES**: short-horizon step-100 pos 3.2e-3 (<1e-2, correct Reynolds dynamics) + v_max clamp 3.0 OK, BUT run-twice **NOT byte-identical** — two browser runs identical through step 400 then diverge (~0.11 by step 1000): browser Dawn FP non-determinism amplified by flock sensitivity (RADV wgpu-native was deterministic). Mandatory determinism criterion fails; surfaced (§5a), not widened. deploy gated OFF |
| physarum | webgpu-headless-chromium | network-canonical-seed42-step5000 | 1.66 | i7-12700KF-linux-7.0 | (this commit) | 2026-06-09 | baseline (5.1 web-deploy; browser-WebGPU, ANGLE-Vulkan RX 6800 XT). Established gate = new_canonical (determinism + mass-balance). **PASS**: run-twice byte-identical (integer fixed-point atomicAdd<u32> → order-independent in-browser too); total_mass 22499.996 vs canonical 22500 (rel **1.7e-7** < 1e-3). deploy gated OFF |
| sph-water-diff | python (Taichi) | sph-water-diff-recover-v0z-8part-seed42 | 0.211 | i7-12700KF-linux-7.0 | (this commit) | 2026-06-11 | baseline (Phase-6 C-1 unit U-1; FIRST `particle-fluids/sph-water-diff` row + FIRST Phase-6 sim row. gate-12 value = the characteristic differentiable op: ONE full inverse-problem solve (recover the shared initial vertical velocity v0z; plant v0z=-0.20, synthesize the target final positions, recover via exact-Newton SGD lr=1/H, H=2N(dt*T)^2, over the tape-differentiable free-fall forward — time-indexed needs_grad fields, n_particles=8, 8 steps, dt=1e-3), warm-cache 3-run min (2 iters; recovered v0z err ~3.1e-14, final loss < 1e-24 — the map is EXACTLY linear so the Newton step solves the quadratic in one move). D-DET MEASURED bit-exact same-stack-same-hw: forward AND gradient both bit-identical run-to-run (single-thread CPU f64, seed-pinned interior cloud IC); registry `[particle-fluids.sph-water-diff.{forward,gradient}]` HOLD (atomic_ops sum-only); no EFECT. Single-stack — gate-14 N/A; WU-F differentiable-axis variant-equivalence applies (diff.forward == sph-water-stack-d `_evolve` free-fall positions, MEASURED np.array_equal BIT-EXACT; density id-order pair sum vs parent 27-cell spatial hash MEASURED max_rel 0.0 — well within rel<=1e-3 cap 1e-2; diff evaluated under conftest f64 before the parent's re-init). Gradient golden 3 DISTINCT independent anchors: A1 free-fall control analytic 2N(dt*T)^2(v0z-v0z*) (autodiff == analytic EXACT — the map is linear in v0z, gravity/IC cancel), A2 central-FD baseline on dLoss/dh (autodiff-vs-FD ~1.6e-10/-1.3e-10/-7.7e-12 rel), A3 Monaghan-spline pair-density analytic d(rho)/dh = -(m*sigma_3/h^4)(3(1+f(q))+q*f'(q)) (autodiff == analytic, both spline branches; distinct physical term/parameter/method). SHIFT (charter section 3.1 + probe section 2): plan-prose ParamSpec (viscosity/surface-tension/damping) -> live landed-physics params (v0z, h) — the parent's R-S3/S6 canonical forward has no pressure/viscosity feedback; the EXP-C neighbor-search surface is regime-scoped to the fixed-topology interior cloud (free-fall preserves relative positions exactly; fixtures away from q=1/q=2 spline knots). Consumer of the schema-1.1.0 `gradient_fields` key (dLoss_dv0z). Capture run-twice byte-identical; sha256s in the U-1 landing report) |
| neural-ca-frontier-difflogic | python (Taichi) | neural-ca-difflogic-recover-alpha-16sq-seed42 | 0.469 | i7-12700KF-linux-7.0 | (this commit) | 2026-06-11 | baseline (Phase-6 C-1 unit U-2; FIRST `continuous-ca/neural-ca-frontier-difflogic` row. gate-12 value = the characteristic op: ONE full WU-A inverse-problem solve (plant soft-excitation alpha=0.60, synthesize the 2-step soft-circuit target, recover via Adam lr=0.05 over the tape-differentiable 36-wire multilinear GoL circuit, 16x16 torus; 354 iters; recovered err ~1e-11), warm-cache 3-run min. D-DET MEASURED bit-exact same-stack-same-hw: hard trajectory, soft forward AND gradient all bit-identical run-to-run (forward atomic-free per-cell writes; loss reduction sum-only serialised single-thread); registry `[continuous-ca.neural-ca-frontier-difflogic.{forward,gradient}]` HOLD; NO EFECT (frozen gates, no training - the ratified D-3 scope). Single-stack - gate-14 N/A; parent-equivalence REFRAMED to exact circuit goldens (the landed neural-ca parent is a trained NCA, a different update family): A2 exhaustive-512 GoL equality vs Gardner-1970 (matches=512, EXACT over the whole input space) + blinker/glider fixtures + kernel==python-evaluator <=1e-15 on soft states. Gradient golden 3 DISTINCT independent anchors: A1 multilinear gate closed forms (hard-limit corners EXACT for all 16 gates, midpoint=mean property), A2 the exhaustive-512 count, A3 central-FD baseline on dLoss/dalpha (ad-vs-fd ~1e-11 rel). Descriptor SHIFT vs Appendix D.2.3 (growing-emoji row names the parent's trained-emoji test; problem-scoped descriptor per the U-1/batch-1 precedent; routed D-6). Consumer of the schema-1.1.0 `gradient_fields` key (dLoss_dalpha). Capture run-twice byte-identical (.h5 05fa614e...). Sister corpus seed phase-6-c1-neural-ca-frontier-difflogic (lock 36->37)) |
| lattice-boltzmann-d3q19-frontier-moment-encoded | cpp (Vulkan/lavapipe) | poiseuille-64x32-seed42-step1000 | 34.60 | i7-12700KF-linux-7.0 | (this commit) | 2026-06-11 | baseline (Phase-6 C-1 unit U-3; FIRST Stack-C LBM row + FIRST quantized-state sim row. gate-12 value = the canonical capture invocation: full-horizon stability-guided range calibration (host f64 reference, 1000 steps) + 2-run bit-identity witness + the capturing quantized trajectory (1000 steps x [decode -> BGK collide+Guo -> pull-stream+bounce-back -> 16-bit moment encode] + per-step readback, 64x32x3, 1001 frames). D-DET MEASURED bit-exact same-stack-same-hw (in-run witness, tolerance 0.0, canonical sha 2fe02516...); atomic_ops none (element-wise; encode atomicOr composes disjoint half-words). Anchors: A1 mass+momentum conservation exact-to-FP on the f64 path (1e-12 asserted; quantized mass drift bounded by the closed-form budget); A2 moment-basis M*Minv=I residual <=1e-12 (programmatic rank-19 monomial basis, cond ~19) + 16-bit round-trip <= (hi-lo)/2/65535 (+~1ulp slack); A3 bounded-quantization equivalence vs the landed numpy parent capture, MEASURED canonical-horizon rho 8.5e-15 / u max_abs 3.12e-6 (peak-rel 3.6e-4) -> DECLARED lbm-quantized rel 5e-2 / abs 1e-5 (D-2 amendment 2026-06-11T15-22-14Z); f64 mode MEASURED rho 3.7e-15 / u 1.6e-15 (inside the parent lbm 1e-5 category ~9 orders). SHIFT: no landed Stack-C LBM existed (charter 3.3 premise corrected at probe; scope unchanged). Stage-1b defect honestly recorded: short-warmup calibration clamped accelerating momentum moments (u err ~60% of peak) -> full-horizon ranges. Mutation N/A (C++; mutmut is python-only, the rd2d-stack-c precedent). Capture .h5 dabb947c...; corpus seed phase-6-c1-lbm-me (lock 37->38)) |
