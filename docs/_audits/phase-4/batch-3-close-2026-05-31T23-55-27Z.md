---
date: 2026-05-31T23-55-27Z
author: phase-4 Run-2 Batch-3 close (Claude Code; frontier-algorithm + differentiable-carry; self-driven PHASE 1)
subject: "Phase-4 Run-2 BATCH 3 close — 3/3 core sims LANDED (articulated-pedagogical-diff + particle-lenia + flow-lenia)"
kind: batch-close
verdict: BATCH-3-COMPLETE (3/3 core LANDED + CI-GREEN)
head_sha: 09f15bf
prior_phase_tag: v0.3.0-phase-3
integrity_invariant: "0 HARD_FAIL / 14 SOFT_WARN"
integrity_digest_at_head: 9894964135e582fc3d94448f87bdf8d859a1ff29e3675a45fa04a7f04b40b15f
parent_audits:
  - docs/_audits/phase-4/batch-3-charter-2026-05-31T22-13-27Z.md
  - docs/_audits/phase-4/batch-3-progress-2026-05-31.md
  - docs/_audits/phase-4/batch-2-close-2026-05-31T21-41-02Z.md
evidence_hashes:
  docs/phase4/ledger.md: at-head
  tools/testkit/determinism/registry.toml: at-head
  tools/testkit/equivalence/tolerance.toml: at-head
---

# Phase-4 Run-2 BATCH 3 — close (frontier-algorithm + articulated differentiable carry)

> Self-driven PHASE-1 execution per the ratified charter
> (`docs/_audits/phase-4/batch-3-charter-2026-05-31T22-13-27Z.md`), sim-to-sim on green acceptance.
> The single batch report (the dispatch REPORT spec). FACT = ran/read/measured at the cited HEAD.

## §0 — Headline

| | |
|---|---|
| **Verdict** | **BATCH-3-COMPLETE** — 3/3 core sims LANDED + pushed origin/main + CI-GREEN |
| **Core built (3)** | **articulated-pedagogical-diff** (S14/4.6, Stack E) · **particle-lenia** (S26/4.18, Stack D) · **flow-lenia** (S27/4.19, Stack D) |
| **NOT built (per dispatch)** | difflogic-ca (S28; HELD) · 4.15/4.16/4.17 flow-map trio · 4.21 moment-encoded-lbm · 4.22 gaussian-fluids (5 holds CONFIRMED) |
| **HEAD** | `09f15bf` (pushed origin/main); ledger rows 14/26/27 → **landed** |
| **Integrity (FACT, measured live)** | **0 HARD_FAIL / 14 SOFT_WARN**; §R digest `9894964…b40b15f` (drifts as golden tables land; the COUNTS are the invariant) |
| **render_similarity (0.9242) + variant (0.8702) HARD gates** | UNTOUCHED (no core sim ships render coupling) → **mutation-testing CI job GREEN at every push** (confirmed sims 1+2; sim 3 §S.5 below) |
| **gate-14** | **N/A all three** (single-stack; no cross-stack frontier sibling) |
| **Tag** | NO (I7 — operator-only) |

## §1 — Per-sim results

### 1.1 articulated-pedagogical-diff (S14 / 4.6, Stack E / Warp `wp.Tape`)

| | |
|---|---|
| Stages | 0 `4f25f45` → 1a `8084d23` → 1b `194689d` → 1c `b96c8d7` → 2 `57fefb8` |
| Scope | **single pendulum (n=1)** — on-evidence SHIFT (see §2 C-1) |
| Anchors (≥3, MEASURED) | A1 `∂q̈/∂q = −(g/L)cos q` analytic == autodiff **relerr ≤ 1.9e-16**; A2 central-FD baseline; A3 `∂q̈/∂τ = 1/(mL²)` analytic == autodiff **0.0 EXACT** |
| Inverse problem | recover `(q0, qd0)` from observed final `(q_T, qd_T)` — identifiable; loss **8.99e-17** |
| Forward-equivalence (WU-F) | `diff.forward` qdd == landed parent `aba_forward_dynamics` **bit-exact at n=1 AND n=2** |
| Determinism | MEASURED bit-exact same-stack-same-hw (forward + gradient); `[rigid-body.articulated-pedagogical-diff.{forward,gradient}]` (forward none / gradient sum-only); no EFECT |
| PBT (regime-scoped) | `gradient_matches_finite_difference` (n=1 smooth interior) + `energy_drift_bounded` (re-scoped to horizon ≥ 1 period — HARD RULE 2, threshold UNCHANGED) |
| gate-13 | replay MATCHED at `8084d23` |
| perf (gate-12) | 5.306s (one inverse solve) |
| CI | `test-articulated-pedagogical-diff` GREEN (1m49s) |

### 1.2 particle-lenia (S26 / 4.18, Stack D / Taichi)

| | |
|---|---|
| Stages | 0+1a `cde3306d` → 1b `194689d`→`3569c0b` (1c) → 2 `2eb0c7d` |
| Anchors (≥3, MEASURED) | A1 Taichi engine force == NumPy analytic `−∇E` mirror **~1e-22**; A2 central-FD **~2.4e-10**; A3 `E_total(P+δ)==E_total(P)` translation symmetry **~1e-16** |
| **OPERATOR ANCHOR CORRECTION (load-bearing)** | canonical model is the **LOCAL** rule (web-confirmed: SOS article uses local energy minimisation, contrasts with global descent) → `E_total` NOT monotonic → **A1 shifted from energy-Lyapunov to the force=−∇E identity; NO Lyapunov/monotonicity golden** (would be unsound). LOCAL force sum ≠ 0 (local rule does not conserve momentum) → A3 is the GLOBAL-energy symmetry, not the force sum. |
| Attribution | **Google Research Self-Organising Systems** article (Mordvintsev/Niklasson/Randazzo 2022) — NOT Distill (C-7 correction applied) |
| Determinism | MEASURED bit-exact (force + 10-step rollout); `[continuous-ca.particle-lenia]` atomic_ops=none; no EFECT. **Pointwise-vs-trajectory:** pointwise determinism holds; the GOLDEN is the force/symmetry invariant, not the trajectory |
| Parent-vs-frontier | REFRAMED to invariant posture (particle-based, not pointwise-comparable to grid Lenia) |
| PBT | `force_matches_finite_difference` + `total_energy_translation_invariant` |
| gate-13 | replay MATCHED at `cde3306d` |
| perf | 0.101s (40-step rollout) |
| CI | `test-particle-lenia` GREEN |

### 1.3 flow-lenia (S27 / 4.19, Stack D / Taichi)

| | |
|---|---|
| Stages | 0+1a `b1af459` → 1b (1c `8867556`) → 2 `09f15bf` |
| Anchors (≥3, MEASURED) | A1 mass conservation `Σ A_{t+dt}==Σ A_t` by reintegration mass-balance (`Σ_p I=1`) **rel drift ~1e-16/step — SUMMATION ROUNDOFF, NOT bit-exact**; A2 non-negativity (bilinear-splat); A3 zero-flow identity **0.0 EXACT** |
| **OPERATOR HONEST-TOLERANCE (load-bearing)** | mass conserved to floating-point **summation roundoff (~Nε), NOT claimed bit-exact** (MEASURED, regime-scoped to periodic BC). The **bit-exact run-to-run determinism is DISTINCT** from the summation-tolerance mass invariant — declared separately. |
| Re-routing | **SOUND home of the Phase-3 plain-Lenia `mass_approximately_conserved` invariant FALSIFIED under Quad4** — re-routed here where it holds by construction (NOT widened) |
| Determinism | MEASURED bit-exact (step + rollout); `[continuous-ca.flow-lenia]` atomic_ops=sum-only (reintegration scatter); no EFECT |
| Parent-vs-frontier | REFRAMED to invariant posture (Flow Lenia intentionally not pointwise-equal to grid Lenia) |
| PBT | `total_mass_conserved` + `mass_non_negative` |
| gate-13 | replay MATCHED at `b1af459` |
| perf | 0.130s (40-step rollout) |
| CI | `test-flow-lenia` — §S.5 below |

## §2 — On-evidence shifts (HARD RULE 2; documented, never widen-to-pass)

- **C-1 (articulated WARP-NATIVE-TAPE / single-pendulum scope):** the Stage-0 probe MEASURED `wp.Tape`
  through the landed ABA machine-exact for n=1 but DIVERGENT for n≥2 (relerr 0.197 vs central-FD — the
  inward-pass in-place `ia[i-1]` accumulation is a read-after-write aliasing Warp's reverse pass cannot
  replay). The differentiable variant is **single-pendulum-scoped** (the closed-form gradient moat is
  single-pendulum by construction); a tape-correct multi-link ABA (per-pass/per-link kernels, no
  aliasing; FD-only verification) is **DEFERRED + surfaced**. The BLOCK gate is CLEARED for the sound
  scope; this is the honest "fewer is correct" call, not a full BLOCK.
- **C-2 (particle-lenia LOCAL rule):** A1 re-shaped from energy-Lyapunov to the force=−∇E identity
  (the canonical LOCAL rule's total energy is not monotone). §1.2.
- **C-3 (flow-lenia honest tolerance):** mass conservation declared to summation roundoff (~Nε), NOT
  bit-exact. §1.3.
- **C-4 (articulated PBT `energy_drift_bounded`):** re-scoped to horizon ≥ 1 oscillation period (the
  windowed-mean secular metric is ill-posed sub-period); threshold UNCHANGED (1e-3). §1.1.
- **C-5 (D-SPEC-SPLIT):** the shared `spec-frontier.md` stub split into per-variant
  `spec-frontier-particle.md` / `spec-frontier-flow.md` (one sim per sheet; stub removed at the flow
  landing). Ledger rows 26/27 spec paths updated.

## §3 — Verification disciplines (charter §5/§6)

- **Full 13-gate** per sim (gate-14 N/A all — single-stack). gate-4 ≥3 DISTINCT
  `independent_reference` sources each (Cat-3 enforced). gate-13 replay MATCHED for all three.
- **Determinism MEASURED, not assumed** — all three bit-exact run-to-run; the pointwise-vs-trajectory
  (particle-lenia) and determinism-vs-conservation (flow-lenia) distinctions declared explicitly.
- **PBT ≥2 per sim**, regime-scoped, re-declared on falsification (never widened).
- **Mutation** target registered per sim (advisory §2.13; oracle-grounded; snapshots forbidden). Full
  measure DEFERRED (per-mutant suite cost prohibitive inline — the batch-1/2 advisory posture). The
  HARD-gated `render_similarity`/`variant` targets ship no source from this batch → untouched.
- **§Q LFS** — R2-first (GitHub-LFS budget exhausted): each capture `.h5` pushed to R2 (`git lfs push
  --object-id origin --stdin`, same-shell bootstrap) THEN `git -c lfs.standalonetransferagent= push`;
  R2 round-trip VERIFIED (1/1 uploaded each).
- **§0.3 flat code paths** — `packages/{articulated-pedagogical-diff,particle-lenia,flow-lenia}/`.

## §4 — §S.5 CI sweep (per push)

- **Sim 1 (`57fefb8`):** python-strict GREEN (`test-articulated-pedagogical-diff` 1m49s) + determinism
  + cpp-strict + equivalence + tolerance-budget + integrity + **mutation-testing (HARD render gates)
  GREEN** + audit-append-only + structure + ts-strict. FULL SWEEP GREEN.
- **Sim 2 (`2eb0c7d`):** python-strict GREEN (`test-particle-lenia`) + mutation-testing GREEN +
  integrity GREEN + all others GREEN. FULL SWEEP GREEN.
- **Sim 3 (`09f15bf`):** `test-flow-lenia` GREEN + determinism GREEN + **mutation-testing (HARD
  render gates) GREEN** + integrity GREEN + cpp-strict + equivalence + tolerance-budget +
  audit-append-only + structure + ts-strict all GREEN. FULL SWEEP GREEN (all three batch-3 per-sim
  jobs `test-{articulated-pedagogical-diff,particle-lenia,flow-lenia}` confirmed success).

## §5 — Banked (surface only; NOT fixed mid-batch, per dispatch)

- **B-1 (carry from dispatch) — C-1 papers-not-vendored:** the plan's "13 frontier papers vendored to
  `references/papers/`" is unrealized (`references/papers/` empty). Each cored paper was **web-fetched
  + cited** at Stage 0 (Particle Lenia SOS article; Flow Lenia arXiv:2212.07906; Featherstone Ch.7).
  Routes to the Phase-4 close (spec §12.9 discrepancy).
- **B-2 (carry from dispatch) — `replay_failing_tests` worktree uv-sync tooling:** the gate-13 replay
  reruns in a fresh per-package worktree; the failing-tests-evidence MUST be generated **from that
  worktree env** (the shared `uv sync --all-packages` root .venv leaks the `anyio` pytest plugin into
  the header → mismatch otherwise). REFINES L-PINN-1. Used for all three sims (all MATCHED). The
  batch-2 banked `_checkout_worktree` uv-sync fix is the same root issue.
- **B-3 (multi-link adjoint, articulated):** the n≥2 tape-correct ABA is deferred (heavy; FD-only
  verification). The single-pendulum scope is the honest moat.
- **B-4 (charter `at-head` verify_evidence):** the batch-3 charter's `at-head` evidence_hashes
  (phase-4-plan / lenia.sim / aba) do NOT self-verify even at the charter head_sha `ab934d7` — a
  pre-existing `at-head`-sentinel quirk, NOT a regression from this batch (those files untouched here).
- **B-5 (mutation full-measure deferred):** targets registered; advisory.

## §6 — CONTRADICTIONS vs EXPECTED (collector)

| # | Expected (charter/dispatch) | Measured / done | Disposition |
|---|---|---|---|
| K-1 | articulated diff = multi-link differentiable | n≥2 adjoint MEASURED wrong (0.197) | **SHIFTED** single-pendulum scope (§2 C-1) |
| K-2 | particle-lenia A1 = energy Lyapunov | canonical LOCAL rule → energy not monotone | **CORRECTED** to force=−∇E identity (§1.2; operator-flagged) |
| K-3 | flow-lenia mass = bit-exact | conserved to summation roundoff ~Nε | **HONEST TOLERANCE** declared (§1.3; operator-flagged) |
| K-4 | one `spec-frontier.md` for both Lenia sims | per-variant clarity | **SPLIT** (§2 C-5) |
| K-5 | papers pre-vendored | `references/papers/` empty | **SURFACED** (B-1); web-fetched + cited |
| K-6 | §R digest near batch-2 `4266d65e…` | drifted to `9894964…` (golden tables landed) | **NOTED** — 0 HF / 14 SW invariant HELD |

## §7 — Closing

**BATCH-3-COMPLETE.** 3/3 core frontier-algorithm sims (the articulated differentiable carry + the
two Lenia frontier variants) LANDED + pushed origin/main + CI-GREEN, each on a rigorous anchor
(machine-exact gradient / force=−∇E identity / mass-conservation by construction), none requiring
CUDA, all single-stack (gate-14 N/A). All operator anchor corrections applied (LOCAL-rule energy,
honest summation tolerance, single-pendulum scope). render_similarity/variant HARD gates untouched +
re-confirmed GREEN. NO tag (I7 — operator-only). 5 holds confirmed (difflogic-ca + flow-map trio +
moment-LBM + gaussian-fluids) for future dedicated batches.
