---
date: 2026-05-31T22-13-27Z
author: phase-4 Run-2 Batch-3 charter (Claude Code; frontier-algorithm + differentiable-carry)
subject: "Phase-4 Run-2 BATCH 3 — frontier-algorithm sims (4.4) + the held articulated-pedagogical-diff carry (4.1): membership + per-sim charters + verification-anchor plan (PHASE 0)"
kind: batch-charter
verdict: PROPOSED
head_sha: ab934d7
prior_phase_tag: v0.3.0-phase-3
integrity_invariant: "0 HARD_FAIL / 14 SOFT_WARN"
integrity_digest_at_head: 4266d65ed66b993876a2e5944f9e8dbc3dc89cca58bd0f389134e6661252fc35
parent_audits:
  - docs/_audits/phase-4/batch-2-charter-2026-05-31T20-04-45Z.md
  - docs/_audits/phase-4/batch-2-close-2026-05-31T21-41-02Z.md
  - docs/_audits/phase-4/batch-1-close-2026-05-31T19-42-00Z.md
  - docs/_audits/phase-4/foundation-close-2026-05-30T21-54-07Z.md
evidence_hashes:
  docs/phase4/ledger.md: at-head
  docs/phases/phase-4-plan.md: at-head
  packages/lenia/lenia/sim.py: at-head
  packages/articulated-pedagogical/articulated_pedagogical/aba.py: at-head
---

# Phase-4 Run-2 BATCH 3 — frontier-algorithm sims (§4.4) + articulated differentiable carry (PHASE 0 charter)

> **This is the PHASE-0 batch charter, not a landing.** It proposes the batch membership +
> per-sim charters + the load-bearing verification-anchor plan, then **HARD-STOPs** for
> coordinator anchor-verification + operator ratification. No code is built; no gates are run for
> any sim. Execution (PHASE 1, self-driven sim-to-sim) begins only on operator "continue" (or
> amended membership / anchors / carry ruling).
>
> **Authority precedence:** spec (`docs/architecture.md`) → Phase-4 plan + ledger
> (`docs/phase4/ledger.md`, `docs/phases/phase-4-plan.md`) → conventions
> (`docs/conventions/sub-phase-conventions.md`) → sibling charters / foundation audits. Every cite
> checked at assertion (Convention #8). Citations named-to-source below are **flagged for Stage-0
> web-re-verification** where an exact section/equation has not been read against the primary
> source. **This charter asserts no unread equation number** (Phase-3 + Batch-1 both caught
> wrong/soft anchors; the Batch-1 close shifted 4/4 charter A3 anchors on-evidence).
>
> Findings are **FACT** (ran/read/measured at HEAD `ab934d7`) or **INFERENCE** (reasoned).
> Verdicts four-state (CONFIRMED / SHIFTED / BLOCKED / FLAGGED).

---

## §0 — Headline

| | |
|---|---|
| **Batch theme** | **Frontier-algorithm** sims (spec §4.4 / §5.6–§5.13; ledger Stages 23–30; plan §8.4) **+** the held **differentiable carry** `articulated-pedagogical-diff` (spec §4.1; ledger Stage 14) |
| **Proposed CORE (3)** | **articulated-pedagogical-diff** (S14 / 4.6, Stack E) · **particle-lenia** (S26 / 4.18, Stack D) · **flow-lenia** (S27 / 4.19, Stack D) |
| **Operator-decidable 4th** | **difflogic-ca** (S28 / 4.20, Stack D + WU-A) — RIGOROUS only if scoped to frozen/hand-constructed gates + truth-table goldens (no-training ⇒ no EFECT); heavier than the Lenia pair |
| **HELD (with reasons, §1.3)** | **4.15 clebsch-pfm** · **4.16 edge** · **4.17 vpfm** (Stack-C greenfield flow-map family — "single largest algorithmic frontier", qualitative verification) · **4.22 gaussian-fluids** (greenfield grid-free Gaussian solver; needs WU-B+WU-C; touches the `render_similarity` HARD gate) · **4.21 moment-encoded-lbm** (rigorous anchors EXIST but heavy Stack-C lift on the complex parent LBM) |
| **CUDA-exclusion finding (FACT)** | **NONE of the 8 §4.4 sims strictly REQUIRE CUDA** for their correctness gate (Stack C → lavapipe CPU; Stack D → Taichi CPU; Stack E → Warp CPU — all proven CPU-runnable in Phases 3–4). The holds are **GREENFIELD-HEAVINESS / qualitative-verification** holds, NOT CUDA holds (contrast Batch-2's 4.14 CUDA-favoring rasterizer hold). |
| **Integrity at HEAD `ab934d7` (FACT, measured live)** | **0 HARD_FAIL / 14 SOFT_WARN**, rc 0; §R digest `4266d65e…1252fc35` (unchanged since batch-2-close `0b66a42` — only doc commits since; no golden tables landed) |
| **4.1 HARD-gated modules** | `render_similarity` (0.9242) + `variant` (0.8702) — **NOT touched by any proposed core sim** (no render coupling in articulated-diff / particle-lenia / flow-lenia); re-confirmed passing at each landing per §8 |
| **Verdict** | **PROPOSED** — HARD-STOP for coordinator anchor-verification + operator ratification (§12) |

The proposed core is a coherent **CPU-verifiable, rigorous-anchor** batch: **1 differentiable carry** (Warp `wp.Tape`, pre-verified in batch-2 charter §10) **+ 2 frontier-algorithm Lenia variants** whose honest §4.4 anchors are **conservation / Lyapunov-energy / fixed-point** laws, not closed-form pointwise goldens. Both Lenia frontier sims build on the **landed `packages/lenia/`** parent (Phase-3) and the proven Taichi substrate (Batch-1 `lenia-diff`); the articulated carry builds on **landed `packages/articulated-pedagogical/`** (Phase-3 task-4) and the proven Warp diff substrate (Batch-1 `eulerian-smoke-diff`). "Fewer is correct" is applied **hard** — the four held fluid/Gaussian frontier sims are the heaviest, weakest-anchor surface in §4.4.

---

## §1 — Membership (the load-bearing call)

### 1.1 The §4.4 frontier-algorithm surface (FACT, from `docs/phase4/ledger.md` rows 23–30 + plan §8.4)

Spec §11.5 items 4.15–4.22 = ledger Stages 23–30. All eight have **empty `spec-frontier.md` stubs** (FACT — read live; e.g. `docs/sim-specs/volumetric-grid/eulerian-smoke/spec-frontier.md` is the §6/§8-TODO stub); the per-paper briefing is plan §8.4:

| Stage | Spec | Parent sim | Variant | Stack | Sockets | Paper anchor (plan §8.4) |
|---|---|---|---|---|---|---|
| 23 | 4.15 | volumetric-grid/eulerian-smoke | frontier-clebsch-pfm | C | — | Clebsch-PFM (Li et al., SIGGRAPH 2025 Best-Paper HM) |
| 24 | 4.16 | volumetric-grid/eulerian-smoke | frontier-edge | C | — | EDGE (compressible flow-map, SIGGRAPH 2025) |
| 25 | 4.17 | volumetric-grid/eulerian-smoke | frontier-vpfm | C | — | VPFM (Vortex Particle Flow Maps, SIGGRAPH 2025) |
| 26 | 4.18 | continuous-ca/lenia | frontier-particle-lenia | D | — | Particle Lenia (Mordvintsev et al. 2022) |
| 27 | 4.19 | continuous-ca/lenia | frontier-flow-lenia | D | — | Flow Lenia (Plantec et al., ALIFE 2022; arXiv:2212.07906) |
| 28 | 4.20 | continuous-ca/neural-ca | frontier-difflogic-ca | D | §4.2.A | DiffLogic CA (Google, 2024) |
| 29 | 4.21 | lattice/lattice-boltzmann-d3q19 | frontier-moment-encoded | C | §4.2.B | Moment-encoded 16-bit LBM (Chen, Li, Levin, Wu 2025) |
| 30 | 4.22 | volumetric-grid/eulerian-smoke | frontier-gaussian-fluids | E | §4.2.B + §4.2.C | Gaussian Fluids (Xing et al., SIGGRAPH 2025) |

**Plan §8.4 acceptance posture (FACT, verbatim):** "parent-vs-frontier equivalence passes per declared posture. **Many frontier algorithms produce qualitatively different output, so equivalence may be REFRAMED to 'qualitative agreement on N reference fixtures + render-similarity > threshold.'** Document the posture in the variant spec sheet §6 and the completion report." This is the §4.4 frontier-honesty mandate the dispatch makes load-bearing: **anchor on a rigorous invariant or declare a statistical/qualitative posture honestly — never fake a pointwise golden.**

**Frontier papers NOT pre-vendored (FACT — CONTRADICTION C-1).** Plan amendment line 68 claims "all 13 load-bearing frontier papers vendored to `references/papers/<paper-slug>/` BEFORE dispatch." `references/papers/` **is empty at HEAD** (`ls references/papers/` → nothing). The Chakazul-Lenia vendor (`references/Chakazul-Lenia/`) carries **only base Lenia** (`Python/LeniaF.py`, `LeniaND.py`, `animals.json`), NOT Particle/Flow Lenia. → Stage-0 for each cored frontier sim must **web-fetch + cite (or vendor) the paper** per spec §2.8, NOT "consume from `references/papers/`". Both proposed-core Lenia papers are openly available (Particle Lenia: Google Research Self-Organising Systems web article + Colab; Flow Lenia: arXiv:2212.07906 + ALIFE 2022).

### 1.2 Per-sim verification-surface classification (RIGOROUS vs CHAOTIC/WEAK) — the load-bearing call

| Sim | Stack | Verification surface (honest §4.4 anchor) | Class | CPU? | Disposition |
|---|---|---|---|---|---|
| **4.18 particle-lenia** | D | **Energy-based gradient flow** — `dx/dt = −∇E`; energy is a **Lyapunov function** (monotone non-increasing along local descent), forces = `−∇E` (FD-checkable), **fixed points** at `∇E=0`, translation-invariance of `E` ⇒ zero net force | **RIGOROUS** | YES (Taichi CPU) | **CORE** |
| **4.19 flow-lenia** | D | **Exact mass conservation by construction** — reintegration-tracking transports matter so `Σ A` is algebraically invariant; + non-negativity; + zero-flow identity | **RIGOROUS** | YES (Taichi CPU) | **CORE** |
| **4.20 difflogic-ca** | D + WU-A | **Logic-gate truth tables** — each soft differentiable gate → exact boolean gate in the hard limit (16 closed-form truth-table goldens); a hand-constructed circuit reproduces an exact deterministic CA transition (e.g. Game-of-Life); gradient-matches-FD through the soft gates (WU-A) | **RIGOROUS *iff* scoped** (frozen/hand-set gates, no training ⇒ no EFECT) | YES (Taichi/PyTorch CPU) | **DECIDABLE 4th** (heavier than Lenia pair) |
| **4.21 moment-encoded-lbm** | C + WU-B | **Moment conservation + known D3Q19 moment matrix** (Krüger 2017) + bounded-quantization equivalence vs the landed parent `lattice-boltzmann-d3q19` | RIGOROUS-anchors-exist but **heavy Stack-C lift** on the complex parent LBM | YES (lavapipe) | **HELD** (dedicated Stack-C frontier batch) |
| **4.15 clebsch-pfm** | C | Clebsch-gauge / circulation-helicity preservation; **qualitative** vorticity-preservation at coarse grid — no closed-form pointwise golden | **WEAK/QUALITATIVE** + greenfield-heaviest ("single largest algorithmic frontier", `docs/architecture.md:1140`) | YES (lavapipe) | **HELD** |
| **4.16 edge** | C | Buffer-free compressible flow-map; O(1)-memory claim; **qualitative** agreement | **WEAK/QUALITATIVE** + greenfield | YES | **HELD** |
| **4.17 vpfm** | C | Vortex-particle flow map under dynamic solid boundaries; **qualitative** vortical agreement | **WEAK/QUALITATIVE** + greenfield (heaviest of the three) | YES | **HELD** |
| **4.22 gaussian-fluids** | E | Grid-free Gaussian-basis solver; incompressibility/divergence-free; needs **WU-B+WU-C**; **render_similarity** consumer (HARD gate) | **WEAK/QUALITATIVE** + greenfield + dual-socket | YES (Warp CPU) | **HELD** |

### 1.3 CUDA-exclusion criterion + per-held-sim disposition (the dispatch's required statement)

**Criterion:** EXCLUDE (HOLD for the A100 batch) any sim whose **CORRECTNESS gate genuinely requires CUDA.** Applying it: **no §4.4 sim's correctness gate requires CUDA.** Stack-C sims run on lavapipe (the Phase-3 cloth/LBM CPU precedent), Stack-D on Taichi CPU, Stack-E on Warp CPU — all measured CPU-runnable in Phases 3–4. So the holds below are **NOT CUDA holds** — they are **greenfield-implementation-heaviness + weak-verification-surface** holds (the §4.4 frontier-honesty discipline: HOLD a sim with no sound gate rather than fake a golden). This mirrors Batch-2's **4.12** hold (greenfield SPH heaviness, NOT CUDA) and is **distinct** from Batch-2's **4.14** hold (a genuinely CUDA-favoring differentiable rasterizer).

- **4.15 / 4.16 / 4.17 (flow-map family, Stack C) — HELD (greenfield + qualitative).** These are the SIGGRAPH-2025 flow-map frontier — `docs/architecture.md:1146-1149`. Faithful particle-flow-map + Clebsch-gauge / compressible-flow-map / vortex-particle implementations are the heaviest physics surface in the portfolio, and their honest verification is **qualitative vorticity/circulation preservation** (no closed-form pointwise golden). Building one well is a multi-week Stack-C sub-phase each; three at once violates "fewer is correct." Recommend a **dedicated Stack-C flow-map batch** (where the qualitative-posture + reference-fixture discipline can be designed deliberately).
- **4.22 gaussian-fluids (Stack E) — HELD (greenfield + dual-socket + render HARD-gate).** A grid-free Gaussian-spatial-representation solver (Xing et al. 2025) is greenfield physics, consumes **both** WU-B (sparse) and WU-C (3DGS) simultaneously (plan §4.5 / ledger row 30), and renders → it **touches the `render_similarity` HARD gate as a consumer**. Heavy + cross-cutting; recommend pairing with the neural-rendered / sparse follow-ups.
- **4.21 moment-encoded-lbm (Stack C + WU-B) — HELD (rigorous-but-heavy; operator-decidable).** Unlike the flow-map trio, this one **has sound anchors** (LBM conserves mass + momentum moments; the D3Q19 moment-transform matrix is a known linear map per Krüger 2017; the 16-bit quantization is a bounded-error equivalence vs the landed parent). But it is a **heavy Stack-C Vulkan lift** stacked on the already-complex parent `lattice-boltzmann-d3q19`, and the frontier delta (16-bit moment quantization) is engineering-heavy. **Operator-decidable** for a dedicated Stack-C frontier batch; not recommended as a CPU-Lenia-batch member.

### 1.4 Proposed CORE (3 sims) + decidable 4th — the ratification-ready set

| # | Sim | Stage / spec | Stack | Genuine Phase-4 delta | Honest anchor class | gate-14? |
|---|---|---|---|---|---|---|
| **1** | **articulated-pedagogical-diff** | S14 / 4.6 | E (Warp `wp.Tape`) | differentiable ABA: gradient goldens (`∂q̈/∂q`, FD, `∂q̈/∂τ=H⁻¹`) + an inverse problem (initial-state / torque recovery); smooth gravity-only gradient (no contact) | closed-form gradient (machine-exact) | **N/A** (single-stack diff) |
| **2** | **particle-lenia** | S26 / 4.18 | D (Taichi) | particle-based energy-descent Lenia: energy field + repulsion; local gradient-flow dynamics | **conservation/Lyapunov/fixed-point** | **N/A** (single-stack) |
| **3** | **flow-lenia** | S27 / 4.19 | D (Taichi) | mass-conservative Lenia via reintegration-tracking transport of the affinity-driven flow | **exact mass conservation** | **N/A** (single-stack) |
| *(4)* | *difflogic-ca* | *S28 / 4.20* | *D + WU-A* | *differentiable-logic-gate CA; frozen-gate truth-table goldens + hard-limit + WU-A gradient* | *truth-table (exact) — iff scoped* | *N/A (single-stack)* |

**gate-14 disposition (all):** each is the **only** variant on its stack with **no cross-stack frontier sibling** → **gate-14 N/A** (the Batch-1 / Batch-2 single-stack clarification). What applies instead: **(a)** for articulated-diff, the **WU-F `differentiable`-axis forward-equivalence** vs the landed `articulated-pedagogical` parent; **(b)** for the Lenia frontier sims, the **plan §8.4 parent-vs-frontier posture** — REFRAMED to "qualitative agreement + the rigorous invariant holds" because Particle/Flow Lenia are *intentionally* not pointwise-equal to base Lenia (different dynamics). The equivalence is on the **invariant** (energy descent / mass conservation), not the trajectory.

### 1.5 Coherence statement

The core is a coherent **"CPU-verifiable, rigorous-invariant frontier"** batch: **3 sims on landed parents** (`articulated-pedagogical` + `lenia` ×2) using **proven substrates** (Warp `wp.Tape` from Batch-1 smoke-diff; Taichi from Batch-1 lenia-diff), each gating on a **rigorous anchor** (machine-exact gradient / Lyapunov-energy / exact mass conservation), none requiring CUDA. The articulated carry closes a named Phase-2→Phase-4 hand-off (Batch-1 EXP-A, Batch-2 charter §10); the Lenia pair are the **only two §4.4 sims whose honest verification surface is a sound conservation/energy law rather than a qualitative posture or a multi-week greenfield Stack-C lift.** "Fewer is correct" is applied hard — 3 (+1 decidable), not 8.

---

## §2 — Common batch posture (the frontier-algorithm-sim architecture)

### 2.1 Frontier-algorithm honesty (the §4.4 verification discipline)

Per the dispatch + plan §8.4: §4.4 sims are emergent/algorithmic; their honest anchors are **CONSERVATION laws, SYMMETRY/EQUIVARIANCE, KNOWN FIXED POINTS, or independent REFERENCE-IMPLEMENTATION cross-checks** — not closed-form pointwise goldens. For each cored sim:

- **Particle Lenia** anchors on the **energy functional** (Lyapunov monotonicity + force=−∇E + fixed points) — the defining property of the energy-based formulation (web-confirmed: "particles move to minimize local energy"; energy histories are the paper's analysis tool).
- **Flow Lenia** anchors on **exact mass conservation** (web-confirmed: "the global sum of mass is algebraically preserved by construction via the reintegration tracking scheme") — a closed, exact invariant; **this is where the Phase-3 plain-Lenia `mass_approximately_conserved` invariant — FALSIFIED under Quad4 — actually holds by construction**, so the invariant is RE-ROUTED to its sound home, not widened.
- **articulated-diff** is NOT a §4.4 emergent sim — it is §4.1 differentiable with **machine-exact closed-form gradient anchors** (the Batch-1 differentiable discipline). It carries no chaotic surface (smooth gravity-only ABA).

**Pointwise-vs-statistical determinism distinction (MEASURED, not assumed).** All three cored sims are expected **pointwise bit-exact same-seed-same-hw** (Taichi/Warp CPU serial, the Batch-1/3-Stack-D + Stack-E precedent) — there is no chaotic/sensitive-dependence trajectory golden in the core (the Lenia frontier goldens are the *invariant*, not the trajectory). If a longer-horizon Lenia rollout shows sensitive dependence, the **trajectory** is verified by the **invariant + a short-horizon qualitative fixture**, while **pointwise determinism still holds run-to-run** — that distinction is measured + declared explicitly at Stage 1b (do NOT conflate "deterministic" with "trajectory-golden-stable").

### 2.2 Determinism (MEASURE-then-declare; no EFECT in the core)

Registry rows under `tools/testkit/determinism/registry.toml` (`[<category>.<sim>]`, FACT — `[continuous-ca.lenia]` + `[rigid-body.articulated-pedagogical]` already present, `class="bit-exact"`, `scope="same-stack-same-hw"`, `atomic_ops="none"`). Posture: **MEASURE `run_twice_and_diff` at Stage 1b** → expect `np.array_equal` (Taichi `cpu_max_num_threads=1` / Warp `dim=1` serial). The determinism-sensitive surface per sim: Particle-Lenia the energy-gradient reduction; Flow-Lenia the reintegration-tracking scatter (atomic-ordering — MEASURE, the Batch-1 MPM-scatter lesson); articulated-diff the `wp.Tape` adjoint reduction. **No EFECT** for any core sim (no training-loss distribution — difflogic-ca, IF cored, must be scoped to frozen gates precisely to avoid EFECT).

### 2.3 Replayable capture (gate-9/10) + per-sim mutation (§2.13; advisory)

Captures via `common_py.capture.Writer` (Taichi/Lenia — the landed `packages/lenia/lenia/sim.py:180` shape, schema `1.0.0`) / `common_warp.Capture` + `write_capture` (Warp/articulated). articulated-diff capture carries `gradient_fields` (schema `1.1.0`, the Batch-1 WU-A corpus key). Each sim registers a per-sim mutmut target on **oracle-grounded** tests only (the invariant + closed-form anchors; **snapshots FORBIDDEN**); MEASURE advisory (mutmut unprovisioned in the package venvs — the Batch-1/2 precedent). The HARD-gated `render_similarity`/`variant` targets ship **no source** from this batch → untouched, re-confirmed passing at each landing (§8).

---

## §3 — Per-sim charters

### 3.1 — Sim 1: articulated-pedagogical-diff (Stage 14 / spec 4.6, Stack E) — the carry

> **LIFTED from batch-2 charter §10 (coordinator-pre-verified) + reconfirmed against the LIVE landed ABA code at HEAD `ab934d7` (FACT — §10 of this charter).** Per dispatch item A, no new coordinator anchor-verification is needed for articulated; the §10-reconfirmation is in §10 below.

- **Scope:** the **differentiable** sibling of the landed `packages/articulated-pedagogical/` Featherstone ABA (Phase-3 task-4). Gradient goldens + a concrete inverse problem (spec §6.1: "a 'gradients exist' demo without an inverse problem is not a differentiable sim").
- **Parent (FACT):** `packages/articulated-pedagogical/articulated_pedagogical/aba.py:47` (`aba_forward_dynamics`, three-pass Featherstone ABA via `aba_kernel`); analytic forward anchors `packages/articulated-pedagogical/articulated_pedagogical/analytic.py` (A1 small-angle period, A2 large-angle elliptic-K, A3 Jacobi-cn trajectory) — these are FORWARD anchors; the diff variant adds GRADIENT goldens.
- **Inverse problem:** recover initial joint state `(q0, qd0)` or a torque schedule from an observed final pose — `InitialStateRecoveryProblem` / `ControlProblem` (`common/common-warp/src/common_warp/autodiff/inverse_problem.py:268`/`:272`, CONFIRMED at HEAD).
- **Gradient goldens (≥3 independent anchors): §4.1.**
- **WARP-NATIVE-TAPE (BLOCK-gate, Stage 0):** the landed `aba_forward_dynamics` marshals via `qdd.numpy()` (`packages/articulated-pedagogical/articulated_pedagogical/aba.py:116`, FACT) which **severs the `wp.Tape`** → the diff variant **re-implements the 3-pass ABA recursion as on-device `requires_grad` `@wp.kernel`s inside a single `wp.Tape`** (NOT a wrapper of the NumPy-marshalling reference — the Batch-1 smoke-diff lesson). Stage-0 probe confirms `wp.Tape`-differentiability on Warp CPU before committing; **BLOCK on a genuine adjoint gap.**
- **D-classes:** D-CONTACT (RESOLVED: gravity-only frictionless ⇒ smooth gradient, §10); D-DET (§2.2 measure; bit-exact expected); D-USD (DEFER — Stack-E policy, the Batch-1 smoke-diff precedent); **⚠ D-WARP-ADJOINT** (Stage-0 BLOCK-gate probe).
- **Spec:** de-stub `docs/sim-specs/rigid-body/articulated-pedagogical/spec-diff.md` (ledger row 14 path).

### 3.2 — Sim 2: particle-lenia (Stage 26 / spec 4.18, Stack D)

- **Scope:** Mordvintsev et al. (2022) **Particle Lenia** — the energy-based reformulation. Particles `p_i` carry a field `U(x)=Σ_j K(|x−p_j|)`, a growth map `G(U)`, a repulsion term `R(x)`, and a **total energy** `E = Σ_i [ R(p_i) − G(U(p_i)) ]`; dynamics are **local gradient descent** `dp_i/dt = −∇_{p_i} E`. (Exact functional forms Stage-0 web-re-verified against the SOS article / Colab; **no equation number asserted here.**)
- **Parent (FACT):** `packages/lenia/` (the landed grid Lenia — Quad4 kernel + growth, `packages/lenia/lenia/kernel.py` / `growth.py`). Particle Lenia shares the **kernel/growth shape** but is **particle-based**, not grid-based → it is a genuine reformulation, not a grid variant (plan §8.4 "qualitatively different output" applies).
- **Genuine deltas:** a particle state + the energy functional + the `−∇E` integrator (Taichi); the energy-history diagnostic (tier-3 optional, plan §8.4).
- **D-classes:** D-ENERGY-FORM (⚠ Stage-0 web-verify the exact `R`/`G`/`U` forms + parameters against the primary source); D-INTEGRATOR (Euler gradient-descent step size — the paper's `dt`); D-DET (§2.2 measure); D-PBT (§4.4); D-EQUIV (REFRAMED — invariant-based, not pointwise vs grid Lenia, §1.4).
- **Spec:** de-stub `docs/sim-specs/continuous-ca/lenia/spec-frontier.md` (shared stub with flow-lenia — **⚠ one stub, two sims**: §S-routing decides per-variant spec sheets `spec-frontier-particle.md` / `spec-frontier-flow.md` vs sectioned single file; Stage-0 read the §3.7 folder convention).

### 3.3 — Sim 3: flow-lenia (Stage 27 / spec 4.19, Stack D)

- **Scope:** Plantec et al. (ALIFE 2022; arXiv:2212.07906) **Flow Lenia** — mass-conservative Lenia. The growth field drives a **flow** (an affinity map → a flow velocity `F = −∇(...)`), and matter is **transported** by **reintegration tracking** so total mass is **exactly** conserved (web-confirmed: "the global sum of mass is algebraically preserved by construction"). (Exact flow + reintegration formulas Stage-0 web-re-verified; **no equation number asserted here.**)
- **Parent (FACT):** `packages/lenia/` (grid Lenia; same Quad4 kernel/growth, but the **update rule** is transport-by-flow, not growth-add — the genuine delta).
- **Genuine deltas:** the affinity→flow computation + the reintegration-tracking transport step (the mass-conservation mechanism); periodic-BC mass bookkeeping.
- **D-classes:** D-FLOW-FORM (⚠ Stage-0 web-verify the flow `F` + reintegration scheme); D-REINTEG (the reintegration-tracking discretization — the mass-conservation-determining step; MEASURE atomic-ordering, §2.2); D-DET; D-PBT (§4.4, mass conservation is PBT-1); D-EQUIV (REFRAMED, §1.4).
- **Spec:** de-stub `docs/sim-specs/continuous-ca/lenia/spec-frontier.md` (shared, see §3.2 ⚠).

### 3.4 — Decidable 4th: difflogic-ca (Stage 28 / spec 4.20, Stack D + WU-A) — NOT recommended as default

- **Scope:** Differentiable Logic Cellular Automata (Google, 2024) — a CA whose update rule is a **differentiable logic-gate network** (Petersen-style relaxed gates). Uses WU-A autodiff (§4.2.A socket, ledger row 28).
- **RIGOROUS only if scoped (the membership condition):** anchor on (a) the **16 binary logic gates' exact truth tables** in the hard limit (closed-form goldens), (b) a **hand-constructed circuit reproducing an exact deterministic CA** (e.g. the Game-of-Life transition table — a known golden), (c) **gradient-matches-FD** through the soft gates (WU-A). **If scoped this way, no training ⇒ no training-loss distribution ⇒ no EFECT.** If instead it must *train* to reproduce a target, it acquires an EFECT distributional-determinism surface + a much heavier verification — which is why it is **decidable, not default**.
- **Parent (FACT):** `packages/neural-ca/python/` (the landed Phase-3 NCA — `packages/neural-ca/` has `python/` + `typescript/`). DiffLogic CA is a distinct learned-update family.
- **Disposition:** **OPERATOR-DECIDABLE.** Recommend it as the cheapest rigorous add IF the operator wants a 4th and accepts the frozen-gate/truth-table scope; otherwise HOLD for a learned-CA batch (with the §4.6 learned-dynamics S34/S35 sims).

---

## §4 — VERIFICATION-ANCHOR PLAN (the moat; coordinator-verified)

> Each golden/invariant needs **≥3 independent, source-named anchors** (gate-4; Cat-3 enforces ≥3 distinct `independent_reference.source`). Anchors named below are **flagged for Stage-0 web-re-verification** where the exact section/equation has not been read against the primary source. **No anchor here is asserted as a verified equation number.** No core anchor touches `render_similarity`/`variant` source (the core ships no render coupling).

### 4.1 — Sim 1: articulated-pedagogical-diff — gradient golden `G1` (≥3 anchors)

- **A1 (closed-form `∂q̈/∂q`, single pendulum):** the pedagogical model's single-link EOM is `q̈ = −(g/L)·sin(q)` (CONFIRMED against the live `packages/articulated-pedagogical/articulated_pedagogical/analytic.py:1` docstring `theta'' = -(g/L) sin(theta)` + the gravity-only `packages/articulated-pedagogical/articulated_pedagogical/aba.py:55-56`) ⇒ `∂q̈/∂q = −(g/L)·cos(q)`, closed-form; autodiff == analytic to machine precision. **Source:** rigid-body EOM / Featherstone 2008 Ch.7 (the simple-pendulum limit, independent of the recursion). *Stage-0: confirm sign/scale against the discretized planar `aba_kernel`.*
- **A2 (numerical baseline):** central FD via `common_warp.autodiff.finite_difference_gradient` (`common/common-warp/src/common_warp/autodiff/finite_diff.py:26`, CONFIRMED at HEAD), ε≈1e-4, O(ε²); close-R2 numerical-baseline exemption.
- **A3 (second analytic, different term — `∂q̈/∂τ = H⁻¹`):** at a config where the joint-space inertia `H` is closed-form (single link `∂q̈/∂τ = 1/(mL²)`) → a distinct physical term (torque-sensitivity, not state-sensitivity), distinct parameter (`τ` not `q`), distinct method (inertia-inversion not gravity-linearization). **Source:** Featherstone ABA / the landed task-4 `angular_momentum_about_pivot` invariant family. *Stage-0: web-re-verify the ABA single-link inertia closed form.*
- **Tolerance:** new single-stack `[golden_tolerance.rigid-body.articulated-pedagogical-diff]` (mirror the landed `[golden_tolerance.rigid-body.articulated-pedagogical]` `tools/testkit/equivalence/tolerance.toml:175` + the Batch-1 diff-row precedent — additive bespoke numeric keys, the lenia-tolerance-schema-fix branch; **⚠ §S.2 read schema first**).

### 4.2 — Sim 2: particle-lenia — energy/gradient golden `G1` (≥3 anchors)

- **A1 (Lyapunov / energy-monotonicity — the defining invariant):** along the local gradient-descent update `Δp_i = −η·∇_{p_i}E`, the total energy is **non-increasing** for sufficiently small step: `E(t+1) ≤ E(t)` (to first order `ΔE = −η·|∇E|² + O(η²) ≤ 0`). At a measured small `η`, `E` decreases monotonically over the canonical rollout (a numeric golden: the energy sequence). **Source:** Particle Lenia energy-based formulation (Mordvintsev et al. 2022, SOS web article — "particles move to minimize local energy"; web-confirmed) + gradient-descent monotonicity (hand-derivation). *Stage-0: web-re-verify the exact `E`, `R`, `G`, `U` forms.*
- **A2 (force = −∇E, FD-checked):** the integrator's per-particle velocity equals `−∇_{p_i}E` to machine precision vs a central-FD of the energy functional (independent numeric baseline). **Source:** definition of gradient flow + FD (hand-derivation).
- **A3 (fixed point / symmetry — fully independent):** a **symmetric equilibrium** configuration (e.g. a single isolated particle, or a symmetric pair at the energy-minimizing separation) has `∇E = 0` ⇒ **zero net force ⇒ stationary** (a known fixed point); AND **translation-invariance** of `E` ⇒ the **total force sums to zero** for ANY configuration (a Noether-like momentum anchor, exact). **Source:** translational symmetry of the energy (hand-derivation) + the paper's equilibrium SLPs.
- **Tolerance:** new single-stack `[golden_tolerance.continuous-ca.lenia-particle]` (mirror `[golden_tolerance.continuous-ca.lenia]` `tools/testkit/equivalence/tolerance.toml:131`; **⚠ §S.2**).

### 4.3 — Sim 3: flow-lenia — mass-conservation golden `G1` (≥3 anchors)

- **A1 (exact mass conservation — the defining invariant):** `Σ A_{n+1} == Σ A_n` to machine precision (float64) for EVERY step over the canonical rollout — the reintegration-tracking scheme conserves total mass **algebraically by construction** (web-confirmed). A numeric golden: the per-step mass sequence is constant. **Source:** Flow Lenia (Plantec et al. ALIFE 2022 / arXiv:2212.07906 — "the global sum of mass is algebraically preserved by construction via the reintegration tracking scheme"; web-confirmed) + hand-derivation of the reintegration mass-balance. *Stage-0: web-re-verify the reintegration-tracking discretization.* **This is the sound home of the Phase-3 plain-Lenia `mass_approximately_conserved` invariant that was FALSIFIED under Quad4 — re-routed, not widened.**
- **A2 (non-negativity / boundedness):** mass density stays `≥ 0` for all cells (reintegration distributes non-negative mass) over the rollout — a distinct invariant (range, not sum). **Source:** reintegration-tracking non-negativity (hand-derivation).
- **A3 (zero-flow identity — degenerate):** a configuration with **zero flow** (a uniform field ⇒ zero affinity gradient ⇒ `F≡0`) is **unchanged** by the transport step (`A_{n+1} == A_n` pointwise — transport by zero velocity is the identity). **Source:** hand-derivation (the advection-by-zero-velocity degenerate case).
- **Tolerance:** new single-stack `[golden_tolerance.continuous-ca.lenia-flow]` (mirror as §4.2; **⚠ §S.2**).

### 4.4 — PBT invariants (≥2 per sim, regime-scoped)

| Sim | PBT-1 (variant-axis-specific) | PBT-2 (forward-physics / regime-scoped) |
|---|---|---|
| **1 — articulated-diff** | `gradient_matches_finite_difference` (smooth interior, short horizon, away from the straight-down/up gimbal singularity where the pendulum Jacobian degenerates) | `energy_drift_bounded` (the landed task-4 invariant, `packages/articulated-pedagogical/tests/test_pbt_invariants.py:49`, re-used on the diff forward) |
| **2 — particle-lenia** | `energy_non_increasing` — `E(t+1) ≤ E(t)` for random ICs at the measured small step (the A1 invariant as a property; **regime:** step `η` below the descent-stability bound, short horizon) | `total_force_zero_under_translation` — `Σ_i ∇_{p_i}E = 0` for random configs (the A3 translation-symmetry anchor as a property) |
| **3 — flow-lenia** | `total_mass_conserved` — `\|Σ A_{n+1} − Σ A_n\| ≤ tol` for random ICs (the A1 invariant; **regime:** periodic BC, the reintegration scheme's exact-conservation domain) | `mass_non_negative` — `A ≥ 0` for random non-negative ICs (the A2 invariant) |

**Discipline:** every PBT is **re-declared on falsification, never widened** (HARD RULE 2; the Phase-3 lenia `mass_approximately_conserved` falsification → re-routed here to Flow-Lenia where it is exact; the Batch-1 4× re-declarations).

### 4.5 — Foundation symbols imported (CONFIRMED at HEAD `ab934d7` — see §9)

- articulated-diff: `common_warp.autodiff.{finite_difference_gradient, InverseProblem, InitialStateRecoveryProblem, ControlProblem}` (CONFIRMED) + `common_warp.{Capture, write_capture, init}` + the parent `articulated_pedagogical.aba` / `analytic`.
- particle-lenia / flow-lenia: **no §4.2 socket** (plan §4.5 "Particle Lenia / Flow Lenia … use only WU-F + WU-G + WU-P") → `common_py.capture.Writer` + `common_py.determinism.set_taichi_deterministic` + the parent `lenia.{kernel, growth}` (CONFIRMED — `packages/lenia/lenia/`). The lighter socket footprint is part of why these are the soundest CPU §4.4 candidates.

---

## §5 — Stage cadence (per sim; self-driven sim-to-sim in PHASE 1)

Mirrors the Phase-3 / Batch-1/2 per-sim cadence (**Stage 0 → 1a → 1b → 1c → 2**), trunk-based to `main`, Convention-A new-files-first ≤500-line commits, TDD with real-sha256 failing-output footer.

| Stage | What (per sim) |
|---|---|
| **0** | `python tools/dispatch/preflight-phase.py 4` (exit 0); **§Q `source tools/lfs/setup-lfs-s3-local.sh` first action after anchor probe** (STOP-LFS-PUSH on non-zero, AT ANCHOR TIME); cross-phase replay `v0.3.0-phase-3` (LFS-cache recovery if smudge fails); **web-fetch + cite the frontier paper** (papers NOT vendored, §1.1 — Particle Lenia SOS article; Flow Lenia arXiv:2212.07906) + **web-re-verify the §4 anchors**; resolve per-sim D-classes; **articulated-diff: the WARP-NATIVE-TAPE BLOCK-gate probe** (mirrors smoke-diff). |
| **1a** | Scaffold `packages/<sim>/` (§0.3 flat: `packages/articulated-pedagogical-diff/`, `packages/particle-lenia/`, `packages/flow-lenia/`) + RED. Failing TDD: the rigorous golden (§4.1–4.3), parent-vs-frontier posture, PBT. `failing-tests-evidence/<sim>-<UTC>.txt` + sha256 footer (gate-3; F-RB-1 hook exclusion). Determinism registry row (§2.2; §S.2 read-first). spec sheet §§1–13 (de-stub). |
| **1b** | Implement the algorithm; author the golden table + derivation doc (≥3 anchors); wire parent-vs-frontier (REFRAMED invariant posture for Lenia; WU-F differentiable forward-equivalence for articulated). **MEASURE determinism** (`run_twice_and_diff`) → declare the registry row on evidence (incl. the pointwise-vs-trajectory distinction, §2.1). Capture (`gradient_fields` for articulated). PBT (regime-scoped). |
| **1c** | Tier-1/2 diagnostics; Cat-1/2/3 green; **perf-ledger row (gate-12 — do NOT silently omit, S2-RD2C1 lesson)**; schema-corpus seed `tests/fixtures/legacy-captures/phase-4-<sim>.h5` (LFS, §Q same-shell push); gate-13 worktree replay (pre-synced; the Batch-2 Warp/Hypothesis determinism kit for articulated-diff). Register the mutmut target + MEASURE (advisory). **Confirm `render_similarity` + `variant` HARD gates pass.** |
| **2** | Per-sim landing fold: ledger row → `landed`; spec de-stub check; §R two-field integrity; append-only verify; `verify_evidence` no-regression; **§S.5 FULL CI sweep green**; progress note; Convention #12 SHA back-fill; **NO tag** (I7). |

At batch close: a `batch-3-close-<UTC>.md` report (per the dispatch REPORT spec — membership + per-sim gate results + SHAs + anchors-used + on-evidence shifts + chaotic-sim statistical postures + determinism measured + PBT regimes + gate-14 N/A + render_similarity/variant HARD-gate confirmation + §S.5 + §R digest + CONTRADICTIONS).

---

## §6 — Thirteen-gate map (batch-common specialization)

| Gate | spec §3.5 / D.6 | frontier-algorithm-sim specialization |
|---|---|---|
| 1 | spec sheet + §6 posture | de-stubbed `spec-diff.md` / `spec-frontier.md` §6: rigorous-invariant golden + parent-vs-frontier (REFRAMED) + determinism |
| 2 | pre-impl probe | `tools/testkit/probes/reports/<sim>.md` (articulated-diff: the WARP-NATIVE-TAPE BLOCK-gate) |
| 3 | failing suite + sha256 footer | `failing-tests-evidence/<sim>-<UTC>.txt`; F-RB-1; gate-13 replays it |
| 4 | golden (Cat 3), ≥3 independent anchors | §4.1–4.3 (≥3 distinct sources each); the §4.4 honest-anchor form (gradient / energy / mass) |
| 5 | Tier-1 diagnostics | per-sim boundedness / conservation diagnostics |
| 6 | category Tier-2 | per-category Tier-2 (rigid-body / continuous-ca) + optional tier-3 (energy history / mass trace, plan §8.4) |
| 7 | citation chain (Cat 1) | Featherstone / Particle-Lenia SOS / Flow-Lenia arXiv + landed-parent cites (full repo-relative `path:line`) |
| 8 | public API (Cat 2) | `<sim>` CLI + the algorithm API |
| 9 | replayable capture | `.h5` (gradient_fields for articulated; field/particle trajectory for Lenia) |
| 10 | determinism ↔ capture | §2.2 row ↔ capture sidecar `claimed` (MEASURED; pointwise-vs-trajectory distinction) |
| 11 | PBT ≥2 (§2.14) | §4.4 (variant-axis-specific + a conservation/physics invariant) |
| 12 | perf-ledger row (§2.15) | algorithm wall-clock row (do NOT omit) |
| 13 | landing replays failing tests | gate-3 hash re-witnessed (worktree, Convention E; pre-synced for the Warp sim) |
| **14** | cross-stack equivalence | **N/A** for all (single-stack; §1.4). WU-F differentiable-axis (articulated) / the REFRAMED parent-vs-frontier invariant posture (Lenia) apply instead. |
| **mutation** | §2.13 | per-sim target registered + measured (§2.3); **advisory**. `render_similarity`/`variant` (HARD-gated) untouched → re-confirmed at each landing. |

---

## §7 — D-class routing summary

**Batch-wide (RESOLVED-IN-CHARTER):** D-LAYOUT (`packages/<sim>/` flat, §0.3); D-CI (`python-strict.yml` per-sim `test-<sim>` job; Lenia sims need no LFS-render pull, only the capture fixture); D-TAG (NO — phase-close-only, I7); D-GATE14 (N/A, single-stack); D-DET (§2.2 measure-then-declare; no EFECT in core); D-EQUIV (articulated: WU-F differentiable forward-equivalence; Lenia: REFRAMED invariant posture per plan §8.4).

**Per-sim (RESOLVED-IN-CHARTER unless ⚠):** articulated-diff **⚠ D-WARP-ADJOINT** (Stage-0 BLOCK-gate), D-CONTACT (RESOLVED — gravity-only smooth), D-USD (DEFER); particle-lenia **⚠ D-ENERGY-FORM** (Stage-0 web-verify), D-INTEGRATOR; flow-lenia **⚠ D-FLOW-FORM** + **⚠ D-REINTEG** (Stage-0 web-verify + the mass-conservation-determining discretization), **⚠ D-SPEC-SPLIT** (one `spec-frontier.md` stub serves two sims — Stage-0 §3.7 folder-convention decision).

**Operator-pending (membership, not in-charter-resolvable):** the 5 holds (4.15/4.16/4.17/4.22 greenfield; 4.21 rigorous-but-heavy); the difflogic-ca decidable-4th ruling. See §12.

**Tolerance schema (⚠ Stage-0 §S.2):** three new single-stack `[golden_tolerance.<category>.<sim>]` rows (`rigid-body.articulated-pedagogical-diff`, `continuous-ca.lenia-particle`, `continuous-ca.lenia-flow`). Read `tolerance-schema.json` + the landed lenia/articulated rows BEFORE writing; STOP-SCHEMA-FIT on misfit (the lenia-tolerance-schema-fix branch admits bespoke per-(category,sim) numeric keys — additive, not a new branch).

---

## §8 — Convention operationalization

- **§Q / §Q.6 (LFS)** — Stage-0 first action after anchor probe = `source tools/lfs/setup-lfs-s3-local.sh` (STOP-LFS-PUSH on non-zero, AT ANCHOR TIME). Objects: each `phase-4-<sim>.h5` fixture + the sim capture. Bootstrap in the **SAME shell command** as each `git lfs push` (the ising root-cause); GitHub = `git -c lfs.standalonetransferagent= push`; R2 = `git lfs push --object-id origin --stdin <<<"$OID"` (**remote BEFORE `--stdin`** — the F-MPM-3 arg-order fix). **GitHub-LFS budget EXHAUSTED → R2-first; `git push` exit 0 ≠ R2 received the OIDs — VERIFY** the R2 round-trip.
- **§R (integrity two-field)** — every audit carries `integrity_invariant: "0 HARD_FAIL / 14 SOFT_WARN"` (STOP-D only on a HARD_FAIL or count ≠ 14) + a **measured** `integrity_digest_at_head:`. At HEAD `ab934d7`: `4266d65e…1252fc35` (measured live; the COUNTS are the invariant — the digest drifts as golden tables land).
- **§S.2** — read `tolerance-schema.json` + one existing entry BEFORE appending any tolerance/registry row.
- **§S.5** — within ~2 min of each push, `gh run list --commit "$(git rev-parse HEAD)"`; any push-to-main workflow failure → STOP-CI-RED (check the FULL set incl. the HARD `mutation-testing` `render_similarity`/`variant` jobs).
- **§S6 (real sha256)** — every `evidence_hashes` is a measured sha256 or the `at-head` sentinel; never fabricated, never `: self`; `evidence_hashes` is a YAML **mapping**, not a list.
- **Convention #8 / #12 / E** — cite-at-assertion (web-fetch + re-verify the §4 anchors at Stage 0); two-commit SHA back-fill at each stage close (never `--amend`); gate-13 via `git worktree` (pre-synced for the Warp sim — the Batch-2 determinism kit).
- **F-RB-1 / F-RB-3** — `failing-tests-evidence/` excluded from the trailing-whitespace hook; `# mypy: ignore-errors` scoped to Warp-touching files (articulated-diff); run `mypy --strict` on test files before pushing (foundation-hardening C-1).
- **cat4 / cat1 hooks** — every `path:line` in audits needs the **full repo-relative path** (cat1 scans `tools/testkit/probes/`; the F-SMOKE-2 leading-ellipsis lesson); eof-fixer mutates capture `.json` sidecars (recompute sha256 post-hook); `subject:` colon-space breaks audit YAML.
- **§2.13 HARD-gate guard** — `render_similarity` (0.9242) + `variant` (0.8702) are HARD_FAIL-at-landing; **this batch's core ships neither's source** (no render coupling) → re-confirm both pass at EVERY landing.
- **Batch-2 gate-13 Warp determinism kit** — for articulated-diff (the only Warp+Hypothesis sim here): `wp.config.quiet=True` in conftest; Hypothesis `database=None` + explain-phase removed; pruned-venv evidence capture; pre-synced replay worktree (batch-2-close §6).

---

## §9 — Foundation symbols CONFIRMED present at HEAD `ab934d7` (FACT — live grep/listed)

| Symbol | Location (FACT — grepped/listed live) |
|---|---|
| `finite_difference_gradient` (WU-A) | `common/common-warp/src/common_warp/autodiff/finite_diff.py:26` |
| `InverseProblem` / `InitialStateRecoveryProblem` / `ControlProblem` (WU-A) | `common/common-warp/src/common_warp/autodiff/inverse_problem.py:73`/`:268`/`:272` |
| `wp.Tape` / `requires_grad` on-device precedent (Batch-1 smoke-diff) | `packages/eulerian-smoke-diff/eulerian_smoke_diff/sim.py:8` + `_kernels.py` |
| Parent ABA (articulated-diff build-on) | `packages/articulated-pedagogical/articulated_pedagogical/aba.py:47` (`aba_forward_dynamics`; `qdd.numpy()` tape-sever `:116`; gravity-only `:55-56`) |
| Parent analytic anchors (articulated) | `packages/articulated-pedagogical/articulated_pedagogical/analytic.py` (small-angle / elliptic-K / Jacobi-cn) |
| Parent task-4 PBT (`energy_drift_bounded` / `angular_momentum_about_pivot`) | `packages/articulated-pedagogical/tests/test_pbt_invariants.py:49`/`:78` |
| Parent grid Lenia (particle/flow build-on) | `packages/lenia/lenia/` (`kernel.py`, `packages/lenia/lenia/growth.py:27`, `packages/lenia/lenia/sim.py:114` `LeniaSim`) |
| `common_py.capture.Writer` (Taichi capture, schema 1.0.0) | `packages/lenia/lenia/sim.py:180` (the landed import shape) |
| `common_py.determinism.set_taichi_deterministic` | `packages/lenia/lenia/sim.py:138-144` (the landed import) |
| Determinism registry shape (`[<cat>.<sim>]` class/scope/atomic_ops) | `tools/testkit/determinism/registry.toml:44` (`[continuous-ca.lenia]`), `:80` (`[rigid-body.articulated-pedagogical]`) |
| `[golden_tolerance.continuous-ca.lenia]` / `[golden_tolerance.rigid-body.articulated-pedagogical]` | `tools/testkit/equivalence/tolerance.toml:131` / `:175` |
| Chakazul-Lenia vendor (base Lenia ONLY; Particle/Flow NOT vendored) | `references/Chakazul-Lenia/Python/{LeniaF.py,LeniaND.py,animals.json}` |
| `references/papers/` (frontier papers — **EMPTY**, §1.1 C-1) | `references/papers/` (no entries) |

---

## §10 — Articulated carry: §10-anchor reconfirmation against the LIVE landed ABA code (dispatch item A — FACT)

Per dispatch item A, the batch-2 charter §10 articulated anchors are re-confirmed against the live code at HEAD `ab934d7` (no new coordinator anchor-verification needed):

| §10 claim | Live-code reconfirmation (FACT) | Verdict |
|---|---|---|
| A1 `∂q̈/∂q = −(g/L)·cos q` (pendulum) | `packages/articulated-pedagogical/articulated_pedagogical/analytic.py:1` docstring `theta'' = -(g/L) sin(theta)` ⇒ derivative `−(g/L)cos(q)`; gravity is the only force (`packages/articulated-pedagogical/articulated_pedagogical/aba.py:55-56`) | **CONFIRMED** (pin sign/scale to the discretized `aba_kernel` at Stage 0) |
| A2 central-FD baseline | `common_warp.autodiff.finite_difference_gradient` present at `common/common-warp/src/common_warp/autodiff/finite_diff.py:26` | **CONFIRMED** |
| A3 `∂q̈/∂τ = H⁻¹` (single-link `1/(mL²)`) | ABA returns `qdd` from `(q,qd,tau)` (`packages/articulated-pedagogical/articulated_pedagogical/aba.py:47-116`); single-link inertia closed-form | **CONFIRMED** (web-re-verify the closed form at Stage 0) |
| No-contact / frictionless / gravity-only ⇒ SMOOTH gradient (no contact-differentiation, the Stages 31–33 problem) | `packages/articulated-pedagogical/articulated_pedagogical/aba.py:55-56` verbatim: "tau defaults to zero (free, frictionless joints — gravity is the only generalized force)" | **CONFIRMED** |
| WARP-NATIVE-TAPE caveat (`.numpy()` severs the tape → re-impl on-device) | `packages/articulated-pedagogical/articulated_pedagogical/aba.py:116` `return qdd.numpy().astype(np.float64)` — the tape-sever point | **CONFIRMED** (Stage-0 BLOCK-gate) |

**Net:** the §10 carry charter holds against the live code; the anchors are analytically correct and the smooth-gradient relief (no contact) is verbatim-confirmed. Ready to build on operator IN-ruling.

---

## §11 — Out of scope (this batch)

4.15 clebsch-pfm / 4.16 edge / 4.17 vpfm (HELD — Stack-C greenfield flow-map family, qualitative verification); 4.22 gaussian-fluids (HELD — greenfield grid-free Gaussian solver + WU-B+WU-C + render HARD-gate); 4.21 moment-encoded-lbm (HELD — rigorous-but-heavy Stack-C lift; operator-decidable); difflogic-ca (decidable 4th — built only on an operator IN-ruling + the frozen-gate scope); the sparse / neural-rendered / Newton / learned-dynamics variant classes; USD export (Stack-E DEFER policy); any tag (I7 operator-only).

---

## §12 — Closing criteria, operator-ratification items, CONTRADICTIONS

### 12.1 Verdict

**PROPOSED.** Membership (3-sim core + 1 decidable 4th + 5 holds) + per-sim charters + the ≥3-anchor verification plan + the articulated live-code reconfirmation are drafted; all foundation symbols CONFIRMED at HEAD; integrity 0 HF / 14 SW measured live. No code built. **HARD-STOP for coordinator anchor-verification + operator ratification.**

### 12.2 Operator-ratification items (before PHASE 1)

1. **Membership** — confirm the **3-sim core** (articulated-pedagogical-diff / 4.6; particle-lenia / 4.18; flow-lenia / 4.19), or amend.
2. **difflogic-ca (4.20)** — rule on the decidable 4th: IN (with the frozen-gate / truth-table / no-EFECT scope), or HOLD for a learned-CA batch?
3. **Flow-map trio (4.15/4.16/4.17)** — confirm HOLD (Stack-C greenfield + qualitative), or ratify a dedicated Stack-C flow-map batch lead-in?
4. **4.22 gaussian-fluids** — confirm HOLD (greenfield + WU-B+WU-C + render HARD-gate).
5. **4.21 moment-encoded-lbm** — confirm HOLD (rigorous-but-heavy Stack-C), or ratify inclusion (heavy; documented).
6. **Anchor verification** — the coordinator verifies the §4 anchors against primary sources (Particle-Lenia energy formulation; Flow-Lenia mass-conservation / reintegration; Featherstone single-pendulum gradient); Stage 0 web-fetches the (un-vendored, §1.1) papers + re-verifies.
7. **Cross-phase replay base** — confirm PHASE-1 Stage-0 replay targets `v0.3.0-phase-3` (the Batch-2 base).

### 12.3 CONTRADICTIONS vs EXPECTED (collector)

| # | Expected (dispatch / docs) | Measured / reasoned | Disposition |
|---|---|---|---|
| C-1 | plan amendment line 68: "all 13 frontier papers vendored to `references/papers/` BEFORE dispatch" | `references/papers/` is **EMPTY** at HEAD; Chakazul vendor has base Lenia only | **SURFACED** — Stage-0 web-fetches + cites each frontier paper per §2.8 (not "consume from references/papers/"); both core Lenia papers are openly available |
| C-2 | dispatch: "frontier-algorithm CPU sims (§4.4)" = candidate set of 8 | only **2 of 8** have a sound rigorous CPU anchor cheap enough for this batch (particle/flow-lenia); 4 are greenfield-heavy/qualitative, 1 (moment-LBM) rigorous-but-heavy, 1 (difflogic) rigorous-iff-scoped | **SHIFTED to a 3-sim core (articulated carry + 2 Lenia) + 1 decidable + 5 holds**; "fewer is correct" applied hard (§1) |
| C-3 | dispatch: exclude sims whose correctness gate "genuinely requires CUDA" | **NO §4.4 sim's correctness gate requires CUDA** (Stack C/D/E all CPU-runnable); the holds are **greenfield-heaviness / qualitative-verification**, NOT CUDA | **CLARIFIED** (§1.3) — distinct from Batch-2's 4.14 CUDA-favoring hold; mirrors Batch-2's 4.12 greenfield hold |
| C-4 | dispatch: "do NOT fake a pointwise golden for chaotic sims" | the 2 cored frontier sims anchor on **exact invariants** (energy-Lyapunov / mass-conservation), NOT trajectory goldens; pointwise determinism holds run-to-run while the *invariant* is the golden | **APPLIED** (§2.1, §4.2–4.3) — the pointwise-vs-trajectory distinction declared explicitly |
| C-5 | Phase-3 lenia `mass_approximately_conserved` was FALSIFIED under Quad4 | Flow-Lenia conserves mass **exactly by construction** (reintegration tracking) | **RE-ROUTED** (§2.1, §4.3) — the falsified invariant's sound home; re-declared, not widened |
| C-6 | articulated §10 anchors (batch-2 charter) | reconfirmed against live code at HEAD (§10) — A1/A2/A3 hold; gravity-only smooth-gradient + `.numpy()` tape-sever both verbatim-confirmed | **CONFIRMED** (§10) |
| C-7 | Particle Lenia = "Mordvintsev 2022 (Distill)" (plan §8.4) | it is the **Google Research Self-Organising Systems** web article + Colab (Distill paused 2021) — NOT Distill proper | **NOTED** — minor citation correction (cite the SOS article; the batch-2 neural-ca "Distill publishes no PSNR" precedent) |
| C-8 | §R digest expected near batch-2 `4266d65e…` | measured live at HEAD `ab934d7` = **`4266d65e…1252fc35`** (unchanged — only doc commits since `0b66a42`) | **NOTED** — 0 HF / 14 SW invariant holds |

### 12.4 No tag (I7 — operator-only). No `--amend`. Convention #12 SHA back-fill applies to `head_sha:`.

---

## §13 — STOP

**HARD-STOP.** This is the PHASE-0 deliverable. Do NOT begin building. Awaiting coordinator anchor-verification (focus: the Particle/Flow-Lenia rigorous anchors + the articulated gradient anchors) + operator ratification (§12.2) — resume PHASE 1 on **"continue"** (or amended membership / anchors / carry ruling). A fresh resume re-orients via the dispatch ORIENT list.
