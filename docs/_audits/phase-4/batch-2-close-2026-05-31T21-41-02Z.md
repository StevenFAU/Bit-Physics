---
date: 2026-05-31T21-41-02Z
author: phase-4 Run-2 Batch-2 close (Claude Code; self-driven sim-to-sim execution)
subject: "Phase-4 Run-2 BATCH 2 — neural-rendered frontier sims: CLOSE report (2/2 core LANDED + CI-GREEN)"
kind: batch-close
verdict: LANDED
head_sha: 0b66a42
prior_phase_tag: v0.3.0-phase-3
integrity_invariant: "0 HARD_FAIL / 14 SOFT_WARN"
integrity_digest_at_head: 4266d65ed66b993876a2e5944f9e8dbc3dc89cca58bd0f389134e6661252fc35
parent_audits:
  - docs/_audits/phase-4/batch-2-charter-2026-05-31T20-04-45Z.md
  - docs/_audits/phase-4/batch-1-close-2026-05-31T19-42-00Z.md
  - tools/testkit/probes/reports/3dgs-mpm-sh-update.md
  - tools/testkit/probes/reports/eulerian-smoke-neural.md
evidence_hashes:
  tools/testkit/golden/tables/3dgs-mpm-sh-rotation.json: sha256:88e4f408907aa58c910851884215bfecb7e552117e09d08dbf3004039a115b1f
  tools/testkit/golden/tables/eulerian-smoke-neural-coupling.json: sha256:1b72cfaed85a8acfa518f94df78f58f056c382acb205befa55a7084662bb1372
  docs/sim-specs/neural-rendered/3dgs-mpm/spec-sh-update.md: sha256:d3ea6fef02f1066ee972bb7715de8c8116a9524e1ca6d79fbb91a66553c02226
  docs/sim-specs/volumetric-grid/eulerian-smoke/spec-neural.md: sha256:440f0d2a5728d6223ad085801973772033510aa42081c0b91ab7302bec17d769
  docs/phase4/ledger.md: sha256:6a6b896991d470887ecf836eaadf63d4494b422f316bd60b201c2d292692737d
---

# Phase-4 Run-2 BATCH 2 — neural-rendered frontier sims (CLOSE report)

> Closes the batch-2 charter (`docs/_audits/phase-4/batch-2-charter-2026-05-31T20-04-45Z.md`).
> **Both core sims LANDED + pushed to origin/main + CI-GREEN.** Self-driven sim-to-sim. FACT =
> ran/read/measured; INFERENCE = reasoned. Verdicts four-state (CONFIRMED / SHIFTED / BLOCKED /
> FLAGGED). **NO tag** (I7, operator-only).

## §0 — Headline

| | |
|---|---|
| **Batch theme** | **Neural-rendered** frontier sims (spec §4.3 / §5.11; ledger Stages 19/21) |
| **Membership** | **2 sims** — Sim A **3dgs-mpm-sh-update** (S19/4.11, the deferred Wigner-D SH rotation) · Sim B **3dgs-smoke** (S21/4.13, `eulerian-smoke-neural`) |
| **Status** | **2/2 LANDED + CI-GREEN.** Tip SHAs: Sim A `6b73099` · Sim B `0b66a42` (batch tip = HEAD `0b66a42`) |
| **HELD (per charter ratification)** | 4.12 3dgs-sph (no Stack-E SPH parent) · 4.14 i-PhysGaussian (CUDA-favoring diff-rasterizer gate) · articulated-pedagogical-diff (dedicated diff-carry batch) |
| **Integrity at close HEAD `0b66a42` (FACT, measured)** | **0 HARD_FAIL / 14 SOFT_WARN**, rc 0; §R digest `4266d65e…1252fc35` |
| **4.1 HARD-gated modules** | `render_similarity` (0.9242) + `variant` (0.8702) — **touched as CONSUMER only**; re-confirmed passing at every landing (CI `mutation-testing` + `test-render-similarity` GREEN; local 63/63) |
| **Verdict** | **LANDED** (closed-with-shifted: Sim B's `opacity_monotone_bounded` PBT re-declared on a float32-saturation finding; one PBT regime re-scoped, never widened) |

Both sims are real consumers of the **WU-C `PhysicsCoupling` / `render` / `default_density_to_opacity`**
promotion (the Rule-of-Three justification for promoting the coupling Phase-3→Phase-4). Sim A lands
the one piece task-8 explicitly **deferred** (`3dgs-mpm-sh-update`); Sim B is the **first
smoke→3DGS coupling** in the portfolio.

---

## §1 — Per-sim final verdicts + tip SHAs + 13-gate results

| # | Sim (stack) | Tip SHA | Verdict | 13-gate | gate-14 | mutation |
|---|---|---|---|---|---|---|
| A | 3dgs-mpm-sh-update (E) | `6b73099` | **LANDED** | 13/13 GREEN | N/A (single-stack) | advisory (registered; MEASURE deferred — mutmut unprovisioned) |
| B | eulerian-smoke-neural / 3dgs-smoke (E) | `0b66a42` | **LANDED** (closed-with-shifted: 1 PBT re-declared) | 13/13 GREEN | N/A | advisory (registered; deferred) |

**13-gate (batch-common):** gate-1 spec sheet (`spec-sh-update.md` / de-stubbed `spec-neural.md`,
§1–13); gate-2 pre-impl probe; gate-3 failing suite + sha256 (deterministic — see §6); gate-4
two-pronged golden (numerical coupling ≥3 anchors + perceptual render-similarity); gates 5/6
tier-1/2; gate-7 Cat-1 citations; gate-8 Cat-2 public API; gate-9 replayable capture; gate-10
determinism↔capture (MEASURED); gate-11 ≥2 PBT; gate-12 perf-ledger row; gate-13 landing replay
(deterministic-MATCH, pre-synced; §6). **gate-14 N/A for both** (single-stack neural; no
cross-stack neural sibling) — the WU-C neural-axis floor + physics-equivalence-vs-parent apply.

---

## §2 — Anchors used per golden, NAMED TO SOURCE (both prongs) + shifts

Every numerical coupling golden carries **≥3 genuinely independent, distinct-source anchors**
(gate-4 cat3 distinctness). **No charter A1/A2/A3 anchor needed an on-evidence shift this batch**
(the charter anchors — coordinator-verified against the primary sources — held as drafted; the
batch-1 "anchors shift to the discretized form" pattern did NOT recur because the anchors were
already pinned to the discretized renderer/coupling at the charter).

### Sim A — 3dgs-mpm-sh-update (`tools/testkit/golden/tables/3dgs-mpm-sh-rotation.json`)
- **A1** degree-1 Wigner-D `D₁(R) = P R Pᵀ` closed form (`P = [[0,−1,0],[0,0,1],[−1,0,0]]` from the
  landed renderer's `(−y,+z,−x)` real-SH basis). Hand-verified: `c=(1,0,0)` under `R_z(90°)` →
  `(0,0,−1)`. **Source:** closed-form Wigner-D (matrix algebra).
- **A2** rotation-equivariance vs the LANDED renderer `_eval_sh`: `eval_SH(D₁(R)c, R·d) =
  eval_SH(c, d)` (= PhysGaussian's inverse-rotation-on-view-directions). **Source:** SO(3)
  equivariance (web + coordinator-verified against arXiv:2311.12198v3 Eq. (9)).
- **A3** pure-stretch / identity frozen: `polar(SPD F)=I` → SH unchanged (recovers the task-8 MVP
  "SH frozen"); `polar(orthogonal F)=F`. **Source:** hand-derivation + MVP consistency.
- The golden's expected values were generated by an **INDEPENDENT dipole-rotation oracle** (a
  distinct code path from the `P R Pᵀ` implementation), cross-checked equal to the implementation
  to 1e-12. Degree ≤ 1 scope (degree ≥ 2 raises; documented further extension).

### Sim B — 3dgs-smoke (`tools/testkit/golden/tables/eulerian-smoke-neural-coupling.json`)
- **A1** Beer-Lambert `α = 1 − exp(−d)` at `d ∈ {0, ln2, 5}` → `{0, 0.5, 0.99326…}` (the WU-C
  `default_density_to_opacity`). **Source:** Beer-Lambert volume-rendering opacity.
- **A2** Kerbl 2023 Eq. (6) emission-absorption alpha-compositing: a single centred Gaussian
  (opacity `a`, colour `c`) → centre pixel `≈ a·c` (MEASURED to clear `compositing_abs = 2e-2`;
  render f32 + EWA projected-peak). **Source:** Kerbl et al. 2023 (ACM TOG 42(4)) Eq. (6).
- **A3** zero-density degenerate: `density ≡ 0` → all opacities 0 → background. **Source:** hand-derivation.

### Prong 2 (both) — perceptual render-similarity
Re-render the canonical coupled sim vs OWN committed golden PNGs; clears the §2.12 floors
(PSNR≥28 / SSIM≥0.85 / LPIPS≤0.15). **Sim A observability MEASURED:** `max|rotated − frozen|`
render delta = **0.0828 > 0** (the SH rotation genuinely changes the render → the degree-1
directional-SH scene makes the gate NON-VACUOUS). **Sim B:** smoke visibly renders (max opacity 0.61).

---

## §3 — Determinism posture per sim (MEASURED, not assumed)

Both: **bit-exact, same-stack-same-hw, seed-pinned; no EFECT.** Rasterization atomic-ordering
(`composite_splats` `wp.atomic_add`) is the determinism-sensitive surface (the charter's MPM-scatter
caution) — MEASURED bit-exact on Warp CPU serial `wp.launch`, NOT assumed.

| # | run-twice MEASURE | physics-equivalence-vs-parent |
|---|---|---|
| A | image + `gaussian_sh` + `particle_F` `np.array_equal` (SVD polar + `P R Pᵀ` deterministic NumPy) | particle_pos + particle_F **bit-equal** to `gs_mpm.run_canonical_sim` (identical kernels — by construction) |
| B | image + density + `gaussian_opacities` `np.array_equal` (argsort + Beer-Lambert deterministic NumPy) | per-frame density **bit-equal** to a direct `eulerian-smoke-stack-e` `stable_fluids_step_3d` rollout (MEASURED `np.array_equal`) |

Registry rows: `[neural-rendered.3dgs-mpm-sh-update]` + `[neural-rendered.eulerian-smoke-neural]`,
`class = bit-exact`.

---

## §4 — PBT invariants + regime scope (≥2 per sim; re-declared never widened)

| # | PBT-1 (variant-axis) | PBT-2 (coupling / physics) |
|---|---|---|
| A | `sh_rotation_equivariant` (random `R∈SO(3)`, degree ≤ 1; the A2 oracle as a property) | `covariance_spd_preserved` (`Σ'=FΣFᵀ` SPD, scales > 0 under random det>0 `F`) |
| B | `opacity_monotone_bounded` (**RE-DECLARED**, see below) | `render_similarity_self_identity` (frame rendered twice → PSNR=∞ / SSIM=1) |

**SHIFTED (HARD RULE 2 re-declaration, NOT a widening) — Sim B `opacity_monotone_bounded`:** the
Beer-Lambert map is mathematically bounded in `[0,1)` for ALL `d ≥ 0`, but the model stores opacity
as **float32**, whose epsilon near 1.0 (~6e-8) rounds `1 − exp(−d)` UP to exactly `1.0` for `d ≳ 16`
(e.g. `1−exp(−18) = 1.5e-8 < eps`). The strict `α < 1` is falsified for arbitrary `d`. RE-DECLARED
the **regime** to physical/representation-valid density `d ∈ [0, 10]` (the Taylor-Green smoke IC is
`O(1)` in `[0,1]`; `1−exp(−10)=0.9999546` is float32-strict-safe) — the input domain re-scoped, the
assertion NOT loosened. (The Phase-3 lenia `mass_approximately_conserved` precedent.)

---

## §5 — Per-sim mutation (advisory, §2.13) + the HARD-gated modules

Both registered a per-sim mutmut target (`[targets.gs_mpm_sh_update]`, `[targets.eulerian_smoke_neural]`)
in `tools/testkit/mutation/mutmut-config.toml`, oracle-grounded test surface only (the A1/A2/A3
anchors + equivalence/physics + PBT; **snapshots FORBIDDEN**). **MEASURE deferred-advisory** — mutmut
is unprovisioned in the package venvs (the `--all-packages` workspace sync does not expose the
`mutmut` console script; batch-1-close §6 / C-D precedent). The HARD-gated `render_similarity`
(0.9242) + `variant` (0.8702) ship no source here → **untouched, re-confirmed passing** (CI
`mutation-testing` GREEN at both landings; local `render_similarity` 63/63).

---

## §6 — gate-13 determinism engineering (the load-bearing batch finding)

Neural-rendered RED suites import Warp + Hypothesis, both of which emit **non-deterministic
stdout/output** that breaks the gate-13 normalized-hash byte-stability. Root-caused and fixed
(both sims' conftests now carry the fix; the **first** Phase-4 sims to need it):

1. **Warp module-load timing** — `Module X load on device 'cpu' took Y ms` (Y varies run-to-run) on
   stdout. FIX: `wp.config.quiet = True` in the conftest (before any `@wp.kernel` loads).
2. **Hypothesis explain-phase** — the `# or any other generated value` annotation is
   non-deterministic across a fresh-vs-cached `.hypothesis/` DB (the replay's fresh worktree always
   explores). FIX (Sim B): a conftest Hypothesis profile with `database=None` + the explain phase
   removed.
3. **Plugin-set pollution** — the shared workspace venv accumulates other packages' dev-extra
   plugins (e.g. `pytest-timeout`), changing the `plugins:` line. FIX: capture the evidence after a
   pruning `uv sync --all-packages` (matching the replay worktree's env).
4. **`replay_failing_tests` no-pre-sync uv-build stdout** — the tool runs `uv run` in a fresh
   worktree WITHOUT a prior `uv sync`, so uv's first-run build output intermittently lands on
   stdout. The replay matches **deterministically when the worktree is pre-synced** (5/5 for Sim A;
   MATCH for Sim B). SURFACED candidate: `replay_failing_tests._checkout_worktree` should
   `uv sync --all-packages` the worktree (as `replay_prior_phase` already does).

**gate-13 result (pre-synced replay):** Sim A `d2511406…` MATCH (5/5 deterministic); Sim B
`a3a54e27…` MATCH. Both `failing-tests-evidence` captured in the canonical (clean, warp-quiet,
no-explain) environment. Sim A's 1a/1b/1c were rebuilt once to carry the warp-quiet conftest +
canonical evidence; Sim B's were rebuilt once to additionally carry the Hypothesis det-profile.

---

## §7 — §S.5 CI sweep at each landing push

| # | Landing push | §S.5 result |
|---|---|---|
| A | `953b081→6b73099` | **10/10 workflows GREEN** (MEASURED): `test-3dgs-mpm-sh-update` SUCCESS (full coupled sim + live R2 LFS fetch + smudge-assert + both prongs + PBT); integrity; mutation-testing (render_similarity/variant HARD); determinism; equivalence; cpp-strict; ts-strict; tolerance-budget; audit-append-only; structure |
| B | `6b73099→0b66a42` | **GREEN** (the non-python-strict workflows SUCCESS incl. mutation-testing HARD + integrity + equivalence + determinism; `python-strict` / `test-eulerian-smoke-neural` confirmed at the close push — the live R2 LFS-fetch verification) |

**§Q / §Q.6 (LFS):** each sim's new LFS objects (golden render PNGs + the schema-corpus `.h5`
fixture) were pushed to **R2 first** (`source setup-lfs-s3-local.sh && git lfs push --object-id
origin --stdin`, rc 0) then GitHub (`git -c lfs.standalonetransferagent= push`, LFS upload 5/5 then
4/4). R2 receipt independently verified by the CI `test-<sim>` job's selective R2 LFS pull +
smudge-assert (Sim A confirmed SUCCESS).

---

## §8 — Foundation-symbol confirmation + §0.3 paths

WU-C symbols imported UNCHANGED + confirmed at HEAD: `PhysicsCoupling` /
`default_density_to_opacity` (`common/common-3dgs/src/common_3dgs/coupling.py:97`) / `render` (degree
1/2/3 real-SH eval, `common/common-3dgs/src/common_3dgs/render.py:83`) / `GaussianSplatModel` /
`Camera.look_at`; `render_similarity` `psnr`/`ssim`/`lpips`. Parents: `packages/3dgs-mpm/` (+
`mpm-multimaterial-stack-e`) and `packages/eulerian-smoke-stack-e/`. **§0.3 flat paths:**
`packages/3dgs-mpm-sh-update/` (sibling — the frozen `packages/3dgs-mpm/` MVP NOT mutated) +
`packages/eulerian-smoke-neural/`. Both sims reuse the parent physics **by import** so
physics-equivalence holds by construction.

---

## §9 — CONTRADICTIONS vs EXPECTED (collector)

| # | Expected (charter / dispatch) | Measured / reasoned | Disposition |
|---|---|---|---|
| C-A | charter anchors are PROPOSED, "shift on-evidence if ill-posed" | NO A1/A2/A3 shift needed (anchors were pinned to the discretized renderer/coupling at the charter; coordinator-verified) | **HELD as drafted** (the batch-1 shift pattern did not recur) |
| C-B | gate-14 against the WU-F neural-axis cap | both single-stack → gate-14 N/A; the render-similarity floor + physics-equivalence-vs-parent apply | **CLARIFIED** (§1) |
| C-C | rasterization atomic-ordering "MEASURE, do not assume" | MEASURED bit-exact (Warp CPU serial) for both | **CONFIRMED** (§3) |
| C-D | mutation MEASURED | advisory; deferred (mutmut unprovisioned in the package venv) — consistent with batch-1 | **NOTED** (§5) |
| C-E | Sim A: the canonical scene is degree-0 → SH-rotation is a no-op | CONFIRMED; authored a NEW degree-1 directional-SH scene → rotation observable (0.0828 render delta) | **RESOLVED** (the charter §1.2 FLAG) |
| C-F | gate-13 byte-stable RED evidence | Warp/Hypothesis/plugin/uv non-determinism broke it → 4 root-caused fixes (warp-quiet, Hypothesis no-explain/no-db, pruned-venv capture, pre-synced replay) | **RESOLVED** (§6); `replay_failing_tests` pre-sync SURFACED as a tooling fix candidate |
| C-G | Sim B `opacity_monotone_bounded` for any density | float32 opacity storage saturates `1−exp(−d)` to 1.0 at `d≳16` | **SHIFTED** — regime re-declared to `d∈[0,10]` (physical + float32-strict-safe), never widened (§4) |
| C-H | 4.11 = a full mpm-multimaterial-neural build | the MVP coupling was already landed (task-8); the Phase-4 delta is the SH-update (sibling package; ledger 4.11 → landed via `spec-sh-update.md`) | **CONFIRMED** (charter §1.2; D-PLACEMENT operator-ratified) |
| C-I | §R digest stable | drifted (`0ed0f924`→`4266d65e`) as 4 golden tables + 2 specs landed; 0 HF / 14 SW invariant HELD throughout | **EXPECTED** (digest is content-of-report; counts are the invariant) |

---

## §10 — Closing statement

**Batch-2 LANDED (2/2 core), CI-GREEN, pushed to origin/main at HEAD `0b66a42`.** The
neural-rendered batch validated the WU-C `PhysicsCoupling` / `render` / `default_density_to_opacity`
substrate on **two** distinct couplings — the deferred-from-task-8 **Wigner-D SH rotation**
(degree-1 `D₁(R)=P R Pᵀ`, observable on a new directional-SH scene) and the **first smoke→3DGS**
density→opacity coupling — each two-pronged (numerical coupling golden ≥3 independent anchors +
perceptual render-similarity), each physics-equivalent-to-parent by construction, each bit-exact
deterministic. The HELD set (4.12 3dgs-sph, 4.14 i-PhysGaussian, articulated-pedagogical-diff)
remains operator-decidable for the A100 / dedicated-diff batches per the charter. The batch's
load-bearing engineering finding is the **gate-13 neural-rendered determinism kit** (warp-quiet +
Hypothesis no-explain/no-db + pruned-venv capture + pre-synced replay), now baked into both
conftests and reusable by every later Warp/3DGS sim. **NO tag** (I7 — operator-only). Convention #12
SHA back-fill applies to `head_sha`.
