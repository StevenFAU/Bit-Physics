---
date: 2026-06-01T01-44-34Z
author: phase-4 CLOSE campaign (Claude Code; partial-close at CPU-tier verified frontier portfolio)
subject: "Phase-4 PHASE-LEVEL CLOSE — 9/27 frontier sims LANDED + foundation 8WU + 4.1 hardening + consolidation; 18 deferred-with-a-home; A-8/A-9 corrigenda applied; v0.4.0-phase-4 tag PREPPED (operator pushes)"
kind: phase-landing
verdict: CONFIRMED
head_sha: cccf98d
prior_phase_tag: v0.3.0-phase-3
proposed_phase_tag: v0.4.0-phase-4
integrity_invariant: "0 HARD_FAIL / 14 SOFT_WARN"
integrity_digest_at_head: 9894964135e582fc3d94448f87bdf8d859a1ff29e3675a45fa04a7f04b40b15f
gates: { integrity: PASS, pytest: PASS, equivalence: PASS, determinism: PASS, property: PASS, perf-ledger: PASS, mutation: ADVISORY-2-HARD-PROMOTED-GREEN, tolerance-budget: PASS }
cross_phase_replay:
  prior_phase: v0.3.0-phase-3
  tool_ok: false
  correctness_verdict: CONFIRMED
  deliverable_pytest: "267/267 PASS"
  reds_decomposed: "3 pytest sub-failures (i6/i7/cost-axis META-guards — PASS at this HEAD, fail at frozen tag by design; C3-surfaced bundle) + 1 mutation META-red; ZERO deliverable-correctness regressions"
parent_audits:
  - docs/_audits/phase-4/foundation-close-2026-05-30T21-54-07Z.md
  - docs/_audits/phase-4/foundation-hardening-2026-05-31T00-34-08Z.md
  - docs/_audits/phase-4/batch-1-close-2026-05-31T19-42-00Z.md
  - docs/_audits/phase-4/batch-2-close-2026-05-31T21-41-02Z.md
  - docs/_audits/phase-4/batch-3-close-2026-05-31T23-55-27Z.md
  - docs/_audits/phase-4/mid-phase-state-2026-06-01T00-32-58Z.md
evidence_hashes:
  docs/phase4/ledger.md: at-head
  docs/spec-amendments-proposed.md: at-head
  docs/architecture.md: at-head
---

# Phase-4 PHASE-LEVEL CLOSE — partial close at the CPU-tier verified frontier portfolio

> The phase-close artifact (the §F.3.6 / Phase-5-§0 partial-close mechanism). FACT =
> ran/read/measured at the cited HEAD this session; INFERENCE = reasoned. Four-state
> verdicts (CONFIRMED / SHIFTED / BLOCKED / FLAGGED). Commits direct to `main`
> (trunk-based). The spec freeze (§9.6) LIFTS at this boundary — the Phase-4 proposed
> corrigenda A-8/A-9 are APPLIED (§3). **NO tag pushed** (I7 — operator-only; the
> annotated-unsigned `v0.4.0-phase-4` tag is PREPPED, commands in §7). Authored with
> replay-friendly front-matter (verdict + `gates:` map) so a future phase's cross-phase
> replay against `v0.4.0-phase-4` does NOT hit the missing-landing-file gap Phase 3 hit.

## §0 — Headline

| | |
|---|---|
| **Close shape** | **PARTIAL close** at the CPU-tier verified frontier portfolio — **9 of 27** §11.5 sims LANDED; **18 DEFERRED-WITH-A-HOME** (§4) |
| **LANDED** | foundation **8/8 WU** + 4.1 hardening (**2 HARD-promoted + 3 advisory**) + **9 frontier sims** (batches 1–3) + consolidation **C1–C6**, all pushed origin/main, CI-GREEN |
| **Close HEAD** | this commit (corrigenda + ledger re-scope + this audit); parent `cccf98d` |
| **Integrity (FACT, measured live at close working tree)** | **0 HARD_FAIL / 14 SOFT_WARN**, rc 0; §R digest `9894964…b40b15f` |
| **Integrity meta-test (FACT)** | `pytest tools/integrity/tests/` **95 passed, 0 warnings** — A-9 CLEARED the A4 OpenVDB license-drift SOFT_WARN (the corrigendum's explicit purpose) |
| **Full testkit (FACT)** | `pytest -W error tools/testkit/` **401 passed** at close HEAD |
| **Cross-phase replay vs `v0.3.0-phase-3`** | **CORRECTNESS-VERDICT CONFIRMED** (267/267 deliverable pytest + integrity/equivalence/determinism/property PASS). Tool `ok=False` decomposes to ZERO deliverable regressions (§5.2) |
| **Both HARD mutation gates** | `render_similarity` 0.9242 + `variant` 0.8702 — HELD (CI `mutation-testing` GREEN; consumer-only in all 9 sims) |
| **Corrigenda APPLIED** | **A-8** (papers cited-at-Stage-0, not vendored — IP) + **A-9** (OpenVDB MPL-2.0→Apache-2.0); numbering reconciled forward (§3) |
| **Tag** | `v0.4.0-phase-4` annotated-unsigned **PREPPED**, NOT pushed (I7 — operator commands §7) |
| **Verdict** | **CONFIRMED** — partial phase-close; the 18-sim deferral is SURFACED for operator ratification (§4), not a unilateral commitment |

## §1 — LANDED inventory (FACT, from the committed batch/foundation audits)

### §1.1 — Foundation 8/8 WU (`foundation-close-2026-05-30T21-54-07Z.md`)
WU-P portfolio conventions · WU-A autodiff schema 1.1.0 (26-pair HARD gate) · WU-B
sparse-volume (NanoVDB host surface; runtime CUDA-gated) · WU-C Gaussian Splatting ·
WU-D Newton (CPU+USD landed; **solver runtime BLOCKED — CUDA absent**, operator-ratified
CPU-fallback — SHIFTED) · WU-E learning harness (Lightning 2.6.5 / PhysicsNeMo 2.1.0) ·
WU-F variant equivalence (6 per-axis caps) · WU-G phase ledger (27 rows 1:1 §11.5).

### §1.2 — 4.1 foundation-hardening (`foundation-hardening-2026-05-31T00-34-08Z.md`)
| Target | before → after | Disposition |
|---|---|---|
| `render_similarity` | 0.663 → **0.9242** | **HARD-PROMOTED** (clears 0.85; wired `gate_helpers mutation-promoted-floor`) |
| `variant` | 0.695 → **0.8702** | **HARD-PROMOTED** (clears 0.85) |
| `common_3dgs` | 0.663 → 0.7708 | SOFT_WARN advisory (lever: oracle tests) |
| `code_verification_mms` | 0.215 → 0.438 | SOFT_WARN advisory + SURFACED |
| `property` | 0.170 → 0.6453 | SOFT_WARN advisory + SURFACED |

### §1.3 — 9 frontier sims (batches 1–3) — full-13-gate, gate-14 N/A (single-stack), determinism MEASURED bit-exact, ≥2 PBT regime-scoped
| Ledger | 4.x | Sim (stack) | Batch | Gated-on anchor(s) | On-evidence shift (HARD-RULE-2; never widened) |
|---|---|---|---|---|---|
| 9 | 4.1 | reaction-diffusion-2d-diff (D) | 1 | A1 Fourier-eigenmode ∂Loss/∂D_u (~1e-15) + A3 reaction-ODE-limit | A3 MMS→reaction-ODE-limit analytic |
| 11 | 4.3 | mpm-multimaterial-diff (D) | 1 | A1 ballistic ∂Loss/∂v₀ EXACT + A3 neo-Hookean dσ/dε | A3 DiffTaichi→neo-Hookean numeric |
| 12 | 4.4 | lenia-diff (D) | 1 | A1 Quad4 dG/dμ,dG/dσ (~1e-14) + A3 conv-Jacobian | A3 parameter-free-kernel→conv-Jacobian |
| 13 | 4.5 | eulerian-smoke-diff (E) | 1 | A1 linear-advection 2Mᵀ(Mu₀−t) (~4e-15) + A3 discrete-diffusion | A3 continuous-heat→discrete |
| 14 | 4.6 | articulated-pedagogical-diff (E) | 3 | A1 −(g/L)cos q (≤1.9e-16) + A3 1/(mL²) EXACT | scope multi-link→single-pendulum (n≥2 adjoint gap MEASURED) |
| 19 | 4.11 | 3dgs-mpm-sh-update (E) | 2 | A1 deg-1 Wigner-D + A2 SO(3)-equivariance + render-sim | (anchors pinned at charter) |
| 21 | 4.13 | eulerian-smoke-neural / 3dgs-smoke (E) | 2 | A1 Beer-Lambert + A2 Kerbl Eq.6 + render-sim | PBT `opacity_monotone_bounded`→d∈[0,10] (f32 saturation) |
| 26 | 4.18 | particle-lenia (D) | 3 | A1 force=−∇E (~1e-22) + A3 E_total translation-symmetry | A1 energy-Lyapunov→force=−∇E (LOCAL rule, operator-corrected) |
| 27 | 4.19 | flow-lenia (D) | 3 | A1 mass-balance Σ I=1 (~Nε) + A3 zero-flow EXACT | tolerance bit-exact→summation-roundoff (operator-corrected) |

### §1.4 — Consolidation C1–C6 (`mid-phase-state-2026-06-01T00-32-58Z.md`)
C1 A-8 papers-IP corrigendum (`42394cf`) · C2 gate-13 replay worktree-sync + plugins
normalization (`dc0d336`) · C3 replay correctness-vs-meta `ok=` classification (`eeb6d98`)
· C4 mutation hardening — the **"unprovisioned"→wall-clock-advisory CORRECTION** (see §6) ·
C5 banked sweep + A-9 OpenVDB (`c8b1cac`) · C6 mid-phase-state audit (`cccf98d`).

## §2 — §R integrity digest at close HEAD (MEASURED, §R)

- **Invariant (FACT, measured live this session):** `uv run --no-sync python -m integrity
  --all --mode strict` → **0 HARD_FAIL / 14 SOFT_WARN**, rc 0. The 14 SOFT_WARN are the
  pre-existing `cat5.audit-links` phase-1/2 notes — unchanged by this close.
- **Digest (FACT, measured at the close working tree — corrigenda applied):**
  `9894964135e582fc3d94448f87bdf8d859a1ff29e3675a45fa04a7f04b40b15f` — byte-identical to
  the mid-phase measurement (`be29666`/`c8b1cac`). The A-8/A-9 corrigenda edit DOCS
  (architecture.md / plan / spec-amendments / READMEs / MANIFEST) that do NOT touch the
  integrity report's HF/SW line-set, so the digest is STABLE. Per [[integrity-baseline-digest-method]]
  the **0HF/14SW COUNTS are the invariant** (§R); the digest stability is incidental.

## §3 — Corrigenda APPLIED at this close (spec freeze §9.6 LIFTS) + numbering reconciliation

**Both Phase-4 corrigenda VERIFIED at application (verify-don't-trust), then applied:**

- **A-8 (frontier papers → CITED at Stage-0, NOT vendored).** VERIFIED: `references/papers/`
  holds only `.gitkeep` (0 papers); cat1 citation-chain GREEN (0 HARD_FAIL) for all 9 landed
  sims. IP-critical (public-MIT must not redistribute copyrighted PDFs; §2.4 mandates
  independent derivation). Applied to **architecture.md** §12.9 body+roster-header + §61
  changelog + §1866 phase-gates + Pattern-C (×2) + §F.3.5 entry-gate + LES-row; **phase-4-plan.md**
  §54/§68/§72/§2542/§2610/§2964; **references/README.md** papers-dir note. §2649 checklist
  RETAINED (papers pre-*identified* in the §12.9 roster, *resolved* at Stage-0 — no vendoring
  implied). phase-0-plan historical scaffold prose left as landed-history.
- **A-9 (OpenVDB MPL-2.0 → Apache-2.0).** VERIFIED: `references/openvdb/LICENSE` = Apache
  License 2.0; `references/openvdb/MANIFEST.toml:6` = `Apache-2.0`; NanoVDB headers carry
  `SPDX-License-Identifier: Apache-2.0`. Applied to **architecture.md** §D.3 OpenVDB row
  (license cell now exactly `**Apache-2.0**`; relicense provenance moved to the version
  column — see the regression note below); **phase-4-plan.md** §61/§249/§529/§1672/§1713/§1760/§2897/§2920
  (+ §2881 v6.0 changelog **ANNOTATED, not deleted** — the earlier "MPL not Apache" correction
  is itself now stale); **references/openvdb/MANIFEST.toml** inline note. **Effect CONFIRMED:**
  the integrity meta-test `test_manifest_license_agrees_with_d3_registry` now PASSES with **0
  warnings** (was a UserWarning on every run) — the A4 MANIFEST-vs-§D.3 drift is cleared.
  - **Self-caught regression (verify-don't-trust paid off):** the first A-9 edit put the
    relicense provenance INSIDE the §D.3 license cell. The A4 guard's `_license_token` strips
    only `*`/backtick then compares the 4th cell verbatim to the MANIFEST `Apache-2.0` →
    string-mismatch → the warning PERSISTED. Re-located the provenance to the version column;
    license cell reduced to `Apache-2.0`; warning cleared. The whole point of A-9 is to clear
    this warning — a verbose cell would have silently defeated it.

**NUMBERING RECONCILIATION (one collision, reconciled FORWARD).** The WU-B foundation report +
the `references/openvdb/MANIFEST.toml` inline note recorded the OpenVDB corrigendum as an
informal forward-reference **"A-8"** (predating the central registry); the consolidation pass
then filed papers as **A-8** and bumped OpenVDB to **A-9**. **Chosen consistent numbering:
A-8 = papers-citation, A-9 = OpenVDB-license** (the registry's). The MANIFEST inline reference
is reconciled forward to A-9 (done this close). The append-only-locked WU-B / foundation-close /
consolidation sealed audits that wrote "A-8" for OpenVDB are **NOT retro-edited** (Convention
A + D5); a forward note in `spec-amendments-proposed.md` (the new APPLIED block) + this audit
are the canonical record. **NO collision remains** in the live registry.

## §4 — DEFERRED SCOPE — 18 sims, each deferred-WITH-A-HOME (SURFACED for operator ratification)

27 §11.5 rows; **9 LANDED** (§1.3); **18 DEFERRED.** The ledger (`docs/phase4/ledger.md`) now
marks each deferred row with its target future-phase home + reason. The split (a **planning
recommendation**, NOT a unilateral commitment) groups them into two named homes so none is
orphaned:

### Home 1 — **Phase-4-CUDA** (gated on A100 / CUDA 12 + driver 545+) — 10 sims
CUDA MEASURED absent on this host (WU-D Newton runtime BLOCKED; WU-B `SparseVolume.from_voxels`
CUDA-gated). Sims: **15/16** (NanoVDB sparse runtime), **17** (Stack-C sparse-volume runtime),
**18** (sparse-AMR), **22** (CUDA-favoring diff-rasterizer i-PhysGaussian), **31/32/33** (Newton
solver runtime — locomotion/granular/manipulator), **34/35** (learned-dynamics GNS + LES-closure
training, CUDA-bound).

### Home 2 — **Phase-4-Greenfield-CPU** (CPU-feasible; each needs a base sim / sound-anchor first) — 8 sims
Not hardware-blocked but cannot land soundly until a prerequisite substrate exists or a
verifiable (non-qualitative) anchor strategy is designed: **10** (sph-diff 5th-diff,
operator-decidable), **20** (3dgs-sph — needs Stack-E SPH parent), **23/24/25** (flow-map trio
— new substrate), **28** (difflogic-ca — new substrate), **29** (moment-LBM — sound-anchor
needed), **30** (gaussian-fluids — new 3DGS-fluid substrate).

**Reconciliation:** 9 LANDED + 10 (→Phase-4-CUDA) + 8 (→Phase-4-Greenfield-CPU) = 27 = §11.5
4.1–4.27. NONE silently skipped — every deferral is a hardware blocker or a documented
base-sim/anchor dependency (several operator-HELD in the batch charters). **SURFACED for operator
ratification** (the operator ratifies, re-routes, or provisions an A100 to continue the CUDA tier).

## §5 — CLOSE GATES (run at close HEAD)

### §5.1 — integrity + testkit (FACT, live)
- `integrity --all --mode strict` → **0 HARD_FAIL / 14 SOFT_WARN**, rc 0 (§2). **Expected 0 HF
  — MET.**
- `pytest tools/integrity/tests/` → **95 passed, 0 warnings** (A-9 cleared A4 drift).
- `pytest -W error tools/testkit/` → **401 passed** at close HEAD.

### §5.2 — cross-phase replay vs `v0.3.0-phase-3` (C3-fixed ok=-classification, repointed --audit) — CORRECTNESS CONFIRMED
`replay_prior_phase --prior-phase phase-3 --audit docs/_audits/phase-3/close-R3-R5-task9-20260530T145125Z.md
--gates integrity,pytest,equivalence,determinism,perf-ledger,property,mutation,tolerance-budget`
→ tool `summary: prior_phase=v0.3.0-phase-3 ok=False (correctness) strict_ok=False meta_ok=False`.

**This is NOT a HARD-STOP. The tool `ok=False` decomposes to ZERO deliverable-correctness
regressions (VERIFIED, not assumed):**

| gate | class | replay verdict | substance |
|---|---|---|---|
| integrity | correctness | **PASS** | deliverables reproduce |
| equivalence | correctness | **PASS** | |
| determinism | correctness | **PASS** | |
| property | correctness | **PASS** | |
| pytest | correctness | **FAIL** | the **3 reds are i6/i7/cost-axis META-guards** in `tools/testkit/lfs_migration/` — NOT sim/deliverable tests. **267/267 deliverable tests PASS.** |
| perf-ledger | meta | PASS | |
| mutation | meta | FAIL | tag-frozen baseline-status string (C3-classified meta) |
| tolerance-budget | meta | PASS | |

- **VERIFICATION (the load-bearing check):** reproduced in a `v0.3.0-phase-3` worktree —
  `uv run pytest -W error tools/testkit/` = **3 failed / 267 passed**, the 3 being exactly
  `test_cost_axis_selective_fetch::test_workflow_capture_requirements_registry_is_complete`,
  `test_i6_convention_12::test_backfill_commits_are_separate_and_doc_only`,
  `test_i7_no_agent_tags::test_no_agent_pushed_tag_in_subphase_range`. **All three PASS at THIS
  close HEAD** (run live: `3 passed`) → they are **tag-frozen meta/hygiene drift** (they
  reference post-tag state: the i7 allowlist advanced post-tag, the i6/cost-axis registries
  carry post-tag sims), NOT broken tests and NOT deliverable regressions. This is precisely the
  `pytest`-bundles-meta case the C3 author SURFACED-not-forced.
- **CORRECTNESS VERDICT = CONFIRMED** (the prior phase's deliverables reproduce). The tool's
  `ok` field cannot express it only because the `pytest` gate bundles 3 meta-guards it cannot
  sub-classify — an OPEN surfaced follow-up (a meta-marker / deselection), carried from C3
  (`tools/integrity/integrity/scripts/replay_prior_phase.py:106`).

### §5.3 — §S.5 full CI sweep
Runs on the close push (all workflows). Recorded in §9 after the push completes. Any red on a
CORRECTNESS workflow = STOP (none expected — the close is docs-only over a CI-GREEN HEAD).

## §6 — RECONCILE-THE-RECORD: the C4 mutation characterization correction (annotate, do NOT retro-edit)

The batch-1/2/3 close audits recorded per-sim mutation MEASURE as "deferred — mutmut
**unprovisioned** in the package venv." The consolidation C4 pass MEASURED that **mutmut 2.5.1
IS provisioned and runs end-to-end**; the true constraint is **wall-clock cost** (~1–3 h/sim;
each mutant re-runs the kernel-recompiling per-sim suite). So the §2.13 advisory posture STANDS,
but the lever is **dedicated mutation-runner wall-clock / CI budget + per-sim NumPy-reference
oracle tests**, NOT "provision mutmut." flow_lenia measured **0.478** (322 mutants) this pass —
honest, below the 0.80 floor, NOT widened. The append-only batch audits are **NOT retro-edited**
(Convention A + D5); this §6 + the C6 mid-phase-state audit §3.1 are the canonical going-forward
correction of that "unprovisioned" characterization.

## §7 — TAG PREP (Phase D — operator pushes; agent does NOT, I7)

The tree is clean + pushed to origin/main at the close commit (this audit + corrigenda + ledger
re-scope all landed). The **annotated-unsigned** `v0.4.0-phase-4` tag is PREPPED but NOT pushed
(I7 — operator-only; no GPG/signing required, annotated-unsigned per §D). **Exact operator
commands** (run on the precise close commit — substitute the SHA from `git rev-parse HEAD` after
this lands, reported in §9):

```bash
git tag -a v0.4.0-phase-4 <CLOSE_SHA> -m "Phase 4 close — CPU-tier verified frontier portfolio (9/27 sims + foundation 8WU + 4.1 hardening + consolidation); 18 deferred-with-a-home (Phase-4-CUDA / Phase-4-Greenfield-CPU); A-8/A-9 corrigenda applied"
git push origin v0.4.0-phase-4
```

**Immediate post-tag follow-up (the Phase-3 precedent — allowlist-add is POST-tag).** Adding
`v0.4.0-phase-4` to `OPERATOR_PHASE_TAGS` in `tools/testkit/lfs_migration/test_i7_no_agent_tags.py`
**cannot** be landed in THIS close commit: `test_operator_phase_tags_present` asserts every tag
in that set EXISTS, so listing the tag before the operator pushes it would turn the I7 meta-test
RED (CI fail). Confirmed precedent: `v0.3.0-phase-3` was added at `638b247`, which is a
**descendant** of the tag commit `362179f` (the tag does not contain the allowlist-add) — i.e.
post-tag. **Therefore:** after the operator pushes `v0.4.0-phase-4`, land a one-line follow-up
commit adding it to `OPERATOR_PHASE_TAGS` (mirrors `638b247`). Until then, I7 stays green (the
tag does not yet exist, so `git tag --contains` does not surface it).

## §8 — CONTRADICTIONS vs EXPECTED + SURFACED for operator

| # | Expected | Measured / reasoned | Disposition |
|---|---|---|---|
| X-1 | replay `ok=True` on correctness | tool `ok=False`; pytest bundles 3 META-guards (pass at HEAD, fail at frozen tag) | **CORRECTNESS CONFIRMED** — 0 deliverable regressions; C3-surfaced bundle (§5.2). NOT a HARD-STOP |
| X-2 | A-9 clears the A4 license-drift warning | first verbose-cell edit did NOT clear it (string-mismatch) | **SELF-CAUGHT + fixed** (provenance→version column); warning cleared (§3) |
| X-3 | §R digest drifts at close | digest STABLE (`9894964…`) — corrigenda are docs not HF/SW lines | EXPECTED (§2; 0HF/14SW is the invariant) |
| X-4 | A-9 dependent refs labelled architecture.md lines 1672/2920 | MISLABELED — that content is in phase-4-plan.md; architecture.md had 1 MPL (the §D.3 row) | re-located by content (#8); applied correctly (§3) |
| X-5 | 9/27 = phase incomplete | partial-close is sanctioned (§F.3.6 / Phase-5 §0) | **CONFIRMED partial close**; 18 deferred-with-home surfaced (§4) |

**SURFACED for an operator decision (NOT unilaterally decided):**
1. **The partial-close + 18-sim re-scope itself** (§4): ratify the Phase-4-CUDA (10) + Phase-4-Greenfield-CPU
   (8) homes, OR provision an A100 host + continue the CUDA/learned tier. This close assumes ratify-partial.
2. **The `v0.4.0-phase-4` tag push** (§7) + the post-tag I7-allowlist follow-up.
3. **C3 follow-up** — a meta-marker / deselection on the 3 testkit meta-guards (i6/i7/cost-axis) so
   the `pytest` replay gate sub-classifies cleanly and a future replay reads `ok=True` (carried from C3).
4. **C4 mutation wall-clock** — the per-sim 0.80 floor needs dedicated runner time + NumPy-reference
   oracle tests (the named lever); accept the §2.13 advisory posture indefinitely otherwise.
5. **B-4 `verify_evidence at-head`** — optionally sanction `at-head` as existence-only (loosens the pin).

## §9 — Closing

Phase 4 is **CLOSED partial-complete** at the CPU-tier verified frontier portfolio: **foundation
8/8 WU + 4.1 hardening (2 HARD + 3 advisory) + 9 frontier sims + consolidation C1–C6**, all
LANDED, pushed, CI-GREEN, with both HARD mutation gates holding and the **0HF/14SW** integrity
invariant preserved throughout. The spec-freeze lifted: **A-8** (papers cited-at-Stage-0, IP-safe)
and **A-9** (OpenVDB Apache-2.0) are APPLIED, verified, and numbering-reconciled; A-9's effect
(A4 SOFT_WARN cleared) is confirmed. The cross-phase replay's CORRECTNESS verdict is **CONFIRMED**
(0 deliverable regressions; the tool `ok=False` is the C3-surfaced meta-bundle, verified). **18 of
27 sims are deferred-WITH-A-HOME** to two named future phases (Phase-4-CUDA / Phase-4-Greenfield-CPU),
none orphaned — SURFACED for operator ratification. The annotated-unsigned `v0.4.0-phase-4` tag is
PREPPED with exact operator commands (§7); **NO tag pushed** (I7). Verdict **CONFIRMED**.

> **head_sha convention (mirrors the consolidation mid-phase-state audit `head_sha: c8b1cac`):**
> `head_sha: cccf98d` is the parent HEAD this close was MEASURED against; the close commit (this
> audit + corrigenda + ledger re-scope) lands ON TOP. The precise close-commit SHA (the one the
> operator tags, §7) is reported in the close report after it lands. Reproduce the §R digest live
> per §R.5 (`9894964…b40b15f` at this close working tree).
