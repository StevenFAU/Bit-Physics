---
date: 2026-05-30T21-51-41Z
author: phase-4 wu-g-ledger agent (Claude Code)
subject: "Phase-4 Stage 8 / WU-G (Phase Ledger) — probe + §0.3 code-path ratification"
kind: wu-probe
verdict: INFORMATIONAL
head_sha: 55b3422
prior_phase_tag: v0.3.0-phase-3
integrity_invariant: "0 HARD_FAIL / 14 SOFT_WARN"
parent_audits:
  - docs/_audits/phase-4/wu-f-probe-2026-05-30T21-47-31Z.md
  - docs/_audits/phase-4/pre-dispatch-probe-2026-05-30T17-04-02Z.md
---

# Phase-4 Stage 8 / WU-G — Phase Ledger: probe + build reconciliation

> `verdict: INFORMATIONAL`. WU-G is documentation-only (plan §2359 — no TDD; no
> failing-tests hash). `head_sha` back-filled per Convention #12.

## §0 — What landed (FACT)

- `docs/phase4/ledger.md` — **exactly 27 data rows, 1:1 with spec § 11.5
  4.1-4.27** (verified: 27 rows, 27 unique items). 13 columns (the §4.2.G 11 +
  v9 `PBT invariants declared` + `Perf-ledger row`). Status `planned`.
- `docs/phase4/dependency-graph.md` (foundation→frontier socket map) +
  `docs/phase4/_audits/.gitkeep`.
- **16 variant stubs** created (NONE pre-existed — see §1.2), deduped by
  (sim, variant-axis): 6 `spec-diff.md`, 3 `spec-sparse.md`, 3 `spec-neural.md`,
  4 `spec-frontier.md`. Each carries the §4.2.G stub header + the v9 `§ 6 PBT
  invariant declarations: TODO` and `§ 8 Independent-reference anchors: TODO`.
- New-category READMEs: `docs/sim-specs/rigid-body/README.md` +
  `docs/sim-specs/learned-dynamics/README.md` (created; the category DIRS already
  existed — §1.3).
- Prior WUs P/A/B/C/D/E/F all landed to `main` (confirmed via the WU-C…WU-F
  commits + green CI sweeps).

## §1 — §0.3 SHIFTs (landed reality wins over stale plan prose)

1. **§0.3 CODE-path ratification (consistent with WU-P).** The plan prose
   prescribes `<category>/<sim>/<variant>/` CODE dirs (57 stale occurrences;
   ZERO landed precedent — pre-dispatch probe §5). WU-G ratifies (in the ledger
   header) that Phase-4 variant CODE lands under **flat `packages/<sim>-<variant>/`**
   (WU-P's flat-`packages/` ratification extended); the DOCS path
   `docs/sim-specs/<category>/<sim>/spec-<variant>.md` stays category-nested
   (correct + enforced). The exact flat leaf is a per-variant-stage decision.
2. **`particle-fluid` → `particle-fluids` (plural).** Plan §7.8 maps 4.2/4.12 to
   `particle-fluid/sph-water`; the landed dir is `docs/sim-specs/particle-fluids/sph-water/`.
   Followed landed reality.
3. **The plan's "expected to skip `hybrid-pg/mpm-multimaterial/spec-neural.md`"
   is FALSE.** That stub did NOT pre-exist — Phase-3 task-8 (3dgs-mpm) landed at
   `docs/sim-specs/neural-rendered/3dgs-mpm/`, not as an mpm-multimaterial neural
   variant spec. So the skip-if-exists skipped NOTHING; all 16 stubs were created.
4. **`rigid-body` + `learned-dynamics` category dirs already exist** (with
   `articulated-pedagogical` / `pinn-poisson` from Phase 3) — they are not "new"
   folders; only their `README.md` skeletons were created.
5. **4.6 (diff) parent = `rigid-body/articulated-pedagogical`** (the Phase-3
   pedagogical sim; plan §7.8 placeholder `rigid-body/<Phase-3-pedagogical-sim>`).

## §2 — Acceptance

Ledger 27 rows 1:1 with §11.5; every stub conforms to the §4.2.G template + v9
TODO lines; `integrity --all` 0 HARD_FAIL / 14 SOFT_WARN; markdown/cat-4 grammar
green (commit-hook). No tag (I7). WU-G is the last foundation WU.

## Provenance

WU-G session; documentation-only build commit, direct to `main`. Convention #12
SHA back-fill applies. Integrity invariant preserved.
