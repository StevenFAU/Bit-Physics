---
date: 2026-05-30T21-47-31Z
author: phase-4 wu-f-equiv agent (Claude Code)
subject: "Phase-4 Stage 7 / WU-F (Variant Equivalence) — probe + build reconciliation"
kind: wu-probe
verdict: INFORMATIONAL
head_sha: 5689a2b
prior_phase_tag: v0.3.0-phase-3
integrity_invariant: "0 HARD_FAIL / 14 SOFT_WARN"
parent_audits:
  - docs/_audits/phase-4/wu-e-probe-2026-05-30T21-41-13Z.md
---

# Phase-4 Stage 7 / WU-F — Variant Equivalence: probe + build reconciliation

> `verdict: INFORMATIONAL`. FACT = ran/read; INFERENCE = reasoned. `head_sha`
> back-filled per Convention #12.

## §0 — Probe findings (FACT)

- `tools/testkit/equivalence/` exists (Phase-0 cross-stack harness:
  `EquivalenceVerdict`, `compare_captures`, `load_tolerance_table`;
  `__main__.py`; `tolerance.toml` / `tolerance-budget.toml` / `tolerance-schema.json`).
  WU-F's `variant/` is a **sibling subpackage** with its OWN `compare_captures`
  (the §4.2.F signature: `reference_capture`/`variant_capture`/`tolerances`/
  `at_sim_time`) — distinct module, no symbol collision.
- The equivalence module has NO registration pattern (the `__init__` re-exports
  the cross-stack surface; no plugin registry). So WU-F's C2 is a NO-OP — the
  variant subpackage is consumed as `from equivalence.variant import ...`.
- `tolerance-budget.toml` already carries the Phase-4 carryover (WU-P) with the
  cross-stack per-category caps; WU-F's `assert_within_budget` adds the per-VARIANT-AXIS
  caps in code (not in that TOML — the TOML's `cross_stack` caps are a different axis).

## §1 — What landed (all GREEN)

| Surface | Detail |
|---|---|
| `VariantToleranceSpec` | output_name + absolute_tol + relative_tol + norm (L2/Linf/wasserstein); validated. |
| `compare_captures` | matched-time (time/sim_time diagnostic else step index), per-norm error `<= abs + rel·‖ref‖`, mixed-schema (intersection + skipped_fields), named-missing → ValueError. |
| `EquivalenceReport` | the 9-field §4.2.F dataclass. |
| `assert_within_budget` + `ToleranceBudgetExceeded` | the **6 per-axis caps numerically as specced** (plan §7.7): differentiable rel≤1e-2, sparse abs≤1e-4, neural PSNR≥25/SSIM≥0.7 floor, frontier (no fixed cap), newton abs≤fp16 (~9.77e-4), learned norm-bound≤3×. |
| Tests | 16 (tolerance 9 + compare 5 + PBT 2: identity_variant_passes + tolerance_monotone). Full equivalence suite 18 GREEN. |
| Mutation | `[targets.variant]` registered at threshold **0.85**; measured in the consolidated close-batch (§2.13 SOFT_WARN-advisory). |
| docs | docs/testkit/variant-equivalence.md. |

## §2 — Acceptance

`pytest -W error tools/testkit/equivalence/variant/` GREEN; ruff + mypy --strict
(variant source) clean; `integrity --all` 0 HARD_FAIL / 14 SOFT_WARN. No tag (I7).

## Provenance

WU-F session; probe + build commits, direct to `main`. Convention #12 SHA
back-fill applies. Integrity invariant preserved.
