# C-1 / U-1 `sph-water-diff` — pre-implementation probe (gate 2)

> **Cluster:** Phase 6 / C-1 (charter `docs/phases/phase-6/c1-charter.md`, RATIFIED § 10).
> **Unit:** U-1 = ledger row 10 / spec item 4.2 — `particle-fluids/sph-water` diff variant, Stack D (DiffTaichi, locked v8).
> **Session:** build dispatch 2026-06-11; probe at HEAD `053d5e4`.

## § 1 — Cross-phase audit replay vs `v0.5.0-phase-5` (build-dispatch first action)

`python -m integrity.scripts.replay_prior_phase --prior-phase phase-5 --audit docs/_audits/phase-5/phase-5-close-2026-06-10T12-38-41Z.md --gates integrity,pytest,equivalence,determinism,perf-ledger,property,mutation,tolerance-budget`
→ `summary: prior_phase=v0.5.0-phase-5 ok=False (correctness) strict_ok=False meta_ok=True`; per-gate: integrity/equivalence/determinism/property/perf-ledger/mutation/tolerance-budget **PASS**, `pytest` **FAIL**.

**Decomposition (the load-bearing check, Phase-4-landing §5.2 precedent):** reproduced in a clean `v0.5.0-phase-5` worktree on the home filesystem (`git worktree add … v0.5.0-phase-5` + `uv sync --frozen --all-packages --all-extras`, sync rc=0) → `uv run --no-sync pytest -W error tools/testkit/` = **3 failed, 398 passed**. The 3 reds are EXACTLY the known `tools/testkit/lfs_migration/` META-guards (`test_cost_axis_selective_fetch.py::test_workflow_capture_requirements_registry_is_complete`, `test_i6_convention_12.py::test_backfill_commits_are_separate_and_doc_only`, `test_i7_no_agent_tags.py::test_no_agent_pushed_tag_in_subphase_range`) — the same meta-guard class the Phase-4 landing audit § 5.2 decomposed (tag-frozen guards evaluating post-tag history; all three are GREEN at HEAD in CI). **Zero sim/deliverable test regressions. NOT a HARD-STOP; deliverable correctness CONFIRMED intact.** (Two earlier decomposition attempts were discarded as environment artifacts and are recorded for honesty: a `/tmp` worktree hit a disk-quota-broken `uv sync` (torch wheel), and a `GIT_LFS_SKIP_SMUDGE=1` worktree produced 72 pointer-file `legacy_captures_roundtrip` failures; neither reflects the tag.)

## § 2 — Parent surface (measured at HEAD)

- Parent = `packages/sph-water-stack-d/` (landed Phase 2 Stage 3). Canonical capture `captures/sph-water-stack-d/dam-break-100K-particles-seed42-step1000` — **100K**, not Appendix D.2.3's "1M" (R20-ratified at the Phase-2 plan-drafting close: "100K per R20; spec 1M = Stack-C tier", `docs/_audits/phase-2/sub-phase-sph-water-stack-d/plan-drafting-landing-2026-05-23T23-20-09Z.md:50`). §0.3 landed-reality-wins; D.2.3's stale `1M` rows route to the cluster-close audit with the D-6 registry corrections.
- **Landed canonical physics (R-S3/S6, ratified Phase 2):** semi-implicit-Euler **gravity free-fall** (`_integrate`: `v_z += g·dt; x += dt·v`; no pressure/viscosity force feedback) + **SPH cubic-spline density** as the per-frame field observable (`_compute_density`, 27-cell spatial hash, Monaghan 2005 Eq. 2.7 kernel; Bender & Koschier 2015 Eq. 5 continuity on the golden surface). `canonical_params()` carries `viscosity=0.01` but the step loop does not consume it.
- **SHIFT (documented, minor, scope-preserving):** plan § 8.1 prose assigns ParamSpec "(viscosity, kernel-size, density-base, surface-tension, damping)". Viscosity/surface-tension/damping are NOT in the landed parent forward; a diff variant must differentiate the real landed physics, not a fabricated one (§0.3; EXP-C honesty). **ParamSpec SHIFTS to the live parameters:** `v0z` (shared initial vertical velocity — the plan's "control problem", DiffTaichi throw-to-target precedent = the landed `mpm-multimaterial-diff` recover-v0 pattern) and `h` (kernel size — the SPH-specific surface, via a density-loss kernel-width identification problem). This directly answers batch-1's EXP-C hold ("gradient-through-neighbor-search needs regime-scoping", `docs/_audits/phase-4/batch-1-charter-2026-05-31T10-51-55Z.md:133`): the regime is a **fixed-topology interior cloud** (free-fall preserves relative positions exactly, so the neighbor set is constant over the horizon and the kernel-support boundary is never crossed; fixtures placed away from the q=1/q=2 spline knots).

## § 3 — Plan of record

- **Exemplar:** `packages/mpm-multimaterial-diff/` (particle-based, recover-v0, time-indexed `needs_grad` fields, kernel-factory + `ti.static` unrolling, conftest f64 single-thread runtime). Module layout, test layout, capture shape, registry/mutmut/CI wiring all mirrored.
- **Anchors (≥3 independent, golden table `tools/testkit/golden/tables/sph-water-diff-gradient.json`):** A1 free-fall control gradient closed form `dLoss/dv0z = 2N(dt·T)²(v0z−v0z*)` (hand-derived kinematics; gravity cancels); A2 central-FD baseline (distinct method, multi-particle); A3 kernel-width density gradient `dW/dh = −(σ₃/h⁴)(3f(q)+q f′(q))` two-particle closed form (distinct physical term/parameter/method; Monaghan 2005 + hand-derivation; cross-checked vs the parent's SPlisHSPlasH-anchored golden surface).
- **PBT (≥2, charter §3.1):** `gradient_matches_finite_difference` (rel_tol 1e-3, regime-scoped) + `density_summation_positive` (exact kernel-positivity property).
- **Posture expectation:** bit-exact same-stack-same-hw (single-thread CPU f64; loss reduction = sum-only atomic) — MEASURE then declare in `tools/testkit/determinism/registry.toml` rows `[particle-fluids.sph-water-diff.{forward,gradient}]`.
- **Capture:** `captures/sph-water-diff/sph-water-diff-recover-v0z-8part-seed42.{h5,json}` (schema 1.1.0 `gradient_fields`; batch-1 problem-scoped-descriptor precedent) + corpus seed `tests/fixtures/legacy-captures/phase-6-c1-sph-water-diff.{h5,json}`.
- **LFS preflight (STOP-LFS-PUSH check, batch §Q):** `source tools/lfs/setup-lfs-s3-local.sh` → `lfs-s3 ready` (endpoint + bucket wired) rc=0. GREEN.
- **Tolerance routing:** existing `sph` category; no new category (charter §3.1).
- **Commits (Convention A):** (1) failing tests + stubs + workspace registration + hashed evidence; (2) implementation green + registry + mutmut; (3) capture + spec de-stub + equivalence row + perf-ledger + CI job. ≤500 lines each.
