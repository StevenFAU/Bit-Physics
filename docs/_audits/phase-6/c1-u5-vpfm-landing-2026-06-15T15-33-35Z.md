# C-1 / U-5 `eulerian-smoke-frontier-vpfm` — unit landing report

> **Cluster:** Phase 6 / C-1 (charter `docs/phases/phase-6/c1-charter.md`, RATIFIED § 10).
> **Unit:** U-5 = Phase-4 ledger row 25 / spec § 11.5 item 4.17 — Stack C; the SECOND
> particle-flow-map (PFM) surface, **reusing the U-4 clebsch-pfm substrate** (charter
> § 4 ordering rationale: built while the substrate is freshest).
> **Frontier paper:** Wang, Zhou, Feng, Li, Sun, Chen, Turk, Zhu, "Fluid Simulation on
> Vortex Particle Flow Maps", ACM TOG 44(4), SIGGRAPH 2025, DOI `10.1145/3731198`,
> arXiv:2505.21946. CITE-DON'T-IMPORT.
> **Verdict:** **LANDED** — acceptance ctest 12/12 (doctest 10/10 = 147,722 assertions
> + the REFRAMED fixture gate `vpfm_reframed_equivalence`); corpus roundtrip 42 (lock
> 39→40); full § S.5 CI sweep GREEN at `9850116` — all 10 workflows (structure,
> audit-append-only, equivalence, mutation-testing, tolerance-budget-check, ts-strict,
> integrity, determinism, cpp-strict, `python-strict` through R2).
> **Commit chain (rebased onto origin/main `665c400`, Lane B P6/P7/P8):** `7286133`
> (stage-1a scaffold) → `6f6963f` (stage-1a RED 10/10) → `6e79249` → `f112557` →
> `b417123` (stage-1b GREEN 10/10) → `2739112` (stage-1c-i: REFRAMED gate + spec
> finalization) → `9850116` (stage-1c-ii: canonical capture + corpus + registrations +
> perf) → `<stage-2 SHA back-filled per Convention #12>`.

## Re-anchor / resumption record (this session)

Resumed mid-stage-1c by MEASUREMENT (charter Convention M, re-anchor #8). The prior
session had committed stages 1a/1b (acceptance GREEN 10/10) and updated the capture
writer's dt note, but **the on-disk canonical capture was NaN-poisoned** (frames
250–500 = all 128³ cells NaN) and the spec TBD-1c values were unfilled — stage-1c was
genuinely open. Root cause established by direct measurement + code-read (NOT assumed
from the prior session's comment): the descriptor's nominal dt = 0.005 crosses the
inviscid-TG cascade CFL ceiling 1/(n·dt) = 1.56 at 128³ (MEASURED u_max 6.46 by step
200) and blows up to NaN by step 250; the run loop is pure inviscid (no
dissipation/clamp), so the blowup is advective feedback (reconstructed velocity grows
with the grid-scale cascade, RK4 advection overshoots) — resolution-dependent and
dt-sensitive, with no singular-inversion defect in the path. Energy stayed bounded
(KE ~0.13) while u_max exploded localized → numerical, not a global physical cascade.

## SHIFT — fixed dt = 0.00125 (measured-then-declared; charter § 0.3)

The canonical capture is taken at **dt = 0.00125**, the MEASURED CFL-safe fixed step at
128³ (ceiling 6.25, 4× headroom). The locked descriptor `taylor-green-128cube-seed42-step500`
fixes the **grid (128³) and step count (500)**, both matched VERBATIM; the fixed-dt
VALUE is the documented descriptor-parity adaptation (spec § 1 — the paper's
CFL-adaptive Δt is unused). 500 steps at dt = 0.00125 = physical t = 0.625, the
well-conditioned pre-cascade window; step-based reinit (n_v=20 / n_g=5) runs 4× more
often per physical time, additionally stabilizing. Result: **all 11 frames physical** —
KE 0.124950 → 0.126167 (conserved 0.97 %), u_max bounded ≤ 0.99890 every frame,
div_max 4.6e-14 (exact discrete div∘curl identity), total-vort 1.34e-4.

## Gates (spec Appendix D.6, Stack-C adaptation)

| Gate | Verdict | Evidence |
|---|---|---|
| 1 spec sheet | GREEN | `spec-frontier-vpfm.md` (per-variant; the shared `spec-frontier.md` stub STAYS for 4.16/4.22) — TBD-1c filled from measurement |
| 2 probe | GREEN | `c1-u5-vpfm-probe-2026-06-13T00-19-01Z.md` — anchor re-verified live (arXiv v1; ACM DL 403); SHIFTs: inviscid steady-TG anchor reuse, solid-boundary DEFERRED (periodic descriptor exercises zero boundary code), edge-centred vorticity + compatible curl/div stencil |
| 3 failing-first | GREEN | RED ctest at `6f6963f`: 10 cases, 0 passed (sha `47f097f3…` footer-hashed) |
| 4 goldens (≥3 anchors) | GREEN | A1 closed-form vorticity lift FD-checked (≤1e-8, 216 pts) + exact div∘curl identity (7.3e-15) + carried-ω_a bit-drift EXACTLY 0.0; A2 ‖𝒯ℱ−I‖ 3.461e-9 → declared 1e-7 (O(dt⁴) contraction measured 16.3×) + Eq.-14 Hessian-vs-FD resolution-converging gate; A3 inviscid steady 2D-TG drift 5.411e-4→2e-3, energy 9.678e-4→3e-3, IC residual 1.600e-3→5e-3 |
| 5/6 diagnostics | GREEN | per-frame KE/enstrophy/u_max/mass budget metrics; `init_velocity_residual` MEASURED into result + manifest (1.00345e-4 at 128³ — direct lift, no init pathology) |
| 7 citations | GREEN | DOI 10.1145/3731198 (live-verified, CITE-DON'T-IMPORT) + Li et al. 2025 (U-4 substrate) + Zhou et al. 2024 + Cottet & Koumoutsakos 2000 (Cauchy formula) + Angot et al. 1999 (context) |
| 8 public API | GREEN | `bit_physics::vpfm` header (`include/bit_physics/vpfm/vpfm.hpp`); clean per-target `-O3 -ffp-contract=off` build |
| 9 capture | GREEN | `captures/eulerian-smoke-frontier-vpfm/taylor-green-128cube-seed42-step500.{h5,json}` — D.2.3 descriptor VERBATIM; .h5 sha256 `1e04a359…` (738 MB LFS, in R2 + GitHub LFS); **[x][y][z] axis layout matching the parent**; dt=0.00125 SHIFT (above) |
| 10 determinism↔capture | GREEN | 2-run bit-identity witness `41caa46f…` (tolerance 0.0; **run 2 IS the capture run**); cross-optimization-level identity `-O3` ≡ `-O0` (witness `57035cdb…` at n=32, both builds); registry `[volumetric-grid.eulerian-smoke-frontier-vpfm]` class bit-exact, atomic_ops none; manifest `bit-exact-same-hw` |
| 11 PBT (≥2) | GREEN | `reconstructed_velocity_divergence_free` (exact div∘curl at FP scale every step, swept (n_v,n_g) × both ICs; ≤3.7e-15 → declared 1e-12) + `total_circulation_bounded` (Kelvin: total-vort ≤4.34e-5→2e-4 + slice-circ ≤2.14e-3→1e-2) + carried-ω_a zero-drift bonus |
| 12 perf-ledger | GREEN | 5,888 s canonical invocation (vorticity-lift init + 2-run witness + 738 MB write; ~5.9 s/step/run at 128³, 16.8M particles; ~12.2 GB peak RSS — probe HARD-STOP-5 CLOSED feasible) — `docs/perf-ledger.md` row at `9850116` |
| 13 failing-replay | GREEN | worktree at `6f6963f` rebuilds the RED shape (10 cases, 0 passed via the stage-1b `run_vpfm` throw; the U-3/U-4 banked ctest adaptation — pytest replay tool does not drive ctest) |
| 14 cross-stack | **N/A → REFRAMED gate GREEN** | no cross-stack sibling (charter § 3.5); the REFRAMED frontier-vs-parent metric gate is the equivalence surface — all four clauses green (below) |

## REFRAMED equivalence (measured-then-declared; no new tolerance category)

Variant budget-metric fixture `vpfm-equivalence/variant-budget-metrics.json` derived
once from the canonical capture; the parent side cross-references the U-4 frozen
fixture `clebsch-pfm-equivalence/parent-budget-metrics.json` directly (single-source,
probe § 4.5 — no second parent run; CI never pulls the LFS captures, probe § 4.4).
**Measured contrast:** the landed parent canonical trajectory is numerically BLOWN UP
by step 50 (u_max 1.337e8 → 4.87e20) while the VPFM variant stays physical for the
ENTIRE 500-step run — KE conserved 0.97 %, enstrophy 1.0 → 4.44× by real vortex
stretching, u_max ≤ 0.99890 over every frame (NO saturation regime, unlike U-4's
wave-ceiling u_max ≈ 477) — a ~20-order stability contrast. **Declared clauses
(margins ≥ 2.5×), all GREEN:** (a) frame-0 IC agreement (KE rel 4.016e-4 ≤ 1.2e-3;
enstrophy 4.016e-4 ≤ 1.2e-3; u_max 2.006e-4 ≤ 6e-4; blob FP-tight ≤ 1e-12 — ~50×
tighter than the U-4 wave-fit init); (b) parent step-50 blowup present (≥1e6);
(c) variant physical over the FULL window [0,500] (KE drift ≤ 9.739e-3 ≤ 2.5e-2;
enstrophy/enstrophy(0) ∈ [1.000, 4.439] ⊂ [0.8, 12.0]; u_max ≤ 0.99890 ≤ 2.5; mass
drift ≤ 1.742e-1 ≤ 0.45); (d) stability contrast (variant u_max ≤ 2.5 over ALL frames
vs parent ≥ 1e6). Thresholds declared in spec § 3.5 — no budget widening.

## SHIFTs + findings (documented)

1. **dt = 0.00125 CFL-safe canonical** (above) — the on-disk dt=0.005 NaN capture is
   the discarded first run (direct evidence, not assumed); the silent gap was no
   canonical-scale stability check (the n=16/32 ≤50-step acceptance suite never reaches
   the cascade). Root cause confirmed by code-read (pure advective feedback).
2. **`test_reframed_equivalence.py` was MISSING** — referenced by spec § 3.5 (ctest
   `vpfm_reframed_equivalence`) but absent from the prior session's tree; authored this
   session + registered in ctest, GREEN.
3. **Solid-boundary surface DEFERRED with cause** (probe § 4.2, charter § 3.5
   pre-authorized): the periodic canonical descriptor exercises zero boundary code; the
   paper's cut-cell + Brinkmann no-slip surface would ship untested-by-the-capture.
4. **Vorticity closed-form transcription slip corrected** (stage 1b-i): the probe § 4.1
   2D ω_z (cos·cos) → the FD-verified sin·sin closed form.
5. **Capture axis layout [x][y][z]** matching the parent (the U-4 transpose lesson
   priced in from the start).
6. **Per-target `-O3 -ffp-contract=off`** bit-identical to `-O0` (witness `57035cdb…`);
   tree default -O0 ~10× slower (infeasible at 128³).
7. **Mutation N/A** (C++; mutmut is python-only — the rd2d/U-3/U-4 precedent).
8. **origin/main advanced between sessions** (`b2d8da5` → `665c400`, Lane B P6/P7/P8):
   the 7 U-5 commits (merge-base = the shared stage-0 probe `e1f9317`) rebased CLEAN —
   zero file overlap with the Lane B surfaces; no HARD-STOP.
9. Removed two stray ctest temp files (`tests/Testing/Temporary/**`) committed at stage 1a.

## Ledger fold

`docs/phase4/ledger.md` row 25 → **landed** (this commit). Corpus lock 39→40
(`phase-6-c1-vpfm` seed). Deferral history honored: P4-Greenfield-CPU,
greenfield-needs-base-sim — built on the U-4 PFM substrate as the charter § 4 order
anticipated.

**Next unit per ratified § 4 order:** U-6 `eulerian-smoke-frontier-edge` (D-1 →
EDGE proper, DOI 10.1145/3731193; grid-side flow maps, independent of the particle
substrate but shares the flow-map verification vocabulary).
