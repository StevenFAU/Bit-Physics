---
date: 2026-06-20
author: c1-u6-landing-agent (build session 5; charter § 7.13 self-driving cluster dispatch)
phase: 6
cluster: C-1
unit: U-6
artifact: unit-landing-report
artifact_id: c1-u6-edge-landing
subject: >
  Unit landing report for C-1 / U-6 eulerian-smoke-frontier-edge (EDGE proper, DOI
  10.1145/3731193) — stage-1c canonical capture + REFRAMED gate + perf-ledger row, the
  three open 1c deferrals landed; closes the U-6 build.
verdict: LANDED
verdict-state: GREEN
head_sha: 4d365fc
prior_phase_tag: v0.5.0-phase-5
evidence_hashes:
  "captures/eulerian-smoke-frontier-edge/taylor-green-128cube-seed42-step500.h5": "sha256:6e8e10e93da79bc4f3ae374605b0842db8155b2b8f056f672ab96b6ee2e79c75"
  "tests/fixtures/legacy-captures/phase-6-c1-edge.h5": "sha256:1b2c4fd1e05a723cb2b4f27bd395e30739e6c713941423fba0a2216181e10060"
evidence-paths:
  - "packages/eulerian-smoke-frontier-edge/ (the Stack-C unit)"
  - "docs/sim-specs/volumetric-grid/eulerian-smoke/spec-frontier-edge.md"
  - "captures/eulerian-smoke-frontier-edge/taylor-green-128cube-seed42-step500.{h5,json}"
  - "docs/_audits/phase-6/c1-u6-edge-probe-2026-06-15T15-42-50Z.md"
  - "docs/perf-ledger.md (the gate-12 row)"
  - "docs/phase4/ledger.md (row 24 → landed)"
---

# C-1 / U-6 `eulerian-smoke-frontier-edge` — unit landing report

> **Cluster:** Phase 6 / C-1 (charter `docs/phases/phase-6/c1-charter.md`, RATIFIED § 10
> incl. D-1 = EDGE proper).
> **Unit:** U-6 = Phase-4 ledger row 24 / spec § 11.5 item 4.16 — Stack C; the THIRD and
> final flow-map-family member, **independent of the U-4/U-5 PARTICLE substrate** (grid
> backward flow maps; charter § 3.6).
> **Frontier paper:** Li\*, Wang\*, Li\*, Chen, Wang, Zhu (\*co-first, all Georgia Tech),
> "EDGE: Epsilon-Difference Gradient Evolution for Buffer-Free Flow Maps", ACM TOG 44(4),
> SIGGRAPH 2025, DOI `10.1145/3731193`. CITE-DON'T-IMPORT. (INCOMPRESSIBLE-Euler grid
> flow maps — distinct from the compressible-flowmap paper DOI `10.1145/3731192`; D-1
> conflation closed, charter § 10.)
> **Verdict:** **LANDED** — acceptance ctest 18/18 at HEAD (edge_tests doctest 9/9 =
> 147,718 assertions + the REFRAMED fixture gate `edge_reframed_equivalence`, alongside 14
> sibling tests); corpus roundtrip 65 (lock 40→41); full § S.5 CI sweep GREEN at `63196b2`
> — **38/38 check-runs success** (incl. cpp-strict, python-strict through R2, ts-strict,
> mutation-testing, tolerance-budget-check, integrity, determinism, audit-append-only,
> structure, equivalence + the per-sim test jobs).
> **Commit chain:** `07f5987` (stage-1c spec-frontier-edge.md) → `ccca980` (REFRAMED gate
> + CFL monitor + registrations) → `63196b2` (canonical capture + corpus seed + perf +
> ledger fold) → `4d365fc` (this landing fold) → SHA back-fill (separate, Convention
> #12).

> Every concrete claim below is tagged **FACT** (grep/read/run-verified at HEAD this
> session, Convention #8) or **INFERENCE** (reasoning over the cited FACTs).

## Re-anchor / resumption record (this session)

**FACT.** Session opened on a fully-green origin (`fce4899 == origin/main == HEAD`,
working tree clean). U-6 stages 1a (scaffold, RED 9/9) and 1b (GREEN trajectory) had
ALREADY LANDED and pushed (`b7c752b` "EDGE GREEN trajectory — grid backward flow map
ψ+∇ψ, Cauchy transport, 9/9 GREEN" is an ancestor of origin/main) — the handoff-#4
CONTINUE_FROM (`616f116` LOCAL/UNPUSHED) was STALE on that point. **FACT.** The three
OPEN 1c deferrals were: (1) the BLOCKING canonical 128³ capture with a MEASURED CFL-safe
dt, (2) the REFRAMED equivalence ctest, (3) the perf-ledger row. The item-2 ε-difference
refinement stays deferred (charter § 3.6 scope; not required by gated acceptance — § SHIFT
2 below). **FACT.** Acceptance suite re-verified GREEN at HEAD before any 1c work: 9/9,
147,718 assertions.

## BLOCKING GATE — measured CFL-safe dt at 128³ (the U-5 lesson, re-measured for EDGE)

**FACT (measured, this session).** The descriptor's nominal dt = 0.005 is **NOT** stable
at 128³ for EDGE. A live CFL monitor (u_max + Courant C = u_max·dt·n, every 50 steps —
the shippable diagnostic added to `run_edge`) traced the dt = 0.005 run:

| step | t = step·dt | u_max | Courant C |
|---|---|---|---|
| 0   | 0.00 | 0.999 | 0.640 |
| 50  | 0.25 | 0.938 | 0.600 |
| 100 | 0.50 | 0.933 | 0.597 |
| 150 | 0.75 | **2.065** | 1.322 |
| 200 | 1.00 | **67.28** | 43.06 |
| 250 | 1.25 | **2541** | 1626 |

The inviscid-TG cascade develops by t ≈ 0.7; once u_max crosses EDGE's CFL ceiling
1/(n·dt) = 1.56 (between steps 100 and 150) the run runs away — exactly the U-5 failure
class, re-measured for EDGE (NOT inherited). EDGE's effective ceiling is the advective
Courant limit u_max·dt·n ≲ 1 (the departure-point displacement exceeding one cell).

**FACT (measured, this session).** The canonical capture is taken at **dt = 0.00125** —
the MEASURED CFL-safe fixed step at 128³ (CFL ceiling 1/(n·dt) = 6.25, ~4× the descriptor
ceiling; the run's Courant stays C ≈ 0.15-0.16, ~6.25× headroom on the ceiling). 500 steps
at dt = 0.00125 = physical t = 0.625, the well-conditioned **pre-cascade window** (the
cascade onset measured above at t ≈ 0.7 is OUTSIDE the captured window): u_max bounded
≤ 0.998896 over ALL 11 frames (0.999 at step 0, decaying to ~0.93 mid-run, 0.989 at step
500), KE 0.124950 → 0.129379 (3.5 % drift), enstrophy ×4.42 by real vortex stretching,
div_max 4.80e-14 — no blowup. The locked descriptor
`taylor-green-128cube-seed42-step500` fixes the **grid (128³) and step count (500)**, both
matched VERBATIM; the fixed-dt VALUE is the documented descriptor-parity adaptation (spec
§ 1 / § 5). **INFERENCE.** That EDGE's measured stable window (pre-cascade) coincides with
U-5's (dt = 0.00125, t = 0.625) is expected — both solve the SAME 128³ inviscid-TG flow,
whose under-resolved cascade is a property of the resolution + physics, not the method;
EDGE's advantage is MEMORY (the O(1) claim), not cascade-resolution.

## Gates (spec Appendix D.6, Stack-C adaptation)

| Gate | Verdict | Evidence |
|---|---|---|
| 1 spec sheet | GREEN | `spec-frontier-edge.md` (per-variant; the shared `spec-frontier.md` stub STAYS for 4.22) — all measured-then-declared values filled from the canonical capture |
| 2 probe | GREEN | `c1-u6-edge-probe-2026-06-15T15-42-50Z.md` — anchor re-verified live (project page; no arXiv, ACM DL canonical); SHIFTs: grid-side NEW core, solid-boundary DEFERRED, D-1 conflation closed |
| 3 failing-first | GREEN | RED ctest at stage-1a `616f116`/`(landed)`: 9 cases, 0 passed (sha `4be97e88…` footer-hashed) |
| 4 goldens (≥3 anchors) | GREEN | A1 closed-form vorticity lift FD-checked (≤1e-8, 216 pts) + exact div∘curl identity (≤3.82e-15) + reconstruction golden (O(dx²) ~3.96×, 1.600e-3 at n=32); A2 gradient-evolution ∇ψ-vs-FD resolution-converging (1.097e-2 → 3.414e-3, ~3.2×) + the MEASURED O(1)-memory property; A3 inviscid steady 2D-TG drift 2.974e-4 → 1.5e-3, energy 2.624e-5 → 1e-4, IC residual 1.600e-3 → 5e-3 |
| 5/6 diagnostics | GREEN | per-frame KE/u_max budget metrics; `init_velocity_residual` MEASURED into result + manifest (1.00345e-4 at 128³ — the direct closed-form lift, identical to U-5, no init pathology) |
| 7 citations | GREEN | DOI 10.1145/3731193 (live-verified, CITE-DON'T-IMPORT; NOT …3731192) + DOI 10.1145/3731194 (U-4 parent, single-source REFRAMED) + DOI 10.1145/3731198 (U-5 grid-layer source) + Cottet & Koumoutsakos 2000 (Cauchy formula) |
| 8 public API | GREEN | `bit_physics::edge` header (`include/bit_physics/edge/edge.hpp`); clean per-target `-O3 -ffp-contract=off` build |
| 9 capture | GREEN | `captures/eulerian-smoke-frontier-edge/taylor-green-128cube-seed42-step500.{h5,json}` — D.2.3 descriptor VERBATIM; .h5 / payload sha256 `6e8e10e9…` = the LFS content OID (R2 + GitHub LFS); **[x][y][z] axis layout matching the parent**; dt=0.00125 SHIFT (above) + corpus seed `phase-6-c1-edge` (witness `d6c7c4fc…`, lock 40→41) |
| 10 determinism↔capture | GREEN | 2-run bit-identity witness `508841448d…` (tolerance 0.0; **run 2 IS the capture run** → the capture bytes ARE the asserted-identical bytes: payload checksum = LFS OID = `6e8e10e9…`); registry `[volumetric-grid.eulerian-smoke-frontier-edge]` class bit-exact, atomic_ops none; manifest `bit-exact-same-hw` |
| 11 PBT (≥2) | GREEN | `reconstructed_velocity_divergence_free` (exact div∘curl ≤3.82e-15 every step, swept L × both ICs) + `backward_map_memory_constant` (THE O(1) gate: same trajectory at L=10 vs L=40 → IDENTICAL peak bytes) + `total_circulation_bounded` bonus (total-vort ≤7.21e-14 + circ-drift ≤1.16e-11, FP-scale) |
| 12 perf-ledger | GREEN | 2459 s canonical invocation (~2.5 s/step/run, faster than U-5's ~5.9 s/step — no particles) + the O(1)-memory figures (peak RSS ~3.52 GiB MEASURED, vs U-5's ~12.2 GB + the 240 MiB constant backward-map state at 128³) — `docs/perf-ledger.md` row |
| 13 failing-replay | GREEN | worktree at the stage-1a RED rebuilds the 9-case/0-passed shape (the U-3/U-4/U-5 banked ctest adaptation — pytest replay does not drive ctest) |
| 14 cross-stack | **N/A → REFRAMED gate GREEN** | no cross-stack sibling (charter § 3.6); the REFRAMED frontier-vs-parent metric gate is the equivalence surface — all four clauses green (below) |

## REFRAMED equivalence (measured-then-declared; no new tolerance category)

**FACT.** Variant budget-metric fixture `edge-equivalence/variant-budget-metrics.json`
derived once from the canonical capture by `derive_budget_metrics.py` (metrics
byte-identical to the U-4/U-5 derive script — verified by diff this session); the parent
side cross-references the U-4 frozen fixture `clebsch-pfm-equivalence/parent-budget-metrics.json`
DIRECTLY (single-source, probe § 4.5 — no second parent run; CI never pulls the LFS
captures). **Declared clauses (margins ≥ 2.5×), all GREEN:** (a) frame-0 IC agreement (KE rel
4.016e-4 ≤ 1.2e-3; enstrophy 4.016e-4 ≤ 1.2e-3; u_max abs 2.006e-4 ≤ 6e-4; blob FP-tight
2.20e-16/5.74e-16 ≤ 1e-12 — the SAME direct lift as U-5, matching to the last digit);
(b) parent step-50 blowup present (≥1e6; measured 1.337e8); (c) variant physical over the
FULL window [0,500] (KE drift ≤ 3.545e-2 ≤ 9e-2; enstrophy/enstrophy(0) ∈ [1.000, 4.415]
⊂ [0.8, 12.0]; u_max ≤ 0.998896 ≤ 2.5; mass drift ≤ 1.740e-1 ≤ 0.45); (d) stability
contrast (variant u_max ≤ 0.998896 over ALL 11 frames vs parent ≥ 1e6 by step 50 — an
~8-order contrast). Thresholds declared in spec § 3.5 — no budget widening. ctest
`edge_reframed_equivalence` GREEN.

## SHIFTs + findings (documented)

1. **FACT. dt = 0.00125 CFL-safe canonical** (above) — measured, NOT inherited; the
   dt=0.005 failure trajectory is direct evidence (cascade onset t≈0.7, u_max→2541 by
   step 250). The silent gap the U-5 landing named (no canonical-scale stability check in
   the ≤n=32/≤50-step acceptance suite) is closed by the live CFL monitor.
2. **FACT. The higher-order ε-difference (anchor § 3 item 2) is DEFERRED with cause** —
   EDGE's FIRST Jacobian ∇ψ is evolved EXACTLY by the RK4 variational equation (item 1),
   which suffices for the Cauchy transport and EVERY gated acceptance surface; the
   tetrahedron-based ε-difference for the map's HIGHER derivatives is an accuracy
   refinement not exercised by the gated anchors (charter § 3.6 scope; not required by
   gated acceptance). The unit's distinctive RIGOROUS claim — O(1) memory — is realized
   by the item-1 evolution + reinit (PBT `backward_map_memory_constant` + perf row).
3. **FACT. Solid-boundary surface DEFERRED with cause** (probe § 4.4, charter § 3.6
   pre-authorized): the periodic canonical descriptor exercises zero boundary code.
4. **FACT. Capture axis layout [x][y][z]** matching the parent (the U-4/U-5 transpose
   lesson priced in from the start — `edge.cpp put_field`).
5. **FACT. Per-target `-O3 -ffp-contract=off`** (strict IEEE, no FMA fusion); tree default
   -O0 ~10× slower (infeasible at 128³).
6. **FACT. Mutation N/A** (C++; mutmut is python-only — the rd2d/U-3/U-4/U-5 precedent).
7. **FACT. Kelvin budgets at FP-identity scale** (total-vort ≤7.21e-14, circ-drift
   ≤1.16e-11) — far tighter than the U-5 particle method's ~4e-5 (the grid Cauchy
   transport has no P2G scatter).

## Ledger fold

**FACT.** `docs/phase4/ledger.md` row 24 → **landed** (`63196b2`). Corpus lock 40→41
(`phase-6-c1-edge` seed, witness `d6c7c4fc…`). Deferral history honored: P4-Greenfield-CPU,
greenfield-needs-base-sim — built on the copy-adapted Stack-C grid layer with a NEW
grid backward-flow-map core as the charter § 3.6 scope anticipated.

**This unit is the THIRD and final flow-map-family member and (per the operator's
build-session-5 directive) the FINAL unit of the C-1 close scope.** See the C-1 close
audit `c1-close-<UTC>.md` for the cluster-level verdict, the U-7/U-8 deferral, and the
P0.6 clearance.
