---
date: 2026-06-11
author: lane-b-polish-agent
phase: 6
lane: B
artifact: dispatch-audit
artifact_id: laneB-P2-dispatch-audit
dispatch: "P-2 (Lane B — publish P-1 + ratifications + house-style anchor + landing-v2)"
verdict: LANDED
verdict-state: HARD-STOP-BEFORE-P-3
head_sha_at_start: 8f7879e40b62a0bf378d881c171f529a5b98b0ba
parent_audits:
  - "[[laneB-P1-dispatch-audit-2026-06-11T13-17-27Z]]"
related:
  - "[[laneB-P1-presentation-plan]]"
evidence_paths:
  - docs/design/house-style.md
  - tools/productization/web-deploy/web/pages/index.html
  - tools/productization/web-deploy/web/pages/assets/make-posters.mjs
---

# Lane B / P-2 dispatch audit — publish P-1, ratifications, house-style anchor, landing-v2

> Append-only record for dispatch P-2 (build dispatch). Companion plan:
> `laneB-P1-presentation-plan.md` (ratified by the § 2 rulings below).

## § 1 — Ratification record (operator, via dispatch P-2)

1. **Clone-per-lane operating model.** After the P-1 shared-checkout
   discovery, Steven ratified a dedicated clone per lane. Lane B now works
   exclusively in `~/lanes/laneB/Bit-Physics`; the shared checkout at
   `~/Projects/Bit-Physics` is never operated in again by Lane B. All
   future Lane B dispatches assume this clone.
2. **D-P1.1 → static PNG posters.** Live WebGPU thumbnails rejected (lane
   violation by construction); webm rejected for now (bloat over payoff);
   motion upgrade reconsidered post-v2 if the page reads flat.
3. **D-P1.2 → all three sub-rulings allowed, discipline non-negotiable:**
   (a) live-loop-only uniform writes (presets / cursor-force) with the
   capture path pinned to canonical params — explicit commit call-out +
   full local validate per affected sim, no exceptions; (b) RAF suspension
   for Study mode — pure presentation; (c) display-only `render.wgsl`
   shaders Lane-B-touchable under the full-validate-and-call-out escape
   hatch — every WGSL-touching commit says so explicitly in its message.
4. **D-P1.3 → mandatory local validate** (`pipeline.py` under `CHROME_BIN`)
   before every web-touching push (the browser matrix has no bare-main-push
   trigger). Operator-dispatched matrix runs at milestones: post-landing-v2
   (end of this dispatch), post-pilot, post-full-migration.
5. **Sequencing ratified:** landing-v2 first (this dispatch), then
   shared-chrome starting with the strange-attractors pilot (P-3).

Per § 0.3 these rulings are recorded here as a new append-only entry; the
P-1 plan doc is unchanged (its D-class section is now resolved by this
record).

## § 2 — Work landed (commit chain, this dispatch)

1. P-1 publish: the two P-1 commits cherry-picked from the P-1 worktree
   onto current `origin/main` in the Lane B clone (plan + P-1 audit note;
   SHAs in § 5).
2. `docs/design/house-style.md` — in-repo house-style anchor: canonical
   (eulerian-smoke) + phosphor-instrument (lbm) token sets, IBM Plex
   Mono/Sans-Condensed typography, ratified interaction conventions,
   accent-migration note (#2dd4bf → #36e0cf). Resolves the P-1 Stage-2
   SHIFT; provenance: coordinator-measured from the out-of-repo reference
   sims, pinned at P-2 dispatch time.
3. Poster assets + generator (own commit, new files first): 7 PNGs, 446 KB
   total, plain git. Generated by `make-posters.mjs` — headless Chromium
   photography of the built dists, read-only on the sims.
4. Landing page v2 (single-file rewrite): hero + story, poster cards with
   physics one-liners / backend badges / gate chips, "How this is
   verified" explainer (3 gate kinds, four-state verdicts, ledger links),
   Downloads preserved, house tokens verbatim.
5. This audit note + Convention #12 SHA back-fill (after push).

## § 3 — Poster generation parameters (reproducibility)

All posters: app-default seed 42, built dists at this dispatch's HEAD,
snap Chromium headless (WebGPU/Vulkan), RAF-frame freeze via wrapper
(armed at app-ready, so boot frames don't count). "boost" = CSS filter on
the canvas element at screenshot time — photographic exposure only;
no sim code, parameters, or capture path touched anywhere.

| Sim | Frame(s) | Treatment | px | KB |
|---|---|---|---|---|
| boids-3d | 520 + 16 shots × gap 4 (520–580) | long exposure: `lighten`-stacked, content-bbox crop, boost brightness 2.2 / saturate 1.6 / contrast 1.45 | 512 | 26 |
| neural-ca | 140 | plain element shot | 512 | 10 |
| physarum | 650 | plain element shot (network peak; decays by ~2200, near-gone by 5200) | 512 | 24 |
| reaction-diffusion-2d | 2200 | plain element shot (spot regime) | 512 | 45 |
| ising-classical | 600 | plain element shot (T = 2.27) | 512 | 6 |
| mandelbulb-explorer | 200 (orbit angle 0.80 rad) | plain element shot | 360 | 139 |
| strange-attractors | 420 | bbox zoom (tight 0.62), boost brightness 1.9 / saturate 1.4 | 512 | 197 |

Budget note: total 446 KB ≪ 1 MB budget; strange-attractors (197 KB)
exceeds the ≤150 KB per-file target — accepted for the charisma payoff
with total budget at 45%; revisit if the asset dir grows.

## § 4 — Validation (D-P1.3) + SHIFTs

- Local validate: `pipeline.py validate` over all 7 sims under
  `CHROME_BIN` (snap Chromium, real browser-WebGPU adapter) run before the
  push of this dispatch's web-touching commits. Result recorded in § 5.
- Landing page + posters have no validation surface (copied verbatim by
  the deploy job; P-1 plan § 1.5) — the validate run is the blanket
  D-P1.3 habit, plus regression cover for the unchanged sim bundles.
- **SHIFT (minor, tooling):** Playwright's default RAF-based
  `waitForFunction` polling starves under the poster freeze; the generator
  uses interval polling. Forcing a recomposite after style changes blanks
  WebGPU canvases (texture dropped after present) — the generator never
  recomposites; zoom crops translate the canvas to the viewport origin
  instead.
- **SHIFT (process):** P-1's push was classifier-blocked in the agent
  harness (direct-to-main); Steven authorized P-1 + P-2 pushes in the P-2
  dispatch. Push mechanics recorded in § 5.
- NO compute surface touched: no WGSL, step loops, seeded init,
  capture/gate paths, tolerance or verify code modified. All work is
  docs + landing page + new asset files.

## § 5 — Evidence + Convention #12 back-fill (after push)

- Local validate verdict: **PASS 7/7, 0 deferred, 0 fail** (overall_status
  "pass"; artifacts `/tmp/laneB-P2-validate`). Highlights: run-twice
  byte-identical on all four new_canonical sims; physarum mass_rel_diff
  1.7e-7 (threshold 1e-3); rd2d max_abs_err 2.64e-5 within [defaults
  .reaction-diffusion] rel 1e-4; neural-ca bit-exact (max_abs 0.0); no
  tolerance widened anywhere.
- Commit SHAs: *(back-filled below per Convention #12 — never `--amend`)*

p1_plan_commit_sha: 7f42917  # Convention #12 back-fill (§ 5)
p1_audit_commit_sha: 06ec8f6  # Convention #12 back-fill (§ 5)
house_style_commit_sha: a5b7d4a  # Convention #12 back-fill (§ 5)
poster_assets_commit_sha: 3a04d58  # Convention #12 back-fill (§ 5)
landing_v2_commit_sha: c41257a  # Convention #12 back-fill (§ 5)
p2_audit_commit_sha: 95f4b4c  # Convention #12 back-fill (§ 5)
