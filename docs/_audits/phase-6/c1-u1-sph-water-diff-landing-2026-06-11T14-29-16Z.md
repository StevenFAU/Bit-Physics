# C-1 / U-1 `sph-water-diff` — unit landing report

> **Cluster:** Phase 6 / C-1 (charter `docs/phases/phase-6/c1-charter.md`, RATIFIED § 10).
> **Unit:** U-1 = Phase-4 ledger row 10 / spec § 11.5 item 4.2 — `particle-fluids/sph-water`
> diff variant, Stack D (Taichi `ti.ad.Tape`).
> **Verdict:** **LANDED** — 13 gates green (gate-14 N/A, single-stack), 19/19 tests, suite
> 38/38 corpus roundtrip.
> **Commit chain:** `986f5f0` (stage-0 probe) → `87da3cb` (stage-1a scaffold+RED, evidence
> footer-hashed) → `38da1ef` (stage-1b GREEN + golden) → `cc6f0ed` (stage-1c
> perf/CI/LFS/mutation/gate-13) → `5e810d9` (stage-2 landing fold; SHA back-filled per Convention #12).

## Gates (spec Appendix D.6)

| Gate | Verdict | Evidence |
|---|---|---|
| 1 spec sheet | GREEN | `docs/sim-specs/particle-fluids/sph-water/spec-diff.md` de-stubbed §§ 1–14 |
| 2 probe | GREEN | `docs/_audits/phase-6/c1-u1-sph-water-diff-probe-2026-06-11T13-56-06Z.md` (incl. replay-vs-`v0.5.0-phase-5` decomposition: 3 known lfs_migration META reds, 0 deliverable regressions) |
| 3 failing-first | GREEN | 18-failed/1-pass RED at `87da3cb`; evidence `sph-water-diff-2026-06-11T14-11-32Z.txt`, sha256 footer-hashed in the commit |
| 4 golden (≥3 anchors) | GREEN | `sph-water-diff-gradient.json` 9/9 points; A1 free-fall closed form (EXACT — linear map), A2 central FD (ad-vs-fd ~1e-10 rel), A3 Monaghan-spline analytic (both branches) |
| 5/6 Tier-1/2 | GREEN | `test_diagnostics.py` (health + bounds) |
| 7 citations | GREEN | spec § 7; DiffTaichi anchor live-verified at charter § 2 row 1 |
| 8 public API | GREEN | `sph_water_diff/__init__.py` exports; mypy --strict + ruff clean |
| 9 capture | GREEN | `captures/sph-water-diff/sph-water-diff-recover-v0z-8part-seed42.{h5,json}`, schema 1.1.0 `gradient_fields` (`dLoss_dv0z`), **run-twice byte-identical**; .h5 sha256 `070a51daf6e1e8ed1289b080b320ad30a1c45cf518e0040dc0d0571822fd1507` |
| 10 determinism↔capture | GREEN | MEASURED bit-exact (forward + gradient `np.array_equal`); registry `[particle-fluids.sph-water-diff.{forward,gradient}]`; manifest `bit-exact-same-hw` |
| 11 PBT (≥2) | GREEN | `gradient_matches_finite_difference` + `density_summation_positive` (Hypothesis, derandomized) |
| 12 perf-ledger | GREEN | row appended (0.211 s canonical solve, 3-run min; 2 Newton iters) |
| 13 failing-replay | GREEN | `replay_failing_tests --commit 87da3cb` **match=True**, normalized sha `f0b6dea15cfb188b50f29fc0700ed5e9bd8751c36f17ff1afca2d7a1281f0a61` (B-2 worktree-generated byte-stable evidence committed at stage 1c) |
| 14 cross-stack | **N/A** | single-stack; WU-F differentiable-axis equivalence instead: positions `np.array_equal == True`, density max_rel **0.0** vs parent |

## SHIFTs (documented, scope-preserving)

1. **ParamSpec → live params** (charter § 3.1 / probe § 2 / spec § 1): plan-prose
   (viscosity, surface-tension, damping) are not in the landed parent forward (R-S3/S6);
   ParamSpec covers `v0z` (control) + `h` (kernel-width ID). EXP-C regime-scoped
   (fixed-topology interior cloud).
2. **Descriptor tier**: Appendix D.2.3's `dam-break-1M` row is stale vs the R20-ratified
   100K parent canonical (probe § 2); the diff capture uses the batch-1 problem-scoped
   descriptor pattern. Routes to cluster-close with D-6.
3. **equivalence.md routing**: batch-1 precedent keeps parent `equivalence.md` a gate-14
   artifact; the charter's "diff row" landed as the appended § 6 WU-F section with
   MEASURED values (additive).
4. **Gate-13 evidence flags**: stage-1a ad-hoc evidence was captured without the replay
   tool's `-v --tb=short`; the byte-stable B-2 evidence was authored from the replay
   worktree at stage 1c (generation-worktree path prefix canonicalized to the repo root —
   the path form an in-root capture produces). Both files committed; replay match=True.

## Environment notes (banked for the cluster)

- `/tmp` has a small quota: worktree `uv sync` fails there (torch/numpy copy → os error
  122). Use `TMPDIR=~/.cache/bp-tmp` for replay tools; the home filesystem works.
- Plain `git worktree add` fails on LFS smudge in this clone (`Could not reset index file`);
  `GIT_LFS_SKIP_SMUDGE=1` is safe for test replays that read no LFS content.
- eof-fixer rewrites capture `.json` sidecars (banked lesson #5) — re-add and re-verify.

## Mutation (spec § 2.13)

`[targets.sph_water_diff]` registered (threshold 0.80, advisory). Measurement **deferred**
per the batch-1 close precedent ("advisory — registered; deferred"); cluster-close may
batch-measure.

## Ledger fold

`docs/phase4/ledger.md` row 10 → **landed** (Phase-6 C-1 U-1; deferral history preserved
in-row; the § "Deferred re-scope" Home-2 table stands un-edited as the historical record).

**Next unit per ratified § 4 order:** U-2 `neural-ca-frontier-difflogic` (S28 / 4.20).
