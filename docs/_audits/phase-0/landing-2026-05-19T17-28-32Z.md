---
date: 2026-05-19T17-28-32Z
author: phase-0-block-9-agent
phase: 0
artifact: phase-landing
artifact_id: phase-0-landing
verdict: SHIFTED
shift_summary: "Phase 0 lands with the framework-validated mutation-testing baseline (vs the real per-target kill-rates the spec wanted at landing). Every other Phase-0 acceptance gate CONFIRMED, including all 13 of RD-2D's Layer-4 gates (gate 13 re-CONFIRMED at Block 9 under canonical normalization)."
head_sha: 85da2fc8911230dcd0706cfe08d6ae67c2a5c311
prior_blocks:
  - { block: 1, name: foundation,   verdict: SHIFTED,   head_sha: 1f052dfd10062bea9c20f7767697f7578798335d }
  - { block: 2, name: mms,          verdict: CONFIRMED, head_sha: 32804db3033065a90350c4f8add5fe3b4290ebce }
  - { block: 3, name: harnesses,    verdict: CONFIRMED, head_sha: 89a8b0b115395a77eb4e5f89386d41d3b2201716 }
  - { block: 4, name: vendoring,    verdict: CONFIRMED, head_sha: 52f66e8905904156b60935a3aa2bd9b904d3bf92 }
  - { block: 5, name: integrity,    verdict: CONFIRMED, head_sha: 280fcd027d0c4ae3bca02fd2f83af6717453fb8a }
  - { block: 6, name: diagnostics,  verdict: CONFIRMED, head_sha: 112117855690f07212d1f3c420e353b3ea9ab531 }
  - { block: 7, name: common-ts,    verdict: CONFIRMED, head_sha: 4f97e6beb153173a1ecb90e312800c078425aa3d }
  - { block: 8, name: rd-2d,        verdict: CONFIRMED, head_sha: aa76defc798a985fb9f29780d7269cf77fbc0c1a }
evidence_paths:
  - docs/_audits/phase-0/block-1-foundation-2026-05-19T03-27-54Z.md
  - docs/_audits/phase-0/block-2-mms-2026-05-19T11-28-58Z.md
  - docs/_audits/phase-0/block-3-harnesses-2026-05-19T11-44-35Z.md
  - docs/_audits/phase-0/block-4-vendoring-2026-05-19T13-53-43Z.md
  - docs/_audits/phase-0/block-5-integrity-2026-05-19T14-47-53Z.md
  - docs/_audits/phase-0/block-6-diagnostics-2026-05-19T15-03-05Z.md
  - docs/_audits/phase-0/block-7-common-ts-2026-05-19T15-25-28Z.md
  - docs/_audits/phase-0/block-8-rd-2d-2026-05-19T16-00-36Z.md
  - docs/_audits/phase-0/block-8-rd-2d-2026-05-19T16-00-36Z.addendum-2026-05-19T16-53-30Z.md
  - docs/_audits/phase-0/spec-amendments-proposed.md
  - tools/integrity/integrity/scripts/replay_failing_tests.py
  - tools/testkit/capture/tests/test_legacy_captures_corpus.py
  - tools/testkit/mutation/baseline-2026-05-19T17-16-17Z.json
  - .github/workflows/integrity.yml
  - .github/workflows/determinism.yml
  - .github/workflows/equivalence.yml
  - .github/workflows/ts-strict.yml
  - .github/workflows/audit-append-only.yml
  - .github/workflows/tolerance-budget-check.yml
  - .github/workflows/mutation-testing.yml
proposed_tag: v0.0.0-phase-0
tag_commit_sha: 85da2fc8911230dcd0706cfe08d6ae67c2a5c311
tag_pushed: NO
---

# Phase 0 — landing retro

> Closes Phase 0. Per spec § 7.12 the operator (not the agent) signs and
> pushes `v0.0.0-phase-0` after independent review of this audit. The
> closing line is `Tag pushed: NO (operator action required)`.

## 1. Verdict

**SHIFTED.** Phase 0's substance is fully landed — RD-2D ships as the
Layer-4 reference, all eight prior blocks CONFIRMED, all thirteen RD-2D
Layer-4 gates pass (gate 13 re-CONFIRMED at Block 9 under canonical
normalization). The shift is on a single deliverable: mutation-testing
ships a framework-validated baseline rather than the real per-target
kill-rate numbers spec § 2.13 wanted at landing. Reason and Phase-1
fix-up are documented in § 4 below; thresholds are NOT silently
relaxed.

## 2. Aggregated SHIFTED items across all 8 blocks

FACT — Block 1 (FOUNDATION) — SHIFTED close at `1f052dfd...`:
- Pinned tool versions updated where forward-compatible (Block-1 audit
  records the deltas; propagated via top_level_deps_to_merge).
- `import os` placeholder removed mid-phase per Block-1 audit note.

FACT — Block 4 (VENDORING) — proposal SHIFTED at audit close, then
resolved in Block 5: upstream_sha cleanup applied additively to align
with § 5.1 schema (commit `4efb11c`). No outstanding action.

FACT — Block 8 (RD-2D) — three SHIFTED surfaces, two resolved:
1. **Gate 13 — pytest timing-line non-determinism.** RESOLVED at
   Block 9 via canonical normalization in
   `tools/integrity/integrity/scripts/replay_failing_tests.py`
   (commit `aeb42f4`). Block-8 addendum `52aeb99` upgrades gate 13
   to CONFIRMED. Normalized sha256
   `e0ffb58e0f7b5d910f8c1f6dc3e3dc8d0b36943a424a590b77d721594e0a22f5`
   matches across runs.
2. **PBT `monotone_bounds_uv` proxy.** Still SHIFTED — Phase-1 RD-2D
   admissible-IC region constraint restores the strict
   `[-1e-9, 1+1e-9]` bound. Spec amendment queued.
3. **`.pre-commit-config.yaml` `check-added-large-files` `--maxkb` 10MB
   bump.** Stable as a project-internal pragma; spec amendment not
   required (proposed as foundational value in § 3).

FACT — Block 9 (LANDING) — one new SHIFTED:
- **Mutation-testing real baseline deferred to Phase 1 Stage 1.**
  See § 4 below.

## 3. Aggregated spec-amendments-proposed content

These have lived in `docs/_audits/phase-0/spec-amendments-proposed.md`
during Phase 0 build. Aggregated here for owner review at phase close
(Rule-of-Three not yet triggered for any of them; all are first
surfaces).

FACT — three proposals stand at landing:

| # | Proposal | Source | Disposition at LANDING |
|---|---|---|---|
| 1 | Extend § 5.1 audit front-matter schema for vendoring blocks (`upstream_sha`, `upstream_shas`) | Block 4 | DEFERRED — owner decides at Phase 3 when next vendoring block runs |
| 2 | Spec G.7.5 failing-tests sha256 should exclude pytest timing line (canonical normalization) | Block 8 | ACCEPTED IN PRACTICE — Block 9 implements the normalization in `replay_failing_tests.py`; spec edit pending owner approval. Block-8 addendum re-CONFIRMS gate 13 under the new contract |
| 3 | RD-2D PBT `monotone_bounds` proxy + Phase-1 admissible-IC region | Block 8 | DEFERRED to Phase 1 RD-2D / RD-3D spec amendment |

INFERENCE — Block 9 adds two more proposals that the
`spec-amendments-proposed.md` file should reflect (left to owner to
formally edit at amendment review):

- **`.pre-commit-config.yaml` `--maxkb` cap as foundational value.**
  Spec § 2.7 mandates canonical captures live in-tree; the 500 KB
  default blocks every sim's first-landing commit. Block 8 set 10 MB;
  this should be encoded in spec § 7 as the canonical default rather
  than a per-project pragma.
- **Mutation-testing CI orchestration needs per-workspace-member sync.**
  Block-5's run-mutation.sh assumes a single pytest invocation at the
  repo root works for every target — it doesn't, because workspace
  members import sibling members that need per-member sync. Spec
  § 2.13 should specify per-target runner scoping (now configured in
  `tools/testkit/mutation/mutmut-config.toml`'s `runner` field) or
  defer mutation-testing CI gating to Phase 1 first stage.

## 4. Mutation-testing — Phase 0 SHIFTED, Phase 1 mandatory

FACT — the framework-validated baseline at
`tools/testkit/mutation/baseline-2026-05-19T17-16-17Z.json` declares:
- mutmut 2.5.1 installed (workspace dep, both testkit and integrity);
- mutmut-config.toml parses cleanly with 7 targets;
- every target path exists;
- `run-mutation.sh` exits 0 with a structured framework-validated JSON;
- EXIT/INT/TERM trap restores any `*.bak` mutmut leaves behind.

FACT — what is NOT in the baseline: real per-target kill-rate numbers.
Surfaced via three attempts at real runs during Block 9:
- **Attempt 1 (golden target, no per-target runner).** mutmut tried
  the default `pytest` at repo root; pytest's discovery tripped on
  `packages/reaction-diffusion-2d/tests/test_diagnostics.py` failing
  to import `diagnostics` (the testkit-root pytest invocation doesn't
  have bit-physics-diagnostics on its path the way per-member sync
  arranges). Interrupted via TaskStop; mutmut left
  `tools/testkit/golden/verifier.py` in a mutated state with a `.bak`
  alongside.
- **Mitigation (this LANDING).** Restored `verifier.py` from the
  `.bak`; added an EXIT/INT/TERM trap to run-mutation.sh that
  unconditionally restores any `.bak` files on script exit.
- **Attempt 2 (cat4_draft_time, per-target runner).** Same workspace-
  path issue — the target's pytest path
  `tools/integrity/tests/test_adversarial_coverage.py::...` cannot
  collect at repo root (`ERROR: found no collectors`).

INFERENCE — the Block-5-shipped framework needs per-workspace-member
runner orchestration that Block-5 didn't ship. This is a defect
surfaced AT LANDING that wasn't surfaced at Block 5. Per spec
§ 12.5 the operator decision: (a) re-dispatch Block 5 for a fix
(b) accept the SHIFTED close with the deferral logged as a Phase-1
priority item. This audit takes (b) by default. Owner can override
to (a) at review time.

Decision NOT taken (explicitly): silently lowering kill-rate
thresholds. Per the dispatch directive's standing instruction
("If any of the seven targets falls below its 0.80-0.90 kill-rate
threshold, surface to owner ... do NOT modify thresholds silently"),
the thresholds in `tools/testkit/mutation/mutmut-config.toml` remain
at 0.80–0.90 untouched.

## 5. Step-by-step end-to-end sweep — FACTs

FACT — `cd tools/integrity && uv run python -m integrity --mode strict`:
```
summary: 0 HARD_FAIL, 0 SOFT_WARN
```

FACT — `uv run python -m integrity --all`:
```
summary: 0 HARD_FAIL, 0 SOFT_WARN
```

FACT — `uv run pytest -W error` per workspace member:
- tools/testkit/ — **47 passed in 5.44s** (was 44 at Block 8 close;
  +3 from the legacy-captures corpus test landed at Block 9).
- tools/integrity/ — **28 passed in 0.94s** (was 22 at Block 8 close;
  +6 from `test_replay_failing_tests.py` landed at Block 9).
- tools/diagnostics/ — **22 passed in 0.45s** (unchanged from Block 8).
- packages/reaction-diffusion-2d/ — **14 passed in 7.03s** (unchanged
  from Block 8).
- **Total: 111 passed.** (User dispatch directive cited "88 + 14 = 102";
  the +9 delta is +3 corpus tests + +6 replay-normalizer tests added
  at Block 9.)

FACT — `pnpm vitest run` (common-ts):
```
Test Files  6 passed (6)
     Tests  11 passed | 2 skipped (13)
```
(The 2 skipped tests are spec § 7.8 `skip-in-CI` WebGPU GPU-path
tests; deferred to Phase 1 GPU-host runs per Block 8's deferred_items.)

FACT — `pnpm test:cross-stack`:
```
✓ src/__tests__/cross-stack.test.ts (1 test) 414ms
  ✓ Python h5py reads the TS-written manifest + payload and the values match
```

FACT — `uv run python tools/dispatch/preflight-phase.py 0`:
```
=== Phase 0 preflight ===
  [PASS] tool-available:git
  [PASS] tool-available:python3
  [PASS] tool-available:uv
  [PASS] tool-available:pnpm
  [PASS] tool-available:node
  [PASS] in-git-repo
=== ALL PASSED ===
```

FACT — `pnpm tsc --noEmit` (common-ts) and `pnpm eslint .` — both
clean (no errors).

FACT — adversarial-fixture meta-test
`pytest tools/integrity/tests/test_adversarial_coverage.py -W error`:
```
7 passed in 0.45s
```

FACT — audit prose freshness
`uv run python tools/integrity/scripts/audit_prose_freshness.py`:
```
summary: 0 finding(s)
```

FACT — `verify_evidence.py` on every block report (loop): all PASS
across blocks 1-8 + the Block-8 addendum. Last-line per audit reports
`X pass / 0 fail at head_sha <sha>`.

## 6. Phase-fit verification (per spec Appendix F.3.2)

INFERENCE — does Phase 0's output satisfy Phase 1's preflight?

- **RD-2D canonical capture at the LOCKED descriptor.** ✓ PASS.
  `captures/reaction-diffusion-2d-ref/gray-scott-lambda-128sq-seed42-step2000.{h5,json}`
  exists at the v0.9-locked descriptor. Manifest sha256 hash
  cross-checked by `verify_evidence.py` against the Block-8 audit's
  declared evidence_hashes.
- **common-cpp and common-py NOT yet landed.** ✓ PASS. These are
  Phase 1 Stage 1 work per spec § 11.1; Phase 0 ships common-ts only.
- **13-gate template instantiated for RD-2D as Phase-1 reference.**
  ✓ PASS. RD-2D's Block-8 audit + Block-8 addendum together document
  each gate's status; Phase 1 sims copy the gate enumeration verbatim.
- **Failing-tests replay infrastructure ready.** ✓ PASS at Block 9.
  `replay_failing_tests.py` with canonical normalization is the
  Phase-1 first-stage preflight gate.
- **Legacy-captures corpus seeded + replay test active.** ✓ PASS at
  Block 9. `phase-0-rd-2d-ref.{h5,json}` is the first entry; Phase 4
  WU-A's schema-bump acceptance gate replays through this test.
- **CI workflows active.** ✓ PASS at Block 9 (this commit).
  integrity, determinism, equivalence, ts-strict, audit-append-only,
  tolerance-budget-check, mutation-testing all flipped to `if: true`.
  The local pre-push sweep above verifies each workflow's command set
  passes on the current tip; CI's first-push verdict remains to be
  observed once the operator pushes (or once GitHub Actions picks up
  the next push that lands in the operator's review window).

## 7. Deferred items for Phase 1

INFERENCE — aggregated from each block's `deferred_items` + Block-9
surfaces:

- **Real per-target mutation-testing baseline.** Block 9 SHIFTED.
  Block 5's run-mutation.sh needs per-member-sync orchestration so
  mutmut's pytest invocation can collect each target's tests without
  workspace-root pytest-collection failures. First Phase-1 stage
  must produce real baseline numbers OR the per-target thresholds
  are explicitly relaxed by owner with documented rationale.
- **WebGPU GPU-path testing with a real adapter.** Block 8 deferred.
  Phase 0 ships the WGSL + TypeScript driver but does not exercise
  them in CI (the 2 vitest tests skipped above). Phase 1 runs the
  WebGPU implementation on a GPU host and verifies cross-stack
  equivalence against the canonical capture at rtol=1e-4, atol=1e-6.
- **Real `periodic_bc_satisfied` wrap-stencil PBT.** Block 8 deferred
  via the smoothness-proxy (tolerance 2.0). Phase 1 replaces the
  proxy with an actual wrap-stencil PBT once the admissible-IC region
  is defined.
- **Block 6 spectral diagnostic defaults evaluated against a sim that
  uses them.** Block 8 noted that `check_spectral_content` (with
  `cutoff_fraction=0.5`, `max_high_fraction=0.1`, `rtol=1e-10`) is
  never invoked from RD-2D, so the defaults are not exercised. First
  Phase-1 sim that exercises this surface decides if the defaults
  hold.
- **RD-2D PBT IC region constraint.** Block 8 deferred. Define
  admissible-IC region inside [0, 1] (V-mass below threshold, etc.)
  and constrain the Hypothesis strategy; restore strict
  `[-1e-9, 1+1e-9]` `monotone_bounds_uv` invariant.
- **MMS for Gray-Scott (RD-2D).** Block 8 deferred. Extends Block 2's
  heat-eq-1D MMS pipeline to a coupled two-species non-linear PDE
  (Phase 1+ solution-verification work).
- **Schema-version backward-compat corpus consumers (Phase 4 WU-A).**
  Block 8 seeded; Phase 4 WU-A's first schema bump round-trips
  through the new reader.
- **First cross-phase audit replay (Phase 1 first stage).** Spec
  G.7.5's prior-phase replay — Phase 1 replays Phase 0's
  failing-tests evidence using `replay_failing_tests.py`'s canonical
  normalization.
- **Cat 4 harder grammars.** Per Phase-0 plan § 9 decision-22.
- **common-cpp, common-py.** Per spec § 11.1 item 0.12 (common-ts is
  Phase 0; the other two ship in Phase 1).
- **Spec-amendments-proposed cleanup.** Three outstanding proposals
  (§ 3 above); owner reviews at Phase 1 kickoff.

## 8. Banked patterns from Phase 0

INFERENCE — patterns emerged that may graduate to spec Appendix G
(Rule-of-Three watch list):

- **TDD chain across session boundary** (Block 8). The
  failing-tests commit (`ec7045e`) was authored in one session;
  the implementation commit (`aa76def`) in the next. The
  `Failing-tests-output-hash-witnessed:` footer + the addendum
  re-anchor pattern made this safe. First instance; rule-of-three
  not triggered.
- **Convention-12 SHA back-fill addendum (not --amend).** Block 9
  addendum on Block 8 — separate follow-up commit upgraded gate 13
  from SHIFTED to CONFIRMED without rewriting history. First
  instance; rule-of-three not triggered.
- **Per-target runner config in mutmut-config.toml.** First-time
  surface that the framework's per-target runner cannot use a single
  workspace-root pytest invocation. Pattern lives in
  `tools/testkit/mutation/mutmut-config.toml` for whoever picks up
  the Phase-1 mutation-testing baseline fix-up.
- **Canonical normalization for replay equality.** Captured in
  `replay_failing_tests.py` as a documented module docstring + 6 unit
  tests; the contract is "stable per-test-outcome reproducibility,
  not byte-stable platform-header reproducibility." Pattern may apply
  to other reproducibility tools later.

## 9. Defects caught at landing

FACT — three defects surfaced by Block-9 steps that no prior block
caught:

1. **mutmut interrupt left source mutated.** Working-tree
   `tools/testkit/golden/verifier.py` had `points_tested = None`
   (mutation) with a `.bak` of the original. The bug existed silently
   between the moment of TaskStop and the moment Block 9 ran the
   testkit pytest sweep. Restored, then added EXIT/INT/TERM trap to
   prevent recurrence.
2. **Workspace-root pytest collection misalignment.** `pytest
   packages/reaction-diffusion-2d/tests/test_diagnostics.py` fails
   from the repo root because the workspace venv's namespace doesn't
   include `diagnostics` (it's a separate workspace member that needs
   per-member sync). All in-repo invocations sync from their package
   first; the only consumer that didn't was the mutation-testing
   orchestrator. Documented in § 4.
3. **`verify_evidence.py` rejected the addendum's missing `head_sha`.**
   First-time use of an addendum file; the script's front-matter
   contract requires `head_sha`. Resolved by adding the addendum's own
   commit SHA to its front-matter (commits in this landing batch).

## 10. Conventions honored

- **Convention #8** — every API surface in this audit was grep- or
  test-verified at execution time.
- **Convention #12** — Block-8 addendum landed as a separate
  follow-up commit (`52aeb99`); no `git --amend`.
- **Convention A** — Block 9 commit chain (chronological):
  `aeb42f4 feat(integrity)` → `52aeb99 docs(phase-0)` →
  `b18f68b chore(testkit)` → `8b706d1 test(testkit)` →
  `ee24e16 chore(mutation)` → `0e3a11e ci(phase-0)`. Plus the
  Cat-5 cleanup commit `9c7bbf0` from the prior Block-9 session.
- **Convention M** — re-anchored on phase-0-plan § 7.9, spec § 7.12,
  spec G.7.5, and every prior block's audit before authoring this
  retro.
- **FACT / INFERENCE tagging.**

## 11. Phase-0 acceptance gate checklist (per § 8)

| # | Criterion | Status | Note |
|---|---|---|---|
| 1 | All 13 spec § 11.1 deliverables landed (0.1–0.13) | ✓ PASS | Cross-referenced via each block's audit |
| 2 | RD-2D hits all 13 Layer-4 gates | ✓ PASS | Block 8 + Block-9 addendum (gate 13 CONFIRMED under canonical normalization) |
| 3 | `integrity --all` clean | ✓ PASS | 0 HARD_FAIL, 0 SOFT_WARN |
| 4 | `pytest -W error` green across testkit, integrity, diagnostics | ✓ PASS | 47 + 28 + 22 = 97 passed; +14 RD-2D = 111 total |
| 5 | `pnpm vitest run` green | ✓ PASS | 11 passed, 2 spec-§7.8 skips |
| 6 | `pnpm test:cross-stack` passes | ✓ PASS | 1 passed |
| 7 | All CI workflows active + green on tip | ⚠ DEFERRED (operator-observed) | Local pre-push sweep verifies each workflow's command set; CI verdict on push observed by operator |
| 8 | Convention A visible in git history | ✓ PASS | RD-2D `ec7045e` precedes `aa76def`, witness-hash footer matches |
| 9 | Adversarial-fixture meta-test passes | ✓ PASS | 7 passed in 0.45s |
| 10 | Mutation-testing baseline committed | ⚠ SHIFTED | Framework-validated, not real per-target scores. See § 4 |
| 11 | Schema-corpus seed round-trips | ✓ PASS | Block-9 test `test_legacy_capture_round_trips` |
| 12 | Perf-ledger has first RD-2D row | ✓ PASS | `docs/perf-ledger.md` per Block 8 |
| 13 | Branch-protection doc committed (`docs/ops/branch-protection.md`) | ✓ PASS (committed Block 1) | Operator confirms repo-side rules at tag review |
| 14 | `v0.0.0-phase-0` tag on `main` | ⚠ DEFERRED to operator | Per spec § 7.12 |
| 15 | Landing audit CONFIRMED/acceptably-SHIFTED + final `Tag pushed: NO` line | (this file) | See § 12 below |

## 12. Closing summary

Proposed tag: `v0.0.0-phase-0`
Tag commit SHA: `85da2fc8911230dcd0706cfe08d6ae67c2a5c311`
  (= this landing retro's commit; SHA back-filled per Convention #12 in
   a follow-up commit since the retro's content references itself)

Tag pushed: NO (operator action required)
