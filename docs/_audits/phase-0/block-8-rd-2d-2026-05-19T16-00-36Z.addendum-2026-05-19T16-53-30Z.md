---
date: 2026-05-19T16-53-30Z
author: phase-0-block-9-agent
phase: 0
artifact: block-addendum
artifact_id: block-8-rd-2d-addendum
parent_artifact_id: block-8-rd-2d
parent_audit: docs/_audits/phase-0/block-8-rd-2d-2026-05-19T16-00-36Z.md
head_sha: 52aeb99
verdict: CONFIRMED
convention: "#12 (SHA back-fill, separate follow-up commit, NOT --amend)"
evidence_paths:
  - tools/integrity/integrity/scripts/replay_failing_tests.py
  - tools/integrity/tests/test_replay_failing_tests.py
gate_under_review: 13
gate_13_prior_status: SHIFTED-with-explanation
gate_13_new_status: CONFIRMED
normalized_sha256: e0ffb58e0f7b5d910f8c1f6dc3e3dc8d0b36943a424a590b77d721594e0a22f5
raw_sha256_at_block_8_close: ee9f2d3de9bc1ddf1a0826d672c6d6994abc270b9bc515db6ff32096b27b2988
---

# Block 8 — RD-2D addendum: gate-13 CONFIRMED under canonical normalization

> Append-only follow-up to block-8-rd-2d-2026-05-19T16-00-36Z.md. Per
> spec § 7.5 (audits are append-only) + Phase-0 plan Convention #12 (SHA
> back-fill is a separate follow-up commit, not `git --amend`). The
> parent audit's gate-13 verdict `SHIFTED-with-explanation` is upgraded
> to **CONFIRMED** by the Block-9 LANDING wiring of the canonical
> normalization that the parent audit itself proposed (§ 3 of the
> parent + amendment in `docs/_audits/phase-0/spec-amendments-proposed.md`).

## 1. What changed

FACT — `tools/integrity/integrity/scripts/replay_failing_tests.py` is
now committed (was untracked at the parent audit's HEAD `aa76def`).
The script implements the canonical normalization defined below and
re-verifies any failing-tests Convention-A commit against its committed
evidence file.

FACT — Canonical normalization defined by the script's
`normalize_pytest_output(raw, paths_to_canonicalize)` function neutralizes
three sources of pytest-output non-determinism on the same hardware:

| # | Source | Neutralization |
|---|---|---|
| 1 | Pytest timing-summary line (`... in 0.52s ===`) | Replace elapsed-time field with `NN.NNs`. |
| 2 | Interpreter-path suffix on the platform header line (varies with venv name and `python`/`python3` symlink) | Replace `-- <path>` suffix with `-- <INTERPRETER>`. |
| 3 | Absolute repo paths in test-failure tracebacks (real checkout vs replay worktree) | Both prefixes substituted with `<REPO>`; worktree path placed outside the repo to avoid prefix-shadowing. |

The first source is the one surfaced at Block 8 § 3; the second and
third surfaced when the script was first run end-to-end at Block 9.
None of the three affects per-test outcome, error type, traceback
content, or captured-output bytes — those are the load-bearing
reproducibility claims.

## 2. Gate-13 re-verification

FACT — `uv run python -m integrity.scripts.replay_failing_tests --commit
ec7045e6efc728845f5728ab975b6f88d8d5e8d3 --evidence tools/testkit/failing-tests-evidence/reaction-diffusion-2d-ref-2026-05-19T15-43-23Z.txt
--pytest-target packages/reaction-diffusion-2d`:

```
  commit                  ec7045e6efc728845f5728ab975b6f88d8d5e8d3
  evidence                <REPO>/tools/testkit/failing-tests-evidence/reaction-diffusion-2d-ref-2026-05-19T15-43-23Z.txt
  expected_sha256_norm    e0ffb58e0f7b5d910f8c1f6dc3e3dc8d0b36943a424a590b77d721594e0a22f5
  actual_sha256_norm      e0ffb58e0f7b5d910f8c1f6dc3e3dc8d0b36943a424a590b77d721594e0a22f5
  match                   True
  raw_lines_differing     14
```

INFERENCE — Gate 13 is now CONFIRMED verbatim under canonical
normalization. The 14 differing raw lines are accounted for by the three
neutralized sources (1 timing line, 1 platform line, 12 traceback paths)
— every line outside those sources matched byte-for-byte at the raw
level, which is exactly the reproducibility claim spec G.7.5 wants the
gate to make.

## 3. Coherence with the parent audit

The parent audit's table-row for gate 13 read
`⚠ SHIFTED (with explanation)` and `89.` aggregate verdict was
`CONFIRMED. 12/13 gates PASS verbatim`. The aggregate verdict is
unchanged at CONFIRMED. The gate-13 row is upgraded to
`✓ PASS (under canonical normalization)` retroactively as recorded
here; the prior row text remains in place per append-only discipline.

## 4. Raw vs normalized hashes — both retained

- **Raw sha256 of the evidence file at Block 8 close**:
  `ee9f2d3de9bc1ddf1a0826d672c6d6994abc270b9bc515db6ff32096b27b2988`
  (still the byte-content hash; recorded in the parent audit's
  `evidence_hashes` map and in commit `ec7045e`'s footer; used by
  Cat-3 numerical-evidence integrity checks).
- **Normalized sha256 for replay equality**:
  `e0ffb58e0f7b5d910f8c1f6dc3e3dc8d0b36943a424a590b77d721594e0a22f5`
  (canonical-normalization hash; used by gate 13 and by Phase-1+
  replay_failing_tests CI invocations).

The two hashes coexist: the raw hash certifies the file's bytes haven't
been tampered with; the normalized hash certifies the test-run is
reproducible. Convention #8 is preserved (no fabrication of either).

## 5. Conventions honored

- **Convention #12** — separate follow-up commit (not `git --amend`).
  The parent audit and its commits remain unchanged; this addendum is
  a new append-only file.
- **Convention M** — re-anchored on parent audit § 3, on
  `docs/_audits/phase-0/spec-amendments-proposed.md`'s timing-line
  amendment, and on the live `replay_failing_tests.py` source before
  authoring.
- **FACT / INFERENCE tagging.**

## 6. Forward effect

The activated normalization unblocks gate 13 as a CI-enforceable check
(Block 9 LANDING flips the determinism + integrity workflows). Phase-1
Stage 1 will replay Phase-0's failing-tests commits as part of its
preflight; the canonical normalization is the load-bearing contract.
