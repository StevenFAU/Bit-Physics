---
date: 2026-05-25T23-30-00Z
author: reaction-diffusion-2d-stack-c-stage-2-agent
phase: 2
artifact: sub-phase
artifact_id: sub-phase-reaction-diffusion-2d-stack-c-landing
subject: "Sub-phase landing audit. VERDICT: CONFIRMED. The 8th and FINAL spec § 11.3 cross-stack port (reaction-diffusion-2d -> Stack-C Vulkan/C++ on Mesa lavapipe) and the FIRST Stack-C per-sim port. gate-14 is cross-stack BIT-EXACT (within_tolerance=True, max_abs_err=0.0 across all 11 frames x {U,V}) -- the THIRD shape-(a) instance overall and the FIRST on a non-Warp backend, establishing the SECOND zero-seed-difference backend pair (Vulkan/C++-f64-lavapipe-NoContraction <-> NumPy). Sub-phase-bounded landing per operator routing (Option 1): Phase-2 is SUBSTANTIVELY complete (8/8 ports) but the FORMAL close (phase-level closing audit + v0.2.0-phase-2 tag) is a dedicated Stage 9 pass per Phase-2 plan § 2.12. Doc layer reconciled (methodology § 6.7/§ 6.8 Option-alpha; conventions § L.7 O-1 + § L.9 D16; cpp.md; charter §§ 1/2/7; CHANGELOG). Stage-2-discovered gate-12 perf-ledger row restored in-stage (S2-RD2C1). Portfolio sweep ZERO regressions; ctest 7/7; integrity 0 HF / 14 SW (baseline HELD); replay HELD. All 14 gates GREEN. LOCAL-ONLY (no push, no tag; D12)."
verdict-state: CONFIRMED
head_sha: PENDING-BACKFILL
head_sha_at_checkpoint: PENDING-BACKFILL
parent_audits:
  - docs/_audits/phase-2/sub-phase-reaction-diffusion-2d-stack-c/stage-1c-checkpoint-2026-05-25T22-00-00Z.md
  - docs/_audits/phase-2/sub-phase-reaction-diffusion-2d-stack-c/stage-1b-checkpoint-2026-05-25T21-30-00Z.md
  - docs/_audits/phase-2/sub-phase-reaction-diffusion-2d-stack-c/stage-1a-checkpoint-2026-05-25T21-00-00Z.md
  - docs/_audits/phase-2/sub-phase-reaction-diffusion-2d-stack-c/stage-0-checkpoint-2026-05-25T20-30-00Z.md
evidence_paths:
  - docs/_audits/phase-2/sub-phase-reaction-diffusion-2d-stack-c/stage-2-evidence/portfolio-sweep-2026-05-25T23-30-00Z.txt
  - docs/_audits/phase-2/sub-phase-reaction-diffusion-2d-stack-c/stage-2-evidence/stage-2-integrity-sweep-2026-05-25T23-30-00Z.txt
  - docs/_audits/phase-2/sub-phase-reaction-diffusion-2d-stack-c/stage-2-evidence/stage-2-replay-2026-05-25T23-30-00Z.txt
  - docs/_audits/phase-2/sub-phase-reaction-diffusion-2d-stack-c/stage-2-evidence/perf-ledger-measurement-2026-05-25T23-30-00Z.txt
  - docs/conventions/cross-stack-equivalence-methodology.md
  - docs/conventions/sub-phase-conventions.md
  - docs/common/cpp.md
  - docs/phases/sub-phase-reaction-diffusion-2d-stack-c.md
  - docs/perf-ledger.md
  - CHANGELOG.md
evidence_hashes:
  - "stage-2-integrity-sweep: sha256:c19492add530f3a5a0d723777cf818a702b7019ee664c733695364aa6d22cb52"
  - "stage-2-replay: sha256:9399fc337160dd20a3aeefdad6bc8d93edb7918ea5e8d005253d3ce718909f34"
  - "canonical-capture.h5: sha256:00081dc42b192e653656705ca81c1c974735728efae10b6b2f9b6c0bce032f42"
---

# Sub-Phase Landing Audit — reaction-diffusion-2d → Stack-C (Vulkan / C++)

> **VERDICT: CONFIRMED.** The **8th and FINAL** spec § 11.3 cross-stack port and the
> **FIRST Stack-C (Vulkan / C++) per-sim port**. gate-14 is **cross-stack BIT-EXACT**
> (`within_tolerance=True`, `max_abs_err=0.0`). Lands **LOCAL-ONLY** (D12) and
> **sub-phase-bounded** (operator routing Option 1 — § 7). All 14 gates GREEN.

## § 1. The sub-phase arc

`reaction-diffusion-2d` → Stack-C is the only **Vulkan / C++** per-sim port in the
portfolio and the first consumer of the `common-cpp` § 1.9.1-cpp substrate matured at
`sub-phase-common-cpp-bootstrap`. The Phase-1 NumPy Gray-Scott reference
(`gray-scott-lambda-128sq-seed42-step2000`, **f64**) was ported to GLSL `double`
compute on Mesa **lavapipe** (`precise` → SPIR-V `NoContraction`).

The arc was clean — no Hard-Rule-2 falsification (contrast `eulerian-smoke-stack-e`):
the plan-drafting refresh predicted **shape (a) BIT-EXACT** grounded in a *measured*
step-1 cross-stack difference of exactly `0.0` (probe § 6 Part B), and every stage
confirmed it: Stage 1b ckpt 2 (canonical-scale full-horizon `max_abs_err=0.0`),
Stage 1c formal gate-14 (`within_tolerance=True`, `max_abs_err=0.0`, 22 entries =
11 frames × {U,V}). This is the **THIRD shape-(a) instance** overall (after MPM-E +
smoke-E + LBM-E) and the **FIRST on a non-Warp backend**, establishing the **SECOND
zero-seed-difference backend pair**: Vulkan/C++-f64-lavapipe-NoContraction ↔ NumPy.

**One Stage-2-discovered gap, closed in-stage (S2-RD2C1):** the Stage-2 portfolio
regression sweep surfaced that **gate-12 (perf-ledger row)** was never landed for this
port — every other Phase-2 port added its per-port row at Stage 1b, but no Stack-C
commit touched `docs/perf-ledger.md`, so the Stage-1b "gates 4-13 GREEN" claim
over-stated gate-12. Restored in-stage via the established per-port convention
(measure + append; § 3.5), analogous to the smoke-E Stage-2 IC-16 restoration
(§ L.8 S2-SME1 precedent).

## § 2. Gate ledger (14 gates)

All gates verified at HEAD via the fresh top-level C++ build (`ctest` 7/7) + the
committed stage checkpoints. Phase-1 gates 1–3 (spec docs / probe / failing-tests)
are inherited; gates 4–14 are this port's.

| Gate | Status | Witness |
|---|---|---|
| 4 code-verification (MMS) | GREEN | MMS L2 order **2.0008** ∈ 2.0±0.5; 4-grid ladder N∈{16,32,64,128}, t_final=0.05, manufactured-source kernel (S0-RD2C1) — `test_mms_order.cpp` |
| 5 Tier-1 diagnostics | GREEN | NaN/Inf-clean; 11 captured frames finite (n²=16384 U,V) |
| 6 Tier-2 (scalar-field) | GREEN | scalar-field diagnostics finite over the trajectory |
| 7 citations | GREEN | Gray-Scott / Pearson-1993 reaction-diffusion provenance |
| 8 public API | GREEN | port public surface; § 1.9.1-cpp socket consumed (not extended) |
| 9 captures + corpus | GREEN | canonical capture (LFS oid `00081dc42b…`, 2.94 MB) + schema-corpus fixture (round-trip 17 → **19** entries) |
| 10 determinism | GREEN | `assert_deterministic_run` (runs=2, tol=0.0) bit-identical; witness `493ffbba…` (O-2 ckpts 2/3; Q-CPP1/Q-CPP3) |
| 11 PBT | GREEN | RD bounded/dissipative + field-range invariants (§ L.4) |
| **12 perf-ledger** | **GREEN — restored at Stage 2 (S2-RD2C1)** | `reaction-diffusion-2d \| vulkan-cpp \| gray-scott-lambda-128sq-seed42-step2000 \| 1.13 \| i7-12700KF-linux-6.17` (omitted at Stage 1b; § 1) |
| 13 failing-tests replay | GREEN | Stage-1a RED anchor (5/5 `NotImplemented`) → HEAD `ctest -R rd2d_stack_c_tests` PASS |
| **14 cross-stack** | **GREEN — BIT-EXACT** | `within_tolerance=True`, `max_abs_err=0.0`, 22 entries (11 frames × {U,V}); `rd2d_stack_c_gate14` ctest PASS; equivalence.md § C |

§ L.7 **O-2 four-checkpoint determinism chain COMPLETE**: ckpt 1 (Stage 0 R-A1
`9d8ca9b0…`) → ckpt 2 (Stage 1b canonical-scale reproduction) → ckpt 3 (Stage 1b
2-run determinism `493ffbba…`) → ckpt 4 (Stage 1c formal gate-14).

## § 3. Verification sweeps (Stage 2; FACT)

- **C++ ctest (both CMake targets — common-cpp + RD-2D-Stack-C; fresh top-level
  build):** **7/7 PASS** including `rd2d_stack_c_gate14` (gate-14 cross-stack,
  `within_tolerance=True`, `max_abs_err=0.0`). Evidence:
  `stage-2-evidence/portfolio-sweep-…txt`.
- **Python tool members (pytest):** testkit **74 passed** (incl. legacy-captures
  corpus round-trip), integrity **56 passed**, diagnostics **93 passed**; corpus
  round-trip (19 entries incl. `phase-2-reaction-diffusion-2d-stack-c`) **19 passed**.
  RD-2D-Stack-C is a CMake target (NOT a uv member; D11) and touched **no** Python
  workspace-member source, so per-sim suites are unaffected by construction; the sweep
  targets the corpus fixture (the only Python-side delta) + the cat 1–5 contract sweep.
- **Integrity `--all --mode strict`:** **0 HARD_FAIL, 14 SOFT_WARN**; report digest
  `c19492add530f3a5a0d723777cf818a702b7019ee664c733695364aa6d22cb52` = baseline
  **EXACT MATCH** (HELD — now 20 contiguous sub-phases entering; the Stage-2 doc edits
  + perf-row + this audit add no new report lines, re-confirmed after authoring).
  Evidence: `stage-2-evidence/stage-2-integrity-sweep-…txt`.
- **Bit-identity replay invariant (§ D.3):** **HELD** —
  `9399fc337160dd20a3aeefdad6bc8d93edb7918ea5e8d005253d3ce718909f34` (8/8 gates PASS,
  ok=True) vs `v0.1.0-phase-1`. Evidence: `stage-2-evidence/stage-2-replay-…txt`.

**VERDICT: ZERO REGRESSIONS.** No gate failure on any of the 23 uv members + the
Stack-C CMake target + the common-cpp CMake target.

## § 4. N1 enumeration — every placeholder-bearing audit across the sub-phase (Convention #12)

Each stage's checkpoint carried a single `head_sha` placeholder, back-filled in that
stage's terminal SHA-back-fill commit (separate; never `--amend`). Full chain
(verified resolving at HEAD):

| Stage | Implementation/evidence commit(s) | Checkpoint (head_sha) | SHA back-fill |
|---|---|---|---|
| plan-drafting (held) | `4f9e523`/`8605a31`/`f772f71`/`a33cb0b` (HISTORICAL) | (held; no charter) | — |
| plan-drafting-refresh | `5583f6e` probe · `970bbcb` charter · `12117df` landing | (landing) | `6ac0ec5` |
| Stage 0 | (pre-flight; no impl) | `04a7d8f` | `f1cb102` |
| Stage 1a | `415f276` scaffold | `aef57aa` | `49ab0a4` |
| Stage 1b | `732aeee` impl+CMake · `8a9d6fc` capture | `39bcb1b` | `53b6012` |
| Stage 1c | `2bd30c2` gate-14+equivalence+fixture | `fa58d8c` | `d5d93fa` |
| **Stage 2 (this)** | methodology+conventions · cpp.md · charter · CHANGELOG+perf-row · sweep-evidence · **this landing** | `<this landing>` | (terminal back-fill) |

N1 confirmed: every placeholder-bearing audit was back-filled to its own
committing-commit SHA in a separate commit. This landing's `head_sha` /
`head_sha_at_checkpoint` are placeholder-deferred (back-filled in the terminal commit).

## § 5. Stage-2 deliverables landed (sub-phase-bounded)

| Deliverable | Landed |
|---|---|
| methodology § 6.7 within-sim corroboration #2 (RD-2D Stack-D `~1.9e-14` vs Stack-C `0.0`) | ✓ |
| methodology § 6.8 SECOND backend pair (Vulkan/C++-f64↔NumPy, n=1; **Option α** per charter § 6) | ✓ |
| conventions § L.7 O-1 shape-(a) **fourth-instance / first-non-Warp** note | ✓ |
| conventions § L.9 Q-CPP2 + cpp.md § 4 **D16** FloatControls-f32-scoped cleanup note (lean) | ✓ |
| charter §§ reconcile — § 2 Stage-1b row SHIFTED (S0-RD2C1 two-kernel + ladder); § 1/§ 2/§ 7 Phase-2-close routing | ✓ |
| CHANGELOG sub-phase entry (8/8 substantive completeness noted; **NO** phase-2-formal-close marker) | ✓ |
| gate-12 perf-ledger row restored (S2-RD2C1) | ✓ |
| portfolio + integrity + replay sweeps | ✓ (§ 3) |

## § 6. § 6.8 home-routing decision — Option α (charter-resolved; NOT a surface)

The dispatch flagged the **§ 6.8 (extend) vs § 6.9 (new section)** choice as
surface-eligible. It is **resolved by the charter**, not genuinely-non-obvious:
charter § 6 at HEAD explicitly directs *"Bank in methodology § 6.8 (additional data
point) + § 6.7,"* and the already-landed `equivalence.md` § C.2 cites "§ 6.8 backend
pair." Per **Convention M** (charter explicit assignments trump coordinator framing,
§ L.5 S1c-1), this is **Option α — extend § 6.8**.

**Rationale + the β consideration (surfaced for the record, D-class).** Option β (a new
§ 6.9) had substantive appeal: § 6.8 as authored is Warp-specific (title, table, and a
Warp-port-counted graduation qualifier), and RD-2D-Stack-C is the first *non-Warp* pair
— so folding it in required generalizing § 6.8 from "Warp pair (n=2)" to a consolidated
two-backend-pair section. But the charter's explicit § 6.8 assignment + the
sim-spec's existing § 6.8 reference govern; β would orphan the § C.2 pointer. Landed α:
§ 6.8 now hosts both pairs (Warp n=2 sub-observation preserved intact; Vulkan/C++ n=1
added as an additive amendment), framed "n=3 bit-exact across two backend pairs,
SUGGESTIVE not established." The § 6.8 title's prior Warp-exclusive "(n=2)" framing was
minimally generalized (a small modificative edit, since leaving it would contradict the
new content); body additive. Banked as **D-S2-RD2C1**.

## § 7. § 13 — Phase-2 status + operator routing + banked-for-cleanup

### 7.1 Phase-2 substantive completeness + formal-close routing (operator Option 1)

`FACT` This is the **8th of 8** spec § 11.3 cross-stack ports. Reconciliation against
spec § 11.3 (`docs/architecture.md` § 11.3) at HEAD:

| spec § 11.3 item | ports | landed |
|---|---|---|
| 2.1 RD-2d → Stack C, Stack D | RD-2D-**C** (this), RD-2D-D | ✓ ✓ |
| 2.2 SPH → Stack D | SPH-D | ✓ |
| 2.3 MPM → Stack E | MPM-E | ✓ |
| 2.4 Smoke → Stack D and Stack E | Smoke-D, Smoke-E | ✓ ✓ |
| 2.5 LBM → Stack D and Stack E | LBM-D, LBM-E | ✓ ✓ |

8/8 enumerated ports landed. (MPM-Stack-D is an additional port beyond the spec
§ 11.3 "warrant" enumeration; not double-counted.) So **Phase-2 is SUBSTANTIVELY
COMPLETE**, and the comprehensive cleanup sub-phase + the deferred **LFS-architecture
sub-phase (D13, deferred since smoke-Stack-D)** both become **routable**.

**The FORMAL Phase-2 close is NOT executed in this pass (operator routing, Option 1).**
The charter is authoritative *that* Phase-2 closes (§ 1/§ 7); the close *mechanism* is
**Phase-2 plan § 2.12 "Stage 9 — Landing"** — a dedicated phase-level closing-audit
session that (i) reads every prior sub-phase report, (ii) runs cross-cutting sweeps
(equivalence over all ports, perf-ledger >2× source-stack-baseline flagging,
`verify_evidence` over every report → REFUTED on failure, append-only vs
`v0.1.0-phase-1`), and (iii) ends with a proposed **`v0.2.0-phase-2`** tag (operator-only
push; D12). That mechanism was authored for the *superseded single-linear-dispatch*
model (Stage 0 + Stages 1–8 + Stage 9), so under the sub-phase execution model it
warrants its own dispatch with cross-cutting scope exceeding this sub-phase's charter.
**Operator routes Stage 9 — Landing as a dedicated pass** after this Stage 2 lands and
is pushed. This sub-phase's landing is the clean bridge: substantive completeness here;
formal close at Stage 9.

### 7.2 Consolidated banked-for-cleanup inventory (probe input for the comprehensive cleanup sub-phase)

Spanning all 9 Phase-2 sub-phases (RD-2D-D, SPH-D, LBM-D, MPM-D, Smoke-D, MPM-E,
Smoke-E, LBM-E, RD-2D-C) + common-cpp/common-warp bootstraps:

- **D16** — `assert_deterministic_float_controls()` is f32-scoped; extend to assert the
  f64 levers (`…Float64`) for an explicit f64 determinism contract rather than relying
  on inherent IEEE-754 f64 + NoContraction (conventions § L.9 Q-CPP2; cpp.md § 4;
  RD-2D-Stack-C banked, empirically-sufficient-not-defect). Lives in common-cpp (locked).
- **S2-RD2C1** — gate-12 perf-ledger row was omitted at Stage 1b; restored at Stage 2.
  Cleanup signal: the per-port perf-row should be a Stage-1b acceptance check, not a
  landing-time discovery (consider a gate-12 pre-landing assertion in the per-port
  template).
- **B-CPPB2** — charter § 4 `project-state.md` mention vs no-such-file (common-cpp banked).
- **sha256_util.hpp shim** — retained for the 1a evidence link; removable when 1a
  audits are historical (common-cpp banked).
- **R-CPPB2 / Q-CPP5** — CI Mesa/LLVM-pin + exact-digest assertion scoping for
  `cpp-strict.yml` portability (common-cpp banked).
- **S0-LBME1** — coordinator dispatch-hygiene drift (LBM-E; now formalized as § L.5
  S1c-1 process precedent; reinforced thrice at common-cpp — § L.9 process-precedents).
- **`uv sync --all-packages --all-extras`** dev-extras-prune nuance (LBM-E / § L.8 S1b-SME3).
- **Methodology § 6 header staleness** — § 6 header still reads "Fifth-pair refinements
  (eulerian-smoke-stack-d Stage 2)" but the section now spans through the ninth pair +
  two backend pairs (§ 6.8). § 6.8's own title likewise compounds (now two pairs). Both
  are scope-attribution staleness; cleanup candidates (banked since smoke-E § 13).
- **Conventions § L.7 / § L.8 subsection-title attribution staleness** — § L.7 attributes
  to `mpm-multimaterial-stack-e Stage 2` but now carries cross-sub-phase refinements
  through the fourth shape-(a) instance; minor attribution drift.
- **Conventions § M cumulative-shift inventory staleness** — § M.6 records "65 documented
  to date" (frozen at sph-water Stage 2); the running count is now **240** (carried in the
  per-sub-phase audit chain). § M has not been maintained since Phase-1-era; re-sync at
  cleanup.
- **Missing CHANGELOG entries** — smoke-Stack-D / common-warp-bootstrap / MPM-Stack-E
  were noted missing at smoke-E § 13; the `[Unreleased]` section's Phase-2 entries are
  also split across two locations (some appended after the `[0.1.0-phase-1]` header).
  CHANGELOG re-organization + back-fill is a cross-cutting cleanup item. (RD-2D-Stack-C's
  own entry was added adjacent to common-cpp-bootstrap, matching the de-facto recent
  location.)
- **D17** — Phase-1-canonical 2D-ref re-characterization candidate (smoke-E enumeration).
- **Stray taylor-green captures** — untracked `captures/eulerian-smoke-stack-{d,e}/taylor-green-128cube-seed42-step500.{h5,json}`
  in the working tree (LBM-E § 13 carry-in); gitignore-or-remove candidate. NOT touched
  this sub-phase (held-local per D14 disposition; § 8).
- **Integrity baseline-digest derivation** — codified at common-cpp § L.5 / memory
  `integrity-baseline-digest-method`; keep documented.
- **S1c-RD2C1** — the C++ gate-14 "un-skip" is a cross-language ctest, not a pytest
  skip-marker removal; § L.5 documentation candidate at cleanup.

## § 8. Cumulative shifts + S/O/D banks

**Cumulative shifts: 239 (entering Stage 2) → 240 (final).**

| Shift | Stage | Summary |
|---|---|---|
| S1b-RD2C1 | 1b | IC sourcing — port consumes the reference step-0 IC (NumPy PCG64 not reproduced; isolates ported dynamics) |
| S1b-RD2C2 | 1b | port .h5 file-checksum ≠ reference while state datasets byte-identical (diagnostics naive-vs-pairwise sum + HDF5 metadata); gate-14 compares datasets |
| S1c-RD2C1 | 1c | C++ gate-14 "un-skip" = cross-language ctest, NOT pytest skip-marker removal (§ L.5 S1c-1) |
| **S2-RD2C1** | **2** | **gate-12 perf-ledger row omitted at Stage 1b; restored at Stage 2 via established per-port convention** |

**D-class (final state):** D11–D17 RATIFIED; D16 BANKED for cleanup (§ 7.2);
**D-S2-RD2C1** (this stage) — § 6.8 Option α home-routing (charter-resolved; § 6).

**Doc layer landed this stage:** methodology § 6.7 + § 6.8; conventions § L.7 O-1 +
§ L.9 Q-CPP2; cpp.md § 4; charter §§ 1/2/7; CHANGELOG; perf-ledger gate-12 row.

## § 9. Boundaries honored (Convention A / dispatch BOUNDARIES)

NOT touched: Phase-1 source / `common-cpp` / `common-warp` / Stack-D implementation
(locked from Stage 1b); `tolerance.toml` (D17 no-op, 4th skip); the held-local
taylor-green captures (D14, not this sub-phase's). NO source extension (perf-ledger
row + doc edits are doc-landing, not source). NO push, NO tag (operator-only; D12).
The **§ 2.12 phase-level closing audit, cross-cutting sweeps, and `v0.2.0-phase-2` tag
proposal are explicitly DEFERRED** to the dedicated Stage 9 pass (operator routing
Option 1; § 7.1). The methodology/conventions/cpp.md/charter/CHANGELOG/perf-ledger
edits + the in-stage gate-12 restoration ARE this stage's deliverables, not boundary
breaches.

## § 10. Verdict

**sub-phase-CONFIRMED.** `reaction-diffusion-2d` → Stack-C lands **LOCAL-ONLY** with
all 14 gates GREEN; gate-14 cross-stack **BIT-EXACT** (`within_tolerance=True`,
`max_abs_err=0.0`) — the THIRD shape-(a) instance and the FIRST on a non-Warp backend,
establishing the SECOND zero-seed-difference backend pair (Vulkan/C++-f64-lavapipe-
NoContraction ↔ NumPy). The 8th and final spec § 11.3 port: **Phase-2 is substantively
complete (8/8)**; the formal close is the dedicated Stage 9 pass (§ 2.12). Portfolio
sweep ZERO regressions; `ctest` 7/7; integrity 0 HF / 14 SW (baseline `c19492ad…`
HELD); replay HELD (`9399fc33…`). Cumulative shifts **240**. O-2 chain complete (4/4).
§ 6.8 Option α (charter-resolved). gate-12 perf-row restored in-stage (S2-RD2C1).
No `-phase-N` tag (D12). `head_sha` placeholder-deferred (back-filled in the terminal
SHA-back-fill commit).
