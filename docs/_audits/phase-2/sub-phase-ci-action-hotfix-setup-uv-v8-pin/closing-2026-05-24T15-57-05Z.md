---
date: 2026-05-24T15-57-05Z
author: ci-action-hotfix-setup-uv-v8-pin-sub-phase-agent
phase: 2
artifact: sub-phase
artifact_id: sub-phase-ci-action-hotfix-setup-uv-v8-pin
subject: "Single-stage focused-infrastructure HOTFIX — post-push CI failure of sub-phase-ci-action-migration-and-banked-cleanup (HEAD e662564): 6 of 9 workflows red, all using astral-sh/setup-uv@v8. Root cause (web-fetch-confirmed at setup-uv v8.0.0 release notes): the maintainer DISCONTINUED publishing moving major/minor tags at v8 as supply-chain hardening (tj-actions precedent) — @v8 does not resolve. Fix: pin all 6 to the immutable @v8.1.0 (current latest v8.x). Other 3 actions (checkout@v6 / setup-node@v6 / pnpm@v6) keep moving tags (still published) — out of scope. 6 single-line edits; every other line byte-for-byte preserved. Verification: pyyaml 9/9 valid; integrity c19492ad byte-identical (streak HELD); bit-identity 9399fc33…909f34 HELD (28th+ invocation); no Python sweep warranted (workflow-YAML-only). Verdict CONFIRMED. Hotfix shift H1 documented (NOT cumulated per § M.6); cumulative remains 152. No -phase-N tag; PUSH IS OPERATOR ACTION (remote-CI re-validation pending)."
verdict-state: CONFIRMED
head_sha: d9b78540453f07212d2ef6fdd247a643d42e0d36
head_sha_at_checkpoint: d9b78540453f07212d2ef6fdd247a643d42e0d36
parent_audits:
  - docs/_audits/phase-2/sub-phase-ci-action-migration-and-banked-cleanup/landing-2026-05-24T15-29-43Z.md
  - docs/_audits/phase-2/sub-phase-ci-action-migration-and-banked-cleanup/landing-sha-back-fill-2026-05-24T15-29-43Z.md
evidence_paths:
  - docs/_audits/phase-2/sub-phase-ci-action-hotfix-setup-uv-v8-pin/evidence/integrity-sweep-2026-05-24T15-57-05Z.txt
  - docs/_audits/phase-2/sub-phase-ci-action-hotfix-setup-uv-v8-pin/evidence/replay-2026-05-24T15-57-05Z.txt
  - .github/workflows/python-strict.yml
  - .github/workflows/determinism.yml
  - .github/workflows/equivalence.yml
  - .github/workflows/integrity.yml
  - .github/workflows/mutation-testing.yml
  - .github/workflows/tolerance-budget-check.yml
evidence_hashes:
  docs/_audits/phase-2/sub-phase-ci-action-hotfix-setup-uv-v8-pin/evidence/integrity-sweep-2026-05-24T15-57-05Z.txt: sha256:c19492add530f3a5a0d723777cf818a702b7019ee664c733695364aa6d22cb52
  docs/_audits/phase-2/sub-phase-ci-action-hotfix-setup-uv-v8-pin/evidence/replay-2026-05-24T15-57-05Z.txt: sha256:9399fc337160dd20a3aeefdad6bc8d93edb7918ea5e8d005253d3ce718909f34
---

# Closing Audit — Sub-Phase CI-Action-Hotfix-Setup-UV-v8-Pin

> **Single-stage focused-infrastructure HOTFIX** (sibling, not child; conventions § A.1 hotfix flavor +
> § A.2 single-repair-audit shape). LOCAL-ONLY; PUSH IS OPERATOR ACTION (§ 7).

## § 1. Scope + failure framing

(FACT — coordinator dispatch + the just-landed sub-phase's closing audit § 11.) The
`sub-phase-ci-action-migration-and-banked-cleanup` landing (HEAD `e662564`) was pushed; **6 of 9
workflows went red**, all six the ones using `astral-sh/setup-uv@v8` (the Stage-1a S-CI2 bump from `@v6`).
The 3 green workflows do not use setup-uv (`structure.yml`, `audit-append-only.yml`, `ts-strict.yml`).
The other bumped majors (`actions/checkout@v6`, `actions/setup-node@v6`, `pnpm/action-setup@v6`) still
publish moving tags and resolved fine.

This is the **single-stage hotfix** the prior sub-phase's § 11 explicitly anticipated ("If CI red:
surface immediately; a follow-up hotfix sub-phase dispatches"). The prior § 11 flagged `setup-uv@v8` as
the most plausible red surface but guessed the mechanism as "requiring a runner Node feature" — the
ACTUAL root cause (§ 3) is simpler and is documented here as hotfix shift **H1**: the `@v8` moving major
tag **does not exist** (the maintainer discontinued moving tags as supply-chain hardening), so the
`uses:` line fails to resolve before any runtime concern.

## § 2. Routing consumed

(FACT — dispatch OPERATOR ROUTING, already decided.)
- **Pin form: `@v8.1.0`** (immutable specific-version tag; current latest v8.x re-fetched at edit time — § 3).
- **Other 3 actions** (`checkout@v6` / `setup-node@v6` / `pnpm/action-setup@v6`): **leave on moving tags** (still published; resolving). Out of scope.
- **S-2.1** (Stack-D filterwarnings taichi-`SyntaxWarning` gap): **stays banked.** Out of scope.

## § 3. Web-fetch FACTs (Task 1)

(FACT — `github.com/astral-sh/setup-uv/releases`, fetched 2026-05-24 at edit time per Convention #8.)

- **(a) v8.0.0 release-notes language (verbatim):** *"To increase security even more we will stop
  publishing minor tags. You won't be able to use `@v8` or `@v8.0` any longer."* The notes motivate it:
  *"We do this because pinning to major releases opens up users to supply chain attacks like what
  happened to [tj-actions]."* This confirms `@v8` (and `@v8.0`) are unresolvable by design; only
  immutable specific-version tags (e.g. `@v8.1.0`) work.
- **(b) Current latest v8.x:** **`v8.1.0`** (released 2026-04-16; introduced the `no-project` boolean
  input). Used as the pin target — matches the coordinator's 2026-05-24 fetch.
- **No posture reversion:** setup-uv has NOT republished `@v8` as a moving tag → no Hard Rule 2; the fix
  proceeds as routed.

## § 4. Per-workflow edits (Task 2)

(FACT — `git diff .github/workflows/`; HEAD `e662564`, no drift at edit time.) **6 files, 6 single-line
changes** (`uses: astral-sh/setup-uv@v8` → `uses: astral-sh/setup-uv@v8.1.0`); every other line in every
file byte-for-byte preserved (end-anchored substitution; verified by full `git diff`):

- `.github/workflows/python-strict.yml:19` (the `lfs: true` checkout `with:` block above it untouched)
- `.github/workflows/determinism.yml:21`
- `.github/workflows/equivalence.yml:18`
- `.github/workflows/integrity.yml:17`
- `.github/workflows/mutation-testing.yml:21`
- `.github/workflows/tolerance-budget-check.yml:27`

`ts-strict.yml` has no setup-uv → untouched. The other 3 setup-uv-free workflows untouched. No stray
unpinned `@v8` remains.

## § 5. Verification (Task 3)

(FACT — `evidence/` outputs.)

| Check | Result |
|---|---|
| pyyaml `safe_load` all 9 workflows (actionlint absent → fallback per Stage-1a precedent) | **9 / 9 VALID** |
| Bit-identity invariant replay (§ D.5) | replay-output sha256 `9399fc33…18909f34` — **HELD (28th+ invocation)** |
| Integrity sweep (`--all --mode strict`) | `0 HARD_FAIL, 14 SOFT_WARN`; sha256 `c19492ad…cb52` — **byte-identical to the MPM Stack-D close baseline** (streak HELD) |
| Cross-package regression sweep | **NOT WARRANTED** — this hotfix touches workflow-YAML only; ZERO Python/TS surface changed. No test behavior can differ; a sweep would re-confirm the just-landed sub-phase's GREEN state with no new signal. |

Local verification cannot exercise the remote runner (where `@v8.1.0` resolution is the real test); that
is the operator push (§ 7). Locally, the YAML is valid, the integrity surface is byte-stable, and the
bit-identity invariant holds.

## § 6. Banked-items roll-up

- **CLOSED:** the `astral-sh/setup-uv@v8` unresolvable-pin failure (6 workflows repaired to `@v8.1.0`).
- **STAYED-BANKED:** S-2.1 (Stack-D taichi-`SyntaxWarning` filterwarnings gap); `actionlint` not installed;
  `check-yaml` pre-commit hook coverage of `.github/workflows/`; supply-chain immutable-pin migration for
  the other 3 actions (`checkout` / `setup-node` / `pnpm` still on moving tags — a future hardening effort
  may pin them to specific versions too, matching the setup-uv posture).
- **NEW-BANKED-OBSERVATION:** **Action-version web-fetch must distinguish (a) latest released version from
  (b) usable pinning forms.** Release notes may document a deviation from the default "pin to major"
  assumption (the setup-uv v8.0.0 precedent: the maintainer discontinued moving tags as supply-chain
  hardening, so `@v8` does not resolve even though v8.x releases exist). **Future workflow-action
  plan-drafting probes (and the D3 re-fetch-at-edit-time step) must read the release notes for the
  specific major being targeted — not merely confirm the version exists.** Had the Stage-1a D3 re-fetch
  read the setup-uv v8.0.0 notes (vs only confirming "latest major = v8"), this failure would have been
  caught pre-push. (Contrast: `actions/checkout`, `actions/setup-node`, `pnpm/action-setup` still publish
  moving major tags, so their `@v6` bumps were correct.)

## § 7. Remote-CI expectation post-push

(This audit RECORDS the expectation; push is operator action.)
- The **6 previously-red** workflows (`python-strict`, `determinism`, `equivalence`, `integrity`,
  `mutation-testing`, `tolerance-budget-check`) are expected **GREEN** — `setup-uv@v8.1.0` resolves to an
  immutable release.
- The **3 previously-green** workflows (`structure`, `audit-append-only`, `ts-strict`) are expected to
  **STAY GREEN** (untouched).
- **Operator action:** push; confirm **9/9 CI green**. If any workflow still red post-push: surface
  immediately. (No agent push; spec § 7.12.)

## § 8. Verdict

**CONFIRMED.** The hotfix is a clean, single-purpose 6-line pin that resolves the documented failure
mechanism (H1); all locally-verifiable gates GREEN (pyyaml 9/9; integrity byte-identical; bit-identity
HELD 28th+); root cause web-fetch-confirmed at the canonical source. **Hotfix shift H1** (the
setup-uv `@v8`-moving-tag-discontinuation root cause, a deviation from the Stage-1a D3 "pin to latest
major" assumption) is **documented here per conventions § M.6** (hotfix shifts are sibling-documented,
**NOT cumulated** into the per-sub-phase running total) — **cumulative remains 152**. No `-phase-N` tag.

**Hotfix landing-complete LOCALLY. PUSH IS OPERATOR ACTION** (§ 7); remote-CI re-validation pending.

---

This closing audit lands at HEAD `d9b78540453f07212d2ef6fdd247a643d42e0d36` (back-filled per Convention #12 + § B.2 in a
separate `chore(ci-action-hotfix-setup-uv-v8-pin-sha-backfill)` commit; full 40-hex via
`git rev-parse HEAD` at summary-composition time). No `-phase-N` tag.

Verdict: **CONFIRMED**.
