---
date: 2026-05-24T15-29-43Z
author: ci-action-migration-and-banked-cleanup-sub-phase-agent
phase: 2
artifact: sub-phase
artifact_id: sub-phase-ci-action-migration-and-banked-cleanup
subject: "SUB-PHASE LANDING (LOCAL-ONLY; remote-CI validation pending operator push) — focused-infrastructure sub-phase: S-CI2 GitHub Actions Node-20→Node-24 migration (PRIMARY driver) + banked testing-improvements subset. Stage 1a bumped 4 node20 actions to latest node24 majors across 9 workflows (checkout v4→v6, setup-uv v6→v8, setup-node v4→v6, pnpm v4→v6; 17 uses: changes; D4 with:-blocks byte-for-byte preserved, R-CI CLEARED). Stage 1b landed pytest-timeout (§ J.3, D12-b) + LBM representative manifest-equality test (§ J.7, D11, strategy (i); ZERO sealed-source edits). Verification: Python 18-root sweep ZERO code regressions (418 passed + 3 skipped full-collection; LBM +2 only intended delta); TS 20 passed + 2 skipped (baseline-match); integrity c19492ad byte-identical (streak HELD across migration + § J amendment + test); bit-identity 9399fc33…909f34 HELD (27th+); append-only PASS; verify_evidence full 9-audit chain GREEN. Conventions § J.3 (pytest-timeout LANDED) + § J.7 (methodology-precedent #14 strategy-(i) manifest-equality REALIZED) amended additively (69aa39fc→4ac8341a). Verdict SHIFTED-with-notes (S-1b1 manifest-builder-enumeration correction + S-2.1 Stack-D cold-.pyc taichi-SyntaxWarning latent gap surfaced at landing). Cumulative 146→152. No -phase-N tag; PUSH IS OPERATOR ACTION."
verdict-state: SHIFTED
head_sha: <COMMIT_N_SHA_PENDING>
head_sha_at_checkpoint: <COMMIT_N_SHA_PENDING>
parent_audits:
  - docs/_audits/phase-2/sub-phase-mpm-multimaterial-stack-d/landing-2026-05-24T13-45-00Z.md
  - docs/_audits/phase-2/sub-phase-audit-chain-correctness/landing-2026-05-23T23-04-19Z.md
  - docs/_audits/phase-2/sub-phase-ci-action-migration-and-banked-cleanup/plan-drafting-landing-2026-05-24T14-22-36Z.md
  - docs/_audits/phase-2/sub-phase-ci-action-migration-and-banked-cleanup/stage-0-checkpoint-2026-05-24T14-48-58Z.md
  - docs/_audits/phase-2/sub-phase-ci-action-migration-and-banked-cleanup/stage-1a-checkpoint-2026-05-24T15-00-37Z.md
  - docs/_audits/phase-2/sub-phase-ci-action-migration-and-banked-cleanup/stage-1b-checkpoint-2026-05-24T15-10-34Z.md
evidence_paths:
  - docs/_audits/phase-2/sub-phase-ci-action-migration-and-banked-cleanup/stage-2-evidence/python-sweep-2026-05-24T15-29-43Z.txt
  - docs/_audits/phase-2/sub-phase-ci-action-migration-and-banked-cleanup/stage-2-evidence/integrity-sweep-2026-05-24T15-29-43Z.txt
  - docs/_audits/phase-2/sub-phase-ci-action-migration-and-banked-cleanup/stage-2-evidence/replay-2026-05-24T15-29-43Z.txt
  - docs/_audits/phase-2/sub-phase-ci-action-migration-and-banked-cleanup/stage-2-evidence/ts-sweep-2026-05-24T15-29-43Z.txt
  - docs/conventions/sub-phase-conventions.md
  - CHANGELOG.md
  - .github/workflows/python-strict.yml
evidence_hashes:
  docs/_audits/phase-2/sub-phase-ci-action-migration-and-banked-cleanup/stage-2-evidence/integrity-sweep-2026-05-24T15-29-43Z.txt: sha256:c19492add530f3a5a0d723777cf818a702b7019ee664c733695364aa6d22cb52
  docs/_audits/phase-2/sub-phase-ci-action-migration-and-banked-cleanup/stage-2-evidence/replay-2026-05-24T15-29-43Z.txt: sha256:9399fc337160dd20a3aeefdad6bc8d93edb7918ea5e8d005253d3ce718909f34
  docs/conventions/sub-phase-conventions.md: sha256:4ac8341a6cda45016c4e157823a3b5d2b2bd92d185ad367e1a7143c8ec037e0b
---

# Sub-Phase Landing Audit — CI-Action-Migration-and-Banked-Cleanup

> **LOCAL-ONLY landing.** PUSH IS OPERATOR ACTION (§ 11). The S-CI2 migration is locally verified;
> remote-CI validation (the bumped action majors' first execution on remote runners) happens at the
> operator push. This audit RECORDS the remote-CI expectation; it does not validate it.

## § 1. Scope + sub-phase framing

(FACT — charter `docs/phases/sub-phase-ci-action-migration-and-banked-cleanup.md` § 1.) A spec-Phase-2
**focused-infrastructure** sub-phase (mirroring `sub-phase-audit-chain-correctness`) whose PRIMARY,
time-pressured driver is **S-CI2** — the GitHub Actions Node-20 runtime deprecation (Node-24 default
2026-06-16; Node-20 removal "later in fall 2026"). Every workflow action pinned to a Node-20 major was
bumped to its latest Node-24 major, preserving four load-bearing `with:` blocks byte-for-byte. Bundled
the banked testing-improvements subset (pytest-timeout + a representative manifest-equality test) per D6.
NOT a per-sim port; NOT cross-stack. No `-phase-N` tag (D9).

## § 2. Operator routing consumed (D1–D12, all resolved)

| D | Routing | Disposition at close |
|---|---|---|
| D1 | name `sub-phase-ci-action-migration-and-banked-cleanup` | RATIFIED |
| D2 | three-stage (0 / 1a / 1b / 2) | executed |
| D3 | targets = latest-node24 majors re-fetched at edit time | checkout v6 / setup-uv v8 / setup-node v6 / pnpm v6 (Stage 1a fresh fetch) |
| D4 | preservation set (no optionality) | 4/4 byte-for-byte preserved (R-CI CLEARED, § 4) |
| D5 | opt-out env var NO | honored (migration is the fix) |
| D6 | testing-improvements = pytest-timeout + manifest-equality; LBM cosmetic co-located-only | pytest-timeout (D12-b) + LBM manifest test (D11); LBM cosmetic STAYS BANKED |
| D7 | mid-Phase-1 capture regen STAY-BANKED | unchanged |
| D8 | surprise items STAY-BANKED | unchanged |
| D9 | no point-release tag | no tag pushed |
| D10 | manifest-equality additive strategy (i); strategy (ii) banked | strategy (i) realized; (ii) banked |
| D11 | manifest-equality scope (agent proposes) | representative-single-sim = `lattice-boltzmann-d3q19` |
| D12 | pytest-timeout integration shape | shape (b) — workspace dev dep + global ini at testkit |

## § 3. Stage-by-stage summary

(FACT — the per-stage checkpoints + back-fill ledgers.)

| Stage | Verdict | Outcome |
|---|---|---|
| Plan-drafting | SHIFTED-with-notes (S1–S4) | probe + charter; believed-state RATIFIED-with-four-shifts (date 2026-06-16 not 06-02; soft default-switch; +2 under-enumerated node20 actions; testing-improvements provenance) |
| Stage 0 | CONFIRMED (0 shifts) | replay GREEN (bit-identity 24th invocation); D4 set verified verbatim; tolerance NO-OP; testing-improvements scope bounded |
| Stage 1a | CONFIRMED (0 shifts) | S-CI2 migration: 17 `uses:` bumps across 9 workflows; D4 preservation byte-for-byte (R-CI CLEARED); 9/9 YAML-valid; bit-identity 25th |
| Stage 1b | SHIFTED-with-notes (S-1b1) | pytest-timeout (§ J.3, D12-b) + LBM manifest-equality test (§ J.7, D11, strategy (i)); ZERO sealed-source edits; bit-identity 26th |
| Stage 2 (this) | SHIFTED-with-notes (S-2.1) | full verification (§ 4); § J amendments; CHANGELOG; banked roll-up; bit-identity 27th |

## § 4. Verification (Tasks 2.1–2.6)

(FACT — `stage-2-evidence/` outputs.)

| Check | Result |
|---|---|
| **2.1 Python 18-root sweep** | **ZERO code regressions.** 18 roots GREEN (full-collection mode): 14 Phase-1+tools+common-py + 4 Stack-D ports. Phase-1 sims 117 (LBM 10→**12**, the +2 manifest tests — only intended delta); Stack-D ports rd-2d 16 / sph-water 15 / lbm 16 / mpm 15; tools integrity 56 / diagnostics **93** (full all-tiers) / testkit **68** (incl. numba+taichi harnesses); common-py 22 + 3 skipped. **Total 418 passed + 3 skipped.** See S-2.1 (§ 12) for the Stack-D cold-`.pyc` finding — the 4 ports errored on a COLD taichi `.pyc` (latent pre-existing gap; warmed → all pass; NOT a code regression). |
| **2.2 TS sweep** | `common/common-ts` vitest **20 passed + 2 skipped** — baseline-match (MPM Stack-D close). Zero TS regressions. |
| **2.3 Integrity sweep** | `--all --mode strict`: **0 HARD_FAIL, 14 SOFT_WARN**; sweep-output sha256 `c19492ad…cb52` — **BYTE-IDENTICAL to the MPM Stack-D close baseline.** The streak HELD across the Stage-1a S-CI2 migration + the Stage-1b testkit dep + new test + the Stage-2 § J amendment. |
| **2.4 Bit-identity invariant** | replay vs `v0.1.0-phase-1` GREEN (8/8 gates); replay-output sha256 `9399fc33…18909f34` — **HELD (27th+ invocation).** |
| **2.5 Append-only check** | **PASS.** `git diff d6e0671..HEAD` over `docs/_audits/phase-0` + `docs/_audits/phase-1` is EMPTY; no prior phase-2 sub-phase audit modified. Only additive non-`_audits` edits (9 workflows, `tools/testkit/pyproject.toml`, `uv.lock`, the § J conventions amendment, CHANGELOG). CI-level `.github/workflows/audit-append-only.yml` is the mechanical enforcement (now on `actions/checkout@v6` + `fetch-depth: 0` preserved). |
| **2.6 verify_evidence (full 9-audit chain)** | **GREEN.** All 9 sub-phase audits pass `--strict` at their respective head_shas (probe 16/0, plan-landing 11/0, plan-back-fill 3/0, stage-0-checkpoint 8/0, stage-0-back-fill 3/0, stage-1a-checkpoint 11/0, stage-1a-back-fill 3/0, stage-1b-checkpoint 8/0, stage-1b-back-fill 3/0). |

**Reporting-mode note (conventions § B.7 / § 6.1 N1):** the 18-root counts above use **full-collection**
mode (diagnostics + testkit collected across all test dirs, not just their `[tool.pytest.ini_options]`
`testpaths` subset). A bare `pytest` from those package dirs collects fewer (diagnostics 22, testkit 58);
the full-collection figures (93 / 68) match the MPM Stack-D baseline's collection mode and confirm no
hidden failures in the un-`testpath`'d suites.

## § 5. Closing-commit anchor re-check (Task 2.7; § 7.9)

(FACT — re-grep at landing-time HEAD `80be15f`.) All file:line citations across the chain resolve:
- Workflow `uses:` citations resolve at unchanged line numbers (the bumps were in-place single-token
  edits): `.github/workflows/python-strict.yml:14` (`actions/checkout@v6`) + `:19` (`setup-uv@v8`);
  `.github/workflows/audit-append-only.yml:24` (`checkout@v6`); `.github/workflows/ts-strict.yml:15` /
  `:18` (`pnpm/action-setup@v6`) / `:23` (`setup-node@v6`).
- D4 preservation blocks present post-bump: `.github/workflows/python-strict.yml:16` (`lfs: true`),
  `.github/workflows/audit-append-only.yml:26` (`fetch-depth: 0`), `.github/workflows/ts-strict.yml:20`
  (pnpm `version: 10`) + `:25` (`node-version: 22`).
- LBM `packages/lattice-boltzmann-d3q19/lattice_boltzmann_d3q19/sim.py:472`
  (`sim_runner_diagnostic`) resolves.
- Conventions doc anchor moved `69aa39fc…4602bf45` → `4ac8341a…037e0b` (§ J amendment, this stage);
  prior audits' `69aa39fc` citations are sealed historical records (correct at their head_sha).

**No anchor drift.**

## § 6. CHANGELOG entry

(FACT — `CHANGELOG.md`, additive under `## [Unreleased]` after the `sub-phase-mpm-multimaterial-stack-d`
entry.) New `### sub-phase-ci-action-migration-and-banked-cleanup` entry: framing paragraph + Added/Changed
(workflow bumps; pytest-timeout; LBM manifest test) + Verification table + Banked methodology-precedent #14.

## § 7. Conventions doc amendments

(FACT — `docs/conventions/sub-phase-conventions.md`, additive; sha256 `69aa39fc…4602bf45` →
`4ac8341a…037e0b`.) Two additive § J amendments (existing prose unchanged):
- **§ J.3 — pytest-timeout LANDED.** Records that `pytest-timeout>=2.0` + a default per-test ceiling now
  live at `tools/testkit/pyproject.toml` (D12-b); MPM `mls_mpm.py` mutation completion remains banked.
- **§ J.7 — manifest-equality REALIZED (methodology-precedent #14).** Records the strategy-(i) additive
  realization (the literal `build_manifest()` call site does not exist; invoke `sim_runner_*` + assert
  the emitted `.json` minus volatile fields). Integrity sweep confirmed byte-identical post-amendment
  (§ 4; the additive-conventions-amendment-doesn't-perturb-integrity property, per audit-chain-correctness).

## § 8. Banked-items roll-up (Task 2.8)

**CLOSED at this sub-phase:**
- **S-CI2 GitHub Actions Node-20 deprecation** (PRIMARY DRIVER) — migration landed (Stage 1a); R-CI
  CLEARED. Remote-CI validation post-push is operator-owned (§ 11).
- **pytest-timeout requirement** (§ J.3) — landed (Stage 1b, D12-b); § J.3 amended.
- **`sim.py` manifest-equality test** (§ J.7) — landed (Stage 1b, D11 representative-single-sim, strategy (i)); § J.7 amended (#14).

**STAYED-BANKED at close:**
- **LBM `sim_runner_diagnostic` cosmetic descriptor** (D6) — Stage-0 Task 0.6 found no natural
  co-location; Stage 1b strategy (i) is additive (the manifest test LOCKS the current hardcoded
  descriptor without editing `sim.py`). Folds into a future sim-touching effort.
- **Mid-Phase-1 capture regeneration** (D7) — content-equivalent contract → pre-contract captures pass; not needed.
- **Surprise probe § 4 items** (D8) + IC-15 full formalization / cross-stack methodology / atomic-scatter
  / Cat-3 sibling subdirs — out of scope (next cross-stack pair / per-sim).
- **MPM `mls_mpm.py` mutation completion** — the pytest-timeout mechanism now exists to support it; the
  actual run is a mutation-focused effort's deliverable.

**NEW banked observations from this sub-phase (surfaced; NOT folded in):**
- **`actionlint` not installed** (Stage 1a) — the repo's strict-mode CI policy (architecture § G.9) names
  it as the workflow-YAML linter; used a pyyaml fallback. Orthogonal tooling-improvement candidate.
- **`check-yaml` pre-commit hook skips `.github/workflows/`** (Stage 1a) — orthogonal hook-config candidate.
- **S-2.1 — Stack-D ports' taichi-`SyntaxWarning` filterwarnings gap** (Stage 2; § 12) — the 4 Stack-D
  ports filter taichi's `DeprecationWarning` but not its `SyntaxWarning '\['`; masked by a warm taichi
  `.pyc`, exposed by a cold cache. **Recommended follow-up:** add `ignore::SyntaxWarning:taichi.*` (or
  equivalent) to the 4 Stack-D ports' `[tool.pytest.ini_options].filterwarnings`. CI-invisible (no
  workflow runs per-port pytest). Out of scope here (Stack-D packages untouched by this sub-phase).
- **Stage-0 Task 0.5(b) enumeration imprecision** (S-1b1) — closed-as-corrected at Stage 1b; no forward bank.

## § 9. Banked methodology-precedents (Task 2.9)

**NEW — methodology-precedent #14: strategy-(i) manifest-equality pattern** (formalized at conventions
§ J.7, this stage). "The literal `<sim>.sim.build_manifest()` call site does not exist at HEAD; invoke the
existing `sim_runner_*` (or its diagnostic-tier variant), load the emitted `.json` manifest sidecar,
shape-check-then-exclude the volatile `run.wall_clock_seconds` + `payload.checksum` fields (per spec § 2.5
+ § F.3), and assert the remainder equals expected literals (numeric params from module constants); a
run-to-run assertion locks the deterministic subset. Catches the § J.7 manifest-field-literal mutation
class WITHOUT a sealed-source refactor. Landed as a representative-single-sim test." Reusable for any
future per-sim manifest-equality fan-out; strategy (ii) (public builder refactor) stays banked.

**Consumed this sub-phase:** focused-infra Stage-1a/1b template (audit-chain-correctness);
commit-first-then-sha256 (#1); SHA-back-fill N1 enumeration (#2); pre-emptive ruff (#9); representative-subset
artifact class (MPM Stack-D D10); S-CI1 `lfs: true` discipline (the R-CI preservation rationale).
**Produced:** #14 (above) + the full-`.github/workflows/<file>:<line>` audit-citation discipline (plan-drafting).

## § 10. Stage-by-stage commit chain (13 commits + this landing + back-fill)

| Stage | Commits |
|---|---|
| Plan-drafting | `c3fa95c` (probe) / `b7741a1` (charter) / `a0afe82` (landing) / `f202a57` (sha-back-fill) |
| Stage 0 | `5b80a59` (checkpoint) / `4ca88cb` (sha-back-fill) |
| Stage 1a | `8508ed9` (migration feat) / `e67c3cd` (checkpoint) / `aa6b311` (sha-back-fill) |
| Stage 1b | `7ce5d76` (manifest test) / `b580ed0` (pytest-timeout) / `cc1071a` (checkpoint) / `80be15f` (sha-back-fill) |
| Stage 2 | this landing + CHANGELOG + § J amendment (COMMIT N) / SHA back-fill (COMMIT N+1) |

## § 11. Remote-CI expectation + operator action (Task 2.11)

(This audit RECORDS the expectation; it does NOT validate it — push is operator action.)

- **Expected post-push behavior:** all 9 workflows execute against the new majors (`checkout@v6`,
  `setup-uv@v8`, `setup-node@v6`, `pnpm/action-setup@v6`); the four preserved `with:` blocks (D4) operate
  identically (`python-strict` `lfs: true` smudges legacy-captures HDF5; `audit-append-only` `fetch-depth: 0`
  reads the prior tag; `ts-strict` setup-node/pnpm inputs); `pytest-timeout` loads on the testkit-consuming
  jobs. CI green across all 9 expected.
- **Watched-for failure modes:** (a) action-major incompatibility — most plausibly `setup-uv@v8` requiring a
  runner Node feature surfacing for the first time on remote `ubuntu-latest`; (b) `checkout@v6` LFS
  interaction with `lfs: true` on `python-strict` (the load-bearing S-CI1 preservation); (c) `node24`
  default-runtime behavior differences. Locally, YAML validity (pyyaml) + the full git-diff review + the
  bit-identity replay are GREEN; the remote runner is the untested surface.
- **Operator action:** push the sub-phase commit chain to `main`; confirm CI green across all 9 workflows.
  **If CI red: surface immediately; a follow-up hotfix sub-phase dispatches.** (No agent push; spec § 7.12.)

## § 12. Shift reconciliation (Task 2.12)

| Stage | Shifts | Running total |
|---|---|---|
| Entering (MPM Stack-D close) | — | 146 |
| Plan-drafting | +4 (S1 date / S2 failure-mode / S3 under-enumeration / S4 provenance) | 150 |
| Stage 0 | +0 | 150 |
| Stage 1a | +0 | 150 |
| Stage 1b | +1 (S-1b1 manifest-builder-enumeration correction) | 151 |
| **Stage 2** | **+1 (S-2.1)** | **152** |

- **S-2.1 (Stage 2)** — **Stack-D ports' taichi-`SyntaxWarning` filterwarnings gap surfaced at the full
  18-root sweep.** The 4 Stack-D ports (`reaction-diffusion-2d-stack-d`, `sph-water-stack-d`,
  `lattice-boltzmann-d3q19-stack-d`, `mpm-multimaterial-stack-d`) ERRORED on collection under their own
  `filterwarnings = ["error"]` because taichi 1.7.4's `tools/image.py` emits a `SyntaxWarning: invalid
  escape sequence '\['` on recompile, which their filter (covering taichi's `DeprecationWarning` only)
  does not catch. **Root cause: a latent pre-existing gap**, masked at the MPM Stack-D close by a warm
  taichi `.pyc` cache and exposed here when the Stage-1b `uv sync` (for pytest-timeout) invalidated the
  cache. **PROVEN not a code regression:** `git diff d6e0671..HEAD` touched ZERO Stack-D files, and
  warming the `.pyc` (a bare `import taichi`) → all 4 ports pass (rd-2d-stack-d 16 / sph-water 15 / lbm 16
  / mpm 15). **CI-invisible** (no workflow runs per-port pytest). Surfaced as a banked observation (§ 8)
  with a recommended follow-up; NOT fixed here (Stack-D packages are out of scope). Counted as a Stage-2
  shift (a surfaced latent-defect finding).

**Cumulative shift count at sub-phase close: 152.**

## § 13. Next-sub-phase recommendation

Operator-routable. This focused-infrastructure sub-phase clears the S-CI2 time-pressured driver and the
banked testing-improvements subset. The spec-Phase-2 **cross-stack port sequence resumes** (per spec
§ 11.3 + `docs/phases/phase-2-cross-stack-replication.md`): remaining Stack-D/Stack-E ports (smoke →
Stack-D + Stack-E item 2.4; MPM Stack-E Warp item 2.3; the Stack-C/Stack-E variants). A fifth per-sim
cross-stack pair with a non-trivial velocity-gradient / stress-bearing / chaotic / iterative trajectory
would substantively stress-test the deferred IC-15 aspects (#1/#3/#5; MPM Stack-D § 14). **Recommended
quick follow-up:** a tiny hotfix adding `ignore::SyntaxWarning:taichi.*` to the 4 Stack-D ports'
filterwarnings (S-2.1) — closes the latent cold-`.pyc` sweep-fragility before the next port lands.

## § 14. Verdict

**SHIFTED-with-notes** (carries S-1b1 + S-2.1; both substantive deliverables CONFIRMED-quality; ZERO code
regressions; integrity byte-identical; bit-identity HELD; append-only PASS; verify_evidence full chain
GREEN; not BLOCKED; Hard Rule 2 not triggered — S-2.1 is a pre-existing latent gap, proven not a code
regression, surfaced + banked rather than silently adapted).

**Sub-phase landing-complete LOCALLY. PUSH IS OPERATOR ACTION** (§ 11); remote-CI validation pending.

---

This landing lands at HEAD `<COMMIT_N_SHA_PENDING>` (back-filled per Convention #12 + § B.2 + N1
enumeration in a separate `chore(ci-action-migration-and-banked-cleanup-landing-sha-backfill)` commit;
full 40-hex via `git rev-parse HEAD` at summary-composition time). No `-phase-N` tag.

Verdict: **SHIFTED-with-notes**.
