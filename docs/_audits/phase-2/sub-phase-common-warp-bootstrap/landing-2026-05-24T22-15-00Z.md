---
date: 2026-05-24T22-15-00Z
author: common-warp-bootstrap-stage-2-agent
phase: 2
artifact: landing
artifact_id: sub-phase-common-warp-bootstrap-landing
stage: landing
subject: "Sub-phase-common-warp-bootstrap LANDING (Stage 2) CLOSE. VERDICT CONFIRMED. Focused-infrastructure sub-phase: common/common-warp/ is the 20th workspace member + the Stack-E (Python / NVIDIA Warp 1.13.0) §1.9.1 seven-subsystem surface. All six W-Gates GREEN. Stage 2 lands 3 methodology-precedents (S1a-2 GPU device-string discipline; S1b-3 socket-reconciliation Option B; S1c-1 plan-prose-gloss vs spec-verbatim) in conventions § L.5; CHANGELOG additive entry. Verification: 20-root regression sweep ZERO regressions (474 passed + 1 skipped); TS sweep 20 passed + 2 skipped; integrity c19492ad…d22cb52 baseline-MATCH (9th sub-phase); bit-identity replay 9399fc33…718909f34 HELD (40th); append-only PASS; verify_evidence --strict full chain (12 audits) PASS. 1 Stage-2 shift (N1 -W-error-vs-package-config reconciliation). Cumulative 165 -> 176. No -phase-N tag (D10). Local-only per D13 (remote-CI deferred). Next sub-phase: MPM Stack-E (D9), routed separately."
verdict-state: CONFIRMED
head_sha: <COMMIT_2_SHA_PENDING>
head_sha_at_checkpoint: 88ccb8c1e2d8b4e84386e79d832fcaea7fcc6a4f
parent_audits:
  - docs/_audits/phase-2/sub-phase-common-warp-bootstrap/stage-1c-checkpoint-2026-05-24T21-45-00Z.md
  - docs/_audits/phase-2/sub-phase-common-warp-bootstrap/stage-1c-sha-back-fill-2026-05-24T21-45-00Z.md
  - docs/_audits/phase-2/sub-phase-common-warp-bootstrap/stage-1b-checkpoint-2026-05-24T21-01-29Z.md
  - docs/_audits/phase-2/sub-phase-common-warp-bootstrap/stage-1a-checkpoint-2026-05-24T20-17-42Z.md
  - docs/_audits/phase-2/sub-phase-common-warp-bootstrap/stage-0-checkpoint-2026-05-24T20-03-28Z.md
  - docs/_audits/phase-2/sub-phase-common-warp-bootstrap/plan-drafting-landing-2026-05-24T18-47-00Z.md
  - docs/_audits/phase-1/landing-2026-05-20T14-18-00Z.md
evidence_paths:
  - docs/_audits/phase-2/sub-phase-common-warp-bootstrap/stage-1c-checkpoint-2026-05-24T21-45-00Z.md
  - docs/conventions/sub-phase-conventions.md
  - CHANGELOG.md
  - common/common-warp/src/common_warp/__init__.py
  - common/common-warp/examples/hello/sim.py
  - docs/common/warp.md
  - tools/testkit/equivalence/harness.py
evidence_hashes:
  docs/_audits/phase-2/sub-phase-common-warp-bootstrap/stage-1c-replay-2026-05-24T21-45-00Z.txt: sha256:9399fc337160dd20a3aeefdad6bc8d93edb7918ea5e8d005253d3ce718909f34
  docs/_audits/phase-2/sub-phase-common-warp-bootstrap/stage-1c-integrity-sweep-2026-05-24T21-45-00Z.txt: sha256:c19492add530f3a5a0d723777cf818a702b7019ee664c733695364aa6d22cb52
---

# Common-Warp Bootstrap — Landing (Stage 2) Audit

> Sub-phase close. `common/common-warp/` — the first Stack-E (Python / NVIDIA
> Warp) workspace surface — landed as a complete, tested 20th workspace member
> across plan-drafting + Stages 0/1a/1b/1c; Stage 2 is the landing: full
> verification sweep, methodology-precedent formalization, CHANGELOG, banked
> roll-up, clean close. Local-only per D13 (remote-CI deferred behind the
> LFS-architecture sub-phase). No `-phase-N` tag (D10).

## § 1. Scope + framing

(FACT — Stage-2 dispatch SECTION 2 Tasks 2.0–2.13; charter § 2.) A
**focused-infrastructure sub-phase** — NOT a per-sim cross-stack port. It
establishes `common/common-warp/` as the 20th workspace member and the Stack-E
(Python / NVIDIA Warp 1.13.0) workspace surface: the phase-2 plan §1.9.1
seven-subsystem minimal API. The sub-phase enables the 3 forthcoming Stack-E
port sub-phases (MPM, Smoke, LBM). Stage 2 is **additive-only** (Convention A):
a new conventions § L.5 subsection, a CHANGELOG entry, this landing audit, and
the SHA back-fill ledger. No `common/common-warp/` surface, workflow YAML, sim
source, `tolerance.toml`, or testkit touched (§ 14 boundary).

## § 2. Routing consumed

D1–D14 ratified at plan-drafting, all honored through landing. Stage-2-relevant:

- **S0-W1** (1a/1b/1c allocation); **S1a-1** (dispatch-premise-vs-HEAD
  reconciliation); **S1a-2** (GPU device-string discipline — formalized this
  stage); **S1b-1/S1b-2** (device-default + Warp-API quirks); **S1b-3** (§1.9.1
  socket-vs-landed divergence — Option-B refactor landed at Stage 1c;
  formalized this stage); **S1c-1** (plan-prose-gloss vs §1.9.1-verbatim —
  formalized this stage).
- **D9** (next sub-phase MPM Stack-E); **D10** (NO TAG); **D13** (LFS-architecture
  banked; local-only landing).
- **3 methodology-precedents formalized** in conventions § L.5 (Task 2.8;
  S1a-2 / S1b-3 / S1c-1).

## § 3. Stage-by-stage summary

| Stage | Deliverable | Verdict | Shifts |
|---|---|---|---|
| **plan-drafting** | charter + probe + §1.9.1 socket consumption framing | CONFIRMED | 3 (S-W1/2/3) |
| **Stage 0** | empirical pre-flight (Warp 1.13.0 install; CPU bit-determinism `24d44c7e…`; Subsystem-7 design-time bounded-decaying check; W-5 contract) | CONFIRMED | 1 (S0-W1) |
| **Stage 1a** | 20th workspace member + Runtime + Determinism + W-2 mechanism | CONFIRMED | 2 (S1a-1/S1a-2) |
| **Stage 1b** | Capture (W-1) + Particles + Grids + HashGrid + W-5 mechanism | CONFIRMED | 3 (S1b-1/2/3) |
| **Stage 1c** | §1.9.1 socket refactor + Subsystem-7 smoke sim + `warp.md`; W-2/3/4/5/6 complete | CONFIRMED | 1 (S1c-1) |
| **Stage 2** | landing: sweep + § L.5 precedents + CHANGELOG + roll-up | CONFIRMED | 1 (N1) |

## § 4. W-Gate completion summary

| Gate | Mechanism stage | Full-gate stage | Status |
|---|---|---|---|
| **W-1** Capture I/O | 1b | 1c (Subsystem-7 capture written + reloaded) | **GREEN** |
| **W-2** Determinism | 1a (`warp_harness` reproduces `24d44c7e…0746f314`) | 1c (`assert_deterministic_run` + testkit `run_twice_and_diff` on the smoke sim) | **GREEN** |
| **W-3** Smoke sim | n/a | 1c (`examples/hello/`; exercises the public surface) | **GREEN** |
| **W-4** Public API docs | n/a | 1c (`docs/common/warp.md`) | **GREEN** |
| **W-5** Equivalence compat | 1b (synthetic pair) | 1c (`compare_captures` run-twice-and-diff; `within_tolerance=True`, no HARD_FAIL) | **GREEN** |
| **W-6** Integrity | n/a | 1c + this landing (Cat-1/2/4 + `c19492ad…d22cb52` baseline-MATCH) | **GREEN** |

All six GREEN; the §1.9.1 seven-subsystem surface is complete with
socket-verbatim signatures.

## § 5. Verification (Tasks 2.1–2.6)

(FACT — landing-time HEAD `88ccb8c…`; evidence `stage-1c-*` `.txt` invariants.)

- **Task 2.1 — 20-root regression sweep** (cold `.pyc`; each package under its
  OWN pytest config — see § 12 N1): **ZERO REGRESSIONS**, 474 passed + 1 skipped:

  | Root | Result | Root | Result |
  |---|---|---|---|
  | tools/testkit | 68 | packages/eulerian-smoke | 10 |
  | tools/integrity | 56 | packages/lattice-boltzmann-d3q19 | 12 |
  | tools/diagnostics | 93 | packages/mpm-multimaterial | 10 |
  | reaction-diffusion-2d | 14 | reaction-diffusion-2d-stack-d | 16 |
  | strange-attractors | 11 | sph-water-stack-d | 15 |
  | mandelbulb-explorer | 10 | lattice-boltzmann-d3q19-stack-d | 16 |
  | boids-3d | 10 | mpm-multimaterial-stack-d | 15 |
  | physarum | 10 | eulerian-smoke-stack-d | 15 + 1 skip |
  | reaction-diffusion-3d | 8 | common-py | 25 |
  | sph-water | 22 | **common-warp** | **38** |

- **Task 2.2 — TS sweep** (`common-ts` vitest): **20 passed + 2 skipped** (8 test
  files) — baseline maintained.
- **Task 2.3 — Integrity sweep**: `0 HARD_FAIL, 14 SOFT_WARN`,
  **`c19492ad…d22cb52` baseline-MATCH** (byte-identical; streak HELD into the 9th
  sub-phase landing position). W-6 GREEN.
- **Task 2.4 — Bit-identity invariant**: replay → 8/8 gates PASS, `ok=True`;
  output **`9399fc33…718909f34` HELD (40th invocation)**. Stage 1c did not touch
  sealed Phase-1 tagged content; HOLD is the only acceptable result, achieved.
- **Task 2.5 — Append-only**: no `docs/_audits/phase-0/` or `docs/_audits/phase-1/`
  audit edited during this sub-phase (latest commits touching those dirs are all
  `conventions-refactor-post-phase-1`, pre-dating this sub-phase). **PASS.**
- **Task 2.6 — `verify_evidence --strict`**: all **12 sub-phase `.md` audits PASS**
  (plan-drafting 3 + Stage 0 3 + Stage 1a 2 + Stage 1b 2 + Stage 1c 2; the 8 `.txt`
  evidence files carry no front-matter and are content-sha256-verified). Full
  chain GREEN.

## § 6. Closing-commit anchor re-check (Task 2.7 / Convention 7.9)

(FACT — re-grep across the sub-phase audit chain + `docs/common/warp.md` at
landing HEAD.) **No `file:line` path citations** appear in the chain (audits cite
files in prose / without line numbers), so there are **zero stale anchors**. Six
`device="cuda:0"` tokens appear in the two **plan-drafting** audits (committed
before S1a-2 was discovered), but all are inside **inline backtick spans**, which
`cat1.intra-repo` skips (§ M.1 shift #10) — they do NOT trigger a HARD_FAIL
(confirmed by the `c19492ad…d22cb52` baseline-MATCH) and are append-only sealed
(no edit). The S1a-2 discipline (formalized this stage, § L.5) governs future
un-backticked prose; `docs/common/warp.md` + all Stage-1a/1b/1c source name GPU
devices in prose form. **Anchor re-check: CLEAN.**

## § 7. Methodology amendments (Task 2.8)

Three precedents formalized additively in `docs/conventions/sub-phase-conventions.md`
**§ L.5** (new subsection; § L.4 is the eulerian-smoke-stack-d chaotic-regime
locus, so per-sub-phase attribution is preserved — see § 12 locus note):

1. **S1a-2 — GPU device-string discipline.** The bare `cuda:N` token (N a digit)
   in un-backticked prose parses as a `path:line` citation under Cat-1 and
   HARD_FAILs the sweep; name GPU devices in prose form. Cite: Stage 1a § 10.
   Applies to all future Stack-E ports + GPU-naming text.
2. **S1b-3 — socket-reconciliation Option B.** Refactor the implementation to the
   plan socket verbatim before the first downstream consumer; require
   load-bearing-baseline reproduction. Option A (amend the plan) is the
   bounded-risk/wrong-intent fallback. Cite: Stage 1b § 10 + Stage 1c § 4.
3. **S1c-1 — plan-prose-gloss vs spec-verbatim.** Dispatches reference plan
   sections by number for verbatim consumption; paraphrase in coordinator framing
   only. Convention C/M is the execution-time backstop. Cite: Stage 1c § 4.

## § 8. Banked-items roll-up (Task 2.9)

**STAYED-BANKED:**
- LBM `sim_runner_diagnostic` cosmetic.
- actionlint installation; check-yaml hook `.github/workflows/` coverage;
  supply-chain-pin migration for the other 3 actions.
- LFS-architecture sub-phase (D13 ongoing).
- manifest-equality smoke test (D7 deferred).
- Phase-1-canonical re-characterization (banked at smoke landing; not addressed
  this sub-phase).
- **mypy --strict warp partial-stub errors** (banked at this sub-phase Stage 1c;
  pre-existing on the warp partial-stub surface, not refactor-introduced; not a
  landing gate at HEAD; future tooling-improvement alongside actionlint +
  check-yaml coverage).

**CLOSED-IN-THIS-SUB-PHASE:**
- common-warp bootstrap (sub-phase primary deliverable; all 6 W-Gates GREEN).
- S1b-3 socket reconciliation (Option B refactor landed; §1.9.1 verbatim
  signatures shipped; W-2 baseline reproduced under the refactored signature).
- Subsystem-7 design-time prediction verified empirically (1.0 → 0.218683 over
  400 steps, zero increases, mass conserved).

**NEW BANKED OBSERVATIONS:**
- The next 3 Stack-E ports (MPM, Smoke, LBM) inherit common-warp's full surface +
  the chaotic-regime methodology from the smoke-Stack-D landing; each Stack-E port
  plan-drafting probe includes the S6-trajectory-simulation discipline.

**NEW BANKED METHODOLOGY-PRECEDENTS (Task 2.8; § 7):**
- S1a-2 GPU device-string discipline; S1b-3 socket-reconciliation Option B;
  S1c-1 plan-prose-gloss vs spec-verbatim.

## § 9. Banked methodology-precedents (enumeration)

Formalized in conventions § L.5 this stage (statement + concrete-instance
citation + downstream applicability per the § L.4 pattern). See § 7 for the
three-line summary; the conventions doc § L.5 is the authoritative text.

## § 10. Stage-by-stage commit chain (23 commits)

| Stage | Commits | Count |
|---|---|---|
| plan-drafting | probe; charter/landing; (drafting impl); SHA back-fill | 4 |
| Stage 0 | checkpoint + evidence; SHA back-fill | 2 |
| Stage 1a | scaffold `908e194`; impl `327955e`; checkpoint `5d5aefa`; SHA back-fill | 4 |
| Stage 1b | Capture `a8d25d0`; data-structures `fae3350`; checkpoint `59368bd`; SHA back-fill `d5423af` | 4 |
| Stage 1c | refactor `e380385`; smoke sim `921c45b`; W-5 `12ae691`; docs `3f3f650`; checkpoint `03e75f7`; SHA back-fill `659519f` | 6 |
| **Stage 2** | conventions § L.5 `88ccb8c`; this landing + CHANGELOG `<COMMIT_2>`; SHA back-fill `<COMMIT_3>` | 3 |
| **Total** | | **23** |

## § 11. Remote-CI expectation (Task 2.12; deferred per D13)

- Remote CI re-validation remains **DEFERRED** behind the banked LFS-architecture
  sub-phase. **Local verification is load-bearing** for this landing (§ 5).
- **No expected CI changes** from this sub-phase: workflow YAML untouched; the new
  `common/common-warp/` package follows the existing CI workflows already in place
  for workspace members (`.github/workflows/python-strict.yml`'s workspace-walk).
- When the LFS-architecture sub-phase lands and CI returns to a green posture, the
  workspace-wide CI exercises common-warp's surface automatically — no
  sub-phase-specific CI wiring is owed.

## § 12. Shift reconciliation (Task 2.11)

| Stage | Shifts | IDs |
|---|---|---|
| (entering sub-phase) | — | cumulative **165** (carry-forward from eulerian-smoke-stack-d landing) |
| plan-drafting | 3 | S-W1, S-W2, S-W3 |
| Stage 0 | 1 | S0-W1 |
| Stage 1a | 2 | S1a-1, S1a-2 |
| Stage 1b | 3 | S1b-1, S1b-2, S1b-3 |
| Stage 1c | 1 | S1c-1 |
| **Stage 2** | **1** | **N1** |
| **Closing total** | | **176** |

**Stage-2 shift N1 — `-W error`-vs-package-config reconciliation.** The dispatch
Task 2.1 instructed "`pytest -W error` per package". For the 5 Taichi-using
Stack-D ports this blanket CLI flag OVERRIDES each port's own
`filterwarnings = ["ignore::DeprecationWarning:taichi.*", …]` (the documented
upstream Taichi locale + cold-`.pyc` SyntaxWarning per `docs/common/taichi.md`
§ 4.5), erroring at Taichi import-collection time — NOT a regression. Reconciled
(Convention M) to the established cross-package discipline (Stages 1a/1b/1c): each
package is certified under **its own pytest config**. common-warp (own config
`filterwarnings=["error"]`), the Stack-B Phase-1 sims, and the tools pass cleanly
under `-W error`; the Taichi ports pass under their own config (16/15/16/15/15+1).
Zero true regressions either way. (The § M conventions "65" shift inventory is a
stale snapshot frozen at sph-water Stage 2 under a different accounting base; the
live carry-forward count is 165 → 176, matching the eulerian-smoke landing's
"159 → 165". Reconciling § M is out of this landing's scope.)

## § 13. Next-sub-phase recommendation (Task 2.13)

Per **D9** ratified routing: **MPM → Stack-E** is the next per-sim cross-stack
port. No dispatch drafted at Stage 2; operator routes MPM-Stack-E plan-drafting
separately. **Inheritance contract:**
- common-warp surface available — Subsystems 1–6 + the §1.9.1 socket signatures
  (`init(device, deterministic)`; no-arg `deterministic_context()`;
  `assert_deterministic_run(sim_fn, *, runs=2, tolerance=0.0)`); MPM consumes the
  most of the surface (`Particles`, `HashGrid`, `ScalarField3D`).
- W-Gate pattern established (the per-port equivalent runs at gate-14 cross-stack
  equivalence).
- Methodology precedents inherited — chaotic-regime escape-hatch awareness;
  S6-trajectory-simulation discipline at plan-drafting; the 3 new § L.5
  precedents (S1a-2 / S1b-3 / S1c-1); the `tolerance=0.0` vs `>0` determinism
  contract per the D4 CPU/GPU posture.
- O-W7 Warp quirks documented — `int(0)` idiom for kernel-local mutable ints;
  explicit `dtype=` to `wp.from_numpy` for multi-dim scalar arrays; banked
  precedent #7 pure-literal `wp.float32()`/`wp.float64()` seeding in `@wp.kernel`.

## § 14. Verdict

**Sub-phase-common-warp-bootstrap CONFIRMED (landed).** `common/common-warp/` is
a complete, tested 20th workspace member — the Stack-E (Python / NVIDIA Warp
1.13.0) §1.9.1 seven-subsystem surface — with all six W-Gates GREEN. Stage 2
formalized 3 methodology-precedents (§ L.5), added the CHANGELOG entry, and
verified the close: 20-root regression sweep ZERO regressions; TS 20+2; integrity
`c19492ad…d22cb52` baseline-MATCH (9th sub-phase); bit-identity
`9399fc33…718909f34` HELD (40th); append-only PASS; `verify_evidence` full chain
PASS. 1 Stage-2 shift (N1); cumulative **165 → 176**. **No `-phase-N` tag** (D10);
**local-only** landing (D13; remote-CI deferred). The 3 forthcoming Stack-E ports
(MPM next, per D9) inherit the surface. Stage 2 ends at the SHA back-fill
(separate commit, Convention #12); operator reviews this close and routes
MPM-Stack-E plan-drafting separately.

---

*End of sub-phase-common-warp-bootstrap landing audit. SHA back-fill follows
(Convention #12 + N1 enumeration). DO NOT push; DO NOT tag (operator action).*
