---
date: 2026-05-23T21-22-23Z
author: reaction-diffusion-2d-stack-d-sub-phase-agent
phase: 2
artifact: sub-phase
artifact_id: sub-phase-reaction-diffusion-2d-stack-d
subject: "Sub-phase landing — FIRST per-sim cross-stack port under spec-Phase-2 (reaction-diffusion-2d Stack-B WGSL → Stack-D Taichi-DSL). All 14 gates GREEN; gate-14 cross-stack equivalence within_tolerance=True at relative=1e-4 (max_abs_err 1.9e-14, ~10 orders of margin; R-P2 chaotic-divergence empirically falsified for this pair). Portfolio sweep GREEN (Python 360 passed; TS 20 passed + 2 skipped; zero regressions). Integrity sweep 0 HARD_FAIL, 13 SOFT_WARN, byte-identical to 810cd6e3… (FIFTH-in-a-row). FIVE Stage-2 shifts surfaced: (N1) Stack-D capture .json phantom sha a7780645→e1752ceb correction (pre-commit end-of-files-hook root cause); (N2) §B.6 Mode-2 LFS pointer-vs-content drift + falsified dispatch LFS anchor; (N3) fifth-in-a-row byte-identical witness; (N4) coordinator-side dispatch-anchor-propagation discipline; (N5) two banked-for-operator items (§B.6 Option-1 verify_evidence LFS-smudge fix + portfolio-wide capture .json phantom-sha audit). Verdict SHIFTED. No -phase-N tag."
verdict-state: SHIFTED
head_sha: PENDING-CONVENTION-12-BACKFILL
head_sha_at_checkpoint: PENDING-CONVENTION-12-BACKFILL
parent_audits:
  - docs/_audits/phase-1/landing-2026-05-20T14-18-00Z.md
  - docs/_audits/phase-0/block-8-rd-2d-2026-05-19T16-00-36Z.md
  - docs/_audits/phase-2/sub-phase-taichi-integration/landing-2026-05-23T14-45-11Z.md
  - docs/_audits/phase-2/sub-phase-capture-determinism-contract/landing-2026-05-23T17-08-14Z.md
  - docs/_audits/phase-2/sub-phase-reaction-diffusion-2d-stack-d/plan-drafting-probe-2026-05-23T17-33-13Z.md
  - docs/_audits/phase-2/sub-phase-reaction-diffusion-2d-stack-d/plan-drafting-landing-2026-05-23T17-47-51Z.md
  - docs/_audits/phase-2/sub-phase-reaction-diffusion-2d-stack-d/stage-0-checkpoint-2026-05-23T18-10-17Z.md
  - docs/_audits/phase-2/sub-phase-reaction-diffusion-2d-stack-d/stage-1a-checkpoint-2026-05-23T18-31-28Z.md
  - docs/_audits/phase-2/sub-phase-reaction-diffusion-2d-stack-d/stage-1b-checkpoint-2026-05-23T20-35-18Z.md
  - docs/_audits/phase-2/sub-phase-reaction-diffusion-2d-stack-d/stage-1c-checkpoint-2026-05-23T20-53-53Z.md
evidence_paths:
  - docs/conventions/sub-phase-conventions.md
  - docs/phases/sub-phase-reaction-diffusion-2d-stack-d.md
  - docs/sim-specs/continuous-ca/reaction-diffusion-2d/spec-ref-stack-d.md
  - docs/sim-specs/continuous-ca/reaction-diffusion-2d/equivalence.md
  - tools/testkit/probes/reports/reaction-diffusion-2d-stack-d-probe.md
  - tools/testkit/equivalence/tolerance.toml
  - tools/testkit/equivalence/tolerance-budget.toml
  - tools/testkit/mutation/sub-phase-reaction-diffusion-2d-stack-d-2026-05-23T21-22-23Z.json
  - docs/perf-ledger.md
  - CHANGELOG.md
  - docs/dependencies.md
  - captures/reaction-diffusion-2d-ref/gray-scott-lambda-128sq-seed42-step2000.h5
  - captures/reaction-diffusion-2d-ref/gray-scott-lambda-128sq-seed42-step2000.json
  - captures/reaction-diffusion-2d-stack-d/gray-scott-lambda-128sq-seed42-step2000.h5
  - captures/reaction-diffusion-2d-stack-d/gray-scott-lambda-128sq-seed42-step2000.json
  - tests/fixtures/legacy-captures/phase-2-reaction-diffusion-2d-stack-d.h5
  - tests/fixtures/legacy-captures/phase-2-reaction-diffusion-2d-stack-d.json
  - tools/testkit/failing-tests-evidence/reaction-diffusion-2d-stack-d-2026-05-23T18-30-50Z.txt
  - tools/testkit/failing-tests-evidence/reaction-diffusion-2d-stack-d-implemented-2026-05-23T20-34-08Z.txt
  - docs/_audits/phase-2/sub-phase-reaction-diffusion-2d-stack-d/stage-1c-evidence/gate-14-cross-stack-harness-2026-05-23T20-53-53Z.txt
  - docs/_audits/phase-2/sub-phase-reaction-diffusion-2d-stack-d/stage-1c-evidence/gate-14-pytest-post-skip-removal-2026-05-23T20-53-53Z.txt
evidence_hashes:
  docs/conventions/sub-phase-conventions.md: sha256:167fe34911b4d3f49e3e924fcb8261421acac87a3e0931a5d00a3dbcf2c58c2e
  docs/sim-specs/continuous-ca/reaction-diffusion-2d/equivalence.md: sha256:1df9035e34560d5ab30af00f3047c104c3a3576b1d541751e728bf4ee6337744
  tests/fixtures/legacy-captures/phase-2-reaction-diffusion-2d-stack-d.json: sha256:c88bd2c35b82a65f225798c437094ee6cba7b8d4704b4b9f91c5230055b574d3
  captures/reaction-diffusion-2d-stack-d/gray-scott-lambda-128sq-seed42-step2000.json: sha256:e1752ceb0e1847bc3c9a82a7eda2486a4418eec0908876ad016c61e5da27e104
  captures/reaction-diffusion-2d-ref/gray-scott-lambda-128sq-seed42-step2000.json: sha256:585d7d8ab2db7db7b64b498b5436f414835e1e67ffb6a7ad962f3d4803d3a7bc
  captures/reaction-diffusion-2d-stack-d/gray-scott-lambda-128sq-seed42-step2000.h5: sha256:2e93a75164bafdf104b0b247fffdeb5e3d8be0806b5fa42f17b6d5741041b13d
  captures/reaction-diffusion-2d-ref/gray-scott-lambda-128sq-seed42-step2000.h5: sha256:bcae544ae58ceb1fb06f9b8be2441f9116eebd8ea5d21dd616f2daf6f92148f0
  tests/fixtures/legacy-captures/phase-2-reaction-diffusion-2d-stack-d.h5: sha256:2e93a75164bafdf104b0b247fffdeb5e3d8be0806b5fa42f17b6d5741041b13d
  docs/_audits/phase-2/sub-phase-reaction-diffusion-2d-stack-d/stage-1c-evidence/gate-14-cross-stack-harness-2026-05-23T20-53-53Z.txt: sha256:fc161cd206aba1ec42c929865302a04e815c1dc699739e96e90facede260cdc2
  docs/_audits/phase-2/sub-phase-reaction-diffusion-2d-stack-d/stage-1c-evidence/gate-14-pytest-post-skip-removal-2026-05-23T20-53-53Z.txt: sha256:a7eab064df4af8418cf10ebb32905b426a9e2b8e106aed15dbfd5b2a72d8cef1
---

# Sub-Phase Landing Audit — RD-2D → Stack-D

> **Evidence-hash note (read first; §B.6 Mode 2 + Stage 2 N1/N2).** The two
> capture `.h5` `evidence_hashes` above record the **LFS content OID**
> (`2e93a751…`/`bcae544a…`, matching `git lfs ls-files` + a direct `sha256sum`
> on the smudged working-tree file). `verify_evidence` reads the LFS **pointer
> stub** via `git show` and therefore reports an expected-shape mismatch
> (`38b9ce05…`/`8910f111…`) — this is §B.6 **Mode 2**; the content OID is
> load-bearing per §B.1 (§ 7). The Stack-D capture `.json` records the
> **corrected** sha256 `e1752ceb…` (NOT the phantom `a7780645…` that propagated
> through prior checkpoints; §8 N1).

## 1. Scope summary

(FACT — charter § 1.1, § 4.3.)

The **FIRST per-sim cross-stack port sub-phase under spec-Phase-2**. Ports
`reaction-diffusion-2d` from the Phase-0-Block-8-frozen Stack-B (TypeScript /
WGSL / WebGPU) reference to a content-equivalent Stack-D (Python / Taichi-DSL /
CPU) implementation at `packages/reaction-diffusion-2d-stack-d/` through gates
4–14. **All 14 gates GREEN.** The cross-stack-pair methodology template
(`equivalence.md` + per-sim tolerance override + per-frame diff witness format)
is established as the IC-15 candidate inherited by the 7 subsequent cross-stack
port sub-phases (formalization defers to the second cross-stack pair per D5).

**Verdict: SHIFTED** (five Stage-2 shifts; § 8). No correctness defects; the
shifts are an audit-chain provenance correction (N1), a tooling/anchor drift
(N2), a remarkable byte-identical-witness observation (N3), a coordinator-side
discipline precedent (N4), and two banked-for-operator infrastructure items
(N5). The sub-phase is landing-complete; all 14 gates GREEN.

## 2. 14-row gate-status final table

(FACT — charter § 2; Stage 1b + Stage 1c checkpoints + Stage 2 verification.)

| Gate | Status | Witness |
|---|---|---|
| 1 spec sheet | GREEN | `spec-ref-stack-d.md` (Stage 1b; § 9 reconciled at Stage 2 § 2.9d) |
| 2 probe report | GREEN | `tools/testkit/probes/reports/reaction-diffusion-2d-stack-d-probe.md` |
| 3 failing-tests committed | GREEN | Stage 1a `ca9bc0b…`; evidence `685e5cc0…23ad6446` |
| 4 code verification (MMS) | GREEN | observed OOA combined=1.9972 (formal 2.0, ±0.5) |
| 5 Tier 1 (NaN/Inf) | GREEN | canonical capture healthy across 11 frames |
| 6 Tier 2 scalar_field | GREEN | U,V ∈ [0,1] across every captured step |
| 7 Cat 1 citations | GREEN | integrity Cat-1 clean |
| 8 Cat 2 public API | GREEN | integrity Cat-2 clean (new package scanned via tracked `__init__.py` glob) |
| 9 canonical capture + corpus | GREEN | Stack-D capture + schema-corpus entry; round-trips |
| 10 determinism (IC-13) | GREEN | `run_twice_and_diff` content_equivalent=True |
| 11 PBT (3 invariants) | GREEN | 3/3 at n_examples=20 |
| 12 perf-ledger row | GREEN | taichi-cpu 0.568 s (0.61× Stack-B baseline) |
| 13 failing-tests replay | GREEN (structural) | 6/6 ModuleNotFoundError at `ca9bc0b` (Stage 1b step 11 + Stage 2 step 2.6) |
| 14 cross-stack equivalence | GREEN | `compare_captures` within_tolerance=True at relative=1e-4; per-field witness in `equivalence.md` |

## 3. Stage convergence commits (full chain)

(FACT — `git log`; Stage 2 commits are this landing-audit commit + SHA back-fill.)

| Stage | Commits |
|---|---|
| plan-drafting | `d72b80b` / `09faeb7` / `bb10a75` / `ab064a3` |
| Stage 0 | `3756538` / `efbdfbf` / `6ac6baa` |
| Stage 1a | `ca9bc0b` / `ea6153c` / `89b7327` |
| Stage 1b | `276bd85` / `c36a1b4` / `61f1a73` |
| Stage 1c | `2b5353a` / `f863f66` / `2c434cd` |
| Stage 2 | (this landing-audit commit) / (SHA back-fill commit) |

Total: 16 commits through Stage 1c + 2 Stage 2 = **18 commits**.

## 4. Per-deliverable evidence sha256 (Step 2.1 anchor re-check outcome)

(FACT — `sha256sum` / `git lfs ls-files` at HEAD.)

| Artifact | sha256 |
|---|---|
| `equivalence.md` | `1df9035e34560d5ab30af00f3047c104c3a3576b1d541751e728bf4ee6337744` |
| `spec-ref-stack-d.md` (post-§2.9d) | `7d8ef8d76509e096c881850712ad779b2d5782b6c1ead54c0df552e1abf66be3` |
| `tolerance.toml` (post-Stage-1c override; **new baseline anchor**) | `86a1afaede7ffdca8e52494893af39a1a0c7b2ba50d6e27621d194235e52b671` |
| probe report | `a1031a54b39564f6066f1dd5726e9f5db9d190694d288d7bd5528d45e722e68a` |
| mutation artifact | `b085d105dcdb343ca27cb795034caeebdefec87eb0da80747cd99f5bc8a2312d` |
| `perf-ledger.md` | `6e4be3182b1b52aa51d7c2051c7b9250babe3f0d5369fb2101c1d02987708689` |
| `CHANGELOG.md` (post-§2.9a) | `6c479c097266ce03b7927b5d646e032d62637f6455a99676a406ee1e11705d87` |
| `dependencies.md` (post-§2.9b) | `9aebe889365761ebe8a7f817578fe61b2df70c3a420d459bac2e6749b24a7b7a` |
| Stack-D capture `.json` (**CORRECTED**, §8 N1) | `e1752ceb0e1847bc3c9a82a7eda2486a4418eec0908876ad016c61e5da27e104` |
| Stack-D capture `.h5` (LFS content OID) | `2e93a75164bafdf104b0b247fffdeb5e3d8be0806b5fa42f17b6d5741041b13d` |
| Stack-B capture `.json` | `585d7d8ab2db7db7b64b498b5436f414835e1e67ffb6a7ad962f3d4803d3a7bc` |
| Stack-B capture `.h5` (LFS content OID) | `bcae544ae58ceb1fb06f9b8be2441f9116eebd8ea5d21dd616f2daf6f92148f0` |
| schema-corpus `.json` | `c88bd2c35b82a65f225798c437094ee6cba7b8d4704b4b9f91c5230055b574d3` |
| schema-corpus `.h5` (== Stack-D canonical OID) | `2e93a75164bafdf104b0b247fffdeb5e3d8be0806b5fa42f17b6d5741041b13d` |

**Drift caught (§8 N1).** The prior anchor `a7780645…` for the Stack-D capture
`.json` is a **phantom** — it is the sha256 of the manifest content *without* a
trailing newline. The committed blob (since Stage 1b `276bd85`) carries the
newline appended by the pre-commit `fix end of files` hook, and hashes to
`e1752ceb…`. The drift is one byte; the capture content is correct, deterministic,
round-trips via `load_capture`, and gate-14 passed against it. This landing audit
records the **correct** value; prior checkpoints (Stage 1b + Stage 1c) and the
Stage 1c Step-7 footer are NOT corrected in place (Convention #12, no `--amend`).

## 5. Test sweep outcomes (Step 2.2)

(FACT — per-package fan-out per § B.7; counts canonical, sweep-output sha256 informational.)

**Python (360 passed, 0 failed):** sims — boids-3d 10, eulerian-smoke 10,
lbm-d3q19 10, mandelbulb 10, mpm 10, physarum 10, rd-2d 14, **rd-2d-stack-d 16**,
rd-3d 8, sph-water 22, strange-attractors 11 (= 131); tools — integrity 51,
diagnostics 93, testkit 60 (= 204); common-py 25. Net vs the 342 baseline:
**+18** (Stack-D +16 [15 Stage-1b PASS + the cross-stack test un-skipped at
Stage 1c]; testkit +2, a minor pre-existing collection-scope variance — all 60
GREEN). **Zero regressions.**

**TypeScript:** 8 files, **20 passed + 2 skipped** (interactive surfaces) —
baseline holds (Stack-D port is Python-only; no TS behavioral change).

## 6. Integrity sweep outcome (Step 2.4) — FIFTH-in-a-row byte-identical witness (N3)

(FACT — `uv run python -m integrity --all --mode strict`.)

**`0 HARD_FAIL, 13 SOFT_WARN`.** The sweep-output sha256 is
`810cd6e3cac165df11ab166f0b4cc08cdf33b5c3d40240855f176ff123411f98` —
**byte-identical** to the baseline carried since the MPM landing § 7.2.

This is the **FIFTH consecutive byte-identical integrity sweep**:
MPM → conventions-refactor → Taichi-integration → capture-determinism-contract
(fourth, per parent landing § 6) → **this sub-phase (fifth)**. The streak held
despite this sub-phase touching a new sim package + new spec sheet + new probe
report + new canonical capture + new perf-ledger row + a Cat-X additive
`tolerance.toml` override.

The dispatch flagged byte-identical as "suspicious — surface for investigation."
Investigation establishes it is **legitimate, not a coverage gap**:
- Cat 2 (`python_module_exports.py::run_cat2_python_exports`) enumerates every
  tracked `__init__.py` via `repo_tracked_files` git-glob; the new package's 3
  `__init__.py` files ARE tracked and ARE scanned (Stage 1b gate-8 already
  confirmed Cat-2 GREEN for the package). The sweep emits lines only for
  violations; a clean new package emits none → output unchanged.
- The 13 SOFT_WARN are all pre-existing Phase-0/Phase-1 audit-link items; no new
  Cat-5 row from this sub-phase's audits (all well-formed).
- Cat 3 unchanged (no golden table; Step 2.3 NO-OP). Cat 1/4/X clean.

The integrity-sweep system is providing real signal: additive-only discipline is
holding across five sub-phases. (The sweep snapshot recorded here is the
pre-landing-audit-commit state per the established landing convention.)

## 7. Evidence-path verification + append-only check (Steps 2.5 + 2.7)

(FACT — `verify_evidence --strict`; append-only check vs `v0.1.0-phase-1`; conventions § B.6.)

**Evidence-path verification.** All non-LFS, non-phantom `evidence_hashes` match.
Two expected-shape items, both documented:

- **§B.6 Mode 2 — LFS pointer-vs-content (N2; DISPATCH ANCHOR FALSIFIED).** All
  capture `.h5` are LFS-tracked per `.gitattributes` `captures/**/*.h5
  filter=lfs` at HEAD (post the 2026-05-22 git-lfs-migration). `verify_evidence`'s
  `file_at_sha()` reads the LFS pointer stub via `git show` and hashes that
  (`38b9ce05…` Stack-D / `8910f111…` Stack-B), while the audit chain records the
  **content OID** (`2e93a751…`/`bcae544a…`, the load-bearing value per §B.1,
  matching `git lfs ls-files` + a direct `sha256sum` on the smudged file). This is
  §B.6 Mode 2 (first surfaced at MPM N2); it appears in this sub-phase's Stage
  0/1a/1b/1c checkpoints too. **The audit semantics are correct; the tool needs
  fixing.** Disposition: §B.6 **Option 3** (annotate; MPM § 7.3 precedent). The
  dispatch's "§B.6 does not apply (no captures committed via LFS)" anchor is
  **falsified**.
- **Stack-D `.json` phantom (N1).** Corrected to `e1752ceb…` here (§ 4, § 8 N1).

**Append-only check.** Clean (fail=0) against `v0.1.0-phase-1`. New sub-phase
audit files are net-new (allowed); no prior-tag audit content edited or
shortened. Legitimate additive amendments (NOT violations), explicitly documented:
- `tolerance.toml` — additive `[overrides.reaction-diffusion-2d]` (Stage 1c);
  preserved the existing "No per-sim overrides at Phase 0 close" comment.
- `test_cross_stack_equivalence.py` — SKIP removal (Stage 1c); a Stage-1a artifact
  of THIS sub-phase, not a prior-protected file.
- `spec-ref-stack-d.md` § 9 — reconciliation (Stage 2 § 2.9d); a Stage-1b artifact
  of THIS sub-phase.
- **Conventions doc sha256 unchanged** at
  `167fe34911b4d3f49e3e924fcb8261421acac87a3e0931a5d00a3dbcf2c58c2e` through the
  full sub-phase lifecycle (no Stage-1 or Stage-2 amendments). N6 docstring check:
  all 3 new sub-package `__init__.py` files start with `"""`.

## 8. Inherited shifts + Stage-2 shifts

**Cumulative inherited: 115** entering Stage 2 (113 entering Stage 1c + 2 Stage-1c
shifts). Per-stage this sub-phase: plan-drafting ×3 (N1/N2/N3) → Stage 0 ×0 →
Stage 1a ×1 (`uv sync --all-extras` requirement) → Stage 1b ×2 (gate-13 wording
clarification; `ti.types.ndarray()` kernel-arg) → Stage 1c ×2 (taxonomy resolution
gap; IC-15 candidate) → **Stage 2 ×5** (below).

| ID | Description |
|---|---|
| **N1 (Stage 2)** | **Stack-D capture `.json` phantom sha correction: `a7780645…` → `e1752ceb…`.** Root cause: the Stage 1b agent computed the manifest sha256 **in memory before commit**; the pre-commit `fix end of files` hook then appended a trailing `\n`. The committed blob carries the newline → `sha256(blob) = e1752ceb…`. The drift is a one-byte trailing-newline difference; the capture content is correct, deterministic, round-trips, and gate-14 verified against it. The phantom propagated through the Stage 1b checkpoint + Stage 1c checkpoint + Stage 1c Step-7 footer + the Stage 1c/Stage 2 dispatch anchors. Per Convention #12 (no `--amend`) those prior records are NOT corrected in place; **this landing audit § 4 is the canonical going-forward record**, making the discrepancy explicit and discoverable. Stage 1c's propagation of the dispatch anchor without re-verifying was a Convention #8 lapse at agent level; the upstream propagation of the unverified sha through ≥4 dispatch contracts was a Convention #8 lapse at coordinator level. Both banked. |
| **N2 (Stage 2)** | **§B.6 Mode-2 LFS pointer-vs-content drift surfaced in this sub-phase's audit chain; dispatch LFS anchor falsified.** Captured `.h5` are LFS-tracked; `verify_evidence` hashes pointer stubs while §B.1 records content OIDs as load-bearing. Disposition: §B.6 Option 3 (annotate, MPM § 7.3). Banked methodology-precedent for coordinator-side dispatch hygiene: assertions about LFS coverage MUST be verified against `.gitattributes` at HEAD, not inferred from sub-phase shape. |
| **N3 (Stage 2)** | **Fifth-in-a-row byte-identical integrity sweep witness** (`810cd6e3…`) despite genuine additive surface expansion (§ 6). Documented as remarkable structural-correctness evidence; the integrity-sweep system provides real signal. The dispatch's "suspicious" framing was overly cautious; investigation established legitimacy (Cat 2 scans the new package via tracked `__init__.py` glob). |
| **N4 (Stage 2)** | **Dispatch-anchor-propagation discipline at coordinator level.** Banked precedent: sha256 anchors propagated across dispatches MUST be re-verified against the committed artifact at each propagation point — especially for text artifacts where pre-commit hooks can introduce trailing-newline drift. Convention #8 applies to the dispatching coordinator, not just the executing agent. Applies to all subsequent dispatches. |
| **N5 (Stage 2)** | **Two banked-for-operator-routing items established beyond this sub-phase's scope** (§ 10): (a) §B.6 Option 1 — `verify_evidence` LFS-smudge correctness hotfix sub-phase; (b) portfolio-wide audit of whether other capture `.json` files share the hook-induced phantom-sha pattern (lookback window = pre-commit `end-of-files` hook activation date). |

**Cumulative shift count at Stage 2 close: 115 + 5 = 120** entering the next sub-phase.

## 9. Sub-phase coherence outputs

(FACT — charter § 11.3.)

- **First per-sim Stack-D port** in the portfolio; structural exemplar for the 7
  subsequent cross-stack port sub-phases.
- **First true matching-sim cross-stack equivalence witness** (gate 14):
  `within_tolerance=True` at `relative=1e-4`; per-field max_abs_err peaks at
  `1.9e-14` (step 1600 U) with ~10 orders of margin; step 0 bit-identical.
- **R-P2 empirically falsified for this pair** at the full step-2000 horizon — the
  Stage-1b algebraic optimism (NumPy-bit-identical IC + algebraically-identical
  update + FP-accumulation-only difference) is confirmed. **NOT auto-inherited**
  by subsequent pairs (each runs its own step-horizon analysis).
- **IC-15 candidate methodology-template established**: `equivalence.md` +
  at-budget per-sim `tolerance.toml` override (physics-family `sim.category` →
  numerical-method tolerance-category) + per-frame diff witness format.
  Formalization defers to the second cross-stack pair (D5).
- **Wall-clock (informational):** Stack-D Taichi-cpu single-threaded 0.568 s vs
  Stack-B NumPy-vectorized 0.931 s (0.61×) — banked expectation for subsequent
  Taichi-cpu sims.

## 10. Banked items final-state table

| Item | State |
|---|---|
| D1 sub-phase naming | RESOLVED |
| D2 Stage-1 decomposition (1a/1b/1c) | RESOLVED — all three CONFIRMED |
| D3 tolerance relative=1e-4 | RESOLVED — note: charter's "no per-sim override" falsified; at-budget override now in place (resolution wiring, not widening) |
| D4 full step-2000 horizon | RESOLVED — gate-14 GREEN at full horizon; step-horizon documented |
| D5 cross-stack methodology partial scope-in | RESOLVED — `equivalence.md` authored; full consolidation defers to second cross-stack pair |
| D6 sibling workspace member | RESOLVED |
| §B.6 Option 1 — `verify_evidence` LFS-smudge fix | **BANKED for operator routing** (NEW, N5a). Portfolio-wide tooling surface (every sub-phase's evidence check); focused-infrastructure sub-phase shape (mirrors Taichi-integration, not per-sim). **Supersedes** the Taichi-integration-era "evidence_paths LFS remediation" banked item — note the supersession explicitly. |
| Portfolio-wide capture `.json` phantom-sha audit | **BANKED for operator routing** (NEW, N5b). Lookback window = pre-commit `end-of-files` hook activation date; any capture `.json` sha256 recorded before activation is potentially clean, after is potentially affected. |
| IC-15 spec-template formalization | DEFERS to second cross-stack pair |
| Cross-stack verification methodology full-consolidation | DEFERS to second cross-stack pair (D5) |
| Testing-improvements (incl. Taichi-DSL mutmut characterization, B17 re-bank target) | BANKED (unchanged) |
| Mid-Phase-1 capture regeneration | BANKED (unchanged) |
| LBM/MPM `sim_runner_diagnostic` seed-propagation defect | BANKED for downstream routing (unchanged; not RD-2D) |
| Stage-0 R-P1 task-scope expansion for cross-stack ports (end-to-end harness invocation, not just parser perf) | BANKED methodology-precedent (informs next cross-stack dispatch) |

## 11. Phase coherence note

(FACT — charter § 3.2, § 11.3.)

IC-13 + IC-14 (capture-determinism-contract) consumed verbatim. IC-11 + IC-12
(Taichi-integration) consumed verbatim. This sub-phase establishes the **IC-15
candidate** at the methodology + at-budget-resolution-wiring level; formal IC-15
spec-template numbering pends the second cross-stack pair. No conventions-doc
amendment this sub-phase (sha256 unchanged at `167fe349…f2c58c2e`).

## 12. Next-sub-phase recommendation

No agent recommendation (operator-routable). Candidates per charter § 11.3:
sph-water Stack-D, eulerian-smoke Stack-D, LBM-D3Q19 Stack-D, or a Stack-C
variant. Each consumes the IC-15 candidate methodology (per-sim `tolerance.toml`
override mapping its physics-family `sim.category` to its numerical-method
tolerance-category: `particle-fluids→sph`, `volumetric-grid→smoke`,
`lattice→lbm`). The two N5 banked items (§B.6 Option-1 verify_evidence LFS fix +
portfolio-wide capture `.json` phantom-sha audit) are operator-routable as focused
infrastructure work independent of the next cross-stack port.

**Tag posture:** NO `-phase-N` tag. Optional `v0.1.11` non-phase point-release is
a banked operator decision (lean: NO tag, per conventions-refactor /
Taichi-integration / capture-determinism-contract precedent).

---

This landing audit lands at HEAD `PENDING-CONVENTION-12-BACKFILL` (back-filled per
Convention #12 + § B.2 tightened-discipline in a separate
`chore(reaction-diffusion-2d-stack-d-stage2-sha-backfill)` commit; full 40-hex via
`git rev-parse HEAD` at summary-composition).

Verdict: **SHIFTED** (N1–N5) — all 14 gates GREEN; sub-phase landing-complete.
