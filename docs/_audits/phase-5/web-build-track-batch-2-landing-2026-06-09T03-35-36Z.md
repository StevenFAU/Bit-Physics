---
date: 2026-06-09T03-35-36Z
author: phase-5 web-build track PHASE-1 batch-2 session (Claude Code)
subject: "Phase-5 web-build track — batch 2 (neural-ca + ising) build-and-validate landing. neural-ca capture_roundtrip BIT-EXACT (same-shader); ising observable statistical-equivalence (energy z=0.3). Both run-twice byte-identical; gate runs the committed .wgsl via wgpu-native on the real GPU; browser smoke is the §6.1 DOM-load fallback."
kind: sub-phase-landing
artifact: sub-phase
verdict: CONFIRMED
verdict-state: CONFIRMED
phase: 5
sub_phase: "web-build-batch-2"
head_sha: 59d133014abbc6bf7a49ba188351a01ccb15104a
prior_phase_tag: v0.4.0-phase-4
integrity_invariant: "0 HARD_FAIL / 14 SOFT_WARN"
parent_audits:
  - "[[web-build-track-batch-1-landing-2026-06-09T03-17-42Z]]"
  - "[[web-build-track-charter-2026-06-09T02-39-17Z]]"
evidence_paths:
  - tools/productization/web-build/gpu_gate.py
  - packages/neural-ca/web/src/main.ts
  - packages/ising-classical/web/src/main.ts
evidence_hashes:
  tools/productization/web-build/gpu_gate.py: sha256:88c5b0de48139fadd22a488cb7d8b4411a33fdc51d4727f6dd9eaf172419468d
  packages/neural-ca/web/src/main.ts: sha256:2d8b97eba7fc689afda6fd1acd4893b16355f7fbbc27b88ade2d6ef01b0f7204
  packages/ising-classical/web/src/main.ts: sha256:ef829fe8ea5b7907f8775bb8fa67eaaeb8e397f642a88a86ebd5880b0213f6cc
---

# Phase 5 — web-build track — batch 2 (neural-ca + ising) landing

> Build-and-validate of web frontends 3-4 of seven, upstream of 5.1. Each sim
> cleared its CHARTER-named gate, MEASURED live (#8). FACT = ran/read/measured
> at the cited HEAD; INFERENCE = reasoned. Commits direct to `main`. NO tag (I7).

## §0 — Headline

| | |
|---|---|
| **Build/validate commit** | `c700276` (batch-2 new files). This audit + back-fill land on top; `head_sha` per Convention #12. — FACT |
| **Result** | **2 PASS / 0 BLOCKED.** neural-ca `capture_roundtrip` (bit-exact); ising `observable` (statistical-equivalence). — FACT |
| **Vite build (§6.1)** | neural-ca ✓ exit 0 (15.8 kB); ising ✓ exit 0 (12.8 kB). Both tsc --noEmit clean. — FACT |
| **Headless WebGPU** | **§6.1 FALLBACK for BOTH** (DOM-load smoke). Correctness gate runs the identical committed `.wgsl` via wgpu-native on the **real RX 6800 XT (RADV/Vulkan)** — as batch 1 (no headless browser WebGPU in-env). — FACT |
| **Tolerance rows added** | **NONE.** neural-ca resolves via the existing `[defaults.continuous-ca]` 0.0/0.0 (bit-exact); ising is observable (no tolerance.toml touch). `tolerance.toml` unchanged. — FACT |
| **Integrity (live)** | **0 HARD_FAIL / 14 SOFT_WARN** — invariant HELD; report digest unchanged from baseline (no integrity-scanned surface touched). — FACT |
| **render_similarity / variant** | **0.9242 / 0.8702 HARD floors UNAFFECTED** — pure additions; no such source touched. — FACT |
| **Verdict** | **CONFIRMED** — both sims PASS on their charter-named gate with no SHIFT (the headless-WebGPU / pnpm SHIFTs were already surfaced in batch 1 and are unchanged here). |

## §1 — Per-sim results (FACT)

### neural-ca — `capture_roundtrip` — **PASS** (bit-exact, the strongest gate)
- **Vite build**: exit 0, tsc clean. Implements the previously-STUBBED driver
  (`typescript/src/index.ts` threw "Stage 1b-B"): a two-dispatch step
  (`update` then `mask`) over three rotating state buffers + the weights buffer,
  mirroring `neural_ca.wgsl_harness`. Ships the committed
  `typescript/src/nca_inference.wgsl` + the converted checkpoint
  (`nca-weights.bin` 33 KB + `nca-layout.json`) as fetchable assets.
- **Named gate**: the committed `nca_inference.wgsl` run via wgpu-native (the
  repo's `wgsl_harness`) is **BIT-EXACT (max_abs=0.0)** vs the WGSL canonical
  `captures/neural-ca-ref/growing-emoji-64sq-seed42-step1000-wgsl` (21 frames,
  rgba), **run-twice byte-identical**. The canonical was generated by this exact
  shader+harness, so the same-shader round-trip is exact. Resolves via the
  existing `[defaults.continuous-ca] = 0.0/0.0`; **no row added.**
- **Checkpoint**: the LFS checkpoint is already materialized locally (real
  bytes, not a pointer — per the 5.3 landing) → no R2 fetch needed.
- **Headless**: §6.1 DOM-load fallback PASS. NOT real headless browser WebGPU.

### ising-classical — `observable` — **PASS** (statistical-equivalence)
- **Vite build**: exit 0, tsc clean. Ships the committed `src/metropolis.wgsl`;
  spin-lattice render + observable capture-export (spins + energy/magnetization).
  Seed-42 numpy IC frozen to `ising-ic-seed42.bin`.
- **Named gate (charter, ratified decision 4)**: NOT a spin-field round-trip —
  the WGSL in-shader PCG hash ≠ NumPy PCG64, so a field match would be fake. The
  WGSL and NumPy implementations are **algorithmically identical Metropolis**
  (same `dE = 2·s·(J·Σnbr + h)`, same `exp(-dE/T)` acceptance — VERIFIED by
  reading both). The gate is: **run-twice BYTE-IDENTICAL** (✓, mandatory per the
  new-canonical discipline) + **STATISTICAL equivalence** of the self-averaging
  `energy_per_spin` to the NumPy reference ensemble. MEASURED over 6 seeds:
  WGSL mean −1.423 vs NumPy mean −1.418, combined SEM 0.017 → **z=0.32** (≪ the
  z<3 threshold). `|M|` (broad near Tc) also consistent (z=0.06). A single run
  scatters widely (one draw gave E=−1.47, |M|=0.71 — a valid near-critical
  sample), which is precisely why the field/tight-observable match is fake and
  the ensemble gate is the honest one.
- **Headless**: §6.1 DOM-load fallback PASS. NOT real headless browser WebGPU.

## §2 — §S.5 full sweep (this batch)

- **Local pre-push (FACT):** integrity `--all --mode strict` **0 HF / 14 SW**
  (digest unchanged); both apps `tsc --noEmit` clean + `vite build` exit 0; both
  GPU gates PASS on the real GPU; both DOM smokes PASS (§6.1 fallback); ruff
  check + format clean on the modified harness Python.
- **render_similarity (0.9242) + variant (0.8702) HARD floors: UNAFFECTED** —
  pure additions; no `render_similarity/`/`variant/` source touched;
  `tolerance.toml` unchanged.
- **Post-push CI** back-filled at the SHA-backfill commit below.

## §3 — CONTRADICTIONS vs EXPECTED (collector)

| # | Expected (charter) | Measured | Disposition |
|---|---|---|---|
| C-1 | neural-ca "strongest round-trip; small measured override if epsilon" | **BIT-EXACT 0.0** (not epsilon) — same shader + frozen checkpoint on the same GPU | CONFIRMED — no override needed (decision 6 trigger not met) |
| C-2 | ising statistical-observable gate | a single run scatters (E=−1.47/|M|=0.71); the ensemble mean is consistent (energy z=0.32) | CONFIRMED — ensemble gate; single-run/field match correctly rejected as fake |
| C-3 | headless browser WebGPU | unavailable in-env (batch-1 SHIFT, unchanged) | wgpu-native gate + §6.1 fallback |
| C-4 | render/variant floors | pure additions; untouched | UNAFFECTED |

## §4 — Closing

Web-build batch 2 is COMPLETE; verdict **CONFIRMED**. neural-ca (the previously
stubbed driver, now implemented) clears a **bit-exact** same-shader round-trip
against the WGSL canonical (run-twice byte-identical; `[defaults.continuous-ca]`
0.0/0.0, no row added); ising clears the ratified **observable** gate
(run-twice byte-identical + energy_per_spin statistical-equivalence to the NumPy
ensemble, z=0.32 — NOT a fake field match). No tolerance was widened or added;
`tolerance.toml` is unchanged. Integrity held 0 HF / 14 SW; the
render_similarity (0.9242) + variant (0.8702) HARD floors are UNAFFECTED. Four
of seven web frontends are now built (rd2d, mandelbulb, neural-ca, ising). Batch
3 (boids + strange-attractors + physarum) remains. NO tag (I7).
