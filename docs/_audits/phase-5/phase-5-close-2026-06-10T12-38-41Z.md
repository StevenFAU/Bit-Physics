---
date: 2026-06-10T12-38-41Z
author: phase-5-close-agent
phase: 5
artifact: phase-close
artifact_id: phase-5
verdict: SHIFTED-with-notes
verdict-state: SHIFTED
head_sha: d25196e576f0f9e4bc363930a3629bba4af56b30
prior_phase_tag: v0.4.0-phase-4
integrity_invariant: "0 HARD_FAIL / 16 SOFT_WARN"
parent_audits:
  - "[[sub-phase-web-deploy-5.1-landing-2026-06-09T04-12-03Z]]"
  - "[[sub-phase-binary-release-5.2-landing-2026-06-08T15-48-00Z]]"
  - "[[sub-phase-pypi-release-5.3-landing-2026-06-08T14-54-09Z]]"
  - "[[sub-phase-render-passes-5.4-landing-2026-06-09T01-01-11Z]]"
  - "[[sub-phase-preprint-extraction-5.5-landing-2026-06-09T02-18-00Z]]"
  - "[[sub-phase-warp-deprecation-migration-2026-06-08T16-12-24Z]]"
  - "[[web-build-track-charter-2026-06-09T02-39-17Z]]"
  - "[[web-build-track-batch-3-and-close-2026-06-09T03-54-41Z]]"
  - "[[browser-divergence-resolution-landing-2026-06-09T13-24-25Z]]"
ci_runs:
  - { workflow: web-deploy.yml, run: 27210300628, head: 89ea841, result: "0/7 (Validate env: empty uv venv + readiness defect)" }
  - { workflow: web-deploy.yml, run: 27244441494, head: 6a5065e, result: "4/7 (waitForFunction arg-slot defect unmasked; boids finding)" }
  - { workflow: web-deploy.yml, run: 27246062969, head: cb4da99, result: "5/7 (neural-ca finding; harness fix confirmed)" }
  - { workflow: web-deploy.yml, run: 27247859138, head: 13b38e5, result: "5 green + 2 FAIL-PENDING by design (measurement pass)" }
  - { workflow: web-deploy.yml, run: 27275878776, head: ab9f372, result: "7/7 GREEN (declared bounds adjudicated)" }
---

# Phase 5 — CLOSE (productization)

> Aggregate close over the five productization pipelines (5.1–5.5), the
> Warp-deprecation migration, the 7-frontend web-build track, the
> browser-divergence resolution, and the five-run web-deploy CI campaign that
> resolved Phase-5's last open item (cross-backend browser delivery on lavapipe).
> FACT = read/ran/measured this session (#8). Append-only ledger discipline
> (§ 7.5 / G.7) verified against `v0.4.0-phase-4`. NO tag pushed by this agent
> (I7) — tag proposal to operator in § 8.

## § 0 — Headline

| | |
|---|---|
| **Scope closed** | 5 sub-phases (web-deploy 5.1, binary-release 5.2, pypi-release 5.3, render-passes 5.4, preprint-extraction 5.5) + warp-deprecation migration + web-build track (7 Stack-B frontends) + browser-divergence resolution + web-deploy CI campaign runs #1–#5. — FACT |
| **Final CI state** | web-deploy run #5 (`27275878776`, on `ab9f372` == origin/main): **7/7 GREEN on Mesa lavapipe** — the phase's last open item. — FACT |
| **Sub-phase verdicts** | 5.1 SHIFTED-with-notes; 5.2 SHIFTED; 5.3 SHIFTED; 5.4 SHIFTED; 5.5 SHIFTED; warp-migration SHIFTED — every SHIFT documented landed-reality, none hides a divergence. — FACT (read this session) |
| **Tolerance** | NONE added or widened across the entire phase: `tolerance.toml` / `tolerance-budget.toml` / `gpu_gate.py` ESTABLISHED_THRESHOLDS + parity guard byte-unchanged from 5.1 landing through close. Cross-backend bounds are a separate, opt-in, measured-then-declared fallback class (§ 1). — FACT |
| **Integrity** | 0 HARD_FAIL / 16 SOFT_WARN at close (baseline documented at § 13-era; the +2 vs phase-start are charter cat5 citation warnings, absorbed). — FACT |
| **Close checks** | append-only invariant vs `v0.4.0-phase-4`: **PASS** (every prior-tag audit is a byte-prefix of HEAD). verify_evidence: all repo-path hashes PASS; 7 non-repo-path entries fail by construction (pre-existing at their landings — B-4-class quirk, § 7). — FACT |
| **Verdict** | **SHIFTED-with-notes** — phase delivered; the honest limitations are § 6, the named deferrals § 5. |

## § 1 — Close-gate verification: round-2 declarations (coordinator-verified this session)

The § 17 round-2 bound declarations were never coordinator-verified; verified now
as a close gate:

- **Provenance:** every literal in `_CROSS_BACKEND_DECLARED_BOUNDS` carries an
  in-code measured-provenance comment (10× the worst measured delta, lavapipe
  value cited with its RADV counterpart) and matches the § 17 measured table.
  No orphan literals; nothing reverse-engineered. — FACT (read at HEAD)
- **Run #5 adjudication (measured vs declared, lavapipe):**

| Observable | Run-#5 measured | Declared bound | Margin |
|---|---|---|---|
| boids polarization Δ | 1.0555e-06 | 1.1e-05 | 10.4× |
| boids mean_speed Δ | 2.8172e-05 | 2.9e-04 | 10.3× |
| boids speed_std Δ | 1.3979e-05 | 1.4e-04 | 10.0× |
| boids mean_dist_to_centroid Δ | 9.6943e-06 | 9.7e-05 | 10.0× |
| neural-ca step-50 max_abs | 1.7881e-07 | 1.8e-06 | 10.1× |

  Both sims `passed=True` with `run_twice=True` on lavapipe;
  `declared_bounds` echoed in the notes (no silent path). boids' informative
  pointwise 0.0354 reproduced verbatim — measured, reported, not gated. The
  run-#5 lavapipe deltas are byte-identical to run #4's (per-backend
  deterministic), so margins sit exactly at the declared 10× headroom. — FACT
- **Non-contingency 5 sims:** passed their UNCHANGED established gates with
  values identical to the § 15 baselines (rd2d 2.0394e-05 @ rel 1e-4; ising
  z=1.33; mandelbulb 2.9024e-05 run_twice=True; strange overshoot 0.0
  run_twice=True; physarum total_mass 22499.9964 run_twice=True). — FACT

Close gate: **PASS**.

## § 2 — Aggregate: what Phase 5 delivered

| Unit | Verdict | One-line landed reality |
|---|---|---|
| **5.1 web-deploy** | SHIFTED-with-notes | Build-and-validate over the 7 Stack-B frontends through REAL headless browser-WebGPU; deploy job gated off; browser WebGPU proven available locally (the web-build track's "unavailable" was an about:blank probe artifact); ultimately 7/7 green on CI lavapipe (§ 3). |
| **5.2 binary-release** | SHIFTED | 2 qualifying Stack-C CMake packages validated (clean-build-dir isolation, no Docker; cmake≥4.0 policy flag; Windows/macOS DEFERRED-to-Phase-6). |
| **5.3 pypi-release** | SHIFTED | 15-sim pool: 14 PASS through the § 3.8 bootstrap gate; 1 BLOCKED surfaced (sh-update Warp-deprecation → resolved by the warp migration below). |
| **5.4 render-passes** | SHIFTED | R4 canonical (eulerian-smoke) through convert→export→render→verify, decoded-pixel bit-exact; de-Docker'd Blender; cloud job operator-dispatch only. |
| **5.5 preprint-extraction** | SHIFTED | R4 canonical (pinn-poisson) byte-identical extraction + clean compile, zero unresolved cites; de-Docker'd TeX; cloud job operator-dispatch only. |
| **warp-deprecation migration** | SHIFTED | 3 live occurrences migrated (version-adaptive guard); numerics-free proven bit-identical per touched sim; unblocked 5.3's sh-update. |
| **web-build track** | delivered | 7 Stack-B WebGPU frontends, each re-gated against its own established gate on wgpu-native (gpu_gate.py), thresholds parity-guarded. |
| **browser-divergence resolution** | CONFIRMED | The 3 browser failures (rd2d, neural-ca, boids) were ONE capture/live-RAF data race; fixed with a shared lock across all 7 frontends; no shader/tolerance/atomics change. |

## § 3 — The web-deploy CI campaign (runs #1–#5): the lavapipe column, filled honestly

1. **Run #1** (`89ea841`) — 0/7. Two stacked environment defects: the workspace
   ROOT `[project]` declares no dependencies, so the workflow's bare `uv run`
   resolved an EMPTY venv (`ModuleNotFoundError: numpy` post-capture for 5 sims);
   plus a readiness timeout for the other 2. Diagnosis REFUTED the prior session's
   "not a missing dependency" inference (audit § 13).
2. **Run #2** (`6a5065e`, deps fixed) — 4/7, which unmasked the deeper harness
   defect: Playwright's `waitForFunction(fn, arg, options)` had its options object
   in the `arg` slot, so EVERY declared driver timeout had been de-facto 30 s
   since 5.1 landed (including the declared 300 s capture wait); the run-2
   readiness widening was a no-op. Also produced the first genuine lavapipe
   finding: **boids-3d** deterministic pointwise divergence 0.0354 vs 0.01
   (audit § 14).
3. **Run #3** (`cb4da99`, 3-arg fix + instrumentation surfaced) — 5/7. neural-ca's
   76.5 s pipeline bring-up measured (2.5× the old de-facto ceiling); its first
   real verdict: bit-exact broken at 1.0133e-06 on lavapipe ALU (audit § 15).
4. **Run #4** (`13b38e5`, ratified contingency mechanism, bounds UNDECLARED) —
   5 green + 2 FAIL-PENDING **by design**, emitting every charter observable;
   run-twice True on lavapipe for both findings (audit § 16).
5. **Run #5** (`ab9f372`, bounds declared at 10× worst measured) — **7/7 GREEN**
   (§ 1; audit § 17).

## § 4 — Prediction ledger (carried from § 16)

| Prediction | Measured outcome | Verdict |
|---|---|---|
| neural-ca bit-exactness breaks on a non-RADV ALU (Decision 4) | broke at 1.0133e-06 | **AS PREDICTED** |
| rd2d is the fragile sim needing the observable gate (Decision 2) | clears its ESTABLISHED 1e-4 gate on lavapipe at 2.0394e-05 | **DID NOT diverge** |
| boids robust post-RAF-fix | deterministic but 0.0354 vs 0.01 pointwise on lavapipe | **INVERTED** |

## § 5 — Named deferred items (carried open out of Phase 5)

1. **`boids-3d-wgsl-precision-review`** (§ 16 decision 4, DEFERRED-WITH-CAUSE):
   sim-owner inspection of the boids WGSL update kernel for fma/contraction and
   precision-pragma differences explaining the lavapipe-only 0.0354 pointwise
   divergence. The observable gate ships delivery; it does not close this.
2. **Phase-4-CUDA** (phase-4 mid-phase-state § 4.1): the 7 CUDA-bound § 11.5
   frontier rows (need an A100-class CUDA 12 host; CUDA proven absent here).
3. **Phase-4-Greenfield-CPU** (phase-4 mid-phase-state § 4): the
   greenfield-needs-base-sim frontier class — base sims first, then ports.
4. Inherited 5.2/5.4/5.5 deferrals: Windows/macOS binary targets (Phase 6);
   render-passes + preprint-extraction cloud jobs remain operator-dispatch-only.

## § 6 — Honest limitations

- **Per-pipeline content depth is thin by design.** 5.2 validated 2 packages,
  5.4 and 5.5 validated ONE R4 canonical each. The pipelines are proven
  mechanisms, not exhaustively-exercised surfaces; widening the pool is future
  work, not a claim this close makes.
- **Cross-backend WebGPU portability is a FINDING, not a solved property.**
  Quoting the § 16 charter meaning statements now in force: for boids-3d and
  neural-ca, "lavapipe green = deterministic + clamped/bounded/alive + declared
  observable agreement on a foreign ALU; NOT canonical equivalence (which stays
  proven on RADV/wgpu-native only)" — and § 17: "Canonical equivalence (0.01
  pointwise short-horizon; bit-exact 0.0/0.0) remains proven on RADV/wgpu-native
  ONLY, where the untouched established gates stay authoritative." A genuinely
  new backend family may diverge beyond the declared 10×-headroom bounds; that
  would be a finding to measure, not a number to bump.
- **Backend-family sample size is 2** (RADV, lavapipe). The declared bounds'
  10× headroom is justified from the measured ≤4.05× cross-family spread — two
  families is the honest minimum for "cross-backend", not a survey.
- **The deploy job has never run** (gated off all phase, by design): Phase 5
  ships artifact-ready; no Pages publish has been exercised end-to-end.
- **Harness-defect history is part of the record:** every driver timeout was
  de-facto 30 s from 5.1 landing until run #3's fix; pre-fix local green runs
  passed inside that accidental window (no verdict was wrong, but the declared
  timeouts were not the operative ones). Recorded, not retro-edited (§ 14(c)).
- **verify_evidence non-repo-path entries** (§ 7): 7 evidence_hashes entries in
  three landed audits never self-verified (session-local artifacts / external
  pins) — a pre-existing recording practice, disposed per the phase-4 B-4
  precedent, surfaced here rather than silently re-counted as green.

## § 7 — Close checks (measured this session)

| Check | Result |
|---|---|
| append-only invariant, `v0.4.0-phase-4` → HEAD, all docs/_audits/ | **PASS** (no deletion, no shortening, prior content a byte-prefix everywhere) |
| verify_evidence over all 14 phase-5 audits | 11 audits fully PASS; 3 audits PASS on all repo-path hashes with 7 total non-repo-path entries failing by construction: 5.1 (5: local `capture/<sim>/capture-0.json` run outputs + a gitignored `dist/` asset), 5.4 (1: external `.h5` label), 5.5 (1: external TinyTeX tarball pin). Pre-existing at each audit's own landing (`capture/` has never been tracked: 0 files at any ref). Disposition: B-4-class quirk (phase-4 batch-3 close precedent), NOT a regression of this close. |
| integrity --all | **0 HARD_FAIL / 16 SOFT_WARN** (documented baseline) |
| web-deploy smoke | 9/9 |
| HEAD == origin/main == run-#5 head | `ab9f372` — FACT |

## § 8 — SURFACED for operator: tag proposal (I7 — this agent does NOT tag)

Phase-5 close tag, annotated-unsigned, on the close commit (commands in the
close report; message text mirrors this audit's § 0). After tagging, note the
`web-v*` push trigger on web-deploy.yml does NOT match `v0.5.0-phase-5` — no
accidental CI dispatch results from the phase tag.

## § 9 — Closing

Phase 5 (productization) is **CLOSED — SHIFTED-with-notes**. Five pipelines
delivered and validated (web-deploy, binary-release, pypi-release,
render-passes, preprint-extraction), the Warp deprecation migrated, 7 Stack-B
WebGPU frontends built and re-gated, the browser-divergence resolved to a
single harness race, and the cross-backend browser-delivery question driven to
a fully-measured answer over a five-run CI campaign: two genuine deterministic
lavapipe ALU findings (boids-3d, neural-ca), chartered into an opt-in
observable gate class with measured-then-declared bounds (10× worst measured;
≥2 orders below the f32-fragile scale), closing at **7/7 GREEN on lavapipe**
with the canonical gates byte-untouched and authoritative on RADV/wgpu-native.
No tolerance added or widened anywhere in the phase. Integrity 0 HARD_FAIL.
Append-only ledger verified back to `v0.4.0-phase-4`. NO tag pushed (I7) —
proposal surfaced § 8.

appended_by: phase-5-close-agent
close_commit_sha: d25196e  # Convention #12 back-fill
