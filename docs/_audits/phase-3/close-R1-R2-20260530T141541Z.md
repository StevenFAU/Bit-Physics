---
date: 2026-05-30T14-15-41Z
author: phase-3 close campaign — R1 + R2 (Claude Code)
subject: "Phase-3 close R1 (mutation moat + meta-test CI) and R2 (anchor-independence gate) — landing audit"
verdict: R1-SUBSTANTIAL-R2-LANDED
head_sha: 68313609d59d6016d4eaf30ee27ab70fe636d12c
prior_phase_tag: v0.2.0-phase-2
integrity_invariant: "0 HARD_FAIL / 14 SOFT_WARN"
integrity_digest_at_head: 45eed4cacb64b711c461f6e3b76958a646e6b0517302c3921bce2f18ef3018d2
invariants_at_head: [I1, I2, I3, I4, I5, I6, I7]
tag: none
evidence_paths:
  - tools/testkit/golden/tests/test_dfsph_density_generator.py
  - tools/testkit/golden/tests/test_generators_reproduce_tables.py
  - tools/integrity/tests/test_cat4_path_line_assertions.py
  - tools/testkit/property/tests/test_harness_constraining.py
  - tools/testkit/code_verification/mms/tests/test_analyze_constraining.py
  - tools/integrity/integrity/cat3_numerical/golden_values.py
  - tools/integrity/tests/test_cat3_anchor_independence.py
  - tools/testkit/mutation/mutmut-config.toml
  - .github/workflows/integrity.yml
---

# Phase-3 Close — R1 (mutation moat) + R2 (anchor-independence gate)

## R1 — the mutation moat (B-2 / B-2a) + meta-test CI (n-7) + advisory (n-8)

### Per-target before → after (measured, killed/(killed+survived))
| target | before | after | status |
|---|---|---|---|
| `sph_water_dfsph_generator` (B-2a) | **0.0000** (0/127) | **0.7874** (100/127) | raised; commit `4c9fadf` |
| `cat4_draft_time` (B-2, the live citation hook) | **0.0669** (33/493) | **0.5823** (283/486) | raised; commit `3af97c6` |
| `golden` (B-2a generalization) | 0.2696 | raised (8 generators now producer-covered) | not fully re-measured (see note) |
| `property` (B-2) | 0.2034 | +51 constraining tests added | re-measure deferred (note) |
| `code_verification_mms` (B-2) | 0.2650 | +29 constraining tests added | re-measure deferred (note) |

Baselines committed at `tools/testkit/mutation/phase-3-close-<target>-<UTC>.json`
for the two fully re-measured targets.

### B-2a — the purest moat-hollow (RESOLVED)
`sph_water_dfsph_generator` scored 0.000 because its runner tested the SIM
reference (`dfsph.density`) against the frozen golden table, never the generator.
New `test_dfsph_density_generator.py` (24 tests) exercises `_f`/`_fprime`/
`compute_canonical` against independent closed-form Monaghan-1992 anchors +
regenerate-and-compare → **0.000 → 0.7874**. The 27 survivors are ~13
provably-EQUIVALENT mutants (branch-join `<`/`<=`, `h**n` with h≡1, tolerance
boundary, cosmetic prints); the non-equivalent kill rate ~0.88 clears 0.80.

**Generalization (B-2a "THEN GENERALIZE"):** audited all 9 generators under
`tools/testkit/golden/generator/`. Found the same shape on **7 more**: BAD
(no test called the producer) — boids, mandelbulb, mls_mpm, physarum; WEAK
(sim/C++ tests only) — d3q19, lorenz, cloth_catenary. New
`test_generators_reproduce_tables.py` (27 tests) gives every generator
producer coverage (verify()==0 / verify(missing|perturbed)==1; cloth
build_*() dict-equality). cubic_spline + dfsph already covered.

### cat4 — the live citation hook (RAISED 8.7×)
The mutmut runner ran ONE adversarial test exercising only the does-not-exist
branch; the grammars b/c suites (already 14 tests) were never run. Broadened the
runner to all three grammars + added `test_cat4_path_line_assertions.py` (14
branch tests). **0.0669 → 0.5823.** Residual to 0.90 is concentrated in the
`grammars/` subpackage (phrase_in_file/api_shape suites are thin) — documented
for a follow-up grammar-coverage pass; grammar (a) is now branch-covered.
(A cat1 self-inflicted regression — literal `path:line` fixtures in the test
source — was hotfixed in `4c5998f` by assembling citations at runtime.)

### property + MMS frameworks (constraining tests added)
`test_harness_constraining.py` (+51) drives every invariant `check_fn` in both
directions at exact tolerance/bound boundaries plus the harness aggregate logic;
`test_analyze_constraining.py` (+29) pins the OOA log-log fit, L2-norm formula,
the pass/fail threshold boundary, derive.py source==PDE-residual, the FTCS
stencil/CFL guard, and the broken-solver falsifiable meta-test (spot-verified RED
on 3 injected source mutants).

**Note on the deferred re-measures (honest residual).** The `property` and
`code_verification_mms` mutmut targets have `path = tools/testkit/<module>` which
**includes the tests/ subtree**, so the new constraining test files are
themselves in the mutation surface — a full re-measure dilutes the SOURCE-coverage
signal with test-assertion mutants, and each run is ~30–60 min (1100–1300
mutants). The audit's reproduced baselines (property 0.2034, mms 0.2650) stand as
the pre-improvement numbers; the source-coverage improvement is demonstrated by
the targeted tests (mms RED-verified on injected source mutants). A clean
post-improvement number is best taken from the weekly mutation baseline. Flagged
as a follow-up; **not faked.**

### n-7 — integrity meta-test wired into CI (RESOLVED)
`integrity.yml` now runs `uv run pytest tests/` before `--all`; architecture.md's
claim that the meta-test "is itself part of CI" is now true.

### n-8 — §2.13 advisory targets acknowledged (RESOLVED)
Added a §2.13 paragraph enumerating the advisory per-sim/satellite mutmut targets
(non-blocking, 0.80 advisory floor, CI excludes packages/**).

### Harness-fidelity control
Re-confirmed implicitly: the dfsph generator reproduced the audit's 0/127 before
the fix; the cat4 broadening produced a monotone increase consistent with the
added test breadth. No fabricated numbers.

## R2 — anchor-INDEPENDENCE gate (M-3) then data (M-4/M-5) — LANDED

Gate-first (per the brief). `cat3_numerical/golden_values.py`:
- now requires **≥3 DISTINCT normalized `independent_reference.source`** values
  (was: ≥3 populated fields — three identical restatements passed);
- explicit numerical-baseline **EXEMPTION** (`derivation.upstream=
  n/a-numerical-baseline` → AUDIT_LOG, never silent);
- M-3 secondary: de-hard-coded `_SUBDIRS_PICKED_UP` → recursive `tables/` walk.

A full-repo distinct-source **survey** confirmed only the two rigid-body tables
fall below 3 distinct (6dof=1, double-pendulum=1); **every other golden already
has ≥3 distinct** — so the stricter rule breaks no landed table (the cubic-spline
"2 distinct" worry was a string-truncation artifact; it has 3 distinct).

Proof the gate now fails (`test_cat3_anchor_independence.py`, 8 tests):
3-identical → HARD_FAIL "1 DISTINCT"; 2-distinct → HARD_FAIL; 3-distinct → pass;
numerical-baseline → exempt AUDIT_LOG; nested-subdir table now discovered.

M-4/M-5 data: both rigid-body tables already self-declared
`upstream=n/a-numerical-baseline`; their `baseline_note` now cites the §2.4
exemption explicitly (their independent_reference entries are physical sanity
checks, not 3 independent anchors). Live gate at HEAD: **0 HARD_FAIL**, the 2
tables correctly exempt.

## Verification (measure, not copy)
- integrity `uv run python -m integrity --all --mode strict` → **0 HARD_FAIL,
  14 SOFT_WARN** at HEAD `6831360`; digest `45eed4ca…`.
- integrity meta-test (now incl. new cat3 + cat4 suites) → 78 passed.
- §S.5 sweep: integrity CI job GREEN on `6831360` (the cat1 regression on
  `3af97c6` is resolved).

## Surfaced for the operator punch-list
- **§2.13 posture decision (gate vs advisory)** — the brief's [OPERATOR] item.
  Current posture: SOFT_WARN in CI / HARD_FAIL at landings, but CI only runs
  `--baseline` (never measures real scores). DECIDE: promote to a measured gate
  (with the raised coverage), or formally state advisory-only in §2.13.
- **cat4 grammars/ follow-up** — raise phrase_in_file/api_shape coverage to reach
  the 0.90 floor (currently 0.582).
- **property/mms post-improvement re-measure** — take from the weekly baseline
  (target path includes tests/, diluting an ad-hoc run).
