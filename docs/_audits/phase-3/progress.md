# Phase 3 — progress bridge (append-only)

> State-bridging surface per `docs/phases/phase-3-plan.md:628-659` (§3.5) + §9.4, adapted from
> the v8 single-agent per-task schema to the matured **per-sub-phase cadence** (plan-drafting →
> Stage 0 → 1a/1b/1c → Stage 2). One entry per stage/sub-phase, in order. Append-only; never edit
> a prior entry. The v8 schema's "Branch merged at SHA / PR" rows are trunk-based-superseded
> (`docs/phases/phase-3-plan.md:46`) → "Landed at SHA" (no PR). Initialized at the first sub-phase
> plan-drafting.

## sub-phase-phase-3-common-3dgs — plan-drafting — 2026-05-28

- **Stage:** plan-drafting (first Phase-3 sub-phase; re-frames v8 execution into the sub-phase cadence).
- **Landed at SHA:** commit chain `191df72` (K-2) → `598de5a` (probe) → `b6230663` (charter + audit + this file) → SHA-back-fill (COMMIT 4). Trunk-based to `main`; no PR; no tag (I7).
- **Verdict:** SHIFTED (plan ready for Stage 0 *with* two operator-pending Stage-0 gates + execution-model re-frame).
- **Artifacts:** charter `docs/phases/sub-phase-phase-3-common-3dgs.md`; probe `docs/_audits/phase-3/sub-phase-phase-3-common-3dgs-probe-2026-05-28T00-05-29Z.md`; audit `docs/_audits/phase-3/sub-phase-phase-3-common-3dgs-plan-drafting-2026-05-28T00-05-29Z.md`.
- **First sub-phase = task-1 common-3dgs** (§4.1 default; dependency-graph re-anchor confirms, no different-choice STOP).
- **Next stage should know:**
  - **Stage 0 is GATED by two operator-pending preconditions:** (a) Inria gaussian-splatting SHA must be pinned in §2 of `phase-3-plan.md` (all 5 external SHAs PENDING); (b) pre-dispatch-review `docs/_audits/phase-3/pre-dispatch-review-<UTC>.md` must be filed. Do NOT vendor without the pinned SHA (Convention #8); do NOT dispatch without the review.
  - D-A (task-1-vs-task-2 sequencing), D-B (catalog stack-drift, per-sim), D-C (render determinism class), D-D (capture-writer), D-E (intermediate tag v0.2.2-sub-phase-phase-3-common-3dgs, lean YES) await operator routing — charter § 5.
  - §6.1 task prompt uses stale API names (`GaussianSet`/`forward_splat`) + branch ceremony — §3.2.1 `GaussianSplatModel`/`render` + trunk-based govern (charter §1.3).
  - K-2 fixed in `phase-3-plan.md` (7→0 stale golden-paths). S9-PHASE2-1/2/3 encoded into the Stage-2 landing-audit template (charter § 4).
- **Banked / forward:** Phase-4 pre-dispatch review is a separate operator track. Catalog Lenia B/E-vs-D drift routes at task-3/lenia plan-drafting.

## sub-phase-phase-3-common-3dgs — Stage 0 — 2026-05-28 — BLOCKED (STOP-B)

- **Stage:** Stage 0 (pre-flight + anchor re-check + external-SHA pin) — **halted at FIRST ACTION**.
- **Landed at SHA:** blocker audit committed to `main` (no PR; no tag, I7). HEAD at session start `da176e3` (== `origin/main`; plan-drafting chain tip, no successor — Convention M).
- **Verdict:** BLOCKED. **STOP-B** — Phase-3 pre-dispatch-review ABSENT (`docs/_audits/phase-3/pre-dispatch-review-*.md` does not exist; v9 amendment `docs/phases/phase-3-plan.md:34`).
- **Artifacts:** `docs/_audits/phase-3/sub-phase-phase-3-common-3dgs-stage-0-BLOCKED-2026-05-28T00-35-30Z.md`.
- **What did NOT run:** anchor probe (integrity Cat 1–5 / I1–I7 / verify_evidence sweeps), cross-phase replay `--prior-phase phase-2`, external-SHA pinning. No `phase-3-plan.md` §2 edit; no vendoring.
- **Operator action to unblock:** run the Phase-3 phase-plan-review session; land `docs/_audits/phase-3/pre-dispatch-review-<UTC>.md` (spec § 7.4 Convention E-addendum). STOP-A (Inria + 4 other SHAs) is resolved by the agent at Stage-0 resumption per the coordinator-ratified delegation (2026-05-28) — NOT a separate operator commit.
- **On resumption:** fresh Stage-0 session re-runs FIRST ACTION (STOP-B → PASS), then full anchor probe + replay + 5-upstream SHA pinning per the original dispatch.

## sub-phase-phase-3-common-3dgs — Stage 0 — 2026-05-28 — CONFIRMED (supersedes BLOCKED)

- **Stage:** Stage 0 (pre-flight + anchor re-check + external-SHA pin) — **COMPLETE**. Resumed dispatch with **STOP-B removed** (operator-ratified 2026-05-28: pre-dispatch-review overhead retired; charter ratification substitutes).
- **Landed at SHA:** chain `c7c562e` (§2.18 SHA pins) → Stage-0 audit commit → SHA back-fill. Trunk-based to `main`; no PR; no tag (I7). HEAD at session start `e8c8d16` (== `origin/main`; BLOCKED chain tip, no successor — Convention M).
- **Verdict:** CONFIRMED. Anchor probe clean (integrity `c19492ad…d22cb52` byte-identical 0 HARD_FAIL / 14 SOFT_WARN; I1–I7 hold; verify_evidence 7/7 audits 0-fail; I7 test 16/16). Replay `--prior-phase phase-2` → `ok=True` 8/8. No STOP fired.
- **Artifacts:** Stage-0 audit `docs/_audits/phase-3/sub-phase-phase-3-common-3dgs-stage-0-2026-05-28T00-59-06Z.md` (supersedes the BLOCKED audit, which stays append-only); §2.18 added to `docs/phases/phase-3-plan.md`.
- **5 external SHAs pinned (§2.18; all web-fetched + verified, Convention #8):** Inria gaussian-splatting `54c035f7` (main HEAD, no tags; **NON-COMMERCIAL license — first non-permissive upstream, binds task-8 + Phase-4 WU-C**); PhysGaussian `8339ed6a` (main HEAD; **NO LICENSE — cite-only here, task-8 must resolve**); Bender PBD `d0894bdb` (master HEAD; MIT); PhysicsNeMo `766e485a` (release v2.1.0; Apache-2.0); Chakazul/Lenia `adfc5429` (master HEAD; MIT). All security-advisory clean. No STOP-A.
- **Banked for operator (LFS):** R2 credentials absent in agent sessions + GitHub-LFS budget exhausted → the phase-2 replay worktree smudge failed; recovered by repopulating the local git-lfs object cache from verified working-tree content (OID==sha256, byte-identical). Future replays/worktree checkouts depend on this local-cache path until a backend is restored.
- **Next stage = Stage 1a (scaffold + RED).** Inherits: Inria SHA `54c035f7` (vendor `references/3DGS-reference/` at 1b, non-commercial clause binds); §3.2.1 API names (`GaussianSplatModel`/`render`/`Camera`/`load_ply`/`save_ply`); D-C (default bit-exact/same-stack-same-hw registry row at 1a, measure 1b); D-D (probe-discovered smoke-sim pattern; common-py PNG writer default).

## sub-phase-phase-3-common-3dgs — Stage 1a — 2026-05-28 — CONFIRMED

- **Stage:** Stage 1a (scaffold + RED-failing-tests). Trunk-based to `main`; no PR; no tag (I7).
- **Landed at SHA:** `5070965` (ci: pre-commit references/ exclusion) → `4407dcb` (docs: probe report) → `c5273ef` (feat: scaffold + vendored Inria + registry) → `ed4e501` (test: RED tests + evidence) → audit + this entry → SHA-back-fill (Convention #12).
- **Verdict:** CONFIRMED. Anchor probe clean (integrity `c19492ad…d22cb52` byte-identical 0 HARD_FAIL / 14 SOFT_WARN, WITH all new files staged; I1–I7 hold; verify_evidence 8/8 audits 0-fail; I7 test 16/16). No STOP fired.
- **Artifacts:** package `common/common-3dgs/` (23rd workspace member); probe `tools/testkit/probes/reports/common-3dgs.md`; vendored `references/3DGS-reference/` @ Inria SHA `54c035f7` (NON-COMMERCIAL, read-only); determinism `tools/testkit/determinism/registry.toml` (NEW surface, D-C default row); RED evidence `tools/testkit/failing-tests-evidence/common-3dgs-2026-05-28T01-28-53Z.txt` (`sha256:f1f80a02…626c84c6`); audit `docs/_audits/phase-3/sub-phase-phase-3-common-3dgs-stage-1a-2026-05-28T01-35-19Z.md`.
- **RED state:** `9 failed (NotImplementedError), 1 passed`. Failing-tests-output-hash byte-reproducible (--tb=line + prefix/duration-normalized; gate-13 re-runs to the same hash; PBT derandomize=True, database=None).
- **D-C:** DEFAULT `bit-exact / same-stack-same-hw` declared (atomic_ops=none, subgroup_ops=none); MEASURED at Stage 1b. **D-D:** RESOLVED to common-3dgs `save_png` (no common-py RGB-image writer exists; `plot_field_2d` is a colormapped field plot).
- **§0.3 SHIFTs (follow-discovered, surfaced):** `MANIFEST.toml` not `manifest.yaml`; `python-strict.yml` not `build-py.yml` (test-common-3dgs job at 1b); CHANGELOG `### sub-phase-…` not `## Phase 3`; determinism `registry.toml` is a new surface.
- **Next stage = Stage 1b (implementation + thirteen-gate + D-C measurement).** Replace every NotImplementedError; vendor already done; run 13 gates (Gate 14 N/A — single-stack); MEASURE D-C; shared-file updates (docs/common/3dgs.md, README, CHANGELOG, glossary, justfile, python-strict.yml test-common-3dgs job, tolerance-budget Phase-3 carryover, perf-ledger, schema-corpus fixture); RED→GREEN witnessing `sha256:f1f80a02…626c84c6`.

## sub-phase-phase-3-common-3dgs — Stage 1b — 2026-05-28 — CONFIRMED

- **Stage:** Stage 1b (implementation + thirteen-gate + D-C measurement). Trunk-based to `main`; pushed; no tag (I7).
- **Landed at SHA:** `87fe557` (feat: §3.2.1 impl, RED→GREEN, witnesses `sha256:f1f80a02…`) → `d9aa0e7` (docs) → `dd1c3ec` (chore: CI job + recipes + tolerance-budget + registry) → audit + this entry → SHA-back-fill.
- **Verdict:** CONFIRMED. 10/10 tests GREEN; ruff + mypy --strict clean; integrity `c19492ad…d22cb52` byte-identical 0 HARD_FAIL; testkit regression 34/34; I1–I7 hold.
- **D-C MEASURED bit-exact (max_abs_diff=0.0, identical sha256 over two renders):** the bit-exact / same-stack-same-hw declaration HOLDS; registry row unchanged; **no STOP-J** (no distributional/EFECT re-characterization).
- **D-D:** common-3dgs `save_png` (matplotlib imsave). **STOP-E cleared:** §3.2.1 supports task-8's per-frame working-copy mutation pattern.
- **Thirteen gates:** PASS (sim-specific — golden tables / tier-3 / gate-14 — N/A with §2.11 infra surrogates; gate-14 N/A single-stack). Gate-13 failing-tests replay reproduces the hash byte-identically.
- **SHIFTED (DEFERRED, operator):** schema-corpus fixture `tests/fixtures/legacy-captures/phase-3-common-3dgs.{h5,json}` GENERATED + corpus-test-GREEN 21/21, but its `.h5` is LFS-routed and BOTH LFS backends are unavailable in agent sessions (push EOF; R2 creds absent + GitHub-LFS budget exhausted). Commit DEFERRED; fixture reproducible via `just run-3dgs-smoke`; `.h5` sha256 `651dbe45…4653f1`. HARD RULE 2 — surfaced, not improvised around.
- **Artifacts:** impl `common/common-3dgs/src/common_3dgs/{model,camera,render,_kernels,image_io}.py` + `examples/smoke_3dgs/sim.py`; `docs/common/3dgs.md`; CI `test-common-3dgs` in `.github/workflows/python-strict.yml`; audit `docs/_audits/phase-3/sub-phase-phase-3-common-3dgs-stage-1b-2026-05-28T02-01-44Z.md`.
- **Next stage = Stage 1c (mutation baseline ≥80% + PBT confirmation + verify_evidence + append-only + integrity sweep + landing).** Determinism row locked bit-exact.

## sub-phase-phase-3-common-3dgs — Stage 1c — 2026-05-28 — SHIFTED

- **Stage:** Stage 1c (mutation baseline + verdict landing). Trunk-based to `main`; pushed; no tag (I7).
- **Landed at SHA:** `e66e069` (test: second-pass tightening — `test_render_sh.py` + `test_validation.py` + `test_render_values.py` first-pass + `mutmut-config.toml` `common_3dgs` target registration) → `e258950` (test: regenerate Stage-1b carry-in legacy-capture fixture under R2-creds-unblocked posture; LFS-tracked .h5 + sidecar) → `549c383` (test: Stage-1c mutmut baseline JSON — 958/691/50/217, score 0.7610) → audit + this entry → SHA-back-fill.
- **Verdict:** SHIFTED (graded variant per phase-3-plan §2.15). Mutation score = **0.7610** (691 / (691 + 217)); 0.80 floor unmet by 3.9 pp; +14.5 pp vs the prior session's 0.6160 first-pass baseline. The 0.80 threshold in `tools/testkit/mutation/mutmut-config.toml` is **NOT widened** (phase-3-plan §6.0 anti-pattern; STOP-I not exercised).
- **Anchor probe:** integrity `c19492ad…d22cb52` byte-identical 0 HARD_FAIL / 14 SOFT_WARN; I1–I7 hold; verify_evidence sweep 0-fail across plan-drafting + probe + stage-0 + stage-0-BLOCKED + stage-1a + stage-1b (no regression).
- **STEP A — survivor bucketing (pre-tightening, 850 mutants prior session):** render.py 215 (199 surv + 16 susp; SH higher-order + `_quaternions_to_matrices` off-diagonals + EWA Jacobian + constants); model.py 55 (validators + activation pair + PLY internals); camera.py 46 (validation + look_at proj-matrix internals + defaults); _kernels.py 51 (Warp kernel constants + accumulators + clamp); image_io.py 4.
- **STEP B — tightening:** 26 NEW test functions across `test_render_sh.py` (9 SH-coefficient + quaternion-rotation tests) + `test_validation.py` (17 Camera + GaussianSplatModel + image_io tests). All-tests pytest 51/51 GREEN; ruff clean.
- **STEP C — re-run:** 958/691/50/217 — kill rate 0.7610. Per-file: `camera.py` 0.808, `image_io.py` 0.977, `_kernels.py` 0.743, `model.py` 0.746, `render.py` 0.747.
- **STEP D — verdict:** SHIFTED (70-79% bracket). The Warp-kernel + NumPy-preprocessor inner-arithmetic surface is structurally hard to cover beyond ~76% within a non-overlapping test budget (§ 6.1 of the audit).
- **Carry-in CONSUMED:** Stage-1b § 5 DEFERRED legacy-capture fixture regenerated under the R2-creds-unblocked posture (`e258950`). New .h5 file sha256 = `2087402de9…649f4a9` (differs from Stage-1b reported `651dbe45…4653f1` because manifest embeds `wall_clock_seconds`; the **payload** rgb_image is bit-exact across regenerations — D-C governs payload, not file).
- **Banked (Phase-3 lesson L-3DGS-1):** "Neural-rendered category mutation threshold may need calibration; revisit at task-8 dispatch with the 3DGS-MPM consumer providing additional pixel-exact rotation / SH coverage." Forward-routes to task-8 (3dgs-mpm sub-phase plan-drafting).
- **Artifacts:** baseline `tools/testkit/mutation/baseline-2026-05-28T03-23-44Z.json`; tests `common/common-3dgs/tests/test_render_{sh,values}.py` + `test_validation.py`; fixture `tests/fixtures/legacy-captures/phase-3-common-3dgs.{h5,json}`; audit `docs/_audits/phase-3/sub-phase-phase-3-common-3dgs-stage-1c-2026-05-28T03-25-29Z.md`.
- **Next stage = Stage 2 (sub-phase landing audit).** Closes as `closed-with-shifted-1` (the SHIFTED item = the mutation-score gap; L-3DGS-1 routes the calibration to task-8). Intermediate tag lean = YES `v0.2.2-sub-phase-phase-3-common-3dgs` (D-E, charter § 3); operator-pushed only (I7).
- **Push-confirmation (2026-05-28, post-dispatch).** Stage-1c 5-commit chain pushed `e4f8ea5..d6303e8` to `origin/main` after operator R2-credential injection. LFS object `2087402de9…` uploaded to **GitHub-LFS** (pre-receive accepted; one-shot `-c lfs.standalonetransferagent= push`) **and** synced to **R2** via `git lfs push --object-id --stdin` through the `lfs-s3` standalone agent (M3-mechanism precedent). Post-push: HEAD == `origin/main` == `d6303e8`; integrity baseline `c19492ad…d22cb52` byte-identical 0 HARD_FAIL / 14 SOFT_WARN; verify_evidence 16/0 at `head_sha d8e4c483b47a`; I7 `test_i7_no_agent_tags` 2/2 GREEN (no agent-pushed tag in range). STOP-LFS cleared; Stage 2 dispatch READY.

## sub-phase-phase-3-common-3dgs — Stage 2 — 2026-05-28 — closed-with-shifted-1

- **Stage:** Stage 2 (sub-phase landing audit). Trunk-based to `main`; pushed; agent does NOT tag (I7).
- **Landed at SHA:** `c761aa9` (test: extend I7 allowlist for `v0.2.2-sub-phase-phase-3-common-3dgs`) → landing audit + this entry → SHA-back-fill. Parent `7d08d8f` (Stage-1c push + progress note tip).
- **Verdict:** **closed-with-shifted-1** (phase-3-plan §2.15 graded closing variant). One SHIFTED item carried = Stage-1c mutation 0.7610 vs the 0.80 floor; threshold **UNCHANGED**. STOP-D / STOP-H / STOP-LFS / STOP-A2 / STOP-REPLAY / STOP-I7 all NOT fired.
- **Anchor probe:** integrity `c19492ad…d22cb52` byte-identical 0 HARD_FAIL / 14 SOFT_WARN; I1–I7 hold (§7 of the audit); all five phase tags resolve; verify_evidence sweep 0-fail across stage-0 (12/0), stage-0-BLOCKED (7/0), stage-1a (12/0), stage-1b (14/0), stage-1c (16/0).
- **STEP A — LFS fixture-anomaly diagnosis:** 12 fixtures under `tests/fixtures/legacy-captures/` flagged by `git lfs fsck` as `unexpectedGitObject`. Provenance trace: **PRE-EXISTING** from `v0.1.0-phase-1` (9 small placeholders + 3 raw HDF5 binaries); state UNCHANGED at `v0.2.0-phase-2`, `v0.2.1-sub-phase-lfs-architecture`, and HEAD. Tagging now does NOT regress; **DIAGNOSED-OUT-OF-SCOPE**. Banked as **SIBLING-FIXTURE-LFS** sibling sub-phase candidate (`legacy-capture-fixture-lfs-reconciliation`). The Stage-1c-added `phase-3-common-3dgs.h5` is CLEAN (LFS pointer-tracked, OID `2087402de9…`).
- **STEP B — I7 allowlist extension:** `v0.2.2-sub-phase-phase-3-common-3dgs` added to `OPERATOR_NONPHASE_TAGS` (commit `c761aa9`); guard mechanism UNCHANGED (mutation-probed); test 2/2 GREEN.
- **STEP C — closing sweep:** Cat-X tolerance-budget 0/0; mutation threshold 0.80 UNCHANGED; integrity baseline byte-identical; append-only 0 M/D vs v0.2.0 (and 2 sanctioned Ms vs v0.2.1 from the lfs-arch own SHA back-fill chore `e1fc154`); failing-tests replay spot-check `sha256:f1f80a02…` MATCH; perf-ledger 3dgs-smoke row present; pytest `common/common-3dgs/tests/` 51/51 GREEN.
- **STEP D — landing audit:** `docs/_audits/phase-3/sub-phase-phase-3-common-3dgs-landing-2026-05-28T11-12-05Z.md`. Consolidates Stage-0/0-BLOCKED/1a/1b/1c via `evidence_hashes` mapping (S9-PHASE2-1 — does NOT re-narrate); supernumerary reconciliation explicit (S9-PHASE2-2); no project-state.md / check_append_only anchors (S9-PHASE2-3).
- **STEP E — tag proposal:** `v0.2.2-sub-phase-phase-3-common-3dgs` (D-E ratified YES; charter §3 — external dependency + durable architecture). **Operator-pushed only** (I7); agent does NOT tag. Pre-tag checklist documented in §9 of the landing audit.
- **D-class final:** D-A task-1-first; D-B per-sim deferred (Lenia task-3); D-C bit-exact / same-stack-same-hw (LOCKED Stage 1b); D-D common-3dgs `save_png` (matplotlib); D-E YES (tag proposed).
- **Banks carried forward:** L-3DGS-1 (mutation threshold calibration → task-8 dispatch); SIBLING-FIXTURE-LFS (12-fixture reconciliation → candidate sibling sub-phase).
- **Sub-phase: COMPLETE.** First Phase-3 sub-phase landed. Next: operator pushes the intermediate tag; second Phase-3 sub-phase (task-2 render-similarity by §4.1, or operator-routed alternative) becomes dispatchable.

## sub-phase-phase-3-render-similarity — plan-drafting — 2026-05-28

- **Stage:** plan-drafting (second Phase-3 sub-phase; the remaining infrastructure root after task-1 common-3dgs LANDED at `v0.2.2-sub-phase-phase-3-common-3dgs`).
- **Landed at SHA:** commit chain `9a0ebe1` (probe report) → `333dc35` (charter + audit + this entry) → `5d9cd95` (Convention #12 head_sha back-fill) → `872e308` (corrigendum: restore 14 SOFT_WARN + audit evidence_hashes) → `<back-fill-2>` Convention #12 head_sha re-back-fill. Trunk-based to `main`; no PR; no tag (I7).
- **Verdict:** CONFIRMED (no SHIFTED — D-LOC is resolved-in-charter; no operator-pending external gate; pre-dispatch-review RATIFIED-REMOVED at common-3dgs Stage 0 carries forward; no git-upstream SHA pin gates this sub-phase because deps are PyPI).
- **Artifacts:** charter `docs/phases/sub-phase-phase-3-render-similarity.md`; probe `docs/_audits/phase-3/sub-phase-phase-3-render-similarity-probe-2026-05-28T11-34-56Z.md`; audit `docs/_audits/phase-3/sub-phase-phase-3-render-similarity-plan-drafting-2026-05-28T11-34-56Z.md`.
- **Second sub-phase = task-2 render-similarity** (§3.1 dep-graph re-anchor: the remaining infrastructure root once task-1 landed; HARD-blocks task-6 + task-8; no different-choice STOP).
- **Anchor probe:** integrity `c19492ad…d22cb52` byte-identical 0 HARD_FAIL / 14 SOFT_WARN; I1–I7 hold; verify_evidence 0-fail across all 8 Phase-3 audits incl. the BLOCKED stage-0 artifact; phase-0/1/2 historical fails unchanged (no new regression). PyPI: `lpips==0.1.4` (no advisories), `scikit-image==0.26.0` (no open advisories) — both WEB-fetched at probe per Convention #8.
- **Next stage should know:**
  - **D-LOC RESOLVED-IN-CHARTER** → `tools/testkit/render_similarity/` package per §3.2.2 most-recent-normative + v8 locked-item-3 + v4 amendment-4 concurrence. The §6.2 + §3.1-deliverable-map references to `tools/testkit/equivalence/render_similarity.py` are stale (surfaced, NOT edited into `phase-3-plan.md`).
  - **Stage 0 dispatch is READY** — no operator-pending gates. Stage 0 re-anchors + replays `--prior-phase phase-2` + re-verifies PyPI versions + records D-WEIGHTS/D-DET/D-ANCHOR/D-TAG default leans in a Stage-0 amendment.
  - D-WEIGHTS (LPIPS weights — lean lazy runtime-fetch + CI cache); D-DET (lean bit-exact same-stack-same-hw, CPU-only LPIPS + pinned weights, MEASURE Stage 1b); D-ANCHOR (PSNR hand-derivation + SSIM Wang 2004 + LPIPS BAPPS-tiny-subset OR self-consistency + 1 published; STOP-D-ANCHOR if un-anchorable without large fetch); D-TAG (lean YES `v0.2.3-sub-phase-phase-3-render-similarity`, §D.2 (a)+(b) STRONGLY met, operator-pushed) — charter § 5.
  - **Stage-1a probe items (NOT formal D-class):** D-HARNESS-CLI (lean — add `tools/testkit/equivalence/__main__.py` + `--mode` flag; existing harness is programmatic-only); D-SCHEMA (lean — additive `[render_similarity.<category>.<sim>]` table family in `tolerance.toml` + schema extension; render-similarity thresholds are NOT relative/absolute tolerances).
  - **Mutation threshold ≥ 0.85** (NOT 0.80; phase-3-plan §6.0 v9 amendment `:1248`). Charter Stage 1c pre-routes 0.78-0.849 as SHIFTED-bank-not-widen (forward-routes calibration into L-3DGS-1 evidence base), <0.78 as BLOCKED.
  - **Adversarial fixtures DESIGN-SHIFTED** to testkit-local `tools/testkit/render_similarity/tests/fixtures/adversarial/` with its own meta-test (NOT under `tools/integrity/tests/fixtures/adversarial/cat3_wrong_render_similarity/` as v9 amendment `:1250` proposed — that would silently break the integrity meta-test contract; no `run_cat3_render_similarity` handler exists).
- **Banked / forward-routed:** L-3DGS-1 (common-3dgs Stage 1c neural-rendered mutation calibration; final consumer = task-8) — render-similarity's Stage 1c mutation result is one input to this calibration evidence base; SIBLING-FIXTURE-LFS (common-3dgs Stage 2; 12 legacy-capture placeholders) — render-similarity adversarial fixtures live in a DIFFERENT dir, no overlap.

## sub-phase-phase-3-render-similarity — charter-v2 (adversarial-fixture placement on-evidence + R-7 fix) — 2026-05-28

- **Stage:** charter-revision (plan-edit only; not a stage execution).
- **Landed at SHA:** commit chain `<investigation-commit>` (investigation audit) → `<charter-v2-commit>` (charter v1→v2 + R-7 fix + this entry) → `<back-fill>` Convention #12 head_sha back-fill on the investigation audit. Trunk-based to `main`; no PR; no tag (I7).
- **Verdict:** DECIDED — testkit-local placement REAFFIRMED on evidence (charter-v1 rationale REWRITTEN — the "would silently break the integrity meta-test contract" claim was wrong; meta-test is hand-written-per-fixture, no auto-discovery). Decision rule branches 2 (#1 identical CI breadth/freq → coverage argument collapses) AND 3 (#2 wiring fights framework) BOTH apply; converge on testkit-local.
- **Evidence (FACT, from the investigation audit):**
  - `.github/workflows/integrity.yml:5-8` and `.github/workflows/python-strict.yml:5-8` BOTH trigger on `push branches:[main]` + `pull_request` with NO path filters → **identical CI breadth / frequency**; no coverage advantage to centralizing in integrity. The "broader-coverage" hypothesis falsified by config.
  - `tools/integrity/integrity/runner.py:28-37` `_REGISTRY` is hand-keyed by Cat 1-5 + Cat-X **semantic** categories; Cat 3 = `run_cat3_golden_values` (numerical correctness of vendored algorithm implementations, `docs/architecture.md:724`). Image-pair classification is outside Cat 3's authoritative scope.
  - `docs/architecture.md:673` positions `render_similarity/` as a Layer-0 testkit subdir (parallel to `code_verification/`, `golden/`, `determinism/`, `equivalence/`), NOT under `tools/integrity/`.
  - `tools/integrity/tests/test_adversarial_coverage.py:53-180` meta-test is a series of **hand-written test functions per fixture family** (no auto-discovery loop). Dropping a fixture under `tools/integrity/tests/fixtures/adversarial/` without a corresponding hand-written test function does NOT break the meta-test; it just sits inert (charter-v1's rationale-mechanism was wrong; charter-v2 records the correct three-evidence stack).
- **Charter-v2 edits applied (in-place; not a new file):**
  - Front-matter: `version: charter-v2 …`; revisions log appended.
  - § 1.1 item 5: rationale rewritten with the three-evidence stack (identical CI breadth/freq + Cat semantic mis-fit + arch.md Layer-0 placement); explicit "no `run_cat3_render_similarity` handler is wired."
  - § 1.3 inherited-vs-reframed row: rationale rewritten; the v9 amendment's `cat3_wrong_render_similarity/` naming is naming-by-analogy drift, not category fit.
  - § 2 Stage 1b deliverable "Adversarial fixtures": clarifies the hand-written-per-fixture meta-test pattern, the CI wiring via the future `test-render-similarity` job (modeled on `.github/workflows/python-strict.yml:67-96` (test-common-3dgs job)), and "no integrity handler is wired."
  - § 7 R-7: corrected — `evidence_hashes` = MAPPING (path → sha256), `evidence_paths` = LIST. Verified against common-3dgs Stage-1c audit canonical shape.
  - § 7 R-8: rewritten from "documented coverage gap" → "naming-convenience drift, NOT coverage gap." The testkit-local placement closes no coverage. Pre-existing `docs/architecture.md:768` ≠ live-CI-config gap is BANKED (not owned by this sub-phase; render-similarity inherits no part of it because its meta-test runs in CI via `test-render-similarity`).
- **D-LOC unchanged:** `tools/testkit/render_similarity/` package. This revision does NOT touch D-LOC.
- **Anchor probe:** integrity `c19492ad…d22cb52` byte-identical 0 HARD_FAIL / 14 SOFT_WARN; I1–I7 hold; verify_evidence 0-fail across all 10 prior Phase-3 audits (incl. plan-drafting + probe + investigation predecessors); I7 test 16/16; integrity adversarial meta-test `pytest tools/integrity/tests/` 56/56 locally.
- **Banked observations:** (1) `docs/architecture.md:768` claims the integrity meta-test "is itself part of CI" but `pytest tools/integrity/tests/` is not invoked by any current workflow — pre-existing gap, candidate sibling sub-phase ("integrity-meta-test-ci-wiring"). (2) Memory note [[cat1-scans-probes-evidence-hashes-mapping]] needs correction: evidence_hashes = mapping; evidence_paths = list. Memory amended this session.
- **Forward-routed:** no new D-class items; D-WEIGHTS / D-DET / D-ANCHOR / D-TAG default leans unchanged; Stage-1a probe items D-HARNESS-CLI / D-SCHEMA unchanged. Stage 0 dispatch remains READY.

## sub-phase-phase-3-render-similarity — Stage 0 — 2026-05-28 — CONFIRMED

- **Stage:** Stage 0 (pre-flight + PyPI verify + cross-phase replay). Trunk-based to `main`; no PR; no tag (I7).
- **Landed at SHA:** Stage-0 audit commit → SHA back-fill (Convention #12, separate commit). Parent `40ce87b` (charter-v2 chain tip).
- **Verdict:** CONFIRMED. No STOP fired.
- **Anchor probe:** integrity `c19492ad…d22cb52` byte-identical 0 HARD_FAIL / 14 SOFT_WARN; I1–I7 hold; verify_evidence sweep 0-fail across all 11 prior Phase-3 audits (incl. BLOCKED stage-0 artifact + render-similarity probe + plan-drafting + fixture-investigation predecessors); I7 test 2/2; all five phase tags resolve.
- **Cross-phase replay:** `--prior-phase phase-2 → v0.2.0-phase-2 ok=True` 8/8 (integrity/pytest/equivalence/determinism/perf-ledger/property/mutation/tolerance-budget all PASS). LFS-cache recovery NOT needed this session.
- **PyPI verify (Convention #8 WEB-fetch):** `lpips==0.1.4` (2021-08-25, NOT yanked, 0 advisories), `scikit-image==0.26.0` (NOT yanked, `requires_python>=3.11` compatible with testkit's `>=3.12`, 0 advisories), `torch` transitive (`lpips` declares `>=0.4.0`; modern wheels available). STOP-PYPI not fired.
- **Tolerance-budget:** Phase-3 carryover (opened at common-3dgs Stage 0, `tools/testkit/equivalence/tolerance-budget.toml [phase]` block) verified-only, NOT re-opened. render-similarity adds no `[budgets.<cat>.cross_stack]` row (not a cross-stack-equivalence consumer).
- **D-class ratified into Stage-0 amendment block:** D-LOC RESOLVED-IN-CHARTER (`tools/testkit/render_similarity/` package); D-WEIGHTS lazy fetch + CI cache (Stage 1b probe); D-DET bit-exact same-stack-same-hw CPU-only LPIPS (Stage 1b measure); D-ANCHOR PSNR hand-derivation + SSIM Wang 2004 + LPIPS self-consistency + ≥1 published reference (Stage 1b probe; STOP-D-ANCHOR if un-anchorable); D-TAG YES `v0.2.3-sub-phase-phase-3-render-similarity` annotated/operator-pushed (I7 allowlist extension Stage 2); D-HARNESS-CLI/D-SCHEMA leans held (Stage 1a probe items).
- **Mutation threshold for Stage 1c:** **≥ 0.85** (NOT 0.80; phase-3-plan §6.0 v9 amendment `:1248` — neural-sim gate). Pre-routed: ≥0.85 CONFIRMED; 0.78–0.849 with equivalent-mutant rationale SHIFTED-bank-not-widen (feeds L-3DGS-1 evidence base); <0.78 BLOCKED.
- **Artifacts:** Stage-0 audit `docs/_audits/phase-3/sub-phase-phase-3-render-similarity-stage-0-2026-05-28T12-44-20Z.md`.
- **Next stage = Stage 1a (scaffold + RED tests + D-HARNESS-CLI/D-SCHEMA probe + failing-tests-hash).** D-HARNESS-CLI lean: add `tools/testkit/equivalence/__main__.py` + `--mode` flag. D-SCHEMA lean: additive `[render_similarity.<category>.<sim>]` table family + `tolerance-schema.json` additive extension.

## sub-phase-phase-3-render-similarity — Stage 1a — 2026-05-28 — CONFIRMED

- **Stage:** Stage 1a (scaffold + RED tests + D-HARNESS-CLI / D-SCHEMA Stage-1a probe). Trunk-based to `main`; no PR; no tag (I7).
- **Landed at SHA:** `f8769d5` (feat: scaffold — package + harness CLI + tolerance schema) → `5e01023` (test: RED smoke-contract + PBT, Failing-tests-output-hash `sha256:88b5194b…b6`) → Stage-1a audit + this entry → SHA-back-fill (Convention #12 separate commit).
- **Verdict:** CONFIRMED. No STOP fired.
- **Anchor probe:** integrity `c19492ad…d22cb52` byte-identical 0 HARD_FAIL / 14 SOFT_WARN (the net-new package adds no Cat-finding); I1–I7 hold; I7 test 2/2 GREEN; existing equivalence + capture regression suite 37/37 GREEN; tolerance-schema additive extension roundtrip PASS (existing `tolerance.toml` validates clean against the extended schema).
- **D-HARNESS-CLI lean (a) RATIFIED:** `tools/testkit/equivalence/__main__.py` argparse CLI with `--mode {render-similarity}` dispatching to `render_similarity.harness_mode.run(...)`. The existing `compare_captures` programmatic surface (`tools/testkit/equivalence/harness.py:86-185`) is UNCHANGED — no destructive refactor → STOP-CLI not fired.
- **D-SCHEMA lean RATIFIED:** additive top-level `render_similarity` key in `tools/testkit/equivalence/tolerance-schema.json` with category→sim→{psnr_min, ssim_min, lpips_max} nested tables. Existing `[defaults.<cat>]` / `[overrides.<sim>]` validators bit-identical. SCHEMA ONLY — no Phase-3 rows added (tasks 6 and 8 add at their dispatch per charter § 1.1 item 3). STOP-SCHEMA not fired.
- **RED state:** `16 failed, 1 passed`. The 16 are `NotImplementedError(_STAGE_1A_SHELL)` from psnr/ssim/lpips smoke+PBT+error-case rows; the 1 GREEN is `test_ms_ssim_raises_not_implemented` — the `ms_ssim` shell IS the Phase-4-WU-C contract (`docs/phases/phase-3-plan.md:380`), its NotImplementedError matches the smoke assertion. Stage 1b inverts the 16 FAIL to PASS while `ms_ssim` KEEPS its NotImplementedError.
- **Failing-tests-output-hash:** `sha256:88b5194b83c97d363410f3efc8edd3b1fef4d99833cc221f7e18a88dafba18b6` — byte-reproducible across three runs (commit-time witness, immediate re-run, post-ruff-format re-run). Recipe diverges from common-3dgs's only by adding `; s/[[:space:]]+\$//` to the sed pipeline (Hypothesis `Falsifying example` array dumps emit trailing spaces; the trailing-whitespace pre-commit hook normalizes them on the on-disk file).
- **§0.3 SHIFTs (follow-discovered, surfaced):** none beyond the charter's already-recorded §1.3 / §6.2 surface map.
- **Artifacts:** package `tools/testkit/render_similarity/{__init__.py,metrics.py,harness_mode.py,tests/{__init__.py,test_metrics_smoke.py,test_metrics_pbt.py}}`; CLI dispatcher `tools/testkit/equivalence/__main__.py`; schema extension `tools/testkit/equivalence/tolerance-schema.json` (+ tolerance.toml comment block); testkit manifest updates `tools/testkit/pyproject.toml` (hatch packages + mypy files + mypy lpips/skimage/torch overrides + pytest testpaths); RED evidence `tools/testkit/failing-tests-evidence/render-similarity-2026-05-28T12-54-18Z.txt`; audit `docs/_audits/phase-3/sub-phase-phase-3-render-similarity-stage-1a-2026-05-28T12-56-47Z.md`.
- **Next stage = Stage 1b (implementation + 3 anchors + adversarial fixtures + 13-gate + D-DET measurement + shared files).** Replace 16 NotImplementedError raises with §3.2.2 implementations (PSNR hand-derived formula; SSIM via `skimage.metrics.structural_similarity`; LPIPS via `lpips` PyPI + CPU eval + no_grad + pinned weights); ms_ssim STAYS NotImplementedError (Phase-4-WU-C). Add deps to `tools/testkit/pyproject.toml`: lpips==0.1.4 + scikit-image>=0.26 + torch (mypy override block already wired). RED→GREEN witnessing `sha256:88b5194b…b6` (footer `Implements-failing-tests-from: 5e01023` + `Failing-tests-output-hash-witnessed: sha256:88b5194b…`).

## sub-phase-phase-3-render-similarity — Stage 1b — 2026-05-28 — CONFIRMED

- **Stage:** Stage 1b (implementation + 3 anchors + adversarial fixtures + 13-gate + D-DET measurement + shared files + CI). Trunk-based to `main`; no PR; no tag (I7).
- **Landed at SHA:** `c42a4a4` (feat: PSNR/SSIM/LPIPS impl + deps; RED→GREEN witness `sha256:88b5194b…b6`) → `9bbeb1e` (test: 3 anchors + adversarial fixtures + D-DET measurement) → `bedef6a` (chore: shared files + CI test-render-similarity job) → Stage-1b audit + this entry → SHA back-fill (Convention #12).
- **Verdict:** CONFIRMED. No STOP fired.
- **Anchor probe:** integrity `c19492ad…d22cb52` byte-identical 0 HARD_FAIL / 14 SOFT_WARN (implementation + deps + workflow + shared files + adversarial fixtures + CHANGELOG add no Cat-finding); I1–I7 hold; render_similarity 29/29 GREEN (smoke 14 + PBT 3 + anchors 6 + adversarial 2 + determinism 4); regression check capture+equivalence+render_similarity 66/66; ruff + ruff-format + mypy --strict clean.
- **RED→GREEN witness:** Stage-1a hash `sha256:88b5194b…b6` cited in `c42a4a4` footer (`Implements-failing-tests-from: 5e01023…` + `Failing-tests-output-hash-witnessed: sha256:88b5194b…`); gate-13 replay PASS (`sha256sum tools/testkit/failing-tests-evidence/render-similarity-2026-05-28T12-54-18Z.txt` → matches).
- **D-WEIGHTS LANDED:** lazy runtime-fetch + `actions/cache` for `~/.cache/torch/hub/checkpoints` (key on Python + lpips + `uv.lock` hash) + R-3 mitigation via `_assert_bundled_weights_hash` (sha256 of v0.1 linear-head `.pth` on first load; alex `df73285e…835c0`, vgg `a78928a0…32868`). STOP-WEIGHTS not fired (no LFS-vendoring).
- **D-DET MEASURED bit-exact / same-stack-same-hw:** 4/4 metrics (psnr, ssim, lpips-alex, lpips-vgg) bit-exact across two same-process calls (`out_1 == out_2`). The Stage-0 amendment lean HOLDS. **STOP-DET not fired** — no re-characterization to distributional/EFECT needed. R-4 cross-hardware caveat (GPU LPIPS diverges from CI CPU) documented in `metrics.py` docstring + `docs/testkit/equivalence.md`.
- **D-ANCHOR 3 references LANDED:** Anchor 1 (PSNR hand-derivation, `10*log10(MAX_I²/MSE)`) uint8 + float32 variants; Anchor 2 (SSIM Wang 2004 reflexivity + constant-pair luminance term Eq. 6); Anchor 3 (LPIPS self-consistency `< 1e-4` + Zhang 2018 monotonic-under-perturbation `eps=0.05<0.10<0.20`). All grounded in-test per Convention #8 — no fabrication, no large fetch. STOP-D-ANCHOR not fired.
- **Adversarial fixtures + meta-test LANDED:** `tools/testkit/render_similarity/tests/fixtures/adversarial/{ssim_false_positive,lpips_false_negative}/` with `image_a.npy + image_b.npy + manifest.json` per family (testkit-local placement per charter-v2 evidence: identical CI breadth/freq + Cat 1-5 semantic mis-fit + arch.md:673 Layer-0); meta-test `test_adversarial_coverage.py` hand-written-per-fixture (mirrors `tools/integrity/tests/test_adversarial_coverage.py:53-180` form, NO auto-discovery, NO integrity handler). Both meta-test functions PASS at thresholds `ssim_max=0.5` / `lpips_max=0.05`.
- **Thirteen-gate verdict (spec §3.5 v2.4):** 7 PASS + 1 PASS-informational + 1 PASS-substitute + 5 N/A (infra-surrogate or single-stack). Gate-14 cross-stack equivalence N/A (render-similarity IS the equivalence tooling; mirrors common-3dgs Stage-1b gate-14 N/A). All gates accounted for; no widening, no STOP.
- **Stage-1a fixture-size SHIFT-from-RED (surfaced):** `test_metrics_smoke.py:_SMOKE_HW` lifted from 8 → 64 because LPIPS AlexNet's 5-layer max-pool cascade collapses 8x8 input → 0x0. 64x64 = Zhang 2018 BAPPS canonical size (~ms CPU eval; PSNR/SSIM exactness preserved). RED-state contract preserved (RED commit `5e01023` evidence file unchanged on disk; sha256 `88b5194b…b6` still witnesses impl commit).
- **Shared files:** `CHANGELOG.md` (### sub-phase-phase-3-render-similarity); `docs/glossary.md` (PSNR, SSIM, LPIPS, MS-SSIM, perceptual loss); `docs/testkit/equivalence.md` (Render-similarity mode section — Cat-2 doc↔impl contract); `tools/testkit/equivalence/README.md` (NEW — both surfaces documented); `.github/workflows/python-strict.yml` (NEW `test-render-similarity` job).
- **Artifacts:** impl `tools/testkit/render_similarity/metrics.py` + `harness_mode.py` (shell stays); tests `tests/{test_metrics_smoke,test_metrics_pbt,test_anchors,test_adversarial_coverage,test_determinism}.py` + `tests/fixtures/adversarial/{ssim_false_positive,lpips_false_negative}/{image_a,image_b}.npy + manifest.json`; deps in `tools/testkit/pyproject.toml`; CI `.github/workflows/python-strict.yml:test-render-similarity`; audit `docs/_audits/phase-3/sub-phase-phase-3-render-similarity-stage-1b-2026-05-28T13-13-19Z.md`.
- **Next stage = Stage 1c (mutation baseline ≥ 0.85 + verdict + register render_similarity target + Stage-1c audit).** Register `[targets.render_similarity]` in `tools/testkit/mutation/mutmut-config.toml` mirroring `[targets.common_3dgs]` shape; runner `uv run --no-sync pytest tools/testkit/render_similarity/tests/ -x -q --tb=no`; threshold = 0.85 (NOT 0.80, anti-pattern to widen); ≥0.85 CONFIRMED / 0.78-0.849 SHIFTED-bank-not-widen (feeds L-3DGS-1) / <0.78 BLOCKED (STOP-MUT).

## sub-phase-phase-3-render-similarity — Stage 1c — 2026-05-28 — SHIFTED

- **Stage:** Stage 1c (mutation baseline + verdict landing + tightening). Trunk-based to `main`; no PR; no tag (I7).
- **Landed at SHA:** `2859a6c` (test: register render_similarity mutmut target + 2 killing tests) → `44101ab` (test: Stage-1c mutmut baseline JSON 66/84 kill-rate 0.7857 verdict SHIFTED-bank-not-widen) → Stage-1c audit + this entry → SHA back-fill (Convention #12 separate commit).
- **Verdict:** SHIFTED-bank-not-widen (per charter §2 Stage 1c brackets: 0.78 ≤ 0.7857 < 0.85). No STOP fired.
- **Mutmut full run on `tools/testkit/render_similarity/metrics.py`:** 84 mutants total / 66 killed / 18 survived / kill rate **0.7857**. Threshold **UNCHANGED at 0.85** (anti-pattern STOP-I not exercised; phase-3-plan.md:1248 neural-sim gate).
- **Source-only target shape:** `path = "tools/testkit/render_similarity/metrics.py"` (NOT the package root, which would include `tests/` and pollute the kill-rate with test-file mutation noise). Mirrors common-3dgs's `src/common_3dgs/` precedent; rationale recorded inline in `mutmut-config.toml`.
- **Tightening passes (2 killing tests; ≤~20 budget):**
  - `test_wrong_channel_count_raises_value_error` (parametrized psnr/ssim/lpips, 4-channel input → `ValueError`) → kills mutant #41 (`a.shape[2]` → `a.shape[3]` in `_validate_pair`).
  - `test_lpips_dtype_invariance_uint8_vs_float32_normalized` (asserts `lpips(uint8) == lpips(float32 normalized)` bit-exactly under D-DET) → kills mutant #72 (`img/max_i` → `img*max_i` in the LPIPS `[0, MAX_I]→[-1, 1]` normalization). Test design: float32 max_i=1.0 is no-op for the mutation; uint8 max_i=255 saturates AlexNet on the mutated path. No machine-specific numerical golden values asserted — the invariance equality is bit-exact under same-stack-same-hw per D-DET.
- **Equivalent-mutant catalogue (18 survivors):** 11 string/fstring message mutations (un-killable without anti-pattern over-assertion on error-text wording): #17, #21-23, #34, #37, #42, #44, #46-47, #84. 3 no-observable-effect mutations: #28 (`verbose=False→True` diagnostic toggle), #30 (`requires_grad_(False)→True` no-op under `torch.no_grad`), #31 (`_ = torch → _ = None` unused-marker). 4 LPIPS arithmetic mutations symmetric across dtype paths (require machine-specific numerical references to detect): #73 (`*2→/2`), #74 (`*2→*3`), #75 (`-1→+1`), #76 (`-1→-2`). All 4 verified via Stage-1c apply-and-test probe: dtype-invariance test PASSES under each.
- **Banked / forward-routed:** **L-3DGS-1 calibration evidence input** (consumed). common-3dgs Stage 1c banked "Neural-rendered category mutation threshold may need calibration; revisit at task-8 dispatch with the 3DGS-MPM consumer providing additional pixel-exact rotation / SH coverage" — render-similarity's 0.7857 alongside common-3dgs's 0.7610 strengthens the structural-ceiling argument (~0.78–0.80 under non-anti-pattern test budgets); task-8 dispatch consumes both.
- **Anchor probe:** integrity `c19492ad…d22cb52` byte-identical 0 HARD_FAIL / 14 SOFT_WARN; I1–I7 hold; I7 test 2/2 GREEN; render_similarity 33/33 GREEN (was 29; +3 parametrized channel-count + 1 dtype-invariance); failing-tests replay `sha256:88b5194b…b6` MATCH (Stage-1a evidence preserved).
- **Artifacts:** mutmut target `tools/testkit/mutation/mutmut-config.toml` (`[targets.render_similarity]`); baseline JSON `tools/testkit/mutation/sub-phase-phase-3-render-similarity-2026-05-28T14-01-50Z.json`; 4 new tests in `tools/testkit/render_similarity/tests/test_metrics_smoke.py`; audit `docs/_audits/phase-3/sub-phase-phase-3-render-similarity-stage-1c-2026-05-28T14-14-36Z.md`.
- **Next stage = Stage 2 (sub-phase landing audit + I7 allowlist + tag proposal).** Closes as `closed-with-shifted-1` (the SHIFTED item = the Stage-1c mutation 0.7857 vs 0.85 floor; L-3DGS-1 routes the calibration to task-8). Intermediate tag lean = YES `v0.2.3-sub-phase-phase-3-render-similarity` (D-TAG, charter § 3); annotated, operator-pushed (I7); I7 allowlist extension lands at Stage 2 mirroring common-3dgs `c761aa9`.

## sub-phase-phase-3-render-similarity — Stage 2 — 2026-05-28 — closed-with-shifted-1

- **Stage:** Stage 2 (sub-phase landing audit + I7 allowlist extension + closing sweep + tag proposal). Trunk-based to `main`; pushed; agent does NOT tag (I7).
- **Landed at SHA:** `596eb73` (test: extend I7 allowlist for `v0.2.3-sub-phase-phase-3-render-similarity`) → landing audit + this entry → SHA back-fill. Parent `6386033` (Stage-1c SHA back-fill).
- **Verdict:** **closed-with-shifted-1** (phase-3-plan §2.15 graded closing variant). One SHIFTED item carried = Stage-1c mutation 0.7857 vs 0.85 floor; threshold UNCHANGED in `mutmut-config.toml`. STOP-D / STOP-H / STOP-LFS / STOP-I7 / STOP-MUT / STOP-I all NOT fired.
- **Anchor probe:** integrity `c19492ad…d22cb52` byte-identical 0 HARD_FAIL / 14 SOFT_WARN; I1–I7 hold; all five phase tags resolve; verify_evidence sweep 0-fail across plan-drafting (12/0), probe (16/0), fixture-investigation (18/0), stage-0 (28/0), stage-1a (26/0), stage-1b (38/0), stage-1c (20/0); pytest render_similarity + I7 35/35 GREEN.
- **STEP A — I7 allowlist extension:** `v0.2.3-sub-phase-phase-3-render-similarity` added to `OPERATOR_NONPHASE_TAGS` (commit `596eb73`); guard mechanism UNCHANGED (mutation-probed — fake `agent/v0.0.42-fake-push-attempt` still rejected; additive extension does not weaken the check); test 2/2 GREEN.
- **STEP B — closing sweep:** Cat-X tolerance-budget N/A (no rows; not a cross-stack consumer); mutation threshold 0.85 UNCHANGED; integrity baseline byte-identical; append-only 0 M/D vs `v0.2.0-phase-2` and 1 sanctioned M vs `v0.2.2-sub-phase-phase-3-common-3dgs` (`progress.md` mutable surface, common-3dgs precedent); failing-tests replay `sha256:88b5194b…b6` MATCH; perf-ledger N/A (infra task); closing anchor re-check Convention 7.9 PASS (Stage-1c line-293 → line-291 HARD_FAIL caught + fixed during commit; actual `ms_ssim` shell raise lives at `tools/testkit/render_similarity/metrics.py:291`). Pytest render_similarity + I7 35/35.
- **STEP C — landing audit:** `docs/_audits/phase-3/sub-phase-phase-3-render-similarity-landing-2026-05-28T14-20-30Z.md`. Consolidates plan-drafting + probe + fixture-investigation + Stage-0/1a/1b/1c via `evidence_hashes:` mapping (S9-PHASE2-1 — does NOT re-narrate); supernumerary reconciliation explicit (S9-PHASE2-2 — §6.2 deliverables A–J reconciled + K/L/M supernumeraries documented: adversarial fixtures, mutation baseline, I7 extension); no `project-state.md` / `check_append_only` anchors (S9-PHASE2-3 — append-only verified via `git diff`).
- **STEP D — tag proposal:** `v0.2.3-sub-phase-phase-3-render-similarity` (D-TAG ratified YES; charter §3 — three external PyPI deps + durable architecture gating all Phase-4 neural-rendered sims, both §D.2 conditions strongly met; STRONGER than common-3dgs argument). **Operator-pushed only** (I7); agent does NOT tag. Pre-tag checklist documented in §9 of the landing audit. Form: annotated (`git tag -a`), NOT signed.
- **D-class final:** D-LOC `tools/testkit/render_similarity/` package (RESOLVED-IN-CHARTER); D-WEIGHTS lazy fetch + R-3 sha256 + CI actions/cache (LANDED Stage 1b); D-DET bit-exact / same-stack-same-hw CPU-only LPIPS (MEASURED HELD Stage 1b); D-ANCHOR 3 anchors (LANDED Stage 1b); D-HARNESS-CLI lean (a) (RATIFIED Stage 1a); D-SCHEMA additive top-level `render_similarity` key (RATIFIED Stage 1a); D-TAG YES `v0.2.3-sub-phase-phase-3-render-similarity` (PROPOSED Stage 2; operator-pushed).
- **Banks carried forward:** L-3DGS-1 (consumed — 0.7857 alongside common-3dgs 0.7610 strengthens neural-rendered mutation-threshold structural-ceiling argument; task-8 consumes both); SIBLING-FIXTURE-LFS (carried forward separately; different dir, no overlap); integrity-meta-test-ci-wiring (charter-v2 bank, candidate sibling sub-phase — render-similarity's meta-test does NOT inherit the gap; rides `test-render-similarity` CI job).
- **Sub-phase: COMPLETE.** Second Phase-3 sub-phase landed. Next: operator pushes the intermediate annotated tag; third Phase-3 sub-phase (a sim task per §4.1; task-3/3a/4/5/6/7/8 routable; task-6 + task-8 HARD-deps now satisfied) becomes dispatchable.

## sub-phase-phase-3-lenia — plan-drafting — 2026-05-28 — CONFIRMED

- **Stage:** plan-drafting (third Phase-3 sub-phase; first SIM after two infrastructure roots common-3dgs `v0.2.2` + render-similarity `v0.2.3`).
- **Landed at SHA:** commit chain `1f7ec42` (D-B investigation) → `d5587b4` (probe) → `3ca8aa8` (charter + plan-drafting audit + this entry) → SHA back-fill (Convention #12, separate commit). Trunk-based to `main`; no PR; no tag (I7).
- **Verdict:** CONFIRMED. No STOP fired.
- **D-B (the crux):** RESOLVED-IN-CHARTER as **Stack D** per the sibling investigation audit (`docs/_audits/phase-3/sub-phase-phase-3-lenia-db-investigation-2026-05-28T14-38-32Z.md`). Investigation #1-#4 (each FACT-cited): plan §4.1 `:764-765` rationale + §6.3 `:1287` ROLE + §6.3 `:1370` Taichi-seed + §3.2.4 `:426-433` + §3.2.5 `:479-486` pre-baked rows all concur on Stack D; catalog Appendix B `:4683` row reads as **tier-accessibility crosswalk** (per `:4634` column header "Tier 0 / Tier 1 / Tier 2 stacks"), NOT a single-stack mandate; catalog's own §5.2.2 `:1065` narrative explicitly says "Stack D (Taichi or PyTorch), with WebGPU deploy variant" — agreeing with the plan. Phase-3 task-3 is **terminal** (`docs/phases/phase-3-plan.md:325`); no downstream Stack-B consumer. Catalog Appendix B row recorded as **surfaced-read-as-tier-crosswalk** in the charter — NO catalog edit (catalog-v3.0 is separate amendment track per Convention M), NO plan edit. STOP-DB **not fired** (the prompt's expected outcome).
- **Anchor probe:** integrity `c19492ad…d22cb52` byte-identical 0 HARD_FAIL / 14 SOFT_WARN; I1-I7 hold; verify_evidence sweep 0-fail across **all 17 prior Phase-3 audits** (15 entering this session — common-3dgs 8 + render-similarity 7 — plus the D-B investigation 8/0 + probe 29/0); all six phase / sub-phase tags resolve; `uv sync --all-packages` clean; K-2-at-HEAD CONFIRMED (`grep -n code_verification/golden` empty); §6.3 task-3 surfaces all present.
- **Chakazul/Lenia (vendoring target):** SHA `adfc542939266de7f4bb7ebb552e8499701ee107` byte-equal between §2.18 Stage-0 pin (fetched 2026-05-28T00:54Z) and this session's probe re-fetch (2026-05-28T14-38-32Z). License MIT; security advisories empty; active. STOP-PIN **not fired**.
- **D-class ratified into the charter:**
  - **D-B Stack D** (RESOLVED-IN-CHARTER on FACT — sibling investigation audit; surface-only; no catalog edit, no plan edit).
  - **D-MUT-SCOPE NO** (RESOLVED-IN-CHARTER on FACT — §6.0 item 12 `:1054-1058` testkit-adjacent-only; §6.3 VERIFICATION POSTURE `:1369-1373` golden+PBT+determinism). Stage 1c is verdict-landing only (no mutation gate).
  - **D-FFT real-space default** (lean — §6.3 D `:1344-1346`; FFT opt-in conditional on Stage 1b stable AND bit-exact probe; STOP-FFT for silent non-determinism between inputs).
  - **D-DET bit-exact same-stack-same-hw via Taichi seed** (§3.2.5 `:479-486` pre-baked row; MEASURE at Stage 1b; STOP-DET re-characterize distributional+EFECT per smoke-stack-e gate-14 precedent if NOT bit-exact).
  - **D-TAG YES `v0.2.4-sub-phase-phase-3-lenia`** (lean — §D.2 (a) Chakazul external vendoring + (b) durable sim architecture both strongly met; operator-pushed; I7 allowlist extension at Stage 2 mirrors `c761aa9` + `596eb73`. Operator-pending caveat: if phase-close-only tagging is now policy, lean reverts to NO).
- **Stale §6.3 surfaces (Convention M — surface-only re-frames, NO plan edit):** branch ceremony `:1290-1291` (v8 trunk-based supersedes); "Sub-phase 3.1" framing `:1287` (this is the third Phase-3 sub-phase by execution order); multi-claude-session handoff `:1295-1302` (v8 single-agent); ANCHOR-PROBE 1 "Clone, sub-branch" `:1318` (trunk-based). **§0.3 SHIFT-from-discovered (mathematical):** §6.3 E anchor-1 "kernel at r=0 (peak K(0))" `:1351` — Quad4 evaluates K(0)=0, NOT a peak; peak at r=0.5 (K=1). Stage 1b re-grounds against Chakazul derivation.
- **First-SIM-in-Phase-3 friction surfacing (load-bearing per charter §1.1):** Lenia exercises — for the first time end-to-end in Phase 3 — the following pipeline surfaces: per-sim golden table (≥3 anchors), Tier-3 module tree creation (`tools/diagnostics/tier3/` does NOT exist at HEAD), per-sim PBT module under `tools/testkit/property/sims/lenia/`, TDD output-hash footer for a SIM commit, SIM-task vendoring (`references/Chakazul-Lenia/` at pinned SHA), schema-corpus seed `.h5` push with LFS+R2 mirror (the first SIM `.h5` since lfs-architecture landed; common-3dgs's was infra), perf-ledger row append, per-category tolerance/determinism rows, per-sim CI job (`test-lenia`), PBT-invariant declaration in spec-ref §6, mathematical drift discovery (the "peak K(0)" math vs Quad4 evaluation). Friction predicts friction in **every later Phase-3 SIM** (rigid-body, cloth, NCA, PINN, 3DGS-MPM); R-11 mandates loud naming in stage audits.
- **Banked / forward-routed:** L-3DGS-1 (carried forward — Lenia is not neural-rendered); SIBLING-FIXTURE-LFS (carried forward — Lenia's `phase-3-lenia.h5` increments corpus by one but does NOT close the sibling sub-phase); integrity-meta-test-ci-wiring (carried forward — Lenia's testkit-property module rides existing pytest-testpaths CI); **first-SIM-friction-portfolio-scale** (NEW bank — every later Phase-3 SIM inherits this sub-phase's resolution).
- **STOP-conditions filed in charter §6 (none fired at plan-drafting):** STOP-D, STOP-H, STOP-REPLAY, STOP-PIN, STOP-D-ANCHOR, STOP-DET, STOP-FFT, STOP-LFS, STOP-PBT, STOP-CAT-X, STOP-I7, STOP-K2-AT-HEAD, STOP-PROSE-MATH, STOP-TIER3-DIR, STOP-DB. All routed by stage (Stage 0 / 1a / 1b / 1c / 2).
- **Artifacts:** D-B investigation `docs/_audits/phase-3/sub-phase-phase-3-lenia-db-investigation-2026-05-28T14-38-32Z.md`; probe `docs/_audits/phase-3/sub-phase-phase-3-lenia-probe-2026-05-28T14-38-32Z.md`; charter `docs/phases/sub-phase-phase-3-lenia.md`; plan-drafting audit `docs/_audits/phase-3/sub-phase-phase-3-lenia-plan-drafting-2026-05-28T14-38-32Z.md`.
- **Next stage = Stage 0 (pre-flight + integrity baseline + verify_evidence sweep + cross-phase replay per matured per-sub-phase cadence + tolerance-budget continuous-ca cap probe).** Conditional STOPs: STOP-REPLAY (LFS-cache recovery via [[replay-needs-lfs-cache-recovery]] applies BEFORE declaring blocked), STOP-CAT-X (if no `[budgets.continuous-ca.golden]` cap exists in `tolerance-budget.toml`, surface for operator routing). No operator-pending external-state gates at plan-drafting close.

## sub-phase-phase-3-lenia — Stage 0 — 2026-05-28 — CONFIRMED

- **Stage:** Stage 0 (pre-flight + integrity baseline + verify_evidence sweep + cross-phase replay + Chakazul SHA re-confirm + tolerance-budget cap probe). Trunk-based to `main`; no PR; no tag (I7).
- **Landed at SHA:** `ebb76a5` (docs(phase-3): lenia Stage 0 audit + progress entry) → SHA back-fill (Convention #12, separate commit). Parent `4ee54e8` (lenia plan-drafting chain tip).
- **Verdict:** CONFIRMED. No STOP fired.
- **Anchor probe:** `git rev-parse HEAD == origin/main == 4ee54e8`; six phase tags resolve; integrity `c19492ad…d22cb52` byte-identical (0 HARD_FAIL / 14 SOFT_WARN); I7 test 2/2 PASS; `uv sync --all-packages` clean; working tree clean; I1–I7 hold.
- **verify_evidence sweep:** 19 audits / 0 fail across all `docs/_audits/phase-3/*.md` (excluding `progress.md` which is intentionally non-front-mattered).
- **Cross-phase replay:** `python -m integrity.scripts.replay_prior_phase --prior-phase phase-2 --audit docs/_audits/phase-2/landing-2026-05-26T02-30-00Z.md --gates integrity,pytest,equivalence,determinism,perf-ledger,property,mutation,tolerance-budget` → 8/8 PASS `ok=True`. STOP-REPLAY not fired; LFS-cache recovery not required.
- **Chakazul/Lenia SHA re-confirm:** WEB-FETCH `2026-05-28T15-12-47Z` → SHA `adfc542939266de7f4bb7ebb552e8499701ee107` **byte-equal** across §2.18 pin (`2026-05-28T00:54Z`) + probe re-fetch (`2026-05-28T14-38-32Z`) + this Stage-0 re-fetch (`2026-05-28T15-12-47Z`). License MIT; not archived; advisories clean. STOP-PIN not fired.
- **Tolerance-budget cap probe (FRICTION #1, first-SIM portfolio-scale signal):** `tolerance-budget.toml` has only `cross_stack` budgets (`closed_form`, `reaction-diffusion`, `sph`, `mpm`, `smoke`, `lbm`). NO `[budgets.<category>.golden]` cap shape exists. Lenia's `golden_kernel_abs=1e-6` / `golden_kernel_rel=1e-5` / `golden_trajectory_abs=1e-4` (Stage 1b deliverable) lands un-capped-by-design. STOP-CAT-X **not fired** under current shape (cap doesn't exist, so cannot be exceeded). Operator routing options at landing review: (a) accept golden_* as un-capped-by-design (anchor IS the budget); (b) extend tolerance-budget.toml with `[budgets.<category>.golden]` caps in a future sub-phase; (c) per-sim `golden_*_abs/_rel` as self-bounded per §2.4 independent-reference anchors. **Every later SIM hits the same surface.**
- **Stale §6.3 surfaces re-confirmed at HEAD:** branch ceremony / "Sub-phase 3.1" framing / multi-claude handoff / `K(0)` peak math — all re-frames documented in charter §1.2, NOT plan-edited.
- **§0.3 SHIFT-from-discovered carried into Stage 1a:** plan §6.3 prescribes `continuous-ca/lenia/python/` at repo root; on-disk convention at HEAD is `packages/<name>/` (per `packages/reaction-diffusion-2d/`, `-stack-c`, `-stack-d`, `reaction-diffusion-3d`, `eulerian-smoke`, `lattice-boltzmann-d3q19`, `mpm-multimaterial`, etc.). Stage 1a re-anchors via dedicated probe and decides on-evidence per §0.3. Surfaced as **FRICTION #2** (first-SIM portfolio-scale signal — every later SIM faces the same on-disk-vs-plan-prescription friction).
- **Banked / forward-routed:** FRICTION #1 (tolerance-budget golden-cap shape, routes to Stage 1b acceptance + landing review); FRICTION #2 (`packages/<name>/` vs `continuous-ca/lenia/python/` prescription, routes to Stage 1a on-evidence decision); §0.3 anchor re-grounding (Quad4 K(0)=0, NOT peak — routes to Stage 1a re-grounding + Stage 1b golden anchor).
- **Artifacts:** Stage-0 audit `docs/_audits/phase-3/sub-phase-phase-3-lenia-stage-0-2026-05-28T15-12-47Z.md`.
- **Next stage = Stage 1a (scaffold + RED + failing-tests-hash + Chakazul anchor probe + §0.3 packages/-vs-continuous-ca/ on-evidence decision + Quad4 anchor re-grounding probe).**

## sub-phase-phase-3-lenia — Stage 1a — 2026-05-28 — CONFIRMED

- **Stage:** Stage 1a (scaffold + RED + failing-tests-hash + Chakazul anchor probe + §0.3 packages/-vs-continuous-ca/ on-evidence decision). Trunk-based to `main`; no PR; no tag (I7).
- **Landed at SHA:** `107b9ad` (scaffold: package + spec-ref + probe + workspace registration) → `de92946` (RED tests + failing-tests-evidence) → `10ec46e` (Stage-1a audit + progress entry) → SHA back-fill (Convention #12, separate commit). Parent `b0efe5e` (Stage-0 SHA back-fill).
- **Verdict:** CONFIRMED. No STOP fired.
- **RED state:** 10 failed (uniform `NotImplementedError` from packages/lenia/lenia/{kernel.py:55, growth.py:52, sim.py:77,85} shells — NOT collection error per phase-3-plan.md:1337) / 4 passed (test_sim_shells.py asserting shells properly raise — shells ARE the contract; analogous to render-similarity's `test_ms_ssim_raises_not_implemented`).
- **Failing-tests-output hash:** `sha256:5ff5e74175e9a5318f3fbed82b494477365eae83dd7e57795305ec81849a51f0` (byte-reproducible across 3 captures pre-/at-/post-commit). Evidence at `tools/testkit/failing-tests-evidence/lenia-2026-05-28T15-24-41Z.txt`. Commit footer carries `Failing-tests-output:` + `Failing-tests-output-hash:` per spec § 1.3 step 4 + § 6.0 item 6.
- **Anchor probe:** integrity `c19492ad…d22cb52` byte-identical 0 HARD_FAIL / 14 SOFT_WARN; I1–I7 hold; `uv sync --all-packages` clean (lenia + uv.lock updated); chain `ebb76a5 → b0efe5e → 107b9ad → de92946`; Stage-0 verify_evidence 14/0 at recorded base.
- **§0.3 SHIFT-from-discovered ratifications (D-LAYOUT RESOLVED-ON-EVIDENCE):** Plan §6.3 prescribes `continuous-ca/lenia/python/`; on-disk convention is `packages/<name>/` (per 9 prior sim packages). §0.3 declares existing-convention precedence. Stage 1a ratifies `packages/lenia/` + `lenia/` python pkg + workspace registration as 24th member. **NO plan edit** (Convention M — SHIFTED-surface-only). Portfolio-scale precedent for every later Phase-3 SIM.
- **Quad4 anchor re-grounding (RECORDED FACT):** Hand-derivation `K(r)=(4r(1-r))^4` gives `K(0)=0` boundary, `K(0.5)=1` PEAK, `K(1)=0` boundary. §6.3 prose "r=0 (peak K(0))" is mathematically wrong. Three correct anchors at packages/lenia/lenia/kernel.py:14-20 + spec-ref.md §4 + probe.md §9 + the RED kernel tests. NO plan edit (Convention M); STOP-PROSE-MATH NOT fired (math hand-derivable).
- **Scaffold (commit `107b9ad`):** `packages/lenia/{pyproject.toml,README.md,lenia/{__init__.py,__main__.py,kernel.py,growth.py,sim.py}}`; `docs/sim-specs/continuous-ca/lenia/spec-ref.md` (13-section stub with §6 declaring 2 PBT invariants); `tools/testkit/probes/reports/lenia.md` (cat1 probe per testkit template, full repo-relative path:line cites); `pyproject.toml` (workspace member registration as 24th member).
- **RED tests (commit `de92946`):** `packages/lenia/tests/{__init__.py,conftest.py,test_kernel_anchors.py,test_growth.py,test_sim_shells.py,test_determinism.py,test_pbt_invariants.py}`. 5 kernel-anchor tests + 2 growth tests + 4 shell-contract tests + 1 determinism witness + 2 PBT witnesses = 14 total.
- **First-SIM friction notes update (R-11):** #1 (tolerance-budget golden-cap shape) UNCHANGED, lands at Stage 1b acceptance. #2 (packages/-vs-continuous-ca/) **RESOLVED-ON-EVIDENCE** at this stage; precedent for every later SIM. #3 (NEW, Stage 1a): `test_sim_shells.py`'s `pytest.raises(NotImplementedError)` assertions need a Stage-1b rewrite to assert production behavior (mirror inverted of render-similarity's `ms_ssim` Phase-4-WU-C-style shell-stays-raise).
- **D-class status update:** D-B Stack-D RESOLVED-IN-CHARTER (unchanged); D-MUT-SCOPE NO RESOLVED-IN-CHARTER (unchanged); D-FFT real-space-default-lean (decision-by Stage 1b probe); D-DET bit-exact-same-stack-same-hw-lean (MEASURE at Stage 1b); D-TAG YES-lean (decision-by Stage 2); **D-LAYOUT `packages/lenia/` RESOLVED-ON-EVIDENCE at this Stage 1a (§0.3 SHIFT)**.
- **STOPs:** NONE fired. STOP-D, STOP-H, STOP-D-ANCHOR (Stage 1b), STOP-PROSE-MATH (recorded SHIFTED, not fired), STOP-TIER3-DIR (Stage 1b), and all others gated to later stages per charter §6.
- **Banked / forward-routed:** D-LAYOUT precedent (every later Phase-3 SIM); FRICTION #3 (test_sim_shells.py rewrite at Stage 1b).
- **Artifacts:** Stage-1a audit `docs/_audits/phase-3/sub-phase-phase-3-lenia-stage-1a-2026-05-28T15-25-18Z.md`; scaffold commit `107b9ad`; RED commit `de92946`.
- **Next stage = Stage 1b (vendoring + Taichi impl + golden tables w/ ≥3 anchors + Tier-3 + determinism MEASURE + ≥2 PBT shared module + shared files + legacy-capture .h5 seed + perf-ledger row + thirteen-gate).** Conditional STOPs: STOP-D-ANCHOR (Quad4/Orbium grep-cite at vendoring); STOP-DET (if forward conv NOT bit-exact, re-characterize distributional+EFECT); STOP-FFT (if Taichi FFT probe silent-non-deterministic, fall back to real-space); STOP-LFS (.h5 R2 push); STOP-TIER3-DIR (first creation of `tools/diagnostics/tier3/`).

## sub-phase-phase-3-lenia — Stage 1b — 2026-05-28 — SHIFTED

- **Stage:** Stage 1b (vendoring + Taichi impl + golden tables + Tier-3 + determinism MEASURE + PBT shared module + legacy-capture seed + perf-ledger row + tolerance + determinism rows + shared files + 13-gate). Trunk-based to `main`; pushed to origin/main; no tag (I7).
- **Landed at SHA:** `989a7b5` (vendor Chakazul/Lenia) → `11d82b6` (Taichi impl + grep-cites + spec-ref §6 SHIFTED) → `5baf083` (golden + Tier-3 + PBT module + .h5 seed + tolerance + determinism + perf + shared files + CI) → `848f2e4` (Stage-1b audit + progress entry) → SHA back-fill (Convention #12, separate commit). Parent `7fd4341` (Stage-1a SHA back-fill).
- **Verdict:** SHIFTED (two SHIFTED items, NEITHER reverted). No HARD STOP fired; STOP-LFS R2-mirror SURFACED per charter §6.
- **Anchor probe:** integrity `c19492ad…d22cb52` byte-identical 0 HARD_FAIL / 14 SOFT_WARN; I1–I7 hold; `uv sync --all-packages --all-extras` clean; chain `107b9ad → de92946 → 10ec46e → 7fd4341 → 989a7b5 → 11d82b6 → 5baf083`; pytest 14/14 GREEN.
- **Vendoring (Convention #8 grep-cited):** Chakazul/Lenia at SHA `adfc542939266de7f4bb7ebb552e8499701ee107` (MIT) vendored to `references/Chakazul-Lenia/` (5 source files + MANIFEST.toml + README.md). 4 anchors verbatim: Quad4 kernel @ LeniaF.py:493 + LeniaND.py:273; Quad4 growth gn=1 @ LeniaF.py:500 + LeniaND.py:279; Orbium unicaudatus @ animals.json:5 (R=13, T=10, mu=0.15, sigma=0.015, kn=1, gn=1). STOP-D-ANCHOR NOT fired.
- **Implementation (commit `11d82b6`):** `packages/lenia/lenia/` 5-module Taichi backend. `_taichi_kernels.py` carries IC-12 discipline (no `__future__` annotations, no `-> None`, `ti.types.ndarray` not `ti.template`) + explicit `ti.f64` accumulator (Taichi 1.7.4 infers f32 silently otherwise). Real-space Quad4 conv + Quad4 polynomial growth gn=1 + clip-Euler step. CLI per §3.2.6. Test_sim_shells.py rewritten for production behavior per Stage-1a friction #3.
- **Golden tables (≥3 anchors per § 2.4):** `lenia-kernel.json` 9 test-points with 3 hand-derivable anchors (r=0 K=0, r=0.5 K=1 PEAK, r=1 K=0). `lenia-orbium-trajectory.json` 5 test-points (step 0/1/5 aggregates). `lenia-kernel.md` derivation. Tolerances per §3.2.4 pre-baked.
- **Tier-3 — FIRST EVER `tools/diagnostics/tier3/` subtree at HEAD:** `tools/diagnostics/tier3/__init__.py` + `tools/diagnostics/tier3/lenia/{__init__.py,kernel_shape.py,growth_bound.py}`. STOP-TIER3-DIR NOT fired (creating tree did NOT break pytest/imports per post-creation integrity sweep).
- **PBT shared module — FIRST `tools/testkit/property/sims/` subtree:** `tools/testkit/property/sims/{__init__.py,lenia/__init__.py,lenia/invariants.py}` with `monotone_bounds_invariant` + `per_step_change_bounded_by_dt_invariant`.
- **§0.3 SHIFT-on-evidence (HARD RULE 2):** dispatch-suggested `mass_approximately_conserved` PBT invariant is mathematically FALSIFIED for arbitrary IC under Quad4 polynomial growth gn=1 (RED-state witness `de92946`: ~10% mass loss / 5 steps on Gaussian-blob IC). **RE-DECLARED (not widened)** per charter §6 anti-pattern reminder: `monotone_bounds` + `per_step_change_bounded_by_dt`. spec-ref §6 + invariants module + RED tests all updated. STOP-PBT NOT fired (re-declaration, not widening; the failing example WAS the value; the dispatch-suggested invariant was a guess, not a discovered mathematical truth).
- **D-DET MEASURED:** bit-exact same-stack-same-hw via Taichi seed under `set_taichi_deterministic(arch='cpu')`. Two runs at seed 42 grid 64 steps 10 produce np.array_equal field outputs (test_determinism PASSES). Registry row `[continuous-ca.lenia]` locked. STOP-DET NOT fired.
- **D-FFT decision:** REAL-SPACE LANDED. FFT path not attempted (charter §7.2; Taichi FFT determinism not enumerated in architecture.md:962). FFT opt-in deferred to Phase-4+ optimization. STOP-FFT NOT fired (not exercised).
- **Tolerance + determinism rows:** `tools/testkit/equivalence/tolerance.toml [continuous-ca.lenia]` golden tolerances (uncapped per FRICTION #1); `tools/testkit/determinism/registry.toml [continuous-ca.lenia]` Stack-D bit-exact-same-stack-same-hw MEASURED row.
- **Legacy-capture seed (LFS + GitHub-LFS GREEN; R2 STOP-LFS SURFACED):** `tests/fixtures/legacy-captures/phase-3-lenia.h5` (sha256 `6c313a5da53dd341f73accdb7c369564451ccd475fa290c026360e3f39890062`) + sidecar `.json`. GitHub-LFS push GREEN: `git -c lfs.standalonetransferagent= push origin main` uploaded 75KB / 1 object. R2 mirror sync EOF: `git lfs push --object-id --stdin origin <<< OID` returned EOF (no `lfs.customtransfer.lfs-s3.path` configured; AWS_S3_ENDPOINT/S3_BUCKET creds NOT in agent env despite dispatch preamble; mirrors precedent [[phase-3-common-3dgs-stage-1c-shifted-stop-lfs]]). Per charter §6 STOP-LFS clause + dispatch: surfaced, NOT REVERTED. R2 mirror pending operator action.
- **Perf-ledger row:** `lenia | python (Taichi) | orbium-unicaudatus-64sq-seed42-step100 | 0.797 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-28 | baseline` appended to `docs/perf-ledger.md`. Smoke-scale descriptor (charter §6.3 K target was 256sq-step1000; de-scoped to keep row reproducible within session; descriptor locked).
- **Shared files:** CHANGELOG.md sub-phase-phase-3-lenia entry (Added + 3 friction notes + tag-pushing note); docs/glossary.md (Lenia + kernel-convolution CA + Quad4 + growth function); justfile (run-lenia + test-lenia); .github/workflows/python-strict.yml (test-lenia job).
- **Thirteen-gate verdict (spec §3.5 v2.4 / §5.4): 13/13 PASS.** 1 spec-sheet + 2 probe-report + 3 failing-tests + 4 implementation + 5 tests-pass-anchors + 6 Tier-1/2/3 + 7 capture-I/O + 8 perf-bench + 9 Cat-1-5+Cat-X + 10 audit-report + 11 PBT + 12 first-landing-wall-clock + 13 failing-tests-replay-verifiable.
- **D-class status:** D-B Stack-D (unchanged); D-MUT-SCOPE NO (unchanged); D-FFT REAL-SPACE LANDED; D-DET bit-exact-MEASURED-HELD; D-TAG YES-lean-Stage-2; D-LAYOUT packages/lenia/-LANDED.
- **First-SIM friction notes (R-11 portfolio-scale signals):** #1 (tolerance-budget golden-cap shape) UNCHANGED; #2 (packages/-vs-continuous-ca/) RESOLVED-ON-EVIDENCE-LANDED; #3 (test_sim_shells rewrite) RESOLVED-ON-EVIDENCE-LANDED; **#4 NEW (PBT invariant ungroundedness for arbitrary IC) RE-DECLARED**; **#5 NEW (R2 LFS mirror sync EOF) SURFACED**.
- **Artifacts:** Stage-1b audit `docs/_audits/phase-3/sub-phase-phase-3-lenia-stage-1b-2026-05-28T15-51-04Z.md`; vendor commit `989a7b5`; impl commit `11d82b6`; infra-bundle commit `5baf083`.
- **Next stage = Stage 1c (verdict landing, NO mutation gate).** Re-verify golden anchors + PBT + determinism + legacy-capture + perf + append-only + integrity; surface STOP-LFS to closing-status graded variant; ratify SHIFTED-bank-not-widen postures.

## sub-phase-phase-3-lenia — Stage 1c — 2026-05-28 — SHIFTED (closed-with-shifted-2)

- **Stage:** Stage 1c (verdict landing, NO mutation gate per D-MUT-SCOPE NO RESOLVED-IN-CHARTER). Trunk-based to `main`; pushed to origin/main; no tag (I7).
- **Landed at SHA:** `7a0532e` (Stage-1c audit + progress entry) → SHA back-fill (Convention #12, separate commit). Parent `165c46b` (Stage-1b SHA back-fill).
- **Verdict:** SHIFTED (closed-with-shifted-2; both Stage-1b SHIFTED items carried forward). No HARD STOP fired.
- **Anchor probe:** integrity `c19492ad…d22cb52` byte-identical 0 HARD_FAIL / 14 SOFT_WARN; I1–I7 hold; HEAD `165c46b` == origin/main; pytest 14/14 GREEN; I7 2/2 PASS.
- **Golden-anchor re-verification:** `lenia-kernel.json` 3 hand-derived anchors (r=0/0.5/1) + 6 mid-curve all PASS within `golden_kernel_abs=1e-6 / golden_kernel_rel=1e-5`. `lenia-orbium-trajectory.json` step-0/1/5 aggregates PASS within `golden_trajectory_abs=1e-4`.
- **PBT re-verification:** `monotone_bounds` + `per_step_change_bounded_by_dt` 2/2 GREEN at HEAD (SHIFTED-on-evidence Stage-1b re-declaration HELD).
- **Determinism re-verification:** `test_determinism_two_runs_bit_equal` PASSES; D-DET bit-exact same-stack-same-hw HELD.
- **Legacy-capture .h5 re-verification:** sha256 `6c313a5da5…` byte-stable; LFS-tracked; GitHub-LFS HELD. R2 mirror **STOP-LFS CARRIED** (Stage-1b surfaced; agent env lacks customtransfer agent path + AWS_S3_ENDPOINT/S3_BUCKET creds; sync returned EOF; NOT REVERTED).
- **Perf-ledger row byte-stable:** `docs/perf-ledger.md` lenia row at line 45 unchanged from Stage 1b; file sha256 `e04fb8f2…` stable.
- **verify_evidence sweep across 6 lenia stage audits:** 6 PASS / 0 FAIL at HEAD `165c46b`. STOP-H NOT fired.
- **Append-only sweep:** vs `v0.2.0-phase-2` → 164 A / 45 M / **0 D**; vs `v0.2.3-sub-phase-phase-3-render-similarity` → 44 A / 10 M / **0 D**. Append-only HELD.
- **Integrity sweep:** 0 HARD_FAIL / 14 SOFT_WARN byte-identical stderr sha256 `c19492ad…d22cb52`.
- **NO mutation gate** per § 6.0 item 12 testkit-adjacent-only + § 6.3 VERIFICATION POSTURE golden+PBT+determinism. Stage 1c is verdict-landing only.
- **D-class status:** D-B Stack-D (unchanged); D-MUT-SCOPE NO RESOLVED-IN-CHARTER; D-FFT REAL-SPACE LANDED; D-DET bit-exact MEASURED HELD (re-verified at Stage 1c); D-TAG YES-lean (decision-by Stage 2); D-LAYOUT packages/lenia/ LANDED.
- **STOP audit:** STOP-D / STOP-H / STOP-D-ANCHOR / STOP-DET / STOP-PBT / STOP-CAT-X / STOP-FFT / STOP-I7 / STOP-TIER3-DIR / STOP-K2-AT-HEAD / STOP-PIN — all NOT fired at Stage 1c. **STOP-LFS** carried from Stage 1b (R2 mirror); GitHub-LFS HELD; NOT REVERTED.
- **Shifted items carried (operator landing review):**
  1. **PBT re-declaration on math evidence** — `mass_approximately_conserved` mathematically falsified for arbitrary IC under Quad4 polynomial growth gn=1; re-declared (NOT widened) at Stage 1b to `monotone_bounds` + `per_step_change_bounded_by_dt`. Operator confirms the SHIFTED invariants are the durable spec-ref §6 declaration.
  2. **R2 LFS mirror sync EOF** — agent env lacks customtransfer path + AWS creds despite dispatch preamble. Resolution requires either (a) operator pushes the OID `6c313a5da5…` via R2-credentialed environment OR (b) operator configures the agent's `lfs.customtransfer.lfs-s3.path` + AWS env.
- **Banked / forward-routed:** L-3DGS-1 (not relevant — Lenia not neural-rendered); SIBLING-FIXTURE-LFS (carried forward; phase-3-lenia.h5 increments the corpus by one but does NOT close the sibling sub-phase); integrity-meta-test-ci-wiring (carried forward; Lenia rides existing testpaths CI). **First-SIM-friction-portfolio-scale** bank (NEW at plan-drafting): now has 5 friction items (#1 tolerance-budget golden-cap shape; #2 packages/-vs-continuous-ca/; #3 test_sim_shells.py shape; #4 PBT invariant ungroundedness; #5 R2 LFS mirror EOF).
- **Artifacts:** Stage-1c audit `docs/_audits/phase-3/sub-phase-phase-3-lenia-stage-1c-2026-05-28T15-56-13Z.md`.
- **Next stage = Stage 2 (sub-phase landing audit + I7 allowlist + closing sweep + operator-tag proposal).** Closes as `closed-with-shifted-1` if operator ratifies SHIFTED-#1 PBT re-declaration as canonical posture at landing OR `closed-with-shifted-2` if both items count separately. Intermediate tag lean = YES `v0.2.4-sub-phase-phase-3-lenia` (D-TAG, charter § 3); annotated, operator-pushed (I7); I7 allowlist extension at Stage 2 mirrors common-3dgs `c761aa9` + render-similarity `596eb73`.

## sub-phase-phase-3-lenia — Stage 2 (sub-phase landing) — 2026-05-28 — closed-with-shifted-2

- **Stage:** Stage 2 (sub-phase landing audit + I7 allowlist extension + closing sweep + tag proposal). Trunk-based to `main`; pushed; agent does NOT tag (I7).
- **Landed at SHA:** `fcf8546` (I7 allowlist extension for `v0.2.4-sub-phase-phase-3-lenia`) → `49b4123` (landing audit + progress entry) → SHA back-fill (Convention #12, separate commit). Parent `a0fa8f9` (Stage-1c SHA back-fill).
- **Verdict:** **closed-with-shifted-2** per charter §2.15 graded closing variants. Two SHIFTED items carried from Stage 1b (PBT re-declaration on math evidence; R2 LFS mirror sync EOF). All STOP-D / STOP-H / STOP-LFS-on-GitHub / STOP-I7 / STOP-PBT / STOP-CAT-X / STOP-DET / STOP-D-ANCHOR / STOP-K2-AT-HEAD / STOP-PIN / STOP-TIER3-DIR all NOT fired. STOP-LFS R2-mirror carried, NOT REVERTED.
- **Anchor probe:** integrity `c19492ad…d22cb52` byte-identical 0 HARD_FAIL / 14 SOFT_WARN; I1–I7 hold; verify_evidence 7/0 across all 7 lenia stage audits; pytest 16/16 GREEN (14 lenia + 2 I7); chain `7a0532e → a0fa8f9 → fcf8546` since Stage 1c.
- **STEP A — I7 allowlist extension:** `v0.2.4-sub-phase-phase-3-lenia` added to `OPERATOR_NONPHASE_TAGS` (commit `fcf8546`); guard mechanism UNCHANGED (additive entry; frozenset member-check unaffected — a fake `agent/v0.0.42-fake` still HARD_FAILs because it's not in the allowlist); test 2/2 PASS.
- **STEP B — closing sweep:** Cat-X tolerance-budget N/A (no golden cap shape exists; FRICTION #1); integrity baseline byte-identical; append-only 164 A / 45 M / 0 D vs `v0.2.0-phase-2` and 44 A / 10 M / 0 D vs `v0.2.3-sub-phase-phase-3-render-similarity`; failing-tests replay `sha256:5ff5e74175e9a5318f3fbed82b494477365eae83dd7e57795305ec81849a51f0` byte-reproducible; perf-ledger row present + byte-stable; closing anchor re-check Convention 7.9 PASS; pytest lenia + I7 16/16 PASS.
- **STEP C — landing audit:** `docs/_audits/phase-3/sub-phase-phase-3-lenia-landing-2026-05-28T16-00-43Z.md`. Consolidates db-investigation + plan-drafting + probe + Stage 0/1a/1b/1c via `evidence_hashes:` mapping (S9-PHASE2-1; does NOT re-narrate). Two SHIFTED items + first-SIM pipeline-validation verdict (✅ VALIDATED-WITH-5-FRICTIONS-SURFACED).
- **STEP D — tag proposal:** `v0.2.4-sub-phase-phase-3-lenia` (D-TAG ratified YES; charter §3 — Chakazul external vendoring + durable sim architecture both §D.2 conditions STRONGLY met; STRONGER than common-3dgs's single git-dep argument, at-parity-or-stronger than render-similarity). **Operator-pushed only** (I7); agent does NOT tag. Pre-tag checklist documented in landing audit §6. Form: annotated (`git tag -a`), NOT signed.
- **D-class final:** D-B Stack-D RESOLVED-IN-CHARTER (sibling investigation); D-MUT-SCOPE NO RESOLVED-IN-CHARTER (§ 6.0 item 12); D-FFT REAL-SPACE LANDED Stage 1b; D-DET bit-exact-MEASURED-HELD Stage 1b + re-verified Stage 1c; D-TAG YES PROPOSED `v0.2.4-sub-phase-phase-3-lenia` (Stage 2; operator-pushed); D-LAYOUT `packages/lenia/` RESOLVED-ON-EVIDENCE Stage 1a (§0.3 SHIFT).
- **Banks carried forward:** L-3DGS-1 (not relevant — Lenia not neural-rendered; task-8 consumes prior banks); SIBLING-FIXTURE-LFS (corpus +1, sibling unclosed); integrity-meta-test-ci-wiring (carried; Lenia rides existing testpaths CI); **first-SIM-friction-portfolio-scale bank (5 items)**: #1 tolerance-budget golden-cap shape; #2 packages/-vs-continuous-ca/ RESOLVED-ON-EVIDENCE; #3 test_sim_shells.py rewrite RESOLVED; #4 PBT invariant mathematical-ungroundedness RE-DECLARED; #5 R2 LFS mirror EOF SURFACED.
- **First-SIM pipeline-validation verdict (charter §1.1 + R-11):** ✅ VALIDATED-WITH-5-FRICTIONS-SURFACED. The testkit + golden + tier-3 + CI + LFS/R2 + PBT + perf-ledger + spec-ref + per-sim CI job + per-category tolerance/determinism row + 13-gate pipeline ACCOMMODATED Lenia end-to-end. All 13 sim-acceptance gates PASS at Stage 1b. The 5 friction items are LOUD-NAMED portfolio-scale signals for every later Phase-3 SIM. None forced an in-scope hard STOP.
- **Sub-phase: COMPLETE.** Third Phase-3 sub-phase landed. The FIRST Phase-3 SIM is in place. Next: operator pushes `v0.2.4-sub-phase-phase-3-lenia`. Subsequent Phase-3 sub-phases (rigid-body, cloth, NCA, PINN, 3DGS-MPM, common-warp-maturation, phase-3-landing) become dispatchable; the 4 banks (L-3DGS-1, SIBLING-FIXTURE-LFS, integrity-meta-test-ci-wiring, first-SIM-friction-portfolio-scale) carry forward.

## r2-credentials-durability-fix (focused infrastructure pass) — 2026-05-28 — CONFIRMED

- **Stage:** focused infrastructure hygiene fix (NOT a sub-phase; NOT tagged). Mirrors the steady-state-hygiene posture of `sub-phase-phase-2-cleanup`. Closes the recurring failure mode where the per-Phase-3-sim Stage-1c LFS push of a net-new `.h5` fixture EOFs against R2 (banked as friction-portfolio item #5 at lenia landing, predecessor at common-3dgs Stage-1c paste-then-vanish).
- **Landed at SHA:** `ce0d65a06fb86094b6d9693fbb43fe8e8fcea04e` (chain: chore — local lfs-s3 bootstrap + README durable-creds section + banked §Q convention → ci — wire R2 secret consumption into `python-strict` + `cpp-strict` selective-LFS-fetch steps → docs — fix audit + this entry → Convention #12 SHA back-fill). Trunk-based to `main`; no PR; **no tag** (steady-state hygiene).
- **Verdict:** CONFIRMED. Anchor probe clean (integrity at HEAD `beac1fd` = 0 HARD_FAIL / 14 SOFT_WARN, with L-R2CD-1 banked observation that the byte-digest has drifted from `c19492ad…d22cb52` to `688bc195…6de127ff` — invariant counts preserved, byte-identity citation outstanding). verify_evidence 24/24 PASS across all phase-3 + lfs-architecture-landing + phase-2-cleanup-stage-2 audits. I7 holds (no tag created).
- **Two surfaces fixed:**
  - **(a) CI runners** — `.github/workflows/python-strict.yml` job `python-strict` and `.github/workflows/cpp-strict.yml` job `cpp-strict` previously pulled LFS via plain GitHub-LFS despite `tools/lfs/README.md` claiming they fetched via R2. Both now source `tools/lfs/setup-lfs-s3.sh` (with a same-step env block of the four `secrets.R2_*` values) before the selective `git lfs pull`, with a conditional-fallback shape that preserves D4 (GitHub-LFS) for fork PRs (where GitHub doesn't propagate secrets). YAML round-trips clean.
  - **(b) Local agent session** — created `~/.config/bit-physics/r2-credentials.env` (mode 600, OUTSIDE the repo tree, six vars — bucket + account-id + endpoint + region + two secret values pasted inline by the operator and never echoed/logged/committed), added `tools/lfs/setup-lfs-s3-local.sh` as a sourceable wrapper that chains into the unchanged `setup-lfs-s3.sh`. Idempotent. Refuses execution / world-readable creds / missing vars. The lfs-s3 standalone transfer agent is now wired into this clone's `.git/config`.
- **R2 back-fill:** lenia OID `6c313a5da53dd341f73accdb7c369564451ccd475fa290c026360e3f39890062` (74 992 B) pushed to R2 via `git lfs push --object-id origin --stdin` (rc=0). common-3dgs OID `2087402de9…` re-pushed as a non-destructive reachability probe (rc=0; idempotent content-addressed re-PUT). **STOP-LFS surfaced at the lenia landing is now CLOSED.**
- **I5 re-verification:** M4-style sweep with fresh `mktemp -d` `lfs.storage=` per ref (so every byte-check is genuinely from R2, not local cache) — **159/159 PASS, 0 FAIL** across HEAD (33), `v0.2.0-phase-2` (31), `v0.2.1-sub-phase-lfs-architecture` (31), `v0.2.2-sub-phase-phase-3-common-3dgs` (32), `v0.2.3-sub-phase-phase-3-render-similarity` (32); `v0.0.0-phase-0` + `v0.1.0-phase-1` vacuous (pre-LFS).
- **§Q banked convention:** `docs/conventions/sub-phase-conventions.md` §Q (`Q.1`–`Q.5`) — every LFS-touching sub-phase Stage 0 runs `source tools/lfs/setup-lfs-s3-local.sh` as its first action after the anchor probe (so a missing/expired R2 token surfaces as a STOP-LFS-PUSH at anchor time, not at Stage-1c push time). CI workflow-author rule documented for any new LFS-pulling job (mirror the same `env`-block + conditional-source-block shape). Back-fill obligation made explicit: every new `.h5` ends up in R2 by the sub-phase landing.
- **Operator-visible:** push the four commits to `origin/main` (agent push is authorized per dispatch, no tag); next post-merge `python-strict` / `cpp-strict` runs print `lfs fetch route: R2 …` in the *Selective LFS fetch* step (operator-confirmable surface — the agent has no GitHub-Actions secret read scope by design).
- **Banked / forward-routed:**
  - **L-R2CD-1** (integrity-digest carry-forward) — recent landing audits cite `c19492ad…d22cb52` as byte-identical though actual digest at HEAD is `688bc195…6de127ff`; 0-HF / 14-SW invariant intact; routes to a future `phase-2-cleanup`-style audit-citation-hygiene cluster.
  - **L-R2CD-2** (`tools/lfs/r2-bulk-upload.sh` UNION_REFS staleness) — fixed-ref-set hard-codes lfs-architecture-Stage-1c surface; Phase-3 SIM tags (`v0.2.2-…common-3dgs`, `v0.2.3-…render-similarity`, pending `v0.2.4-…lenia`) not enumerated; routes to a follow-up generalization.
  - **L-R2CD-3** (`r2-sweep-proof.yml` ref-set staleness) — same fixed-set shape; routes to the same generalization pass.
- **Next:** Phase-3 sub-phase dispatch can resume (rigid-body / cloth / NCA / PINN / 3DGS-MPM / common-warp-maturation). Every LFS-touching Stage-0 inherits §Q.3 (the bootstrap is the new first-action obligation).
