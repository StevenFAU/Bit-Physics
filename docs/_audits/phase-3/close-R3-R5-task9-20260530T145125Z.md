---
date: 2026-05-30T14-51-25Z
author: phase-3 close campaign — R3/R4/R5 + corrigenda + task-9 (Claude Code)
subject: "Phase-3 close R3 (hollow paths) + R4 (landing hygiene) + R5 (doc minors) + corrigenda A-1..A-7 + task-9 (common-warp maturation) — landing audit"
verdict: R3-R5-CORRIGENDA-TASK9-LANDED
head_sha: 4523a8dfa71e65c830d15f5526a292b0d0aaa0de
prior_phase_tag: v0.2.0-phase-2
integrity_invariant: "0 HARD_FAIL / 14 SOFT_WARN"
integrity_digest_at_head: 45eed4cacb64b711c461f6e3b76958a646e6b0517302c3921bce2f18ef3018d2
invariants_at_head: [I1, I2, I3, I4, I5, I6, I7]
tag: none
evidence_paths:
  - .github/workflows/audit-append-only.yml
  - tools/integrity/integrity/scripts/replay_failing_tests.py
  - docs/conventions/sub-phase-conventions.md
  - docs/spec-amendments-proposed.md
  - docs/architecture.md
  - docs/phases/phase-3-plan.md
---

# Phase-3 Close — R3 + R4 + R5 + corrigenda + task-9

## R3 — hollow / unwired paths (LANDED)
- **B-1** append-only glob: the feed filter `grep '\.ledger\.md$'` matched 0/576
  files (the central trust invariant never enforced). Dropped the grep → guards
  every `docs/_audits/` file present at the prior tag. **Proven**: a one-char
  edit to an existing audit file is now REJECTED (`fail=1`); with the filter
  dropped the live tree holds (576 files, 0 violations). Commit `8a0aeb3`.
- **gate-3 hash robustness**: `normalize_pytest_output` now also normalizes the
  pytest/pluggy/Python version trio + interpreter and collapses `rootdir`/
  `cachedir` generically — gate-3/13 replay is byte-stable across a pytest bump
  or a different checkout (raw evidence keeps the values; only the hash is
  canonical). +2 tests. Commit `735045a`.
- **M-2** branch protection: GitHub repo SETTING — agent has no admin API; on the
  operator punch-list (NOT attempted).

## R4 — landing hygiene (LANDED, with one surfaced)
- **N-1** stub banners: stripped the Stage-1a STUB/SKELETON banners + resolved
  every `TODO(Stage-*)` on the 5 landed sims (lenia, ising, neural-ca, pinn,
  3dgs-mpm); source-pattern fix = a mandatory "spec-ref de-stub check" added to
  the Stage-2 landing row in sub-phase-conventions. Commit `275669d`.
- **N-2** re-captured gate-3 evidence: both task-7/8 evidence bodies are
  correctly anchored at HEAD (task-7 `failing_tests_output_hash: 49c865ad`,
  task-8 `evidence_hashes: 6053e228`, both == live .txt shas). The immutable
  commit footer can't be back-filled; documented the durable rule (landing-audit
  pin is authoritative) in the evidence README. Commit `b198035`.
- **M-7** filename convention (SURFACED, reverted): renaming the 5
  `task-N-*.md` landing audits to the `sub-phase-…-landing-<UTC>` form regressed
  a referencing audit's `verify_evidence` (8/0 → 6/2 — the historical `head_sha`
  can't follow a rename) and would also dangle an append-only-locked
  `evidence_paths` entry. Per HARD RULE 2 (fix fights an invariant), reverted to
  the exact 0/14 + 8/0 baseline; **operator punch-list** (apply prospectively in
  Phase 4, not retroactively).

## R5 — doc minors (LANDED, batched)
M-9 (preflight refs), M-10/m-11 (ten-gate notes), M-16 (eleven→fourteen-gate),
m-8 (integrity/integrity/scripts path), m-2 (§D.10→§D.9), m-3 (§1.6.7→§1.6.6).
Commit `129125e`. **Surfaced, not applied:** n-2 (neural-ca-python tolerance key
is intentional — a test consumes it verbatim); n-3 (particle-fluids plural =
cross-cutting dir rename); **n-4 — LEDGER PREMISE FALSIFIED** (the real tool
default is `captures/pinn-poisson` with NO `-ref`; adding it would make the doc
disagree with the code — discipline #8 trust-live-repo). Lower-value remaining
minors (m-4/m-12/m-13–17/m-19/n-1/n-10) deferred to a follow-up batch.

## Corrigenda A-1..A-7 + M-6 (LANDED, commit `da61e86`)
All 7 applied (spec freeze lifts at close); line numbers had drifted, re-located
by content. **M-6 reconciled with A-1**: canonical = `articulated-pedagogical`
(the LOCKED package leaf + §11.4 + §D.2.3 + 30 files); architecture.md is now
internally consistent (0 `rigid-body-pedagogical`). The cross-surface rename
(captures dir / CI job / plan sim-name / landing audit) is a heavy LFS+append-only
job (M-7 class) — **surfaced for the operator**. A-3 corrected the plan Bender pin
to the SHA task-5 actually vendored (`aa62c44f` = 2.2.0, MANIFEST-verified).
Marked APPLIED in spec-amendments-proposed.md (commit `c78cb95`).

## task-9 — common-warp maturation (LANDED, commit `4523a8d`)
Rule-of-three survey (6 consumers): the socket surface is MATURE. Delivered:
- **§Q.6** (banked lever 3): LFS-R2 push-and-VERIFY convention (`git push` exit 0
  ≠ R2 has the OIDs; `--object-id --stdin` not the EOFing args form).
- (gap c) `parse_payload_key` tests (+4) — the untested public ValueError contract.
- (gaps b/d) warp.md §6.4: documented the de-facto capture-key API + STABILITY
  tiers (stable sockets vs experimental f32 surface, 0 landed consumers).
- **Banked with plan** (not forced — landed-sim regression risk): the
  `write_frames_capture` rule-of-three promotion (STRONG; rewrites 6 sims' gated
  capture paths → additive-helper-then-migrate, a dedicated Phase-4 refactor).
- **Lever 1 (NCA matched-RNG, 144 dB):** larger than common-warp maturation (a
  neural-ca sim change) — BANKED for a dedicated neural-ca gate-14 follow-up.
- **Lever 2 (pinn-CI two-tier mirror to NCA):** NCA's `test_train_convergence` is
  a 300-step smoke (~15-20 min) vs pinn's ~70-min full train, so the tax it would
  shave is small — RECOMMENDED-OPTIONAL, surfaced for the operator (lower priority
  than pinn's was).

## Verification
- integrity `0 HARD_FAIL / 14 SOFT_WARN` at HEAD `4523a8d`; digest `45eed4ca…`
  (unchanged since R1-R2 — the doc/test changes add no findings).
- meta-test 80/80 (incl. the new cat3 + cat4 + replay tests).
- 20 campaign commits 4ee0ea9..4523a8d, all pushed; CI green throughout (post-R0
  integrity regression resolved at `6cb56b8`/`6831360`).
