---
date: 2026-05-29
author: phase-3 neural-ca execution Stage 1b-B (Claude Code)
subject: Phase 3 task-6 neural-ca — STAGE 1b-B WGSL inference (Stack B) + checkpoint conversion (exact round-trip) + B-inference capture + gate-13 reproduction GREEN
verdict: CONFIRMED
head_sha: 8970534c5536f5317bd65aa83985d848c7845388
prior_sub_phase_landed_at: 86b0aa5
prior_phase_tag: v0.2.0-phase-2
integrity_invariant: 0 HARD_FAIL / 14 SOFT_WARN
integrity_digest_at_head: b7460150b61213bb8909659ffc7b103d846ad782f5062d272f2ce572e6abb15e
evidence_paths:
  - packages/neural-ca/python/neural_ca/convert_checkpoint.py
  - packages/neural-ca/python/neural_ca/reference/nca_numpy.py
  - packages/neural-ca/python/neural_ca/wgsl_harness.py
  - packages/neural-ca/typescript/src/nca_inference.wgsl
  - packages/neural-ca/python/tests/test_wgsl_inference_reproduction.py
  - tools/testkit/golden/checkpoints/neural-ca-emoji-disk-wgsl.layout.json
  - captures/neural-ca-ref/growing-emoji-64sq-seed42-step1000-wgsl.json
evidence_hashes:
  packages/neural-ca/python/neural_ca/convert_checkpoint.py: sha256:fa01aa8aab42bccd697a23b8ed4f58ee06b64465cdda0f93f5d4a648d2e65e2c
  packages/neural-ca/python/neural_ca/reference/nca_numpy.py: sha256:9923de00a0fe9e407b591de14df39de5041b0118fe7030f413d366ec682f2e12
  packages/neural-ca/python/neural_ca/wgsl_harness.py: sha256:b3fb312ab56ebaa96cc257cad08d30299fcc7c02bb9c6f27872b619568859e6d
  packages/neural-ca/typescript/src/nca_inference.wgsl: sha256:9021fd3d6c16efc9eb9b8dad0a86a2469159e2b66d3b976297d61a10f59a530d
  packages/neural-ca/python/tests/test_wgsl_inference_reproduction.py: sha256:89e197d6c0fe16658bd23f217200d63b909eeb942321d15bfceb0b644ef3641a
  tools/testkit/golden/checkpoints/neural-ca-emoji-disk-wgsl.layout.json: sha256:113b870431e233cb84252f6190a159b832817c573d287b45f0d8daf55fb22f14
  captures/neural-ca-ref/growing-emoji-64sq-seed42-step1000-wgsl.json: sha256:363ecd483685478d798d71e22b9d34bb662420bfae0bd4b73ee196e35f990d62
---

# Phase 3 — sub-phase neural-ca (task-6) — Stage 1b-B audit

> Stack-B (custom WGSL) inference. The checkpoint converts to a WGSL-loadable
> artifact (round-trip bit-identical), the forward shader runs on a GPU host, the
> B-inference capture is committed, and the NumPy-oracle reproduction (gate-13)
> is GREEN. Verdict **CONFIRMED** — Stage 1c (gate-14 D↔B render-similarity)
> unblocked.

## ACTION 1 — checkpoint conversion (D-CHECKPOINT-CONVERSION, EXACT)

`convert_checkpoint.py` reads the `.safetensors` and emits
`neural-ca-emoji-disk-wgsl.bin` (flat little-endian f32, 33280 B, LFS) +
`…-wgsl.layout.json` (per-tensor offset/shape sidecar). **Round-trip
weights-equality: BIT-IDENTICAL** (`np.array_equal` for `w1.weight`/`w1.bias`/
`w2.weight` pre/post) — no quantization, no dtype change. A lossy conversion
would be HARD RULE 2; it is exact.

## ACTION 2 — WGSL forward shader (Stack B, § 2.9 custom WGSL)

`packages/neural-ca/typescript/src/nca_inference.wgsl` — two compute entry
points (`update`: perception conv + update MLP + PCG fire mask;
`mask`: two-pass pre&post alpha alive-masking). Cell-major layout
`(y*grid+x)*CN+c`; weights from the converted flat buffer. Matches the NumPy
oracle + the PyTorch model algorithm.

## ACTION 3 — WGSL inference on a GPU host (§ 7.8, local-only)

`wgsl_harness.py` drives `nca_inference.wgsl` via **wgpu-py** (wgpu-native /
Vulkan) on the **AMD RX 6800 XT (RADV)** — a genuine WGSL-on-GPU execution.
**§0.3 SHIFT-from-discovered:** the environment has no Node WebGPU runtime (only
`@webgpu/types`, no deno), so the committed capture is generated by executing the
SAME committed `.wgsl` via wgpu-py; `index.ts` remains the Phase-5 deploy path
(spec § 7.8 keeps WGSL inference local-only — CI never runs it). Produced
`captures/neural-ca-ref/growing-emoji-64sq-seed42-step1000-wgsl.{h5,json}` (LFS;
21 frames; `claimed=epsilon`).

## ACTION 4 — gate-13 reproduction (CI-visible NumPy oracle)

`test_wgsl_inference_reproduction` inverts RED→GREEN: the pure-NumPy oracle
(`nca_numpy.py`, sharing the WGSL **PCG fire-mask RNG**) reproduces the committed
GPU capture to **max_abs_diff = 3.5e-6** over 1000 steps (step 50: 2.4e-7; step
1000: 3.2e-6) — well under the 1e-4 tolerance. The PCG fire masks are
bit-identical; the only divergence is GPU-vs-CPU f32 conv-reduction order. This
is the ising pytest-against-committed-capture + NumPy-oracle precedent (CI reads
the committed capture; never runs WGSL). mypy --strict + ruff clean.

## ACTION 5 — integrity invariant

`0 HARD_FAIL / 14 SOFT_WARN` held; digest `b7460150…b15e` (unchanged; §R measured).

## Verdict

**CONFIRMED.** Checkpoint conversion exact (round-trip bit-identical); WGSL
forward shader runs on the real GPU; B-inference capture committed; gate-13
reproduction GREEN (oracle ↔ GPU capture 3.5e-6). **Stage 1c (gate-14 D↔B
render-similarity measure + lock, perf, schema-corpus, CI jobs) unblocked.**
