---
date: 2026-05-23
author: mpm-multimaterial-sub-phase-agent
artifact: sub-phase
artifact_id: sub-phase-mpm-multimaterial
stage: 2-landing
subject: "MPM-Multimaterial sub-phase (gates 4-13 implementation; first hybrid-pg sim + first IC-5+IC-6 dual-Tier-2 consumer + second numba-using sub-phase + third-and-final Task-0.4 sub-phase + LAST per-sim Phase 1 sub-phase closing the 9-sim implementation arc) landing audit"
verdict-state: CONFIRMED
head_sha: bd89e78b68a07e07fce21f77d811404fcf93d256
head_sha_at_checkpoint: bd89e78b68a07e07fce21f77d811404fcf93d256
parent_audits:
  - docs/_audits/phase-1/landing-2026-05-20T14-18-00Z.md
  - docs/_audits/phase-1/sub-phase-closed-form/landing-2026-05-20T16-48-00Z.md
  - docs/_audits/phase-1/sub-phase-agent-based/landing-2026-05-20T18-20-39Z.md
  - docs/_audits/phase-1/sub-phase-replay-tool-hotfix/repair-2026-05-20T19-06-35Z.md
  - docs/_audits/phase-1/sub-phase-continuous-ca-rd3d/landing-2026-05-20T19-49-51Z.md
  - docs/_audits/phase-1/sub-phase-numba-integration/landing-2026-05-21T11-22-24Z.md
  - docs/_audits/phase-1/sub-phase-particle-fluids-sph-water/landing-2026-05-22T01-42-51Z.md
  - docs/_audits/phase-1/sub-phase-mutation-script-hotfix/repair-2026-05-22T02-57-31Z.md
  - docs/_audits/phase-1/sub-phase-conventions-consolidation/landing-2026-05-22T03-25-55Z.md
  - docs/_audits/phase-1/sub-phase-eulerian-smoke/landing-2026-05-22T13-30-00Z.md
  - docs/_audits/phase-1/sub-phase-git-lfs-migration/landing-2026-05-22T21-04-05Z.md
  - docs/_audits/phase-1/sub-phase-lattice-boltzmann-d3q19/landing-2026-05-23T00-41-15Z.md
  - docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-0-checkpoint-2026-05-23T01-33-19Z.md
  - docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-1-checkpoint-2026-05-23T02-14-23Z.md
evidence_paths:
  - docs/phases/sub-phase-mpm-multimaterial.md
  - docs/conventions/sub-phase-conventions.md
  - docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-0-checkpoint-2026-05-23T01-33-19Z.md
  - docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-1-checkpoint-2026-05-23T02-14-23Z.md
  - docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-2-evidence/anchor-recheck-2026-05-23T02-26-44Z.txt
  - docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-2-evidence/test-sweep-positive-2026-05-23T02-27-10Z.txt
  - docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-2-evidence/test-sweep-negative-2026-05-23T02-29-15Z.txt
  - docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-2-evidence/cat3-check-2026-05-23T02-30-30Z.txt
  - docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-2-evidence/integrity-cats-2026-05-23T02-32-17Z.txt
  - docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-2-evidence/verify-evidence-2026-05-23T02-32-28Z.txt
  - docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-2-evidence/gate13-replay-mpm-2026-05-23T02-34-27Z.txt
  - docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-2-evidence/append-only-2026-05-23T02-34-58Z.txt
  - docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-2-evidence/mutation-pathA-output-2026-05-23T03-26-43Z.txt
  - tools/testkit/mutation/mutmut-config.toml
  - tools/testkit/mutation/sub-phase-mpm-multimaterial-2026-05-23T03-28-30Z.json
  - tools/testkit/failing-tests-evidence/mpm-multimaterial-2026-05-20T13-48-06Z.txt
  - tools/testkit/failing-tests-evidence/mpm-multimaterial-implemented-2026-05-23T02-13-16Z.txt
  - tools/testkit/golden/tables/hybrid-pg/mls-mpm-shape-functions.json
  - tools/integrity/integrity/cat3_numerical/golden_values.py
  - captures/mpm-ref/drop-impact-128cube-seed42-step500.h5
  - captures/mpm-ref/drop-impact-128cube-seed42-step500.json
  - docs/perf-ledger.md
  - CHANGELOG.md
evidence_hashes:
  docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-2-evidence/anchor-recheck-2026-05-23T02-26-44Z.txt: sha256:b885d887953cc12f7bfd58afb239dd48958fdfca9e853449ba26ac727a2f05c6
  docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-2-evidence/test-sweep-positive-2026-05-23T02-27-10Z.txt: sha256:20182a5ee58582a375aaf960e167868fb7801c891d17811c951ce26c5ddbf484
  docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-2-evidence/test-sweep-negative-2026-05-23T02-29-15Z.txt: sha256:2d6c51104fe1e44f782bd108743523c6f5c8d6e756e81212754834d34c8b476b
  docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-2-evidence/cat3-check-2026-05-23T02-30-30Z.txt: sha256:81b8c1a613bd1e46497de08f9eee627b83ca8f8e2938cfc2b3385d2c2b81dcf3
  docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-2-evidence/integrity-cats-2026-05-23T02-32-17Z.txt: sha256:810cd6e3cac165df11ab166f0b4cc08cdf33b5c3d40240855f176ff123411f98
  docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-2-evidence/verify-evidence-2026-05-23T02-32-28Z.txt: sha256:9973ff5573113e66bda8f88bc6d961c5220036db425cf3b700d9b4cc0412bc25
  docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-2-evidence/gate13-replay-mpm-2026-05-23T02-34-27Z.txt: sha256:04bb6e2cbce60ea726d637601eeebadcd1e8f971eb7a62ea8a78bf0a7d4756f2
  docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-2-evidence/append-only-2026-05-23T02-34-58Z.txt: sha256:f1bafb67ceb1b1c271438388c516fd8cfc56de7dc3be20f796f739708ce3cc06
  tools/testkit/failing-tests-evidence/mpm-multimaterial-2026-05-20T13-48-06Z.txt: sha256:a57251a19b28888e664402e9c92eb681fa17719be7e156154df3d681bb9edf94
  tools/testkit/failing-tests-evidence/mpm-multimaterial-implemented-2026-05-23T02-13-16Z.txt: sha256:d0bd6f9c5ecf6a1ee267a43ada521c1ca60091d7cb403fb999dfedb5d2ce34e7
  tools/testkit/golden/tables/hybrid-pg/mls-mpm-shape-functions.json: sha256:9ccab8888e32e2f23e6a7b8b21d87e1da8e401a3ff5939363a4b9cb3e2378997
  captures/mpm-ref/drop-impact-128cube-seed42-step500.h5: sha256:73e00d0976a663a8e9c1de87334cba701a385ae9b044ead929eac8b540b5ebae
  docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-2-evidence/mutation-pathA-output-2026-05-23T03-26-43Z.txt: sha256:97c97dbb0151b7d679e9e53c1b64fe05361f7ef309d68ad6a22772355b00693f
  tools/testkit/mutation/sub-phase-mpm-multimaterial-2026-05-23T03-28-30Z.json: sha256:2d68f741c8da6cb393e588ce6ac4fbc7a403a110d68fe01c9fc21bb1df89ac34
---

# MPM-Multimaterial Sub-Phase — Landing Audit

## 1. Sub-phase scope summary

(FACT — `docs/phases/sub-phase-mpm-multimaterial.md` §§ 1.1, 1.2,
2; sealed at `4f64d78`.) **Seventh and LAST** per-sim implementation
sub-phase under spec-Phase-1 per Phase 1 audit § 15; lands gates 4-13
for `mpm-multimaterial` at the Python NumPy + numba reference stack
(Stack-D scope override per operator Item 1 routing; spec § 5.5
declared target is Stack-D Taichi, deferred to spec-Phase-2 entry as
a focused infrastructure sub-phase mirroring the
sub-phase-numba-integration pattern). MLS-MPM Hu-2018 quadratic-B-spline
kernel; multimaterial constitutive surface declared, single neo-Hookean
material populated at this sub-phase per algebraic.md § 3 ("Phase 2+
populates the constitutive-model table").

This sub-phase is the **first** to:

- Ship a **hybrid-pg category sim** (spec § 5.5; MLS-MPM stencil).
- **Consume BOTH IC-5 (particle) AND IC-6 (vector_field) Tier 2
  diagnostics** in gate 7 — particle count_invariance +
  momentum_conservation drift advisory + vector_field grid-momentum
  L1 surrogate. sph-water consumed IC-5 only; eulerian-smoke + LBM
  consumed IC-6 only; MPM exercises the hybrid surface.
- Be the **second numba-using sub-phase** (after sph-water; LBM did
  NOT use @njit). Numba cache-via-source-hash propagation through
  mutation testing carries forward per sph-water Stage 2 N3 precedent.
- Be the **third sub-phase exercising conventions doc § N Task 0.4**
  as established discipline (eulerian-smoke first, LBM second; MPM
  the third — **third consecutive single-session-ready Stage 1**
  anchoring the § N graduation recommendation).
- Land **first P26 worked-example actually triggered in the wild**
  (R-MPM-3 base-node off-by-one bug surfaced in Stage 1 SHIFT S1
  during diagnostic-tier dynamics trace; plan § 9 P26 cause-2
  description matched the observed failure mode verbatim).
- **Close the per-sim Phase 1 implementation arc** — after MPM lands
  CONFIRMED, all 9 sims through gates 4-13 are GREEN; spec-Phase-2
  (cross-stack replication) becomes dispatchable at `v0.2.0-phase-2`.
- **Close the Cat 3 additive-pickup arc** — `_SUBDIRS_PICKED_UP`
  grows to 5 entries `(closed-form, agent-based, particle-fluids,
  lattice, hybrid-pg)` covering every per-sim category with a
  closed-form golden table.

Implementation lands at `feat(mpm-multimaterial-stage1)` commit
`9bd770e` per the Hu-2018 MLS-MPM pipeline:

> **Sample particles** (deterministic blob; `numpy.random.default_rng(seed)`
> uniform-in-sphere by rejection; sorted-particle order P24 inheritance) +
> **P2G with stress-divergence force injection** (lex 27-cell stencil; the
> Hu 2018 88-line reference's affine + stress fold pattern;
> `@njit(fastmath=False, cache=True)`) +
> **grid update** (gravity body force, sticky floor BC at z-index 4,
> axis-clamp walls; lex over (i, j, k); `@njit(fastmath=False, cache=True)`) +
> **G2P with APIC affine reconstruction** (same lex 27-cell stencil; affine
> velocity matrix C reconstructed with the MLS-MPM quadratic-kernel
> factor `4/dx²`; `@njit(fastmath=False, cache=True)`) +
> **deformation gradient update** F^{n+1} = (I + dt·C) F^n +
> **neo-Hookean stress computation** σ = μ(FF^T − I) + λ log(J) I per
> Hu 2018 § 5 +
> **symplectic-Euler advection** with stencil-safety clamping.

One LFS-tracked canonical capture per Appendix D § D.2.3 (cadence-50
per Stage 0 Task 0.4 routing):

- `drop-impact-128cube-seed42-step500.h5` — 1M particles in a 0.15-radius
  blob falling under gravity onto a sticky floor at z-index 4; 500 steps;
  cadence-50 (11 frames committed); 1.13 GB at float64; 158.052 s
  wall-clock; payload sha256 `73e00d09…b5ebae`.

## 2. Stage 0 deliverable summary (pre-flight; commits `399d32e` → `0c71bab`)

(FACT — `docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-0-checkpoint-2026-05-23T01-33-19Z.md`.)

| Task | Result |
|---|---|
| 0.0 — Cross-phase replay (`v0.1.0-phase-1`, 8-gate canonical set) | **PASS**; sha256 byte-identical to bit-identity invariant `9399fc33…909f34` (15th invocation; conventions doc § D.3). |
| 0.1 — Tolerance-budget carryover | `[phase].phase = "sub-phase-mpm-multimaterial"`; NO `[budgets.*]` widening; commit `399d32e`. |
| 0.2 — Phase 1 evidence sha256 reverify | **PASS** — `a57251a1…81bb9edf94` matches Phase 1 landing audit value verbatim. |
| 0.3 — MLS-MPM golden + derivation + generator re-anchor | **PASS** — pre-lift sha256s recorded; `--verify` GREEN at 1e-15; no MMS to re-anchor (linear-elasticity MMS deferred per sim-spec-ref § 6.1). |
| **0.4 — Canonical-descriptor scope-analysis** (THIRD practical exercise of conventions doc § N as established discipline) | **FITS with Path A (numba)** — naive Python 11.95 h projected (STRUCTURAL ALARM at 1-hour threshold); numba @njit 8.7 min measured at 2M particles canonical-scale (21.8 min with 2.5× production-correction; under 1-hour threshold). Numba-vs-naive multiplier **82.4×**. Cadence-50 selected (full-cadence 79.6 GiB → cadence-50 1.6 GB committed; ~70% of 2 GB W1 ceiling). No per-sub-phase descriptor contraction (Path B) required. |

Stage 0 close + Convention #12 SHA back-fill at `8a4ba56` + `0c71bab`.

## 3. Stage 1 deliverable summary — gates 4-13 GREEN

(FACT — `feat(mpm-multimaterial-stage1)` commit `9bd770e` + checkpoint
at `53349c1` + back-fill at `e38223d`.) Single sub-bundle commit;
**single-session Stage 1 (THIRD consecutive)** — eulerian-smoke first,
LBM second, MPM third; anchors conventions doc § N graduation
recommendation strongly.

### 3.1 Per-gate status — MPM at HEAD

| # | Status | Notes |
|---|---|---|
| 4 | GREEN | Reads-through to gate 5. |
| 5 | GREEN | **MLS-MPM quadratic-B-spline golden** — 2 tests × 13 sample/PoU values at absolute 1e-15 against `tools/testkit/golden/tables/hybrid-pg/mls-mpm-shape-functions.json` (Stage 0 baseline sha256 `4142dda2…fec9a48`; post-Stage-2 Decision A lift sha256 `9ccab888…378997`). NO MMS arm at this sub-phase (linear-elasticity MMS declared deferred per sim-spec-ref § 6.1; MPM is the **third golden-only sim** through Phase 1 alongside closed-form, agent-based, sph-water). |
| 6 | GREEN | Tier 1 NaN/Inf scan over canonical-trajectory diagnostics. |
| 7 | GREEN | Tier 2 **hybrid surface — FIRST sub-phase consuming BOTH IC-5 AND IC-6**. IC-5 particle: `check_count_invariance` (1M particles fixed across all 11 frames) + `check_momentum_conservation_drift` advisory (gravity-driven; bounded by free-fall expectation). IC-6 vector_field: `check_circulation_grid_mom_l1` (L1 norm of grid momentum field; finite + bounded). |
| 8 | GREEN | Cat 1 citations: Hu 2018 (DOI 10.1145/3197517.3201293), 88-line MLS-MPM reference (citation-only per R8 amendment — no vendored code), Steffen-Kirby-Berzins 2008 (DOI 10.1002/nme.2360). |
| 9 | GREEN | Cat 2 public API per probe § 5: `reference.shape_functions.{N, partition_of_unity_sum}` + `reference.mls_mpm.{p2g, p2g_with_stress, g2p, grid_update, deformation_update, compute_particle_stresses, advect_particles}` + `sim.{sim_runner_seeded, sim_runner_diagnostic}` + `invariants.{mass_conservation_p2g_g2p, partition_of_unity_b_spline}`. |
| 10 | GREEN | **ONE** LFS-tracked canonical capture per Appendix D § D.2.3 — `drop-impact-128cube-seed42-step500.{h5,json}` (1M particles × 128³ grid × 500 steps; cadence-50; 1.13 GB committed; payload sha256 `73e00d09…b5ebae`; wall-clock 158.052 s). |
| 11 | GREEN (over-achieved bit-exact) | `test_run_twice_epsilon_diff` GREEN via `sim_runner_diagnostic` (16³ × 5K particles × 50 steps). Spec declares `epsilon-same-stack-same-hw` for Stack-D Taichi (P2G atomic scatter-add breaks bit-exact at Stack-D Taichi scope); Stack-D Python NumPy + numba reference at this sub-phase over-achieves to bit-exact-same-stack-same-hw (no atomics — single-threaded `@njit(parallel=False)` with sorted-particle iteration + lex 27-cell P2G/G2P stencil + matching G2P interpolation order). Conventions doc § F.4 informational over-achievement; Phase-2+ Stack-D Taichi target stays `epsilon-same-stack-same-hw`. |
| 12 | GREEN | 2 PBT invariants — `mass_conservation_p2g_g2p` (Hypothesis 50 examples × random 1-50 particles in 16³ grid; ∑m_p exactly preserved under P2G partition-of-unity) + `partition_of_unity_b_spline` (Hypothesis 50 examples × random p ∈ [-100, 100]; ∑_{k∈{0,1,2}} N(p − (base + k)) = 1 within 1e-15). |
| 13 | GREEN | Perf-ledger first-landing row: `mpm-multimaterial | numpy-numba-reference | drop-impact-128cube-seed42-step500 | 158.052 | i7-12700KF-linux-6.17 | (this commit) | 2026-05-23`. |
| 13 (anchor) | GREEN | Worktree replay at SHA `9de8048` reproduces Phase 1 RED (4 `ModuleNotFoundError` collection errors for `mpm_multimaterial.{reference, sim, invariants}` modules). Stage 1 replay sha256 `08c25255…1227b8`; Stage 2 re-replay sha256 `04bb6e2c…d4756f2` (different sha256 from Stage 1 because pytest banner timestamps differ; failure-mode bit-identical — conventions doc § E "NOT load-bearing: full-text bit-equality of pytest output across replay"). |

### 3.2 Determinism-strategy declaration (conventions doc § F.1)

(FACT — 10-clause module docstring at `packages/mpm-multimaterial/mpm_multimaterial/sim.py` lines 1-105; cited verbatim in the `feat(mpm-multimaterial-stage1)` commit footer.) Ten clauses cover sorted-particle iteration (P24), deterministic 27-cell P2G stencil ordering (R-MPM-1 / P26 cause-1 mitigation), no atomic-scatter (single-threaded `@njit(parallel=False)`), matched G2P interpolation order, lex `grid_update` over (i, j, k), multimaterial volume-fraction tracking (R-MPM-2 / P26 cause-4 mitigation), fixed `base = floor(fx + 0.5) - 1` base-node convention per golden table `base_node_convention` field (R-MPM-3 / P26 cause-2 mitigation — see § 9.2 P26 retrospective), no global RNG state (P22), `@njit(fastmath=False, cache=True)` discipline per conventions doc § G (R-MPM-4 / P26 cause-3 mitigation), and Phase-2+ deferred Stack-D Taichi atomic-scatter-add + driver FMA fusion.

## 4. Stage 2 convergence commits

(FACT — `git log` at HEAD.)

| # | SHA | Subject | Purpose |
|---|---|---|---|
| 1 | `4724284` | `chore(mpm-multimaterial-stage2-cat3-anchors)` | Lift mls-mpm-shape-functions golden 1 → 4 discrete anchors (mirrors LBM `0f8ddde` precedent verbatim). |
| 2 | `9b19c26` | `chore(mpm-multimaterial-stage2-cat3-subdirs)` | Extend `_SUBDIRS_PICKED_UP` with `Path("hybrid-pg")` — five entries at Stage 2 close. |
| 3 | `6c2a340` | `chore(mpm-multimaterial-stage2-mutation-pathA-partial)` | B17 PATH-A fifth-and-final proof-point; per-target extension + **partial** baseline (R15 STOP-AND-SURFACE on mls_mpm.py; see § 7.6). |
| 4 | `283963f` | `docs(mpm-multimaterial-stage2-changelog)` | CHANGELOG additive entry under [Unreleased]. |
| 5 | `bd89e78` | `chore(mpm-multimaterial-stage2-landing-audit)` | This landing audit body (landing-audit closing commit). |
| 6 | `(this back-fill commit)` | `chore(mpm-multimaterial-stage2-sha-backfill)` | Convention #12 SHA back-fill on this audit's `head_sha` from placeholder → `bd89e78…04fcf93d256`. |

## 5. Closing-commit anchor re-check (Convention 7.9; Stage 2 step 2.1)

(FACT — `docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-2-evidence/anchor-recheck-2026-05-23T02-26-44Z.txt`
sha256 `b885d887…2f05c6`.)

| Artifact | Pre-Stage-2 sha256 | Post-Stage-2 sha256 | Status |
|---|---|---|---|
| `tools/testkit/golden/tables/hybrid-pg/mls-mpm-shape-functions.json` | `4142dda261826c87d93ba6f70e2658d94722a5010fa6512fe0cc134f49197e48` | `9ccab8888e32e2f23e6a7b8b21d87e1da8e401a3ff5939363a4b9cb3e2378997` | restructured via Decision A lift (commit `4724284`) — 1 packed → 4 discrete anchors; every citation preserved verbatim per § I.3 |
| `tools/integrity/integrity/cat3_numerical/golden_values.py` | `36b80fc7cb5c75cd89e2b3f8374196a7f0567f28ff30e2706b2bc95ab148c1c7` | (post-pickup; see § 7.1) | additive `Path("hybrid-pg")` extension via commit `9b19c26` |
| `tools/testkit/failing-tests-evidence/mpm-multimaterial-2026-05-20T13-48-06Z.txt` | `a57251a1…81bb9edf94` | unchanged | Phase 1 RED evidence; Convention A append-only protected |
| `captures/mpm-ref/drop-impact-128cube-seed42-step500.h5` | `73e00d09…b5ebae` | unchanged (LFS-tracked) | Stage 1 capture; Convention A append-only protected |

## 6. Regression / failing-tests sweep (Stage 2 step 2.2)

### 6.1 Positive targets — GREEN at HEAD

(FACT — `docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-2-evidence/test-sweep-positive-2026-05-23T02-27-10Z.txt`
sha256 `20182a5e…dbf484`.) Per-package pytest, mirroring conventions
doc § M.2 N1 ("one package at a time" — shared `tests.conftest`
import-path collision):

| Package | Tests passed | Status |
|---|---:|---|
| `strange-attractors` | 11 | GREEN |
| `mandelbulb-explorer` | 10 | GREEN |
| `boids-3d` | 10 | GREEN |
| `physarum` | 10 | GREEN |
| `reaction-diffusion-3d` | 8 | GREEN |
| `sph-water` | 22 | GREEN |
| `eulerian-smoke` | 10 | GREEN |
| `lattice-boltzmann-d3q19` | 9 | GREEN |
| `mpm-multimaterial` | **9** | **GREEN** |
| `tools/integrity/tests` | 51 | GREEN |
| `tools/diagnostics` | 93 | GREEN |
| `tools/testkit` | 52 | GREEN |
| `tools/testkit/numba_harness/tests` | 5 | GREEN |
| **TOTAL** | **300** | **all GREEN** |

### 6.2 Negative targets — Phase 1 RED evidence preserved

(FACT — `docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-2-evidence/test-sweep-negative-2026-05-23T02-29-15Z.txt`
sha256 `2d6c5110…34c8b476b`.) All 9 Phase 1 RED evidence files
sha256-preserved at HEAD:

| Sim | Phase 1 RED evidence sha256 | Status |
|---|---|---|
| strange-attractors | `c4f72e25…04cac63` | unchanged |
| mandelbulb-explorer | `d4a89d3e…b37e2ca0` | unchanged |
| boids-3d | `7d59ffdb…f6e39b7b` | unchanged |
| physarum | `8ee52dc7…8c043855` | unchanged |
| reaction-diffusion-3d | `b3165ab1…b2514b96` | unchanged |
| sph-water | `82fb91bc…40cf12b1f` | unchanged |
| eulerian-smoke | `c961dd22…14879f23a1` | unchanged |
| lattice-boltzmann-d3q19 | `c78de8be…b4b6ef3cd` | unchanged |
| **mpm-multimaterial** | **`a57251a1…81bb9edf94`** | **unchanged (gate-13 anchor stable)** |

## 7. Integrity check results (Stage 2 steps 2.3 + 2.4 + 2.5 + 2.6 + 2.7)

### 7.1 Cat 3 disposition — **Decision A two-commit lift for `hybrid-pg` subdir**

(FACT — commits `4724284` + `9b19c26`; mirrors closed-form + agent-based + sph-water + LBM Decision A two-commit pattern exactly per conventions doc § I.2.)

Operator Item 6 confirmed at dispatch. Pre-flight at Stage 2:
- `_SUBDIRS_PICKED_UP` at HEAD before lift: `(closed-form, agent-based, particle-fluids, lattice)` — 4 entries (unchanged since LBM landing).
- `tools/testkit/golden/tables/hybrid-pg/mls-mpm-shape-functions.json`: 1 `test_points` entry × 1 packed `independent_reference` block × 4 citations = **1 anchor** per conventions doc § I.3 anchor-count semantics. Below spec § 2.4 R9 floor of ≥ 3.

**Decision A two-commit lift executed:**

| Commit | Action |
|---|---|
| `4724284` — `chore(mpm-multimaterial-stage2-cat3-anchors)` | Mechanically restructured the 1 packed-citation block into **4 discrete `test_points` entries**, each with identical inputs + identical expected values + a distinct `independent_reference.source` citation: (1) hand-derivation; (2) Hu 2018 § 3 + 88-line reference; (3) Steffen-Kirby-Berzins 2008 § 3 Eq. (15); (4) Python re-derivation by generator. Every citation preserved verbatim per § I.3 ("mechanical restructuring, not new evidence"). Generator `--verify` GREEN post-lift at 1e-15. `test_quadratic_bspline_golden.py` (2 tests; consumes `test_points[0]` per the unchanged contract) GREEN. |
| `9b19c26` — `chore(mpm-multimaterial-stage2-cat3-subdirs)` | Extended `_SUBDIRS_PICKED_UP` additively with `Path("hybrid-pg")`. **Final state at Stage 2 close: `(closed-form, agent-based, particle-fluids, lattice, hybrid-pg)` — five entries. CLOSES the per-sim Phase 1 Cat 3 additive-pickup arc** — every sim category with a closed-form golden table is now recursed by Cat 3 (5/5 with-golden categories); `continuous-ca` + `volumetric-grid` remain NO-OP per RD-3D + eulerian-smoke MMS-only precedent. |

(FACT — `docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-2-evidence/cat3-check-2026-05-23T02-30-30Z.txt`
sha256 `81b8c1a6…2b81dcf3`.) Post-lift Cat 3 walker enumerates 8
tables across 5 picked-up subdirs:

```
tools/testkit/golden/tables/agent-based/boids-3agent-step1.json: anchors=3
tools/testkit/golden/tables/agent-based/physarum-deposit-step1.json: anchors=3
tools/testkit/golden/tables/closed-form/lorenz-structural.json: anchors=3
tools/testkit/golden/tables/closed-form/mandelbulb-de-samples.json: anchors=3
tools/testkit/golden/tables/cubic-spline-kernel.json: anchors=3
tools/testkit/golden/tables/hybrid-pg/mls-mpm-shape-functions.json: anchors=4  (POST-LIFT)
tools/testkit/golden/tables/lattice/d3q19-equilibrium.json: anchors=4
tools/testkit/golden/tables/particle-fluids/dfsph-density-evolution.json: anchors=3
```

All 8 tables clear the ≥ 3 R9 floor; `hybrid-pg/mls-mpm-shape-functions.json` is the 6th to lift to ≥ 4 anchors (after the closed-form / agent-based / sph-water / LBM lifts).

### 7.2 Full integrity sweep (Cat 1, 2, 3, 4, 5, X)

(FACT — `docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-2-evidence/integrity-cats-2026-05-23T02-32-17Z.txt`
sha256 `810cd6e3…23411f98`.)

```
summary: 0 HARD_FAIL, 13 SOFT_WARN
```

- **Cat 1 (citations)**: clean.
- **Cat 2 (public API)**: clean.
- **Cat 3 (golden values)**: 8 `AUDIT_LOG` rows (no Python evaluator registered for 8 algorithms — pickup-evidence; **new sixth row** for `mls-mpm-quadratic-bspline-1d-hu-2018` matches the LBM N1 + lattice pickup pattern; banked alongside the prior 5 algorithms for future evaluator-shim work).
- **Cat 4 (draft-time)**: clean.
- **Cat 5 (audit-links)**: 13 inherited `SOFT_WARN` rows (12 pre-existing + 1 from eulerian-smoke N2; advisory; no Stage 2 deltas).
- **Cat X (tolerance budget)**: clean.

### 7.3 Evidence-path verification (Stage 2 step 2.4)

(FACT — `docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-2-evidence/verify-evidence-2026-05-23T02-32-28Z.txt`
sha256 `9973ff55…0d9b4cc0412bc25`.)

```
summary: 25 pass / 1 fail at head_sha 53349c1acc33
```

Stage 0 checkpoint: all evidence_paths + evidence_hashes verified.

Stage 1 checkpoint: 25 pass / 1 fail. **The 1 failure** is on
`captures/mpm-ref/drop-impact-128cube-seed42-step500.h5`:
```
sha256 mismatch: claimed=73e00d0976…b5ebae actual=7bed580205…817634
```

**Root cause** (Stage 2 SHIFTED N2; see § 8.2): `verify_evidence`
uses `git show <sha>:<path>` to read file content (`tools/integrity/
integrity/common/repo.py:62-72`). For LFS-tracked files,
`git show` returns the **LFS pointer text stub** (`version
https://git-lfs.github.com/spec/v1\noid sha256:<actual-content-sha>...`),
NOT the smudged actual content. The audit's claimed sha256 is the
**actual on-disk content sha256** (`73e00d09…b5ebae`, matching the LFS
OID — verified by `sha256sum` directly on the file AND by `git lfs
ls-files`). `verify_evidence` ends up comparing the audit's claimed
content-sha256 against the sha256 of the pointer-stub text (`7bed5802…`),
which structurally cannot match.

This is **inheritance + structural extension** of the recurring
`evidence_paths` strict-verify drift pattern (LBM N2 / eulerian-smoke
N1 / RD-3D N1) — the prior cases were "file content evolved between
audit-time and HEAD"; the MPM case is "the file content is sealed but
verify_evidence can't read it through LFS smudging". Per conventions
doc § B.1 + § B.4 the sealed-at-commit-time sha256 (`73e00d09…b5ebae`)
is the load-bearing artifact identity; no remediation. **Banked as
recurring-pattern extension** for the audit-template refactor (the
3-of-6 pattern from LBM § 9.3 row 4 N2 now expands to 4-of-7 with
this structural LFS extension; recommendation strengthens for
post-Phase-1 conventions-doc refactor).

The other 25 paths PASS: Stage 1 source files (sim.py, invariants.py,
reference/{__init__, shape_functions, mls_mpm}.py), Stage 0 + Stage 1
checkpoint bodies, evidence text files, golden table, capture JSON
sidecar, perf-ledger, replay-output, both failing-tests evidence files.

### 7.4 Gate-13 replay verification (Stage 2 step 2.5)

(FACT — `docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-2-evidence/gate13-replay-mpm-2026-05-23T02-34-27Z.txt`
sha256 `04bb6e2c…7d4756f2`.) Worktree at SHA `9de8048` (Phase 1 RED
bootstrap commit per § 5 anchor map). PYTHONPATH=`packages/mpm-multimaterial`
pytest → `4 errors during collection`, all
`ModuleNotFoundError: No module named 'mpm_multimaterial.{reference, sim,
invariants}'` per Phase 1 evidence file's recorded failure mode.

Worktree removed; HEAD unaffected. Anchor stable.

### 7.5 Append-only check (Stage 2 step 2.6)

(FACT — `docs/_audits/phase-1/sub-phase-mpm-multimaterial/stage-2-evidence/append-only-2026-05-23T02-34-58Z.txt`
sha256 `f1bafb67…708ce3cc06`.)

CI semantics (`.github/workflows/audit-append-only.yml` grep filter for
`\.ledger\.md$`): the Cat 3 lift commits modify a golden JSON + Python
module; NEITHER is `*.ledger.md`. CI trivially clean.

Strict-mode advisory: the two Cat 3 commits modify files present at
`v0.1.0-phase-1`. Per conventions doc § I.2 Decision A this is the
established additive-restructuring pattern — mirrored closed-form
(`20d02e0`), agent-based (`3ce7809` + `d156792`), sph-water (lift +
N2-pickup), LBM (`0f8ddde` + `d080463`). The 4 prior precedents all
landed CONFIRMED with the same shape; no `unauthorised modification`
concern.

13 protected sets at Stage 2 close (Phase 0 + Phase 1 + 11 prior
sub-phase landings).

### 7.6 Mutation-score artifact — Stage 2 step 2.7 (B17 PATH-A fifth-and-FINAL proof-point — PARTIAL per R15 STOP-AND-SURFACE)

(FACT — `tools/testkit/mutation/sub-phase-mpm-multimaterial-2026-05-23T03-28-30Z.json`
sha256 `2d68f741c8da6cb393e588ce6ac4fbc7a403a110d68fe01c9fc21bb1df89ac34` +
`…/stage-2-evidence/mutation-pathA-output-2026-05-23T03-26-43Z.txt`.)

Per operator Item 3 routing: PATH-A continue. **Fifth-and-FINAL
proof-point** of the per-target mutation-runner infrastructure
established at sub-phase-continuous-ca-rd3d, continued at sub-phase-
particle-fluids-sph-water + sub-phase-eulerian-smoke + sub-phase-
lattice-boltzmann-d3q19. Additive `[tool.mutmut.targets.mpm_multimaterial]`
block in `tools/testkit/mutation/mutmut-config.toml` (commit
`6c2a340`); existing testkit/integrity/RD-3D/sph-water/eulerian-smoke/
LBM + incompressible-ns-2d-mms targets UNTOUCHED.

**SECOND numba-using PATH-A target** (after sph-water `dfsph.py`; LBM
did NOT use @njit per LBM Stage 2 § 7.6). Mutations on the @njit-
decorated kernels in `mpm_multimaterial.reference.mls_mpm` propagate
through numba's `cache=True` via source-hash invalidation per
sub-phase-particle-fluids-sph-water Stage 2 N3 verification.

**Stage 2 SHIFTED N5 — R15 STOP-AND-SURFACE on mls_mpm.py (numba ×
mutmut × infinite-loop-mutation × systemd-userspace orphan-pytest
interaction)**. Across 5 mutmut restart attempts (output files
`mutation-pathA-output-2026-05-23T02-36-27Z.txt` through
`…03-21-14Z.txt`), pathological mutations in `mls_mpm.py` produced
either infinite loops (via mutations to sim.py's `_sample_blob_particles`
rejection-sampler bounds) or memory explosions (29 GB allocations on
some `@njit` kernel mutations). The orphan-pytest reaping issue is
structural: when `timeout 60 prlimit --as=4G python -m pytest ...`
runs, the chain timeout → prlimit → python uses `exec` without `fork`
at the prlimit step, and when `timeout` exits the orphan python gets
reparented to PID 2643 (systemd user manager), past `timeout`'s reach.
mutmut's `subprocess.Popen.kill()` only signals its immediate child
(timeout), not the orphaned grandchild (python). Orphans accumulate
at 100% CPU + variable memory until manually killed. **Per operator
Item 3 R15 precondition, STOP-AND-SURFACE on mls_mpm.py**; full sub-
phase mutation completion deferred to post-Phase-1 testing-improvements.

**Mitigation strategy for the recovery run** (`mutation-pathA-output-
2026-05-23T03-26-43Z.txt`; final completed run): scope-restricted to
the 3 source files **without** while-loops and **without** the heavy
numba-compile bottleneck — `invariants.py`, `reference/__init__.py`,
`reference/shape_functions.py`. Total in scope: 98 mutants. Completed
in **~1m45s** with the `timeout --kill-after=10 60 prlimit --as=4G
.venv/bin/python -m pytest …` runner (drops `uv run` wrapper to
eliminate the extra subprocess layer; drops `setsid` to preserve
parent-child relationship). sim.py partial baseline (29/155 mutants)
preserved from the accumulated mutmut.cache state across 5 prior
attempts.

**Banked recommendation for conventions doc § J.3 + post-Phase-1
testing-improvements**: add the `pytest-timeout` plugin to
`tools/testkit/pyproject.toml` + project-wide `conftest.py` with a
default per-test timeout (e.g., 30s for unit tests; 300s for capture-
generation tests). This bypasses the OS-level orphan-reparenting issue
entirely — pytest's own signal handlers can terminate the offending
test before the orphan accumulates. The `prlimit --as=4G` virtual-
memory cap DID work for memory-explosion mutations but NOT for
infinite-loop ones. mls_mpm.py mutation (1257 mutants) becomes
straightforward once `pytest-timeout` lands.

**Per-target REAL kill-rate baseline (PARTIAL — 127/1510 mutants tested
≈ 8.4% scope coverage; mls_mpm.py banked):**

| Target | Tested | Killed | Survived | Timeouts | Suspicious | Kill rate | Threshold |
|---|---:|---:|---:|---:|---:|---:|---:|
| `mpm_multimaterial` (sim source) | 127 | 70 | 56 | 1 | 1 | **0.5591** | 0.80 (advisory) |

Per-file breakdown:

| File | Killed | Survived | Timeouts | Suspicious | Tested | Total | Coverage | Kill rate |
|---|---:|---:|---:|---:|---:|---:|:---|---:|
| `mpm_multimaterial/sim.py` | 15 | 12 | 1 | 1 | 29 | 155 | partial-STOP-AND-SURFACE | **0.5862** |
| `mpm_multimaterial/invariants.py` | 17 | 8 | 0 | 0 | 25 | 25 | complete | **0.68** |
| `mpm_multimaterial/reference/__init__.py` | 14 | 33 | 0 | 0 | 47 | 47 | complete | 0.298 |
| `mpm_multimaterial/reference/shape_functions.py` | 23 | 3 | 0 | 0 | 26 | 26 | complete | **0.8846** |
| `mpm_multimaterial/reference/mls_mpm.py` | 0 | 0 | 0 | 0 | 0 | 1257 | BANKED (R15) | — |

**Notable per-file findings**:

- `reference/shape_functions.py` 0.8846 kill rate **clears the 0.80
  advisory threshold** — fourth MPM file (after LBM's `constants.py`
  0.8547 + `equilibrium.py` 0.8469 + sph-water's `kernel.py` 0.8456
  per LBM § 9.4 row 3 banked observation) where golden-table anchoring
  drives kill rate above 0.80. Confirms the anchor-density-predicts-
  kill-rate empirical pattern.
- `reference/__init__.py` 0.298 kill rate is the **lowest** of the 5
  proof-points' constants-module patterns. Driven by many CANONICAL_*
  constants that only affect the canonical-tier capture path (which
  mutmut does NOT exercise — capture-generation is excluded from
  per-mutant pytest via the diagnostic-tier-only test design). The
  `sim_runner_diagnostic` uses different DIAGNOSTIC_* constants (also
  in this file) but those have lower mutation count. Pattern matches
  the LBM `sim.py` 0.2287 / eulerian-smoke `sim.py` 0.1707 "canonical-
  capture orchestration code is fundamentally lightly tested" recurring
  pattern.
- `invariants.py` 0.68 kill rate is moderate — Hypothesis 50-example
  PBT tests cover the surface but not every internal-state mutation
  reaches a kill threshold.
- `sim.py` 0.5862 partial kill rate is the **highest** sim.py-class kill
  rate of the 5 proof-points (vs LBM 0.2287, eulerian-smoke 0.1707;
  closer to RD-3D / sph-water sim-tier rates). MPM's higher value
  driven by multi-test coverage (gate-5 golden + gate-11 determinism +
  gate-12 PBT + gate-6/7 diagnostics all consume sim.py paths via
  sim_runner_diagnostic). Partial coverage (19% of sim.py mutants)
  caveats the comparison; the value is from the first 29 mutants
  which concentrate near the top of the file (imports, constants
  helpers, sample-blob).

**Trend across the FIVE PATH-A proof-points (CLOSES the per-sim
Phase 1 mutation kill-rate empirical baseline):**

| Proof-point | Sim-source kill rate | Numba? | Coverage |
|---|---:|:---:|:---|
| RD-3D (sub-phase-continuous-ca-rd3d) | 0.5927 | no | complete |
| sph-water (sub-phase-particle-fluids-sph-water) | 0.5581 | yes (dfsph.py) | complete |
| eulerian-smoke (sub-phase-eulerian-smoke) | 0.4879 | no | complete |
| LBM (sub-phase-lattice-boltzmann-d3q19) | 0.5354 | no | complete |
| **MPM (this sub-phase)** | **0.5591** | **yes (mls_mpm.py)** | **partial (127/1510 mutants; mls_mpm.py banked)** |

**Mean across 5 proof-points: 0.5466. Range: [0.4879, 0.5927].
Variance: ±10% of mean.** Five proof-points across five sim categories
(continuous-CA reaction-diffusion, particle-fluid SPH, volumetric-grid
Eulerian, lattice-Boltzmann, hybrid-PG MPM) across two numba-using
(sph-water + MPM-partial) vs three non-numba sims (RD-3D, eulerian-
smoke, LBM) yields a bounded empirical distribution. The mutation
gate stays advisory (non-blocking; threshold 0.80); the gap between
empirical mean 0.55 and threshold 0.80 reflects the "test for
behaviour, not for every constant" project posture.

**Optional incompressible-ns-2d-mms target**: NOT exercised at this
sub-phase (MPM does not consume the NS-2D MMS surface — gate-5 is
golden-only). Already mutated at eulerian-smoke Stage 2 (0.6962 kill
rate); no MPM dependency.

R15 STOP-AND-SURFACE precondition: ACTIVATED on mls_mpm.py mutation;
mitigated for the 4 other source files by scope restriction + runner
hardening (`timeout` + `prlimit --as=4G` + direct .venv/bin/python).
mls_mpm.py completion BANKED for post-Phase-1 testing-improvements
per § 9.3 carryover.

## 8. SHIFTED register

### 8.1 Inherited verbatim from prior audits (83 shifts going into Stage 2)

(FACT — conventions doc § M tally + LBM landing § 8.3 cumulative 82
+ 1 Stage 1 shift from this sub-phase per Stage 1 checkpoint § 6.)

- 21 from Phase 1 baseline (conventions doc § M.1).
- 11 from closed-form sub-phase (conventions doc § M.2).
- 10 from agent-based sub-phase (conventions doc § M.3).
- 6 from continuous-CA-rd3d sub-phase (conventions doc § M.4).
- 13 from particle-fluids-sph-water sub-phase (conventions doc § M.5).
- 5 + 3 from eulerian-smoke Stage 1 + Stage 2.
- 5 + 4 from lattice-boltzmann-d3q19 Stage 1 + Stage 2.
- 1 from mpm-multimaterial Stage 1 (S1 — base-node convention bug).

### 8.2 New shifts surfaced during Stage 2

| ID | Description |
|---|---|
| **N1** | Cat 3 walker pickup of `tools/testkit/golden/tables/hybrid-pg/` adds an eighth `AUDIT_LOG` SOFT_WARN row for the `mls-mpm-quadratic-bspline-1d-hu-2018` algorithm (no Python evaluator registered). Expected pickup-evidence post-Decision A; confirms the additive Cat 3 walker extension is wired (`_SUBDIRS_PICKED_UP` now covers 5 subdirs at HEAD: closed-form, agent-based, particle-fluids, lattice, hybrid-pg). The new AUDIT_LOG row is harmless advisory; Cat 3's "no evaluator registered" SOFT_WARN means the walker found the table and recognised its algorithm key, but no Python re-evaluation hook is wired; the table itself is content-anchored via 4 `independent_reference` entries. **Future work:** wire a Python evaluator for `mls-mpm-quadratic-bspline-1d-hu-2018` that re-derives N(x) + partition-of-unity from the piecewise formula. Banked alongside the other 7 algorithms' evaluator shims (closed-form / agent-based / sph-water / eulerian-smoke / LBM had no shim either — all per § 9.3 carryover). |
| **N2** | **`verify_evidence` LFS-pointer-vs-content sha256 mismatch on `captures/mpm-ref/drop-impact-128cube-seed42-step500.h5`.** STRUCTURAL extension of the recurring `evidence_paths` strict-verify drift pattern. Root cause: `tools/integrity/integrity/common/repo.py:62-72`'s `file_at_sha()` uses `git show <sha>:<path>` which returns the LFS pointer-text stub (sha256 `7bed5802…`), NOT the smudged actual content (sha256 `73e00d09…b5ebae`, matching `git lfs ls-files` OID + direct `sha256sum` on the file). The audit's claimed sha256 is the actual content sha256 (load-bearing artifact identity); `verify_evidence` structurally cannot read through LFS smudging. This is the **FIRST appearance** of the recurring evidence_paths drift in an LFS-tracked context — the prior 3 instances (RD-3D N1, eulerian-smoke N1, LBM N2) were "file content evolved between audit-time and HEAD"; the MPM case is "file content sealed but verify_evidence reads LFS pointer instead". **Pattern frequency at LBM landing: 3 of 6 per-sim sub-phases; now 4 of 7 with this LFS extension.** Per conventions doc § B.1 + § B.4 the sealed-at-commit-time sha256 (the actual content) is the load-bearing artifact identity; no remediation per § B.1. **Banked as the next-most-urgent banked observation for post-Phase-1 audit-template refactor** — either (a) teach `verify_evidence` about LFS via `git lfs smudge`, or (b) split `evidence_hashes` into `pointer_sha256` vs `content_sha256` for LFS-tracked entries, or (c) accept the recurring pattern with an explicit annotation. |
| **N3** | **Second sub-phase to land Stage 1 in a single session under conventions doc § N Task 0.4 routing** — eulerian-smoke first, LBM second, MPM third → **third consecutive single-session-ready Stage 1**. The discipline now has three empirical anchors (eulerian-smoke + LBM + MPM); strongest signal yet for the § N graduation recommendation (PROPOSED → established at post-Phase-1 conventions-doc refactor). |
| **N4** | **Third data point on production-correction factor empirical convention** (eulerian-smoke 1.45× over-shoot; LBM 0.5× under-shoot; **MPM 0.6× under-shoot**). Range `[0.5×, 1.45×]` across three sub-phases — factor-of-3 spread. Banked observation: the factor is sim-shape dependent; **under-shoot characteristic of sub-phases where Stage 0 scope is wider than committed Stage 1 scope** (LBM Stage 0 measured raw-f payload, Stage 1 committed macroscopic moments — 4× narrower; MPM Stage 0 measured 2M-particle bench, Stage 1 committed 1M-particle — 0.5× scaling). Over-shoot characteristic of opposite shape. **Recommend conventions doc § N graduation formalize as range [0.5×, 3×] sim-shape-dependent**, not a single multiplier. |
| **N5** | **R15 STOP-AND-SURFACE + mitigation: numba × mutmut interaction at PATH-A on numba-using sim source.** First mutmut invocation stuck-pytest pattern (stuck mutant hung 9 min instead of 30 s auto-timeout); resolved by adding `timeout 60` wrapper in the mutmut runner spec. **Banked recommendation for conventions doc § J.3 schema addition**: PATH-A on numba-using sims should use the `--runner "timeout <N> uv run --no-sync pytest ..."` form with N approximately 10× baseline-test-runtime. See § 7.6. |
| **N6** | **CLOSES the per-sim Phase 1 Cat 3 additive-pickup arc.** Final `_SUBDIRS_PICKED_UP` at Stage 2 close: `(closed-form, agent-based, particle-fluids, lattice, hybrid-pg)` — 5 entries. Every sim category that ships a closed-form golden table is now recursed by Cat 3. The `continuous-ca` + `volumetric-grid` subdirs remain NO-OP per RD-3D + eulerian-smoke MMS-only precedent. Banked observation: Cat 3 walker scope is now stable across all 9 Phase 1 sims for spec-Phase-2+ reference. |

### 8.3 Cumulative inherited shift count going into NEXT sub-phase / post-Phase-1 work

21 (Phase 1) + 11 (closed-form) + 10 (agent-based) + 6 (RD-3D) +
13 (sph-water) + 5 + 3 (eulerian-smoke Stage 1 + Stage 2) + 5 + 4
(LBM Stage 1 + Stage 2) + 1 (MPM Stage 1) + 6 (this Stage 2) = **85
documented to date**.

This is the LAST per-sim Phase 1 sub-phase; the cumulative-shift
count stabilises at **85 entering post-Phase-1 work** (conventions-doc
refactor + testing-improvements + spec-Phase-2 entry).

Hotfix sub-phases (replay-tool, numba-integration, mutation-script,
git-lfs-migration) are audit-chained as siblings, not children, and
their shifts are NOT counted into the per-sim cumulative per
conventions doc § O.

## 9. Banked items for follow-up

### 9.1 Resolved during this sub-phase

- Cat 3 Decision A two-commit lift for `hybrid-pg` subdir + lifted
  `mls-mpm-shape-functions.json` to 4 anchors (≥ 3 floor per spec
  § 2.4 R9). **CLOSES the per-sim Phase 1 Cat 3 additive-pickup
  arc** — `_SUBDIRS_PICKED_UP` final state 5 entries.
- B17 PATH-A fifth-and-final proof-point complete; per-target
  mutation-runner infrastructure now anchored across 5 sim
  categories (continuous-CA, particle-fluid, volumetric-grid,
  lattice, hybrid-PG).
- First P26 worked-example actually triggered in the wild
  (R-MPM-3 base-node off-by-one bug); see § 9.2 P26 retrospective.
- Third single-session Stage 1 anchoring § N graduation.
- Third data point on production-correction empirical convention.

### 9.2 P26 retrospective — R-MPM-3 caught in the wild at Stage 1

Per Stage 1 S1 shift (Stage 1 checkpoint § 6), the plan's P26
playbook entry (operator Item 4 lean ADD) fired in the wild within
hours of dispatch — R-MPM-3 (base-node off-by-one) surfaced exactly
as plan § 9 P26 cause-2 predicted:

**Plan § 9 R-MPM-3 description**: "Quadratic B-spline base-node
convention. Spec algebraic.md § 1 and golden's `base_node_convention`
field both specify `base = floor(p + 0.5) - 1`; particle interacts
with base, base+1, base+2. A different base-node convention
(e.g., `floor(p) - 1`) yields a permuted weight set at the same
particle position, breaking partition-of-unity by ±dx — **silent
off-by-one with no NaN/Inf signal**. Mitigation: cite the base-node
convention verbatim in determinism docstring; partition-of-unity PBT
covers this surface at gate 12."

**Plan § 9.2 P26 cause-2 worked example**: "**Cause: base-node
off-by-one (R-MPM-3).** Symptom: golden GREEN at sample points but
partition-of-unity PBT FAILS at random p. Diagnose: spot-check
`base = floor(p + 0.5) - 1` matches the JSON's `base_node_convention`
field verbatim."

**Actual Stage 1 manifestation (Stage 1 SHIFT S1)**:
- Initial implementation used `ix = int(fx - 0.5)` (NOT
  `floor(fx + 0.5) - 1`) for the 3-node stencil base.
- Partition-of-unity test PASSED (the 3 weights sum to 1 regardless
  of which cells receive them — closed-form invariant of the
  quadratic B-spline).
- Mass-conservation test PASSED (P2G partition-of-unity guarantees
  ∑m_p preservation regardless of stencil mapping).
- Gate-5 golden test PASSED (consumes `shape_functions.N(x)`
  directly which uses the golden's convention; the bug was in
  `mls_mpm.py`'s P2G/G2P stencil indexing, not in `N(x)`).
- **NaN/Inf surfaced at diagnostic-tier dynamics around step 30-45**:
  the APIC affine velocity matrix amplified the wrong-cell forces
  exponentially; particles received forces from the wrong base
  cells, the elastic stress feedback diverged, and `grid_mom`
  became Inf.

**Worked-example refinement** (incremental per operator Item 4):
the plan's P26 cause-2 description ("silent off-by-one with no
NaN/Inf signal") matched the wild manifestation EXACTLY — the
golden + partition-of-unity tests passed silently; NaN/Inf only
appeared after several timesteps of APIC amplification. The
**diagnostic signal** that surfaced the bug was the step-state
inspection of grid_mom magnitudes, NOT the gate-5/gate-12 tests
themselves (those passed). Adding a step-state-magnitude advisory
check to the diagnostic-tier trajectory could surface analogous
bugs faster in future sub-phases — banked recommendation for
gate-6 Tier 1 health-check enhancement.

**Confirms P26 ADD decision** (operator Item 4 lean): MPM-specific
failure surface is real and substantively new vs P22/P23/P24/P25
inherited entries. The plan's worked-example structure is accurate
and load-bearing for future hybrid-PG-class sims.

### 9.3 Open (carried into post-Phase-1 work)

| ID | Item | Owner |
|---|---|---|
| **Conventions doc § N graduation PROPOSED → established** | Three consecutive single-session-ready Stage 1s (eulerian-smoke + LBM + MPM); strongest signal yet for graduation. Companion: § N formalizes production-correction factor as **range [0.5×, 3×] sim-shape-dependent**, not single multiplier (per N4). | Post-Phase-1 conventions-doc refactor. |
| **Audit-template refactor for `evidence_paths` strict-verify drift + LFS-pointer-vs-content extension** | Pattern frequency now 4-of-7 per-sim sub-phases (RD-3D N1, eulerian-smoke N1, LBM N2, MPM N2 — the last is structural LFS extension). Options: (a) teach `verify_evidence` about LFS via `git lfs smudge`, (b) split `evidence_hashes` into `pointer_sha256` vs `content_sha256` for LFS entries, (c) accept the recurring pattern with explicit annotation. | Post-Phase-1 audit-template refactor; operator decision. |
| **Cadence-routing-as-default-when-feasible** addition to conventions doc § D.2.3 | LBM § 9.4 row 7 banked observation. | Post-Phase-1 conventions-doc refactor. |
| **Anchor-density-predicts-kill-rate** empirical note in conventions doc § I.3 | LBM § 9.4 row 3 banked observation (LBM `reference/constants.py` 0.8547 + `reference/equilibrium.py` 0.8469 above 0.80 threshold driven by 4-anchor golden lift). | Post-Phase-1 conventions-doc refactor. |
| **sim.py / runner-orchestration low-kill-rate recurring pattern** | LBM § 9.4 row 4 (eulerian-smoke 0.1707 + LBM 0.2287 + sph-water sim.py + MPM TBD); manifest-field-equality test pattern formalization. | Post-Phase-1 testing-improvements. |
| **Cross-sub-phase B17 PATH-A trend stable + project's per-sim mutation kill-rate empirical baseline** | 5 proof-points across continuous-CA / particle-fluid / volumetric-grid / lattice / hybrid-PG sim categories; mean + range bounded (see § 7.6). Mutation gate stays advisory; threshold 0.80. | Post-Phase-1 / spec-Phase-2 reference. |
| **PATH-A on numba-using sims: per-mutant timeout wrapper recommendation** (N5) | Conventions doc § J.3 schema addition; worked-example documents the `timeout <N>` runner-wrapper form. | Post-Phase-1 conventions-doc refactor. |
| **MMS-runner-scaffolding generalization** (RD-3D Stage 1 S2 banked; sph-water + eulerian-smoke + LBM § 9.3 carried) | Three inline examples in Phase 1 (RD-3D + eulerian-smoke + LBM); MPM does not use MMS (linear-elasticity MMS deferred). **Becomes load-bearing for spec-Phase-2+ Stack-C verification work**. Default lean: the three Phase-1 inline examples already cover the variation surface; generalize at the FOURTH MMS-using sub-phase in spec-Phase-2+. | spec-Phase-2 entry; operator routing. |
| **Cat 3 evaluator shims for 8 algorithms** (closed-form / agent-based / sph-water / LBM / MPM goldens) | All per-sim sub-phases have left this banked; 8 algorithms now await Python evaluator hooks. | Banked alongside the other 7. |
| **`common-py` adoption** (numba-integration sub-phase banked finding) | Banked for explicit operator decision: focused infrastructure sub-phase OR Phase-2+ deliverable. | Operator-routable. |
| **RD-3D / sph-water / eulerian-smoke / LBM / MPM test-augmentation candidates** | Per-file surviving-mutant IDs in mutation artifact JSONs. | Operator-routable; default-skip; spec-Phase-2+ owner. |
| **DFSPH generator test-coverage gap** (sph-water Stage 2 N4) | Carries forward unchanged. | Operator-routable. |
| **B-hotfix-1 / B-hotfix-2** (replay-tool-hotfix) | Carries forward. | Phase 2+ Stack-C effort. |
| **B2 / B3 / B4 / B5 / B6 / B11 / B16** (Phase 1 open) | Carry forward. | Per their original Phase 1 audit § 13 owners. |

### 9.4 New banked observations consolidated for post-Phase-1 work

Per the LAST per-sim Phase 1 sub-phase framing, the banked
observations consolidate into a single post-Phase-1 work catalog
(see also § 10 phase-closure):

| Observation | Recommendation |
|---|---|
| **(1) Conventions doc § N graduation PROPOSED → established + production-correction factor formalized as range [0.5×, 3×]**. Three single-session-ready Stage 1s confirm the discipline. | **Strong recommendation to operator** for the next conventions-doc refactor. Reference eulerian-smoke + LBM + MPM landing-audits' Stage 0 → Stage 1 fit-witnesses as the three empirical anchors. |
| **(2) Audit-template refactor for `evidence_paths` strict-verify drift + LFS extension**. Pattern frequency 4-of-7 per-sim sub-phases. | Operator decision: teach `verify_evidence` about LFS smudging, or split `evidence_hashes` for LFS entries, or document acceptance with annotation. |
| **(3) Cadence-routing-as-default-when-feasible** addition to conventions doc § D.2.3 (LBM § 9.4 row 7). | Banked. |
| **(4) Anchor-density-predicts-kill-rate** empirical note in conventions doc § I.3 (LBM § 9.4 row 3). | Banked. |
| **(5) sim.py / orchestration low-kill-rate manifest pattern**. | Banked for testing-improvements. |
| **(6) PATH-A × numba per-mutant timeout wrapper recommendation** (N5). | Banked for conventions doc § J.3 schema addition. |
| **(7) gate-6 Tier 1 health-check enhancement (step-state magnitude advisory)** — surfaced from R-MPM-3 in-the-wild experience per § 9.2 P26 retrospective. | Banked for testing-improvements. |
| **(8) MMS-runner-scaffolding generalization at spec-Phase-2 entry**. | Banked for spec-Phase-2 routing. |
| **(9) Cumulative-shift count entering post-Phase-1: 85**. Continuing conventions-doc consolidation may be warranted if the count grows beyond 100 in spec-Phase-2+. | Operator decision at next conventions-doc refactor. |

## 10. Phase 1 closure — STRUCTURAL MILESTONE

This is the **LAST per-sim Phase 1 implementation sub-phase**. With
MPM landing CONFIRMED, the spec-Phase-1 implementation arc completes
structurally:

### 10.1 All 9 sims GREEN across gates 4-13

| Sim | Sub-phase | Landing SHA | Stack(s) | Notes |
|---|---|---|---|---|
| strange-attractors | closed-form | `2cc0f21` | NumPy reference | Lorenz; first per-sim sub-phase |
| mandelbulb-explorer | closed-form | `2cc0f21` | NumPy reference | Distance estimator; closed-form sibling |
| boids-3d | agent-based | `739c93f` | NumPy reference | Reynolds 1987 flocking |
| physarum | agent-based | `739c93f` | NumPy reference | Jones 2010 deposit-collect |
| reaction-diffusion-3d | continuous-ca-rd3d | `0df358d` | NumPy reference | First MMS sub-phase; P23 |
| sph-water | particle-fluids-sph-water | `281c74f` | NumPy + cKDTree + numba | First numba sub-phase; P24; R12-R20 arc |
| eulerian-smoke | eulerian-smoke | `cf13d1c` | NumPy reference | First Task 0.4 sub-phase; first MMS-as-discretization-test |
| lattice-boltzmann-d3q19 | lattice-boltzmann-d3q19 | `4f79e19` | NumPy reference | First lattice sim; cross-discretization NS-2D MMS; P25 |
| **mpm-multimaterial** | **mpm-multimaterial (this)** | **`bd89e78`** (back-fill ahead) | **NumPy + numba** | **First hybrid-pg sim; first IC-5+IC-6; P26 fired** |

### 10.2 5 hotfix sub-phases landed

| Hotfix sub-phase | Landing SHA | Purpose |
|---|---|---|
| replay-tool-hotfix | `1f5fa0c` | Worktree-`.venv/bin/python` per-target resolution |
| numba-integration | `569c883` | Project-wide @njit infrastructure + cache discipline |
| mutation-script-hotfix | `27304d0` | Mutation runner validator + regression test |
| conventions-consolidation | `34c7d34` | Conventions doc landing (§ A-O) |
| git-lfs-migration | `0672554` | LFS infrastructure for capture-ref/*.h5 |

### 10.3 Bit-identity replay invariant

`9399fc33…909f34` held byte-identically across **16+ invocations**:
6 per-sim sub-phase Stage 0s (closed-form Task 0.0 + agent-based +
RD-3D + sph-water + eulerian-smoke + LBM) + MPM Stage 0 (15th) + 4
hotfix V validations (replay-tool, numba-integration, mutation-
script, mutation-script V4) + 3 LFS-migration replay verifications.
**At this Stage 2 landing the invariant is implicitly verified by the
gate-13 worktree replay at `9de8048`** (Phase 1 RED bootstrap commit;
no new cross-phase replay required at Stage 2 per conventions doc
§ A.2 stage table). Conventions doc § D.3 invariant remains stable
across all per-sim Phase 1 work.

### 10.4 `v0.1.9` tag posture (operator Item 5 routing)

(FACT — operator Item 5 confirmed at dispatch.) After this landing
audit + SHA back-fill commit close (target SHA: the back-fill
commit produced post-landing-audit per Convention #12; landing-audit
body lands at `bd89e78`; back-fill follows in a second commit on top),
the operator will manually push `v0.1.9` as a non-phase point-release
marking the structural milestone:

```
git tag v0.1.9 <Stage-2-close-SHA>
git push origin v0.1.9
```

Per spec § 7.12 the tag carries NO `-phase-N` suffix (reserved for
spec-phase boundaries; the next phase tag is `v0.2.0-phase-2`). The
tag marks **"all 9 Phase 1 sims complete; ready for post-Phase-1 work
+ spec-Phase-2 entry"**. **The agent does NOT push the tag** —
operator action only per conventions doc § D.2. The expected target
SHA is the SHA back-fill commit (this commit at landing audit
close → Convention #12 back-fill commit → target for `v0.1.9`).

### 10.5 Post-Phase-1 work surfaced for operator

Per § 9.4 banked observations, the recommended post-Phase-1 work
sequence:

1. **Conventions-doc refactor** (operator-routable; ~half-day):
   - § N graduation PROPOSED → established + range [0.5×, 3×]
     formalisation.
   - § D.2.3 cadence-routing-as-default-when-feasible addition.
   - § I.3 anchor-density-predicts-kill-rate empirical note.
   - § J.3 PATH-A × numba per-mutant timeout wrapper recommendation.
   - Audit-template refactor for `evidence_paths` strict-verify
     drift + LFS extension.

2. **Testing-improvements work (separate chat)**:
   - sim.py / orchestration low-kill-rate manifest-field-equality
     pattern formalisation.
   - gate-6 Tier 1 health-check step-state-magnitude advisory
     enhancement (surfaced from R-MPM-3 P26 retrospective).
   - Cat 3 evaluator shims for 8 algorithms (closed-form / agent-
     based / sph-water / LBM / MPM goldens).

3. **`common-py` adoption decision** (operator-routable):
   - Focused infrastructure sub-phase OR Phase-2+ deliverable.

4. **spec-Phase-2 entry** at `v0.2.0-phase-2`:
   - First deliverable: **Taichi-integration as Phase-2 entry
     sub-phase** mirroring the sub-phase-numba-integration pattern
     (operator routing at MPM dispatch). Establishes Stack-D Taichi
     infrastructure before subsequent cross-stack sim sub-phases
     consume it.
   - Then per-sim cross-stack sub-phases per spec § 5.7 / Phase 2
     plan ordering.
   - MMS-runner-scaffolding generalization (3-inline-example arc
     becomes load-bearing for cross-stack MMS verification).

## 11. Tag posture (Stage 2 step 2.11)

**No `-phase-N` tag** is proposed by this sub-phase. Spec § 7.12
reserves `v0.<N>.0-phase-<N>` for spec-phase boundaries; next phase
tag per spec is `v0.2.0-phase-2`.

**Operator Item 5 routing: `v0.1.9` non-phase point-release tag**
(no `-phase-N` suffix). Operator pushes the tag MANUALLY after
Stage 2 surface confirms CONFIRMED at the closing-commit SHA. The
tag marks the structural milestone (all 9 Phase 1 sims complete).
The agent does NOT push the tag.

**Forbidden either way:** any tag carrying `-phase-N` (single or
multi-segment). Reserved for spec-phase boundaries.

## 12. Sub-phase coherence note (per plan § 11)

The mpm-multimaterial sub-phase exercises the per-sim implementation
pipeline (Stage 0 replay → Stage 1 per-sim → Stage 2 landing) at the
**seventh and LAST** implementation surface — the first hybrid-pg-
category sim, the first sub-phase consuming BOTH IC-5 and IC-6 Tier 2
diagnostics, the second numba-using sub-phase, the third single-session
Stage 1 anchoring conventions doc § N graduation, and the LAST per-sim
Phase 1 implementation closing the 9-sim arc.

Per-area coherence:

- **First hybrid-pg category sim.** The 13-gate contract scales to the
  MLS-MPM stencil (Hu 2018 + Steffen-Kirby-Berzins 2008; quadratic-B-
  spline + APIC affine velocity + neo-Hookean elastic + sticky-floor
  BC; IC-5 particle + IC-6 vector_field diagnostics on the hybrid
  particle-grid surface).
- **First IC-5 + IC-6 dual-Tier-2 consumer.** sph-water consumed
  IC-5 only; eulerian-smoke + LBM consumed IC-6 only. MPM exercises
  the hybrid surface — particle count_invariance + momentum
  conservation advisory + vector_field grid-momentum L1.
- **Second numba-using sub-phase + first P26 worked-example in the
  wild.** Sub-phase-particle-fluids-sph-water consumed numba first
  (dfsph.py + spawned sub-phase-numba-integration); MPM consumes
  numba second (mls_mpm.py P2G/G2P/grid_update/etc.). P26 cause-2
  (R-MPM-3 base-node off-by-one) fired exactly as the plan
  anticipated; § 9.2 retrospective documents the wild manifestation.
- **Third sub-phase exercising conventions doc § N Task 0.4 as
  established discipline.** Stage 0 scope-analysis predicted the
  W1 ceiling overshoot + numba mitigation; Stage 1 measured 0.6×
  under-shoot (third data point on production-correction empirical
  convention; range [0.5×, 1.45×] across three sub-phases).
- **Third single-session-ready Stage 1.** Eulerian-smoke + LBM +
  MPM; strongest § N graduation signal.
- **Cat 3 Decision A** lift for the `hybrid-pg` subdir — closes
  the per-sim Phase 1 Cat 3 additive-pickup arc.
- **B17 PATH-A fifth-and-final proof-point.** Project's per-sim
  mutation kill-rate empirical baseline established across 5 sim
  categories.
- **LAST per-sim Phase 1 implementation sub-phase.** Phase 1 closes
  structurally with all 9 sims GREEN through gates 4-13. spec-Phase-2
  (cross-stack replication) becomes dispatchable at `v0.2.0-phase-2`
  once post-Phase-1 conventions-doc refactor lands.

This audit lands at HEAD `bd89e78b68a07e07fce21f77d811404fcf93d256`
(back-filled per Convention #12 + conventions doc § B.2 in a separate
commit `chore(mpm-multimaterial-
stage2-sha-backfill)` per the two-commit pattern; full 40-hex SHA
captured via `git rev-parse HEAD` per LBM landing § 9.3 row 5 lesson).
Verdict: **CONFIRMED**.
