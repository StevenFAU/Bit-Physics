---
date: 2026-05-22
author: eulerian-smoke-sub-phase-agent
artifact: sub-phase
artifact_id: sub-phase-eulerian-smoke
stage: 2-landing
subject: "Eulerian-smoke sub-phase (gates 4-13 implementation; first volumetric-grid sim + second MMS sub-phase + first practical exercise of conventions doc § N) landing audit"
verdict-state: CONFIRMED
head_sha: 54043e123e1b7215d0189d93fb5f91923d22face
head_sha_at_checkpoint: 54043e123e1b7215d0189d93fb5f91923d22face
parent_audits:
  - docs/_audits/phase-1/landing-2026-05-20T14-18-00Z.md
  - docs/_audits/phase-1/sub-phase-closed-form/landing-2026-05-20T16-48-00Z.md
  - docs/_audits/phase-1/sub-phase-agent-based/landing-2026-05-20T18-20-39Z.md
  - docs/_audits/phase-1/sub-phase-replay-tool-hotfix/repair-2026-05-20T19-06-35Z.md
  - docs/_audits/phase-1/sub-phase-continuous-ca-rd3d/landing-2026-05-20T19-49-51Z.md
  - docs/_audits/phase-1/sub-phase-numba-integration/landing-2026-05-21T11-22-24Z.md
  - docs/_audits/phase-1/sub-phase-particle-fluids-sph-water/landing-2026-05-22T01-42-51Z.md
  - docs/_audits/phase-1/sub-phase-conventions-consolidation/landing-2026-05-22T03-25-55Z.md
  - docs/_audits/phase-1/sub-phase-eulerian-smoke/stage-0-checkpoint-2026-05-22T12-05-00Z.md
  - docs/_audits/phase-1/sub-phase-eulerian-smoke/stage-1-checkpoint-2026-05-22T13-20-00Z.md
evidence_paths:
  - docs/phases/sub-phase-eulerian-smoke.md
  - docs/conventions/sub-phase-conventions.md
  - docs/_audits/phase-1/sub-phase-eulerian-smoke/stage-0-checkpoint-2026-05-22T12-05-00Z.md
  - docs/_audits/phase-1/sub-phase-eulerian-smoke/stage-1-checkpoint-2026-05-22T13-20-00Z.md
  - docs/_audits/phase-1/sub-phase-eulerian-smoke/stage-2-evidence/anchor-recheck-2026-05-22T13-27-55Z.txt
  - docs/_audits/phase-1/sub-phase-eulerian-smoke/stage-2-evidence/test-sweep-positive-2026-05-22T13-28-30Z.txt
  - docs/_audits/phase-1/sub-phase-eulerian-smoke/stage-2-evidence/test-sweep-negative-2026-05-22T13-28-30Z.txt
  - docs/_audits/phase-1/sub-phase-eulerian-smoke/stage-2-evidence/integrity-cats-2026-05-22T13-28-30Z.txt
  - docs/_audits/phase-1/sub-phase-eulerian-smoke/stage-2-evidence/verify-evidence-2026-05-22T13-28-30Z.txt
  - docs/_audits/phase-1/sub-phase-eulerian-smoke/stage-2-evidence/gate13-replay-eulerian-2026-05-22T13-28-30Z.txt
  - docs/_audits/phase-1/sub-phase-eulerian-smoke/stage-2-evidence/append-only-2026-05-22T13-28-30Z.txt
  - docs/_audits/phase-1/sub-phase-eulerian-smoke/stage-2-evidence/mutation-pathA-output-2026-05-22T13-28-30Z.txt
  - tools/testkit/mutation/sub-phase-eulerian-smoke-2026-05-22T13-28-30Z.json
  - tools/testkit/failing-tests-evidence/eulerian-smoke-2026-05-20T13-37-41Z.txt
  - tools/testkit/failing-tests-evidence/eulerian-smoke-implemented-2026-05-22T12-59-22Z.txt
  - captures/eulerian-smoke-ref/taylor-green-128cube-seed42-step500.h5
  - captures/eulerian-smoke-ref/taylor-green-128cube-seed42-step500.json
  - captures/eulerian-smoke-ref/lid-driven-cavity-128sq-re100-seed42-step1000.h5
  - captures/eulerian-smoke-ref/lid-driven-cavity-128sq-re100-seed42-step1000.json
  - docs/perf-ledger.md
  - CHANGELOG.md
evidence_hashes:
  docs/_audits/phase-1/sub-phase-eulerian-smoke/stage-2-evidence/anchor-recheck-2026-05-22T13-27-55Z.txt: sha256:3f048bb8608dd13578c3ed3720661ed93815865786b20ef5360b0f0cd3f4ccb7
  docs/_audits/phase-1/sub-phase-eulerian-smoke/stage-2-evidence/test-sweep-positive-2026-05-22T13-28-30Z.txt: sha256:6f594bdad4a506a0cfea29e250b07ce39c665daada940c7bf25110024dff64f9
  docs/_audits/phase-1/sub-phase-eulerian-smoke/stage-2-evidence/test-sweep-negative-2026-05-22T13-28-30Z.txt: sha256:a05b26ef2f5293697765dfc819a2a32dbed9673d72b8cbaf95eee508aef446c0
  docs/_audits/phase-1/sub-phase-eulerian-smoke/stage-2-evidence/integrity-cats-2026-05-22T13-28-30Z.txt: sha256:4a32c8acc60c4c6e0247f8dd11e4e150909c93ff1bc43c775449716126c07447
  docs/_audits/phase-1/sub-phase-eulerian-smoke/stage-2-evidence/verify-evidence-2026-05-22T13-28-30Z.txt: sha256:e3c5a7d24212486fdd24c7272a9aafb637ffa0e02c9c50c49c6e7e21d5851a10
  docs/_audits/phase-1/sub-phase-eulerian-smoke/stage-2-evidence/gate13-replay-eulerian-2026-05-22T13-28-30Z.txt: sha256:d8f099d23f2984d1cacb7aea4e131a0f5a669c05fa586c80b9287a87d95c95ce
  docs/_audits/phase-1/sub-phase-eulerian-smoke/stage-2-evidence/append-only-2026-05-22T13-28-30Z.txt: sha256:1695a11dff95ce0ba625ee8cab34c3129938bc44b3ab84749a90a294af8006ef
  docs/_audits/phase-1/sub-phase-eulerian-smoke/stage-2-evidence/mutation-pathA-output-2026-05-22T13-28-30Z.txt: sha256:e90dc414de6a31b1179986caf7f4ce74702a18c74dd211830c1d01ae2e8f4265
  tools/testkit/mutation/sub-phase-eulerian-smoke-2026-05-22T13-28-30Z.json: sha256:403c2fcb58e933f4ce5a9b6a91e31532666b2c977aaeaec3e8bc79c7e146a292
  tools/testkit/failing-tests-evidence/eulerian-smoke-2026-05-20T13-37-41Z.txt: sha256:c961dd22c1ca6117af6d9f187d2c0d3aa4d546972496b0f38d11aa14879f23a1
  tools/testkit/failing-tests-evidence/eulerian-smoke-implemented-2026-05-22T12-59-22Z.txt: sha256:aa1a5b19948895ed286bdb058c7cf233fa27445191cb84ea8096ff899c7d53b7
  captures/eulerian-smoke-ref/taylor-green-128cube-seed42-step500.h5: sha256:4604ebdc40b7fdf80c0354c4429f6fb0a12fd566c5bc301ad9ceed60dcd4e2ed
  captures/eulerian-smoke-ref/lid-driven-cavity-128sq-re100-seed42-step1000.h5: sha256:e13b0d052489ed365ccc929873138251c46875e4e568d1ffd8a997bf43123ceb
---

# Eulerian-Smoke Sub-Phase — Landing Audit

## 1. Sub-phase scope summary

(FACT — `docs/phases/sub-phase-eulerian-smoke.md` §§ 1.1, 1.2, 2.) Fifth
per-sim implementation sub-phase under spec-Phase-1; lands gates 4-13
for `eulerian-smoke` at the Python NumPy reference stack
(Stack-D scope). Stack-C C++/Vulkan port deferred to Phase-2+ per
sim spec-ref § 5.

This sub-phase is the **first** to:

- Ship a **volumetric-grid sim** (spec § 5.6).
- Exercise the **NS-2D Taylor-Green-style MMS** (shared with future
  `lattice-boltzmann-d3q19` per Phase 1 Stage 2 shift #18).
- Draft its plan **AGAINST the new conventions doc** at
  `docs/conventions/sub-phase-conventions.md` (committed at `34c7d34`)
  rather than inheriting from the most-recent-template.
- **Practically exercise conventions doc § N PROPOSED Task 0.4
  canonical-descriptor scope-analysis** — validated at Stage 1
  retrospective (Stage 0 estimate accurate within ~50%); landing
  audit recommends graduation PROPOSED → established.
- **Land Stage 1 in a single session** with no R-class
  STOP-AND-SURFACE arcs — contrast with sph-water's six-R-class arc
  (R12-R20). The Stage 0 scope-analysis discipline that this
  sub-phase amortized is the reason.
- Consume **IC-6 Tier 2 vector_field** at sim-test scale (per
  conventions doc § L.2 row 3 — paired with `divergence_free`,
  `circulation`, `helicity`, `energy_spectrum`).

Implementation lands at `feat(eulerian-smoke-stage1)` commit
`6a5f8b4` per the Stam (1999) + Fedkiw-Stam-Jensen (2001) stable-
fluids pipeline:

  **MacCormack-corrected semi-Lagrangian** advection (predictor-
  corrector for 2nd-order spatial + temporal accuracy, spec-ref
  § 6.1 prescribed) + **Jacobi pressure-projection**
  (centered-difference div + grad on collocated grid;
  n_jacobi=20 fixed iter cap per conventions doc § F P24 pattern;
  Stam-on-collocated O(dx²) inconsistent-stencil residual divergence
  documented at S3; MAC-staggered fix deferred to Phase-2+ Stack-C)
  + **Fedkiw 2001 vorticity confinement skeleton** (ε=0 for the
  canonical MMS-anchored capture; live for visualization variants
  in Phase-2+) + **scalar smoke density advection**.

Two canonical captures per Appendix D § D.2.3 (re-anchored against
the probe-vs-Appendix-D drift inherited from Phase 1 Stage 2 shift
#17):

- `taylor-green-128cube-seed42-step500.h5` — 3D Taylor-Green vortex,
  cadence-50 (11 frames over 500 steps per Stage 0 Task 0.4 finding),
  704.1 MB at float64, 691.587 s wall-clock.
- `lid-driven-cavity-128sq-re100-seed42-step1000.h5` — 2D
  lid-driven-cavity (periodic-BC approximation), full cadence (11
  frames over 1000 steps), 4.2 MB at float64, 5.099 s wall-clock.

## 2. Stage 0 deliverable summary (pre-flight; commits `2320f01` → `9670984`)

(FACT — `docs/_audits/phase-1/sub-phase-eulerian-smoke/stage-0-checkpoint-2026-05-22T12-05-00Z.md`.)

| Task | Result |
|---|---|
| 0.0 — Cross-phase replay (`v0.1.0-phase-1`, 8-gate canonical set) | PASS; sha256 byte-identical to bit-identity invariant `9399fc33…909f34` (conventions doc § D.3, 11th invocation). |
| 0.1 — Tolerance-budget carryover | `[phase].phase = "sub-phase-eulerian-smoke"`; NO `[budgets.*]` widening. |
| 0.2 — Phase 1 evidence sha256 reverify | PASS — `c961dd22…879f23a1` matches Phase 1 landing audit. |
| 0.3 — NS-2D MMS reverify | PASS — `solution.py` sha256 `30e490a7…320d8e` + `derivation.md` sha256 `30dfc294…ac86e76` unmodified since Phase 1 Stage 2 commit `216021a`; SymPy ≡ NumPy spot-check at canonical point `(0.3, 0.5, 0.7, ν=0.01)` max diff 9.02e-16 (under 1e-12 tolerance); analytic divergence simplifies to 0. |
| **0.4 — Canonical-descriptor scope-analysis** (FIRST PRACTICAL EXERCISE of conventions doc § N PROPOSED) | **FITS** — 3D Taylor-Green per-step floor 0.93 s (n_jacobi=20); 500-step projection 7.8-15.6 min; storage at cadence-50 ~0.42 GB. 2D lid-driven-cavity per-step 5.6 ms; trivial. NO STOP-AND-SURFACE. |

Stage 0 close + Convention #12 SHA back-fill at `dcef17d` + `9670984`.

## 3. Stage 1 deliverable summary — gates 4-13 GREEN

(FACT — `feat(eulerian-smoke-stage1)` commit `6a5f8b4` + checkpoint
at `c20d24d` + back-fill at `04192f9`.) Single sub-bundle commit;
**single session** (no partial-checkpoint pattern needed).

### 3.1 Per-gate status — eulerian-smoke at HEAD

| # | Status | Notes |
|---|---|---|
| 4 | GREEN | Reads-through to gate 5. |
| 5 | GREEN | NS-2D MMS, inline convergence study (Path-Y operator routing — RD-3D Stage 1 S2 precedent). MacCormack-corrected SL achieves observed OOA **1.99 (advection)** + **2.00 (projection)**; both within ±0.5 of formal p=2 per spec-ref § 6.1. |
| 6 | GREEN | Tier 1 NaN/Inf scan over diagnostic-tier 32³ × 20-step trajectory. |
| 7 | GREEN | Tier 2 vector_field (IC-6) — `check_divergence_free`, `check_circulation`, `check_helicity`, `check_energy_spectrum` GREEN. First IC-6 sim-test consumption. |
| 8 | GREEN | Cat 1 citations: Stam 1999 (DOI 10.1145/311535.311548), Fedkiw-Stam-Jensen 2001 (DOI 10.1145/383259.383260), Taylor-Green 1937 (DOI 10.1098/rspa.1937.0036). |
| 9 | GREEN | Cat 2 public API per probe § 5: `reference.stable_fluids` exports + `sim.{sim_runner_seeded, sim_runner_seeded_2d, sim_runner_diagnostic, compute_canonical_trajectory_3d}` + `invariants.{divergence_free_post_projection, smoke_density_nonneg}`. |
| 10 | GREEN | TWO canonical captures per Appendix D § D.2.3 (re-anchored against probe-vs-Appendix-D drift). |
| 11 | GREEN (over-achieved bit-exact) | `test_run_twice_epsilon_diff` GREEN via `sim_runner_diagnostic` (32³ × 10 steps; sph-water diagnostic-tier precedent). Over-achievement per conventions doc § F.4. |
| 12 | GREEN | 2 PBT invariants — `divergence_free_post_projection` (tolerance 1e-1, sub-phase-empirical Stam-on-collocated floor per S3) + `smoke_density_nonneg`. |
| 13 | GREEN | Perf-ledger: 691.587 s @ 128³ × 500 + 5.099 s @ 128² × 1000; `i7-12700KF-linux-6.17`. |
| 13 (anchor) | GREEN | Worktree replay at SHA `216021a` reproduces Phase 1 RED (4 `ModuleNotFoundError` collection errors). |

### 3.2 MMS convergence-rate ladders (the load-bearing gate 5 artifact)

**Advection MMS** (projection-disabled n_jacobi=0; manufactured source
`S - ∇p_analytic`; dt ∝ dx² so cumulative time-error matches spatial O(dx²)):

| N | dx | dt | n_steps | ‖e_U‖_L² | ‖e_V‖_L² | ‖e‖_L² |
|---:|---:|---:|---:|---:|---:|---:|
| 32 | 3.125e-02 | 9.62e-04 | 21 | 2.98e-04 | 2.98e-04 | 4.22e-04 |
| 64 | 1.563e-02 | 2.44e-04 | 82 | 7.54e-05 | 7.54e-05 | 1.07e-04 |
| 128 | 7.813e-03 | 6.10e-05 | 328 | 1.89e-05 | 1.89e-05 | 2.68e-05 |

Log-log slope fit: **observed OOA = 1.9892**  (formal p=2, ±0.5)  **PASS**.

**Projection MMS** (Helmholtz decomposition; n_iter = 100·N for Jacobi
convergence keeping pace with O(dx²) discretization residual):

| N | dx | n_iter | ‖u_proj - u_sol‖_L² |
|---:|---:|---:|---:|
| 32 | 3.125e-02 | 3200 | 4.27e-02 |
| 64 | 1.563e-02 | 6400 | 1.07e-02 |
| 128 | 7.813e-03 | 12800 | 2.68e-03 |

Log-log slope fit: **observed OOA = 1.9976**  (formal p=2, ±0.5)  **PASS**.

### 3.3 Determinism-strategy declaration (conventions doc § F.1)

(FACT — 8-clause module docstring at `packages/eulerian-smoke/
eulerian_smoke/sim.py` lines 1-91; cited verbatim in the
`feat(eulerian-smoke-stage1)` commit footer.) Clauses:

1. SL backtrace reads only from prior-step immutable arrays.
2. Bilinear/trilinear interp with explicit lex (i, j[, k]) vertex ordering.
3. Jacobi pressure-projection with FIXED iter cap (n_jacobi=20);
   no tolerance-comparison early-stop branch (P24 pattern).
4. No global RNG state; analytic ICs (zero RNG) for canonical captures.
5. Periodic BCs via `np.roll` + `np.mod` (P23 cause-#1 mitigation;
   conventions doc § M.4 S1).
6. No BLAS/FMA path; pure elementwise NumPy + `np.roll`.
7. Capture ordering is step-index sorted; `h5py` default order preserved.
8. Phase-2+ deferred: parallel reductions, driver FMA, subgroup-collective ops.

Spec declares `epsilon-same-stack-same-hw` for Phase-2+ Stack-C
target (sim `determinism.md`); the Python NumPy reference at this
sub-phase achieves `bit-exact-same-stack-same-hw` — gate 11
witnesses via `test_run_twice_epsilon_diff` (epsilon-bound trivially
satisfied at zero diff). Over-achievement is informational only per
conventions doc § F.4; does NOT promote the spec declaration.

## 4. Stage 2 convergence commits

(FACT — `git log v0.1.0-phase-1..HEAD` since Stage 1 close.)

| Stage | Commit SHA | Subject |
|---|---|---|
| (pre) | `e929f1c` | `docs(sub-phase-eulerian-smoke): initial draft of eulerian-smoke implementation plan` |
| 0 | `2320f01` | `chore(eulerian-smoke-stage0-tolerance-budget): sub-phase carryover from phase-1` |
| 0 | `dcef17d` | `chore(eulerian-smoke-stage0-checkpoint): Stage 0 pre-flight complete` |
| 0 | `9670984` | `chore(eulerian-smoke-stage0-sha-backfill): back-fill Stage 0 checkpoint SHA per Convention #12` |
| 1 | `6a5f8b4` | `feat(eulerian-smoke-stage1): implementation through gate 13` |
| 1 | `c20d24d` | `chore(eulerian-smoke-stage1-checkpoint): Stage 1 per-sim implementation complete` |
| 1 | `04192f9` | `chore(eulerian-smoke-stage1-sha-backfill): back-fill Stage 1 checkpoint SHA per Convention #12` (mis-recorded SHA — see § 8.2 N1) |
| 2 | `8d485d1` | `chore(eulerian-smoke-stage2-mutation-pathA): per-target extension + eulerian-smoke + ns2d-mms baselines` |
| 2 | `d477570` | `docs(eulerian-smoke-stage2-changelog): sub-phase-conventions-consolidation + sub-phase-eulerian-smoke entries` |
| 2 | (this commit) | `chore(eulerian-smoke-stage2-landing-audit): sub-phase landing audit` |
| 2 | (next commit) | `chore(eulerian-smoke-stage2-sha-backfill): back-fill landing audit SHA per Convention #12` |

## 5. Closing-commit anchor re-check (Convention 7.9; Stage 2 step 2.1)

(FACT — `…/stage-2-evidence/anchor-recheck-2026-05-22T13-27-55Z.txt`
sha256 `3f048bb8…d3f4ccb7`.)

| Anchor | Recorded sha256 | At HEAD |
|---|---|---|
| `tools/testkit/failing-tests-evidence/eulerian-smoke-2026-05-20T13-37-41Z.txt` | `c961dd22…879f23a1` | matches |
| `tools/testkit/failing-tests-evidence/eulerian-smoke-implemented-2026-05-22T12-59-22Z.txt` | `aa1a5b19…f899c7d53b7` | matches |
| `captures/eulerian-smoke-ref/taylor-green-128cube-seed42-step500.h5` | `4604ebdc…dcd4e2ed` | matches |
| `captures/eulerian-smoke-ref/lid-driven-cavity-128sq-re100-seed42-step1000.h5` | `e13b0d05…43123ceb` | matches |
| `tools/testkit/code_verification/mms/solutions/incompressible_ns_2d/solution.py` | `30e490a7…320d8e` | matches (unmodified since Phase 1 Stage 2) |
| `tools/testkit/code_verification/mms/solutions/incompressible_ns_2d/derivation.md` | `30dfc294…ac86e76` | matches (unmodified) |
| `docs/_audits/phase-1/sub-phase-eulerian-smoke/stage-1-gate13-replay-2026-05-22T13-16-22Z.txt` | `f76077a3…d95c95ce` | matches |

## 6. Regression / failing-tests sweep (Stage 2 step 2.2)

(FACT — `…/stage-2-evidence/test-sweep-positive-2026-05-22T13-28-30Z.txt`
sha256 `6f594bda…0024dff64f9` + `…/test-sweep-negative-2026-05-22T13-28-30Z.txt`
sha256 `a05b26ef…aef446c0`.)

### 6.1 Positive targets — GREEN at HEAD

| Target | Result |
|---|---|
| `packages/eulerian-smoke/tests/` | **10 passed** in 3.42 s (the gates 4-13 GREEN target for this sub-phase) |
| `packages/reaction-diffusion-3d/tests/` | 8 passed in 24.19 s |
| `packages/sph-water/tests/` | 22 passed in 0.60 s |
| `packages/boids-3d/tests/` | 10 passed in 45.92 s |
| `packages/physarum/tests/` | 10 passed in 10.29 s |
| `packages/mandelbulb-explorer/tests/` | 10 passed in 0.25 s |
| `packages/strange-attractors/tests/` | 11 passed in 0.75 s |
| `packages/reaction-diffusion-2d/tests/` | 14 passed in 2.39 s |
| `tools/integrity/` | 51 passed in 1.11 s |
| `tools/diagnostics/` | 93 passed in 0.31 s |
| `tools/testkit/` | 52 passed in 2.71 s |

Total positive: **291 tests across 11 targets, all GREEN.**

### 6.2 Negative targets — still RED with `ModuleNotFoundError`

| Target | Result |
|---|---|
| `packages/lattice-boltzmann-d3q19/tests/` | 5 collection errors (`ModuleNotFoundError`) — expected, pending LBM sub-phase |
| `packages/mpm-multimaterial/tests/` | 4 collection errors (`ModuleNotFoundError`) — expected, pending MPM sub-phase |

## 7. Integrity check results (Stage 2 steps 2.3 + 2.4 + 2.5 + 2.6 + 2.7)

### 7.1 Cat 3 disposition — **NO-OP for `volumetric-grid` subdir**

(FACT — `…/stage-2-evidence/integrity-cats-2026-05-22T13-28-30Z.txt`
sha256 `4a32c8ac…6126c07447`.)

eulerian-smoke ships **NO golden table** (gate-5 is MMS-based per
spec-ref § 7 "No closed-form golden table"). Mirrors RD-3D's
`continuous-ca` NO-OP precedent per conventions doc § I.2 / § M.4 N2.

- Pre-flight verification: `tools/testkit/golden/tables/`
  contains no `volumetric-grid/` subdir at HEAD (verified inline).
- `_SUBDIRS_PICKED_UP` at HEAD remains
  `(Path("closed-form"), Path("agent-based"), Path("particle-fluids"))`
  — NOT extended by this sub-phase. Confirmed.

### 7.2 Full integrity sweep (Cat 1, 2, 3, 4, 5, X)

Aggregate result: **0 HARD_FAIL, 13 SOFT_WARN** (advisory).

The 13 SOFT_WARNs are:

- 5 Cat 3 `AUDIT_LOG` rows — pre-existing inheritance (Cat 3
  evaluator shims for `lorenz-structural-invariants`,
  `mandelbulb-distance-estimator-p8-quilez-2009`,
  `boids-reynolds-1987-3agent-step1`,
  `physarum-jones-2010-4agent-deposit-step1`,
  `dfsph-density-evolution-2particle`). Carried forward per
  conventions doc § L.3.
- 7 Cat 5 `audit-links` advisory pre-existing from Phase 0/1
  (inheritance — conventions doc § O).
- 1 **new** Cat 5 `audit-links` SOFT_WARN: the Task 0.4 scope-analysis
  evidence markdown at
  `docs/_audits/phase-1/sub-phase-eulerian-smoke/stage-0-evidence/scope-analysis-2026-05-22T12-04-30Z.md`
  lacks YAML front-matter. **Documented at § 8.2 N2 as a new shift**
  (advisory-only; evidence files are not audits proper, but Cat 5
  audit-links scans them as audit-referenced).

### 7.3 Evidence-path verification (Stage 2 step 2.4)

(FACT — `…/stage-2-evidence/verify-evidence-2026-05-22T13-28-30Z.txt`
sha256 `e3c5a7d2…1d5851a10`.)

- `stage-0-checkpoint-2026-05-22T12-05-00Z.md`: **21 pass / 0 fail** at
  head_sha `dcef17d…`.
- `stage-1-checkpoint-2026-05-22T13-20-00Z.md`: **0 pass / 1 fail** —
  `head_sha c20d24d6dd24c92b6d3ab44f6db9be8b48d50824 not present in
  repo` (the recorded head_sha was mis-typed during the Stage 1 SHA
  back-fill at commit `04192f9`; the correct closing-commit SHA was
  `c20d24d8d12c0cec633f309b828087b7c11697eb`). **Documented at
  § 8.2 N1.** Per RD-3D Stage 2 N1 precedent (Stage 1 checkpoint
  front-matter mis-recorded a JSON sidecar sha256; Convention #12
  + § B.1 forbid retroactive edit) — the discrepancy is recorded
  here and the Stage 1 checkpoint lands at its sealed back-fill SHA
  with the mis-typed front-matter intact.

### 7.4 Gate-13 replay verification (Stage 2 step 2.5)

(FACT — `…/stage-2-evidence/gate13-replay-eulerian-2026-05-22T13-28-30Z.txt`
sha256 `d8f099d2…d95c95ce`.)

Worktree at SHA `216021a` (Phase 1 Stage 2 eulerian-smoke TDD
bootstrap commit) reproduces Phase 1 RED:

- 4 test files: `test_determinism.py`, `test_diagnostics.py`,
  `test_mms_convergence.py`, `test_pbt_invariants.py`.
- 4 `ModuleNotFoundError` collection errors: `eulerian_smoke.reference`,
  `eulerian_smoke.sim`, `eulerian_smoke.invariants` (the missing
  submodules at the RED bootstrap SHA).
- Summary: `4 errors in 0.05s`.

At HEAD, the same packages/eulerian-smoke/tests/ collection passes
GREEN at 10/10 (positive sweep above).

Phase 1 RED evidence sha256 `c961dd22…879f23a1` remains
**byte-identical at HEAD** (gate-13 anchor intact across the gap to
spec-Phase-2 per spec § 3.5 + conventions doc § E).

### 7.5 Append-only check (Stage 2 step 2.6)

(FACT — `…/stage-2-evidence/append-only-2026-05-22T13-28-30Z.txt`
sha256 `1695a11d…294af8006ef`.)

| Check | Result |
|---|---|
| CI semantics (no `*.ledger.md` files) | PASS — 0 files |
| Protected set 1 — files at `v0.1.0-phase-1` | PASS |
| Protected set 2 — closed-form audit chain at `2cc0f21` | PASS |
| Protected set 3 — agent-based audit chain at `739c93f` (back-fill) | PASS |
| Protected set 4 — replay-tool-hotfix audit chain at `1f5fa0c` (back-fill) | PASS |
| Protected set 5 — continuous-CA-rd3d audit chain at `0df358d` (back-fill) | PASS |
| Protected set 6 — numba-integration audit chain at `569c883` (back-fill) | PASS |
| Protected set 7 — sph-water audit chain at `17850e2` (back-fill) | PASS |
| Protected set 8 — mutation-script-hotfix audit chain at `27304d0` (back-fill) | PASS |
| Protected set 9 — conventions-consolidation audit chain at `34c7d34` (back-fill) | PASS |
| Append-only on `references/SPlisHSPlasH/` since sph-water landing | PASS (empty diff) |

### 7.6 Mutation-score artifact — Stage 2 step 2.7 (B17 PATH-A third proof-point)

(FACT — `tools/testkit/mutation/sub-phase-eulerian-smoke-2026-05-22T13-28-30Z.json`
sha256 `403c2fcb…7e146a292` + `…/stage-2-evidence/mutation-pathA-output-2026-05-22T13-28-30Z.txt`
sha256 `e90dc414…2e8f4265`.)

Per operator Item 3 routing: PATH-A continue (third proof-point of
the per-target mutation-runner infrastructure established at
sub-phase-continuous-ca-rd3d, continued at
sub-phase-particle-fluids-sph-water). Additive
`[tool.mutmut.targets.eulerian_smoke]` +
`[tool.mutmut.targets.incompressible_ns_2d_mms]` blocks in
`tools/testkit/mutation/mutmut-config.toml`; existing testkit/
integrity/RD-3D/sph-water targets UNTOUCHED.

Per-target REAL kill-rate baselines:

| Target | Total | Killed | Survived | Kill rate | Threshold |
|---|---:|---:|---:|---:|---:|
| `eulerian_smoke` (sim source) | 992 | 484 | 508 | **0.4879** | 0.80 (advisory) |
| `incompressible_ns_2d_mms` (MMS solution) | 79 | 55 | 24 | **0.6962** | 0.80 (advisory) |

Per-file breakdown (eulerian-smoke sim source):

| File | Killed | Survived | Total | Kill rate |
|---|---:|---:|---:|---:|
| `invariants.py` | 76 | 59 | 135 | 0.5630 |
| `reference/stable_fluids.py` | 366 | 245 | 611 | 0.5990 |
| `sim.py` | 42 | 204 | 246 | **0.1707** |

The `sim.py` kill rate is notably low because much of `sim.py` is
manifest-builder and capture-runner code (the
`compute_canonical_trajectory_3d` + `_build_manifest_3d` + the
manifest-field equality paths) that the test suite exercises only
through `sim_runner_diagnostic` (32³ × 10 steps), not through the
canonical `sim_runner_seeded` (128³ × 500 steps; ~11.5 min per
invocation, infeasible for mutation testing). The surviving mutant
IDs are enumerated in the artifact JSON; banked for future
test-augmentation work (conventions doc § L.2 — mirrors sph-water +
RD-3D banked test-augmentation candidates).

**Numba-mutation note:** eulerian_smoke does NOT use `@njit` at this
sub-phase (pure NumPy + `np.roll` suffices per Stage 0 Task 0.4
wall-clock budget). The numba-cache propagation surface documented
at sph-water Stage 2 N3 does NOT apply here. If a Phase-2+
optimisation introduces `@njit`, the cache-invalidation discipline
carries forward via conventions doc § G.

**Optional third target** (NS-2D MMS scaffolding) SKIPPED with
documented rationale — eulerian-smoke inlines its MMS convergence
study per Path-Y operator routing, identical to RD-3D PATH-A. The
existing `targets.code_verification_mms` already covers the heat-1D
runner surface against the heat-1D tests; adding a redundant target
would re-mutate the same surface.

R15 STOP-AND-SURFACE precondition (per-mutant test-runtime budget
blowup) NOT TRIGGERED — eulerian-smoke pytest suite runs in ~3.5 s
per invocation; mutation runtime was ~50 min for the 992-mutant
sim-source target and ~6 min for the 79-mutant MMS target.

## 8. SHIFTED register

### 8.1 Inherited verbatim from prior audits (70 shifts going into Stage 2)

(FACT — conventions doc § M tally + sph-water cumulative 65 + 5 Stage 1
shifts from this sub-phase.)

- 21 from Phase 1 baseline (conventions doc § M.1).
- 11 from closed-form sub-phase (conventions doc § M.2).
- 10 from agent-based sub-phase (conventions doc § M.3).
- 6 from continuous-CA-rd3d sub-phase (conventions doc § M.4).
- 13 from particle-fluids-sph-water sub-phase (conventions doc § M.5).
- 5 new at sub-phase-eulerian-smoke Stage 1
  (per Stage 1 checkpoint § 5).

### 8.2 New shifts surfaced during Stage 2

| ID | Description |
|---|---|
| **N1** | Stage 1 checkpoint front-matter mis-recorded the back-fill `head_sha` field. The closing-commit SHA for the Stage 1 checkpoint was `c20d24d8d12c0cec633f309b828087b7c11697eb`; the back-fill commit `04192f9` wrote `c20d24d6dd24c92b6d3ab44f6db9be8b48d50824` (different beyond the first 7 hex digits). `verify_evidence --strict` over the Stage 1 checkpoint at HEAD fails with `head_sha … not present in repo`. The underlying audit body and evidence file references are correct. Per RD-3D Stage 2 N1 precedent (Stage 1 checkpoint front-matter mis-recorded a JSON sidecar sha256; Convention #12 + § B.1 forbid retroactive edit), no retroactive edit is performed — the Stage 1 checkpoint lands at its sealed back-fill SHA with the mis-typed field intact. Future verify_evidence usage against this audit will need to bypass the head_sha check or accept the failure as documented inheritance. **Lesson learned:** an explicit `git rev-parse HEAD | head -c 40` capture between the audit-commit and the back-fill-edit step (rather than transcribing from `git log --oneline -1`'s short SHA) would prevent this class of error. |
| **N2** | Task 0.4 scope-analysis evidence markdown (`scope-analysis-2026-05-22T12-04-30Z.md`) lacks YAML front-matter, producing a new Cat 5 audit-links SOFT_WARN. Evidence files referenced from audit `evidence_paths:` lists are scanned by Cat 5 for YAML structure but evidence-only files (vs audit-proper files) are not expected to have it. The SOFT_WARN is advisory; no remediation at this sub-phase. Pattern to consider for the next conventions-doc refactor: either (a) evidence files referenced from `evidence_paths:` should adopt minimal YAML front-matter, or (b) the Cat 5 audit-links check should skip files under `*/stage-N-evidence/` subdirectories. |
| **N3** | Stage 1 landed in a **single session** with no R-class STOP-AND-SURFACE arcs — first per-sim implementation sub-phase to achieve this (closed-form went via partial checkpoints; agent-based was clean but did NOT include MMS; RD-3D had Stage 1 S2 MMS-runner-skipping decision; sph-water had six R-class arcs). The Stage 0 scope-analysis discipline (conventions doc § N PROPOSED, first practical exercise here) is the reason. The session-budget pattern "Stage 0 measures → Stage 1 fits within ±3× the estimate" is now an empirical convention rule of thumb; recommended to lift to the conventions doc per § 9.3 below. |

### 8.3 Cumulative inherited shift count going into the NEXT sub-phase

21 (Phase 1) + 11 (closed-form) + 10 (agent-based) + 6 (RD-3D) +
13 (sph-water) + 5 (eulerian-smoke Stage 1) + 3 (this Stage 2) =
**73 documented to date**.

The hotfix sub-phases (replay-tool, numba-integration,
mutation-script) are audit-chained as siblings, not children, and
their shifts are NOT counted into the per-sim cumulative — they are
documented separately within each hotfix's repair audit per
conventions doc § O.

## 9. Banked items for follow-up

### 9.1 Resolved during this sub-phase

- Conventions doc § N PROPOSED Task 0.4 canonical-descriptor
  scope-analysis: **first practical exercise** completed, validated
  (Stage 0 estimate accurate within ~50%); see § 9.3 lift
  recommendation.
- Probe-vs-Appendix-D drift for eulerian-smoke: re-anchored at
  Stage 1 step 5 against Appendix D (captures written under Appendix
  D names; probe report sealed as Phase 1 artifact intact).
- Three new playbook entries (P25–P27) explicitly SKIPPED per
  operator Item 4 routing — P22+P23+P24 covered the eulerian-grid
  determinism risk surface.

### 9.2 Open (carried into next sub-phases)

| ID | Item | Owner |
|---|---|---|
| **MMS-runner-scaffolding generalization** (RD-3D Stage 1 S2 banked; sph-water § 9.2 carried) | Still banked. **Now anchored by TWO concrete inline examples** (RD-3D + eulerian-smoke). Operator decides at LBM plan-drafting time: interpolate a focused MMS-pipeline-generalization sub-phase, OR generalize at LBM plan-time, OR inline once more. | Next MMS-using sub-phase (LBM). |
| **eulerian-smoke test-augmentation candidate** | The B17 PATH-A real baseline surfaced 508 surviving mutants in eulerian_smoke source (per-file: `sim.py` 0.1707 — manifest-builder code lightly tested; `invariants.py` 0.5630; `reference/stable_fluids.py` 0.5990). Surviving-mutant IDs enumerated in the artifact JSON. Banked for spec-Phase-2+ when sim-source mutation thresholds become gating. | Operator-routable; default-skip for siblings. |
| **incompressible_ns_2d MMS test-augmentation candidate** | The MMS solution's 0.6962 kill rate is close to the 0.80 advisory threshold; 24 surviving mutants enumerated. Could be lifted with additional test coverage of `source_term` + `evaluate` numerical anchors. Banked. | Operator-routable. |
| **MAC-staggered grid refactor** (S3 surfaced) | The Stam-on-collocated O(dx²) inconsistent-stencil residual divergence is a known limitation. The Phase-2+ Stack-C C++/Vulkan port should implement face-centered velocities (MAC grid) per sim spec-ref § 5. | Phase-2+ Stack-C work. |
| **Stage 1 checkpoint head_sha typo (N1)** | Documented + accepted per conventions doc § B.1 append-only / RD-3D Stage 2 N1 precedent. | No action; documented for posterity. |
| **Cat 5 audit-links YAML check on evidence files (N2)** | Conventions-doc / Cat 5 implementation decision — neither blocking nor obvious which way to resolve. | Operator-routable; banked. |
| Cat 3 sibling subdirs (`hybrid-pg`, `lattice`) | Extend `_SUBDIRS_PICKED_UP` as each category's sims implement + lift goldens to ≥ 3 anchors per spec § 2.4. Template established by closed-form + agent-based + particle-fluids pickups. | Each per-sim implementation sub-phase (`lbm-d3q19`, `mpm-multimaterial`), additive. |
| Cat 3 evaluator shims for the 5 AUDIT_LOG algorithms | Carries forward unchanged. | Banked. |
| RD-3D / sph-water test-augmentation candidates | Carries forward unchanged. | Banked. |
| DFSPH generator test-coverage gap (sph-water Stage 2 N4) | Carries forward unchanged. | Operator-routable. |
| B-hotfix-1 / B-hotfix-2 (replay-tool-hotfix) | Carries forward. | Phase 2+ Stack-C effort. |
| B2 / B3 / B4 / B5 / B6 / B11 / B16 (Phase 1 open) | Carries forward. | Per their original Phase 1 audit § 13 owners. |

### 9.3 New banked observations from this sub-phase (carry-forward for next sub-phase plan-drafting)

| Observation | Recommendation |
|---|---|
| **(1) Conventions doc § N PROPOSED → established graduation.** Task 0.4 produced clean signal at Stage 0, prevented what could have been an R-class arc at Stage 1 (the sph-water pattern), and validated within ~50% accuracy. | **Operator decision pending** — graduate conventions doc § N from PROPOSED to established at the next conventions-doc refactor; reference Task 0.4 as a settled discipline at LBM plan-drafting. |
| **(2) Task 0.4 "production-corrected ~1.5× factor" rule of thumb.** Stage 0 skeletal probe under-estimated by ~45% (0.93 s → 1.348 s measured). The factor accounts for full pipeline overhead (vorticity-confinement skeleton + scalar advection + diffusion + capture serialization) beyond the bare advect+project skeleton. | Add to conventions doc § N as a documented rule of thumb when graduating § N from PROPOSED to established. |
| **(3) "Single-session Stage 1" as an empirical session-budget convention.** This sub-phase landed Stage 1 in a single session because Stage 0 Task 0.4 caught the scope question pre-flight. Pattern: Stage 0 scope-analysis fits within ceilings → Stage 1 budget matches → no continuation needed. | Document in conventions doc § A.2 as an empirical observation; banked for confirmation at LBM (the second Task-0.4-applying sub-phase). |
| **(4) Cat 5 audit-links scan on evidence files.** The N2 finding is the first time this has surfaced; until now evidence files have either been .txt (which Cat 5 doesn't check for YAML) or .md with front-matter. | Operator decides next conventions-doc refactor: tighten evidence-file front-matter convention OR relax Cat 5 check on `*/stage-N-evidence/`. |
| **(5) Convention #12 SHA back-fill execution discipline.** N1 illustrates that the back-fill SHA can be mis-typed when transcribing from short-SHA git output. | Add to conventions doc § B.2 an explicit step: "capture closing-commit SHA via `git rev-parse HEAD` (full 40-hex) and paste into front-matter; do NOT transcribe from short-SHA `git log` output". Banked. |
| **(6) sim.py manifest-builder kill-rate gap.** The eulerian_smoke sim.py 0.17 kill rate is notable — many surviving mutants are in `_build_manifest_3d`'s field-equality paths that the test suite doesn't exercise via the canonical capture (because the canonical capture takes 11.5 min). Pattern likely to recur for any sim with expensive canonical captures (LBM, MPM upcoming). | Banked observation; the test-augmentation work could be served by a `test_manifest_3d_field_equality` test that builds a manifest at small N and checks specific fields. Operator-routable. |
| **(7) Aggregate inherited-shift count crossed 70 entering Stage 2.** 70 going into Stage 2 + 3 Stage 2 shifts = 73 going into next sub-phase. The conventions-consolidation pattern landed at sph-water § 9.3 row 7 has worked through this sub-phase; further consolidation may be warranted as the count grows. | **Operator decision pending** — re-examine at LBM landing whether the conventions doc needs a second refactor or is structurally adequate. |

## 10. Next-sub-phase recommendations

Per Phase 1 audit § 15 + the established next-sub-phase ordering, the
recommended next per-sim implementation sub-phases are:

1. **lattice-boltzmann-d3q19** — second Stack-C surface; **shares the
   NS-2D MMS solution with this sub-phase** per Phase 1 Stage 2 shift
   #18. Now anchored by two concrete inline examples (RD-3D +
   eulerian-smoke). **The MMS-runner-scaffolding generalization
   question becomes load-bearing at LBM plan-drafting** (operator
   decision: interpolate a focused MMS-pipeline-generalization
   sub-phase first, OR generalize at LBM plan-time, OR inline once
   more — three viable paths).

2. **mpm-multimaterial** — Stack-D MLS-MPM at full common-py surface.
   Particle-grid hybrid; will benefit from this sub-phase's perf-
   ledger precedent for multi-canonical-capture sims (`*-ref` with
   two descriptors per Appendix D) and from sph-water's numba
   project infrastructure (the per-particle scatter loops are
   similar shape).

Subsequent per-sim sub-phases inherit:

- The full 73-shift inventory (conventions doc § M tally + this
  audit § 8).
- Conventions doc § N PROPOSED Task 0.4 (now a settled discipline
  pending graduation; see § 9.3 row 1).
- The two-canonical-captures-per-sim pattern (Appendix D § D.2.3
  lists multi-descriptor `ref` rows for `eulerian-smoke` + `lbm` +
  `mpm`; this sub-phase establishes the pattern at sim-test scale).
- The MacCormack-corrected SL convention for any 2nd-order accuracy
  claim on semi-Lagrangian schemes.
- The `np.mod` FP-edge integer-modulus guard (`i0 % N`).
- The per-target mutmut config schema additively (no rework needed;
  add `[tool.mutmut.targets.<sim>]` blocks).
- The Cat 3 NO-OP precedent for MMS-only sims (no golden table; no
  `_SUBDIRS_PICKED_UP` extension).

After eulerian-smoke + LBM + MPM (all in Phase 1 Stage 2 bootstrap,
awaiting per-sim sub-phases), spec-Phase-2 becomes dispatchable at
`v0.2.0-phase-2`.

## 11. Tag posture (Stage 2 step 2.11)

**No `-phase-N` tag is proposed by this sub-phase.** Spec § 7.12
reserves `v0.<N>.0-phase-<N>` for spec-phase boundaries; next phase
tag per spec is `v0.2.0-phase-2`.

**Lean recommendation (conventions doc § D.2 + plan § 5 + § 11.4 +
operator Item 5 routing): NO intermediate tag at all.** Sub-phase
commits accumulate to `main`; the landing audit + per-sim commits +
B17 PATH-A artifact provide the audit trail; cleanest phase-tag
semantics.

**Banked operator decision (plan § 11.4):** optional non-phase
point-release tag `v0.1.5` (no `-phase-N` suffix). Acceptable per
spec § 7.12; operator-pushed only. **Default lean stands: no tag.**

**Forbidden either way:** any tag carrying `-phase-N` (single or
multi-segment). Reserved for spec-phase boundaries.

The agent does NOT push any tag (operator-only per spec § 7.12).

## 12. Sub-phase coherence note (per plan § 11)

The eulerian-smoke sub-phase exercises the per-sim implementation
pipeline (Stage 0 replay → Stage 1 per-sim → Stage 2 landing) at the
**fifth** implementation surface and the **first to demonstrate that
the conventions doc + Stage 0 scope-analysis disciplines** —
established as outputs of the four prior sub-phases — produce a
**predictable, single-session Stage 1** without R-class
STOP-AND-SURFACE arcs.

Per-area coherence:

- **First volumetric-grid sim.** The 13-gate contract scales to a
  new sim category (Stam-Fedkiw stable-fluids; semi-Lagrangian +
  pressure-projection; vector_field IC-6 diagnostics).
- **First sub-phase plan drafted against the conventions doc.** The
  ~250-400 line plan (actual 397 lines) was substantially shorter
  than sph-water's 515 — the consolidation hypothesis is validated.
- **First practical exercise of conventions doc § N PROPOSED Task
  0.4.** Stage 0 scope-analysis estimated 3D Taylor-Green per-step
  0.93 s; Stage 1 measured 1.348 s. Within ~50% accuracy. The
  ~1.5× production-correction factor surfaces as an empirical rule
  of thumb (banked § 9.3 row 2).
- **First multi-canonical-capture sim.** Two captures landed
  (`taylor-green-128cube` + `lid-driven-cavity-128sq`) per Appendix
  D § D.2.3. The pattern is now first-class; LBM + MPM inherit.
- **B17 PATH-A third proof-point.** Per-target runner infrastructure
  works at the third application; no rework needed beyond additive
  config extension. Mutation gate remains advisory; aggregate kill
  rates 0.4879 (sim source) + 0.6962 (MMS solution) below the 0.80
  threshold but consistent with prior sub-phases' patterns. Surviving
  mutant IDs banked for test-augmentation.
- **Cat 3 NO-OP** for the `volumetric-grid` subdir mirrors RD-3D's
  `continuous-ca` NO-OP precedent (MMS-only sims do not ship golden
  tables). Conventions doc § I.2 inheritance.
- **Inline-MMS pattern (Path Y) continued.** The convergence study
  is inlined at `packages/eulerian-smoke/tests/test_mms_convergence.py`;
  `tools/testkit/code_verification/mms/runner.py` UNTOUCHED. With
  two concrete inline examples now (RD-3D + eulerian-smoke), the
  MMS-runner-scaffolding generalization becomes load-bearing at
  LBM plan-drafting (§ 9.2 + § 10).

This audit lands at HEAD `54043e123e1b7215d0189d93fb5f91923d22face`
(back-filled per Convention #12 + conventions doc § B.2 in a separate
commit `chore(eulerian-smoke-stage2-sha-backfill)` per the two-commit
pattern; the per-N1 lesson "capture full 40-hex SHA via
`git rev-parse HEAD`, never transcribe from short-SHA `git log`" was
applied here). Verdict: **CONFIRMED**.
